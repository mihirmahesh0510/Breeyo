---
phase: 05-inventory-management
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx
  - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx
  - apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx
  - apps/mobile/src/features/inventory/components/QuantityStepper.tsx
  - apps/mobile/src/features/inventory/components/FifoBatchDisplay.tsx
  - apps/mobile/src/features/inventory/components/BatchOverrideList.tsx
  - apps/mobile/src/features/inventory/components/ExpiredBatchBlocker.tsx
  - apps/mobile/src/features/inventory/components/StockReceiptForm.tsx
  - apps/mobile/src/features/inventory/hooks/useFifoDispense.ts
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-04
  - INV-05

must_haves:
  truths:
    - "Stock receipt screen creates new batch with quantity, optional lot number, category-enforced expiry date, optional purchase price, and optional supplier per D-01, D-03, D-09, D-11"
    - "Dispense screen shows FIFO auto-selected batch with override option, quantity stepper, and blocks expired batch dispensing per D-22, D-25, D-35"
    - "Stock adjustment sheet requires reason selection from preset list and supports add/remove with notes per D-04"
    - "All stock operations provide success feedback via toast and haptic, and invalidate React Query cache for immediate UI refresh"
  artifacts:
    - path: "apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx"
      provides: "Stock receipt form with batch creation"
      min_lines: 60
    - path: "apps/mobile/src/features/inventory/screens/DispenseScreen.tsx"
      provides: "Dispense flow with FIFO batch display and override"
      min_lines: 60
    - path: "apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx"
      provides: "Stock adjustment bottom sheet with reason selection"
      min_lines: 40
  key_links:
    - from: "apps/mobile/src/features/inventory/screens/DispenseScreen.tsx"
      to: "/api/v1/inventory/items/:itemId/dispense"
      via: "useFifoDispense hook calls API"
      pattern: "dispense"
    - from: "apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx"
      to: "/api/v1/inventory/items/:itemId/receive"
      via: "useMutation for stock receipt"
      pattern: "receive"
---

<objective>
Build the mobile stock receipt screen, dispense screen with FIFO batch display, and stock adjustment bottom sheet. These screens handle the core stock movement operations: receiving new stock, dispensing with FIFO enforcement, and manual adjustments.

Purpose: These screens complete the stock operation UI. They are launched from the item profile action buttons (Plan 04) and scanner quick actions (Plan 05). The dispense screen is the most critical -- it must correctly show FIFO batch selection and handle the expired batch blocking UX.

Output: Three complete stock operation screens/sheets with all supporting components and API integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-inventory-management/05-CONTEXT.md
@.planning/phases/05-inventory-management/05-UI-SPEC.md
@.planning/phases/05-inventory-management/05-04-SUMMARY.md
@.planning/phases/05-inventory-management/05-05-SUMMARY.md

<interfaces>
<!-- From Plans 02+03 API endpoints -->
API Routes:
- POST /api/v1/inventory/items/:itemId/receive -> { batch, movement }
- POST /api/v1/inventory/items/:itemId/dispense -> DispenseResult
- POST /api/v1/inventory/items/:itemId/adjust -> StockMovement

From packages/shared/src/types/inventory.types.ts:
- StockReceiptInput, DispenseInput, DispenseResult, StockAdjustmentInput, StockBatch, BatchDeduction
- EXPIRY_REQUIRED_CATEGORIES

From packages/shared/src/validators/inventory.validators.ts:
- stockReceiptSchema, dispenseSchema, stockAdjustmentSchema

From packages/shared/src/constants/adjustment-reasons.ts:
- ADJUSTMENT_REASONS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build stock receipt screen and stock adjustment bottom sheet</name>
  <files>
    apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx,
    apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx,
    apps/mobile/src/features/inventory/components/StockReceiptForm.tsx
  </files>
  <read_first>
    apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx,
    apps/mobile/src/features/inventory/hooks/useInventoryApi.ts,
    apps/mobile/src/components/FormField.tsx,
    apps/mobile/src/components/BottomSheet.tsx,
    apps/mobile/src/components/Toast.tsx,
    packages/shared/src/types/inventory.types.ts,
    packages/shared/src/validators/inventory.validators.ts,
    packages/shared/src/constants/adjustment-reasons.ts,
    packages/shared/src/constants/inventory-categories.ts
  </read_first>
  <action>
    **1. Create `apps/mobile/src/features/inventory/components/StockReceiptForm.tsx`:**

    Form component per UI-SPEC Stock Receipt Form:
    - Item name displayed as read-only header (pre-filled from navigation params)
    - Quantity field: numeric input, label "Quantity Received", placeholder "Enter total [unit] received", required
    - Lot number field: text input, label "Lot / Batch Number (optional)", placeholder "Enter lot number"
    - Expiry date field: date picker, label "Expiry Date". Show "Required for [Medicine/Vaccine/Consumable]" note when item category is in EXPIRY_REQUIRED_CATEGORIES (D-27). Validate future date.
    - Purchase price field: numeric input, label "Purchase Price per [unit] (Rs)", placeholder "0.00"
    - Supplier field: text input, label "Distributor (optional)", placeholder "Enter distributor name"
    - Validation errors inline per field
    - Props: `itemName`, `unit`, `category`, `data`, `onChange`, `errors`

    **2. Create `apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx`:**

    Screen composition per UI-SPEC:
    - Screen title: "Receive Stock"
    - StockReceiptForm with item context
    - "Receive Stock" filled primary button at bottom
    - "Cancel" text button
    - On submit: validate with stockReceiptSchema, call POST /api/v1/inventory/items/:itemId/receive
    - Success: Haptics.notificationAsync(Success), toast "[N] [unit] of [Item Name] received", navigate back to item profile
    - Loading state: button shows spinner, form disabled
    - Error state: inline validation errors + banner for server errors
    - Route params: itemId, itemName, unit, category
    - Uses useMutation that invalidates ['inventory', 'item', clinicId, itemId] and ['inventory', 'summary'] on success

    **3. Create `apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx`:**

    Bottom sheet per UI-SPEC Stock Adjustment:
    - Sheet title: "Adjust Stock"
    - Current stock display: "Current stock: [N] [unit]"
    - Adjustment type toggle: "Add" / "Remove" (segmented control or toggle buttons)
    - Quantity input: numeric, label "Quantity", placeholder "Enter quantity"
    - Reason selector: required, label "Reason". List of ADJUSTMENT_REASONS as selectable chips or radio group. Error "Select a reason for this adjustment" if missing.
    - Notes field: text input, label "Notes (optional)", placeholder "Additional details..."
    - "Apply Adjustment" filled primary button
    - "Cancel" text button
    - On submit: validate with stockAdjustmentSchema, call POST /api/v1/inventory/items/:itemId/adjust
    - Success: Haptics.notificationAsync(Success), toast "[N] [unit] added to/removed from [Item Name]", dismiss sheet
    - Error: Haptics.notificationAsync(Error), inline errors
    - Invalidates inventory queries on success
    - Props: `itemId`, `itemName`, `unit`, `currentStock`, `onDismiss`
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/mobile/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx contains `Receive Stock`
    - apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx contains `stockReceiptSchema`
    - apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx contains `Haptics`
    - apps/mobile/src/features/inventory/screens/StockReceiptScreen.tsx contains `received`
    - apps/mobile/src/features/inventory/components/StockReceiptForm.tsx contains `Quantity Received`
    - apps/mobile/src/features/inventory/components/StockReceiptForm.tsx contains `Lot / Batch Number`
    - apps/mobile/src/features/inventory/components/StockReceiptForm.tsx contains `Expiry Date`
    - apps/mobile/src/features/inventory/components/StockReceiptForm.tsx contains `Purchase Price`
    - apps/mobile/src/features/inventory/components/StockReceiptForm.tsx contains `Distributor`
    - apps/mobile/src/features/inventory/components/StockReceiptForm.tsx contains `EXPIRY_REQUIRED_CATEGORIES`
    - apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx contains `Adjust Stock`
    - apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx contains `ADJUSTMENT_REASONS`
    - apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx contains `stockAdjustmentSchema`
    - apps/mobile/src/features/inventory/screens/StockAdjustmentSheet.tsx contains `Apply Adjustment`
  </acceptance_criteria>
  <done>Stock receipt screen creates new batch with all fields per D-01/D-03/D-09/D-11, enforces expiry for medicines per D-27. Stock adjustment sheet requires reason from presets per D-04. Both provide haptic + toast feedback and invalidate queries.</done>
</task>

<task type="auto">
  <name>Task 2: Build dispense screen with FIFO batch display, override, and quantity stepper</name>
  <files>
    apps/mobile/src/features/inventory/screens/DispenseScreen.tsx,
    apps/mobile/src/features/inventory/components/QuantityStepper.tsx,
    apps/mobile/src/features/inventory/components/FifoBatchDisplay.tsx,
    apps/mobile/src/features/inventory/components/BatchOverrideList.tsx,
    apps/mobile/src/features/inventory/components/ExpiredBatchBlocker.tsx,
    apps/mobile/src/features/inventory/hooks/useFifoDispense.ts
  </files>
  <read_first>
    apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx,
    apps/mobile/src/features/inventory/hooks/useInventoryApi.ts,
    apps/mobile/src/components/BottomSheet.tsx,
    packages/shared/src/types/inventory.types.ts,
    packages/shared/src/validators/inventory.validators.ts
  </read_first>
  <action>
    **1. Create `apps/mobile/src/features/inventory/components/QuantityStepper.tsx`:**

    Quantity input with +/- buttons per UI-SPEC:
    - Row layout: "-" button (48px height) + numeric TextInput + "+" button (48px height)
    - 100ms scale 0.97 animation on button press + Haptics.impactAsync(Light)
    - Min value: 1 (cannot go below 1 for dispense)
    - Max value: available stock (from batch or total)
    - Disabled state when at min/max (atMin, atMax)
    - Unit label displayed alongside
    - Props: `value`, `onChange`, `min`, `max`, `unit`

    **2. Create `apps/mobile/src/features/inventory/components/FifoBatchDisplay.tsx`:**

    Auto-selected batch info per UI-SPEC:
    - Card showing the FIFO-selected batch details:
      - "Oldest batch selected (FIFO)" caption label (onSurfaceVariant)
      - "From batch: [lotNumber], Exp: [DD MMM YYYY]" (bodyLarge)
      - Available quantity: "[currentQty] [unit] available"
    - "Use different batch" text button for override (D-22)
    - If batch is overridden: show updated batch info with "Override active" label
    - Props: `batch: StockBatch`, `isOverridden: boolean`, `onOverride: () => void`

    **3. Create `apps/mobile/src/features/inventory/components/BatchOverrideList.tsx`:**

    List of available batches per UI-SPEC:
    - FlatList sorted by receivedAt (oldest first, same as FIFO order)
    - Each row: lot number, qty, expiry date, received date
    - Expired batches shown but DISABLED with "EXPIRED" badge and grayed out (D-25)
    - Currently selected batch highlighted
    - Tapping non-expired batch selects it as override
    - Props: `batches: StockBatch[]`, `selectedBatchId: string`, `onSelect: (batchId: string) => void`

    **4. Create `apps/mobile/src/features/inventory/components/ExpiredBatchBlocker.tsx`:**

    Error banner per UI-SPEC:
    - Visible when an expired batch is somehow selected
    - Red error banner: "Cannot dispense -- batch expired on [DD MMM YYYY]"
    - errorContainer background
    - Props: `expiryDate: Date`

    **5. Create `apps/mobile/src/features/inventory/hooks/useFifoDispense.ts`:**

    Hook for dispense API call:
    ```typescript
    export function useFifoDispense(clinicId: string, itemId: string) {
      // useMutation for POST /api/v1/inventory/items/:itemId/dispense
      // On success: invalidate ['inventory', 'item', clinicId, itemId], ['inventory', 'summary'], ['inventory', 'alerts']
      // Returns { dispense, isDispensing, error }
    }
    ```

    **6. Create `apps/mobile/src/features/inventory/screens/DispenseScreen.tsx`:**

    Screen composition per UI-SPEC Dispense Flow:
    - Screen/sheet title: "Dispense [Item Name]"
    - Available stock display: "Available: [N] [unit]"
    - QuantityStepper starting at 1, max = total available
    - FifoBatchDisplay showing oldest non-expired batch (auto-selected)
    - "Use different batch" -> toggles BatchOverrideList visibility
    - ExpiredBatchBlocker visible if expired batch somehow in view
    - "Dispense" filled primary button (disabled when expired or insufficient)
    - "Cancel" text button
    - On submit:
      1. Validate with dispenseSchema
      2. Call useFifoDispense.dispense({ quantity, overrideBatchId?, consultationId?, invoiceId? })
      3. Success: Haptics.notificationAsync(Success), toast "[N] [unit] of [Item Name] dispensed"
      4. If consultationId provided (from EMR D-49): show "Linked to consultation: [Pet Name]" caption
      5. If no consultationId (D-52 counter sale): show "Counter Sale" badge
      6. Navigate back / dismiss
    - Insufficient stock state: error "Only [N] [unit] available. Enter a smaller quantity.", dispense button disabled
    - Submitting state: button spinner, form disabled
    - Route params: itemId, itemName, unit, totalStock, consultationId? (from EMR), batchesData
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/mobile/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx contains `Dispense`
    - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx contains `dispenseSchema`
    - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx contains `QuantityStepper`
    - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx contains `FifoBatchDisplay`
    - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx contains `BatchOverrideList`
    - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx contains `Counter Sale`
    - apps/mobile/src/features/inventory/screens/DispenseScreen.tsx contains `consultationId`
    - apps/mobile/src/features/inventory/components/QuantityStepper.tsx contains `48`
    - apps/mobile/src/features/inventory/components/QuantityStepper.tsx contains `Haptics`
    - apps/mobile/src/features/inventory/components/FifoBatchDisplay.tsx contains `Oldest batch selected (FIFO)`
    - apps/mobile/src/features/inventory/components/FifoBatchDisplay.tsx contains `Use different batch`
    - apps/mobile/src/features/inventory/components/BatchOverrideList.tsx contains `EXPIRED`
    - apps/mobile/src/features/inventory/components/ExpiredBatchBlocker.tsx contains `Cannot dispense`
    - apps/mobile/src/features/inventory/hooks/useFifoDispense.ts contains `dispense`
  </acceptance_criteria>
  <done>Dispense screen shows FIFO auto-selected batch with override option per D-22. Quantity stepper with haptic feedback. Expired batch dispensing blocked per D-25. Counter sale and consultation-linked dispensing supported per D-49/D-52. All operations provide haptic + toast feedback.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/mobile/tsconfig.json` -- mobile app compiles
</verification>

<success_criteria>
1. Stock receipt form enforces expiry for medicine/vaccine/consumable categories (D-27)
2. Stock receipt always creates new batch (D-11)
3. Dispense shows FIFO auto-selected batch with override capability (D-22)
4. Expired batch dispensing is blocked with clear error (D-25)
5. Stock adjustment requires reason from preset list (D-04)
6. Quantity stepper has 48px touch targets for one-handed use
7. All operations provide haptic + toast feedback
8. Counter sale and consultation-linked dispensing both supported (D-49, D-52)
</success_criteria>

<output>
After completion, create `.planning/phases/05-inventory-management/05-06-SUMMARY.md`
</output>
