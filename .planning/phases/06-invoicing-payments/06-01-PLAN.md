---
phase: 06-invoicing-payments
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/.env.example
  - apps/mobile/package.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/vitest.config.ts
  - apps/mobile/src/features/pdf/__tests__/pdf-deps.test.ts
  - pnpm-lock.yaml
autonomous: false
requirements: [BIL-04, BIL-05, BIL-06]

user_setup:
  - service: razorpay
    why: "BIL-05 payment links and BIL-06 webhook confirmation cannot be developed or tested without gateway credentials"
    env_vars:
      - name: RAZORPAY_TEST_KEY_ID
        source: "Razorpay Dashboard -> Account & Settings -> API Keys -> Generate Test Key"
      - name: RAZORPAY_TEST_KEY_SECRET
        source: "Razorpay Dashboard -> Account & Settings -> API Keys (shown once at generation)"
      - name: BILLING_ENCRYPTION_KEY
        source: "Generate locally: openssl rand -hex 32 (32-byte hex key for AES-256-GCM at-rest encryption of per-clinic Razorpay secrets)"
    dashboard_config:
      - task: "Create a test-mode webhook pointing at the tunnelled dev URL with events payment_link.paid, payment_link.partially_paid, payment_link.cancelled, payment_link.expired, refund.processed, refund.failed"
        location: "Razorpay Dashboard -> Account & Settings -> Webhooks"
      - task: "Long-lead item for Beta: each of the 20 pilot clinics needs its own Razorpay account with completed KYC and its own webhook configured (D-29 locks per-clinic keys). Start this in parallel with development."
        location: "Per-clinic Razorpay onboarding"

must_haves:
  truths:
    - "apps/mobile can import expo-print, expo-sharing, expo-file-system, react-native-svg, react-native-qrcode-svg, react-native-paper and @breeyo/ui without a resolution error"
    - "The Phase 4 PDF templates that already ship can be rendered in a test without a missing-module failure"
    - "razorpay 2.9.8 is a declared dependency of @breeyo/api"
    - "The mobile root layout wraps the app in a react-native-paper provider so MD3 components render with the Breeyo theme"
  artifacts:
    - path: "apps/mobile/package.json"
      provides: "Declared mobile dependencies for PDF, QR and MD3 components"
      contains: "react-native-qrcode-svg"
    - path: "apps/api/package.json"
      provides: "Razorpay Node SDK dependency"
      contains: "razorpay"
    - path: "apps/mobile/src/features/pdf/__tests__/pdf-deps.test.ts"
      provides: "Resolution smoke test for the PDF dependency chain"
    - path: "apps/api/.env.example"
      provides: "Documented Razorpay and encryption-key environment variables"
      contains: "BILLING_ENCRYPTION_KEY"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "react-native-paper PaperProvider"
      via: "provider wrapping the Expo Router Slot"
      pattern: "PaperProvider|Provider as PaperProvider"
---

<objective>
Wave 0b (D-30): install the dependencies that shipped Phase 4 code already imports but that were never declared, plus the new Phase 6 packages.

`apps/mobile/src/features/pdf/hooks/useGeneratePdf.ts` imports `expo-print`, `expo-sharing` and `expo-file-system`; `apps/mobile/src/features/patient/screens/PatientDetailScreen.tsx` imports `react-native-paper` and `@breeyo/ui`. None of the five appear in `apps/mobile/package.json`, and `expo-print` does not appear in `pnpm-lock.yaml` at all. Phase 4's consultation screen already fell back to plain React Native components for this reason (STATE.md). Phase 6 adds three more PDF templates and 40+ MD3 components on top of that broken foundation, so it is fixed here.

Purpose: BIL-04 (print/export PDF) and BIL-05 (Razorpay payment links) are unbuildable without these packages, and Expo unified versioning means a bare `pnpm add expo-print` installs the SDK-57 line against this SDK-52 app and breaks the native build.
Output: declared, SDK-52-correct mobile native modules; the Razorpay SDK on the API; a PaperProvider at the mobile root; documented environment variables; a resolution smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-invoicing-payments/06-CONTEXT.md
@.planning/phases/06-invoicing-payments/06-RESEARCH.md
@.planning/phases/06-invoicing-payments/06-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification for the two [ASSUMED] packages</name>
  <files>(no files modified — verification gate only)</files>
  <read_first>
    - .planning/phases/06-invoicing-payments/06-RESEARCH.md (`## Package Legitimacy Audit` — the slopcheck verdicts and dispositions for all eight candidate packages)
    - .planning/phases/06-invoicing-payments/06-UI-SPEC.md (lines 28-31 and `## Registry Safety` — the origin of the `react-native-qrcode-svg` recommendation, which is why it is `[ASSUMED]` rather than `[VERIFIED]`)
    - packages/ui/package.json (the `react-native-paper` version already pinned in the workspace)
  </read_first>
  <what-built>
Nothing yet — this gate runs BEFORE any package-manager install. 06-RESEARCH.md's `## Package Legitimacy Audit` ran slopcheck on 2026-08-12 with these dispositions:

- `razorpay@2.9.8` — `[VERIFIED]`, official first-party SDK (github.com/razorpay/razorpay-node), published since 2016-09-27, 438,478 weekly downloads, sole runtime dep `axios ^1.18.1`, no postinstall script. **No human verification required.**
- `react-native-svg` — `[OK]`, software-mansion, mature 15.x. **No human verification required.**
- `expo-print`, `expo-sharing`, `expo-file-system` — Expo first-party, `[OK]`. **No human verification required.**
- `react-native-qrcode-svg@^6.3.21` — **`[ASSUMED]`**. Discovered from `06-UI-SPEC.md` line 30, not from an authoritative first-party doc. slopcheck `[OK]` and peer deps verified, but registry existence is not provenance.
- `react-native-paper@5.15.3` — already declared in `packages/ui/package.json` and in the lockfile, so it is in-tree already; it is being promoted to a direct mobile dependency at the same version. Listed here for completeness.

No package carried a `[SLOP]` or `[SUS]` verdict.
  </what-built>
  <how-to-verify>
1. Open https://www.npmjs.com/package/react-native-qrcode-svg and confirm: the linked repository resolves to a real GitHub project, the weekly download count is in the tens of thousands or higher, the last publish is within the last 24 months, and the README documents a `value` + `size` prop API matching the usage in 06-RESEARCH.md's `## Code Examples -> QR rendering on device`.
2. Open https://www.npmjs.com/package/react-native-paper and confirm version `5.15.3` exists and is the version already pinned in `packages/ui/package.json`.
3. Confirm neither package declares a `postinstall` script: run `npm view react-native-qrcode-svg scripts` and `npm view react-native-paper scripts` and check the output has no `postinstall` key.
  </how-to-verify>
  <action>
Run no install command in this task. Present the audit summary above to the developer, then run the two read-only provenance checks: `npm view react-native-qrcode-svg scripts` and `npm view react-native-paper scripts`, plus `npm view react-native-qrcode-svg time.modified repository.url` to capture the last-publish date and repository URL. Report those outputs alongside the audit table and wait for the resume signal. Do not proceed to Task 2 until the developer has explicitly approved `react-native-qrcode-svg` or named a replacement. Per the phase security policy, a legitimacy checkpoint is never auto-approvable regardless of any auto-advance setting.
  </action>
  <verify>
    <automated>npm view react-native-qrcode-svg scripts time.modified repository.url &amp;&amp; npm view react-native-paper scripts version</automated>
  </verify>
  <resume-signal>Type "approved" to proceed with installation, or name a replacement QR package.</resume-signal>
  <acceptance_criteria>
    - The human has explicitly approved `react-native-qrcode-svg` or substituted an alternative, and the decision plus the observed download count and last-publish date are recorded in the plan summary.
    - `npm view react-native-qrcode-svg scripts` output contains no `postinstall` key, and the same for `react-native-paper`.
  </acceptance_criteria>
  <done>The `[ASSUMED]` QR dependency has a recorded human decision before any install command runs.</done>
</task>

<task type="auto">
  <name>Task 2: Install and declare the missing mobile native modules, then wire PaperProvider</name>
  <files>apps/mobile/package.json, apps/mobile/app/_layout.tsx, pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/package.json (all of it — note `expo: ~52.0.0`, `react-native: ^0.76.0`, `react: ^18.3.0`, and the absence of the five packages)
    - apps/mobile/app/_layout.tsx (the current root provider composition — QueryClientProvider and any auth provider are already there)
    - apps/mobile/src/features/pdf/hooks/useGeneratePdf.ts (lines 1-10 — the three expo imports that must resolve)
    - apps/mobile/src/features/patient/screens/PatientDetailScreen.tsx (lines 1-14 — the `react-native-paper` and `@breeyo/ui` imports that must resolve)
    - packages/ui/package.json (the `react-native-paper` version already pinned in the workspace — match it exactly)
    - packages/ui/src/theme/ (the Breeyo MD3 theme tokens to feed PaperProvider)
    - .planning/phases/06-invoicing-payments/06-RESEARCH.md (Pitfalls 8, 9 and 10; `## Standard Stack -> Installation`)
  </read_first>
  <action>
Run, from `apps/mobile`:

`npx expo install expo-print expo-sharing expo-file-system react-native-svg`

This is mandatory rather than `pnpm add` — Expo unified versioning means a bare add installs the SDK-57 line against this SDK-52 app (Pitfall 8). Then, from the repo root:

`pnpm --filter @breeyo/mobile add react-native-qrcode-svg@^6.3.21 react-native-paper@5.15.3`

and add `"@breeyo/ui": "workspace:*"` to `apps/mobile/package.json` dependencies (it is imported by `PatientDetailScreen.tsx` but undeclared). Do not change the `expo`, `react` or `react-native` versions. Do not upgrade any existing dependency as a side effect — if pnpm proposes one, pin instead.

Then wire the MD3 provider. In `apps/mobile/app/_layout.tsx`, import `{ Provider as PaperProvider, MD3LightTheme }` from `react-native-paper` and wrap the existing `<Slot />` / `<Stack />` tree in `<PaperProvider theme={breeyoPaperTheme}>`, where `breeyoPaperTheme` spreads `MD3LightTheme` and overrides `colors` with the Breeyo tokens already defined in `packages/ui/src/theme/`: `primary: '#2E7D32'`, `secondary: '#5D4037'`, `tertiary: '#E65100'`, `background: '#FFFBF5'`, `surface: '#FFFBF5'`, `surfaceVariant: '#F5F0EB'`, `error: '#BA1A1A'`, `onSurfaceVariant: '#49454F'`, `outline: '#CAC4D0'`. Import the token values from `@breeyo/ui` if it exports them; only inline the hex values if it does not. PaperProvider must sit inside any SafeArea/GestureHandler root and outside the router Slot so every billing screen inherits the theme.

Do not edit `.github/workflows/ci.yml` in this plan — plan 06-02 owns that file and adds the `Expo dependency check` step (`cd apps/mobile && npx expo install --check`) on this plan's behalf, so an SDK-mismatched native module fails CI rather than the native build (Pitfall 8). Record in the summary that the CI gate is owed by 06-02.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile &amp;&amp; cd apps/mobile &amp;&amp; npx expo install --check &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const d=require('./apps/mobile/package.json').dependencies; for (const k of ['expo-print','expo-sharing','expo-file-system','react-native-svg','react-native-qrcode-svg','react-native-paper','@breeyo/ui']) if(!d[k]) { console.error('MISSING '+k); process.exit(1) }"` exits 0.
    - `grep -c 'expo-print' pnpm-lock.yaml` returns a value greater than `0`.
    - `cd apps/mobile && npx expo install --check` reports no version mismatches.
    - `grep -c 'PaperProvider' apps/mobile/app/_layout.tsx` returns at least `2` (import and usage).
    - `grep -c '#2E7D32' apps/mobile/app/_layout.tsx` returns at least `1`, or the theme is imported from `@breeyo/ui` (verify with `grep -c "@breeyo/ui" apps/mobile/app/_layout.tsx` >= 1).
    - `apps/mobile/package.json` still declares `"expo": "~52.0.0"` and `"react-native": "^0.76.0"` unchanged.
  </acceptance_criteria>
  <done>All five previously-undeclared imports resolve, the two new Phase 6 packages are installed at SDK-52-compatible versions, and every screen renders under the Breeyo MD3 Paper theme.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the Razorpay SDK, document the billing environment variables, and add a PDF dependency smoke test</name>
  <files>apps/api/package.json, apps/api/.env.example, apps/mobile/src/features/pdf/__tests__/pdf-deps.test.ts, apps/mobile/vitest.config.ts, pnpm-lock.yaml</files>
  <read_first>
    - apps/api/package.json (dependency block and the `type: "module"` ESM setting)
    - apps/api/.env.example (existing variable naming style)
    - apps/mobile/src/features/pdf/templates/consultation-summary.ts (the shipped template the smoke test renders — note the `buildOwnerSummaryHtml(clinic, consultation, prescriptions, pet, owner, options)` signature)
    - apps/mobile/src/features/pdf/hooks/useGeneratePdf.ts (the `generatePdf(html, filename)` pipeline the new templates will reuse)
    - apps/mobile/vitest.config.ts (existing test environment and alias configuration)
    - packages/config/tsconfig/base.json (confirms `esModuleInterop: true`, which is what lets `import Razorpay from 'razorpay'` work under NodeNext against a CJS package)
    - .planning/phases/06-invoicing-payments/06-RESEARCH.md (`## Package Legitimacy Audit`; `## Razorpay Integration -> Razorpay credential storage`)
  </read_first>
  <behavior>
    - Test: `import Print from 'expo-print'` resolves in the mobile vitest environment (mocked native module is acceptable; a resolution failure is not).
    - Test: `buildOwnerSummaryHtml(...)` called with a minimal clinic/consultation/pet/owner fixture returns a non-empty string containing the clinic name and `<html`.
    - Test: the returned HTML escapes a clinic name containing `<script>` — assert the output contains `&lt;script&gt;` and does not contain a literal `<script>`.
  </behavior>
  <action>
Run `pnpm --filter @breeyo/api add razorpay@2.9.8`. Pin the exact version (no caret) — 06-RESEARCH.md verified this specific tarball's SDK surface (`paymentLink.create/fetch/cancel/edit`, `payments.refund`, `refunds.fetch`, static `Razorpay.validateWebhookSignature`) and confirmed it declares no `postinstall` script.

Append to `apps/api/.env.example`, following the existing naming style, with a comment on each:
- `BILLING_ENCRYPTION_KEY=` — 32-byte hex key for AES-256-GCM encryption of per-clinic Razorpay secrets at rest (D-29 / plan 06-06). Comment: generate with `openssl rand -hex 32`; rotating it invalidates every stored clinic secret.
- `RAZORPAY_TEST_KEY_ID=` and `RAZORPAY_TEST_KEY_SECRET=` — development-only fallback used when a clinic row has no stored credentials, so local BIL-05 work does not require seeding encrypted secrets.
- `RAZORPAY_WEBHOOK_DEV_TOKEN=` — the per-clinic webhook path token used by the dev tunnel (plan 06-10).

Do not add any real key value. `.env` remains gitignored.

Create `apps/mobile/src/features/pdf/__tests__/pdf-deps.test.ts` implementing the three behaviours above. Mock the native modules with `vi.mock('expo-print', ...)`, `vi.mock('expo-sharing', ...)` and `vi.mock('expo-file-system', ...)` at the top of the file — the point of the test is that the module specifiers resolve and the template function is callable, not that the native bridge works. Add whatever `resolve.alias` or `deps.inline` entry `apps/mobile/vitest.config.ts` needs for these mocks to load. This is the smoke test 06-RESEARCH.md Pitfall 9 asks for and it is the guard that keeps plan 06-15's three new templates from being written against an unresolvable dependency chain.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/mobile test -- src/features/pdf/__tests__/pdf-deps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./apps/api/package.json'); if(p.dependencies.razorpay!=='2.9.8'){console.error(p.dependencies.razorpay);process.exit(1)}"` exits 0.
    - `npm view razorpay@2.9.8 scripts.postinstall` returns empty.
    - `grep -c 'BILLING_ENCRYPTION_KEY' apps/api/.env.example` returns `1`; the same for `RAZORPAY_TEST_KEY_ID`, `RAZORPAY_TEST_KEY_SECRET` and `RAZORPAY_WEBHOOK_DEV_TOKEN`.
    - `grep -Ec '=(rzp_|[a-f0-9]{32})' apps/api/.env.example` returns `0` — no real credential value was committed.
    - `pnpm --filter @breeyo/mobile test -- src/features/pdf/__tests__/pdf-deps.test.ts` reports 3 passing tests.
    - `git status --porcelain` shows no `package-lock.json` at the repo root and no root `node_modules` addition (06-RESEARCH.md records that a prior slopcheck run created these; if present, delete them and restore with `pnpm install`).
  </acceptance_criteria>
  <done>`razorpay@2.9.8` is a pinned API dependency, the billing env contract is documented without secrets, and an automated test proves the PDF dependency chain resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repository | Third-party package code enters the build and runs with full application privilege |
| environment → process | Razorpay credentials and the at-rest encryption key enter the process from the environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | `pnpm add` / `npx expo install` of `razorpay`, `react-native-qrcode-svg`, `react-native-paper`, `react-native-svg`, `expo-print`, `expo-sharing`, `expo-file-system` | mitigate | slopcheck audit recorded in 06-RESEARCH.md `## Package Legitimacy Audit`; blocking human checkpoint (Task 1) for the single `[ASSUMED]` package before any install; `razorpay` pinned to the exact audited version `2.9.8`; postinstall-script absence asserted in acceptance criteria |
| T-06-23 | Information Disclosure | `apps/api/.env.example` | mitigate | Only variable names and generation instructions are committed; acceptance criteria greps for any value matching a credential shape and fails if found |
| T-06-24 | Denial of Service | Expo SDK version drift on native modules | mitigate | `npx expo install` used for all native modules; `npx expo install --check` added as a CI gate |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile` succeeds from a clean checkout.
- `cd apps/mobile && npx expo install --check` reports no mismatches.
- `pnpm --filter @breeyo/mobile test` passes.
- `pnpm --filter @breeyo/api exec tsc --noEmit` exits 0 and `import Razorpay from 'razorpay'` type-checks in a scratch file under `NodeNext`.
- `git diff --stat apps/mobile/package.json` shows additions only — no version changes to `expo`, `react` or `react-native`.
</verification>

<success_criteria>
- Seven previously-missing mobile dependencies are declared at SDK-52-compatible versions.
- `razorpay@2.9.8` is pinned on the API.
- The mobile app renders under a Breeyo-themed PaperProvider.
- The billing environment contract is documented with no secret values committed.
- An automated smoke test proves the PDF dependency chain resolves and escapes HTML.
</success_criteria>

<output>
Create `.planning/phases/06-invoicing-payments/06-01-SUMMARY.md` when done. Record: the exact resolved versions `npx expo install` chose for each native module, the human decision on `react-native-qrcode-svg`, and whether the Razorpay test credentials have been provisioned yet (BIL-05/BIL-06 development is blocked until they are).
</output>
