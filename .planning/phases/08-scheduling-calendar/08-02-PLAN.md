---
phase: 08-scheduling-calendar
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/theme/vetColors.ts
  - packages/ui/src/theme/index.ts
  - packages/ui/src/theme/portal.css
  - packages/ui/scripts/generate-css-tokens.ts
  - packages/ui/src/atoms/StatusBadge/StatusBadge.ts
  - packages/ui/src/atoms/StatusBadge/StatusBadge.test.ts
  - packages/ui/src/i18n/locales/en/common.json
  - packages/ui/src/i18n/locales/hi/common.json
  - packages/ui/package.json
  - apps/mobile/package.json
  - apps/web/package.json
  - apps/web/app/layout.tsx
  - pnpm-lock.yaml
autonomous: false
requirements: [SCH-02, SCH-03]

must_haves:
  truths:
    - "The five bounded vet-identity hues exist as a design token module and are emitted into portal.css as --vet-hue-1 through --vet-hue-5"
    - "StatusBadge renders an Expected variant plus checkedIn, cancelled and completed variants using only existing container colour pairs"
    - "apps/mobile declares @breeyo/ui and react-native-paper so the Phase 3/4 queue screens and every Phase 8 mobile screen can bundle"
    - "apps/web declares @breeyo/ui and socket.io-client and imports portal.css once from the root layout"
    - "A scheduling i18n namespace exists in both en and hi locale files"
  artifacts:
    - path: "packages/ui/src/theme/vetColors.ts"
      provides: "Five-hue categorical vet identity palette with a deterministic index helper"
      contains: "vetColors"
    - path: "packages/ui/src/theme/portal.css"
      provides: "CSS custom properties consumed by the web week grid, now including vet hues"
      contains: "--vet-hue-1"
    - path: "packages/ui/src/atoms/StatusBadge/StatusBadge.ts"
      provides: "expected, checkedIn, cancelled and completed status variants"
      contains: "expected:"
    - path: "apps/web/app/layout.tsx"
      provides: "Single global import of the generated design tokens"
      contains: "portal.css"
  key_links:
    - from: "packages/ui/src/theme/vetColors.ts"
      to: "packages/ui/src/theme/portal.css"
      via: "generate-css-tokens.ts emits a --vet-hue-N property per palette entry"
      pattern: "--vet-hue-"
    - from: "apps/mobile/package.json"
      to: "@breeyo/ui and react-native-paper"
      via: "workspace and registry dependencies that the already-shipped queue screens import"
      pattern: "react-native-paper"
---

<objective>
Land the design-system additions Phase 8's two UI surfaces need, and fix the blocking dependency gap that currently prevents `apps/mobile` from bundling at all.

Purpose: `08-UI-SPEC.md` § Blocking prerequisite records a verified fact — `apps/mobile/package.json` declares neither `@breeyo/ui` nor `react-native-paper`, yet `QueueScreen.tsx`, `QueueBoard.tsx` and `QueueCardItem.tsx` (shipped in Phase 3) already import both. Every mobile deliverable in Phase 8 is blocked on that fix. Separately, D-23's per-vet colour coding needs a bounded categorical palette that is deliberately not a semantic token, D-08/D-13/D-20 need four new `StatusBadge` variants, and D-25's web grid needs `portal.css` actually imported by `apps/web` (today `layout.tsx` imports nothing).
Output: a `vetColors` token module wired into the CSS generator, four new `StatusBadge` variants, a `scheduling` i18n namespace, and corrected dependency manifests for both apps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduling-calendar/08-CONTEXT.md
@.planning/phases/08-scheduling-calendar/08-UI-SPEC.md
@.planning/phases/08-scheduling-calendar/08-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the vetColors token module and emit it into portal.css</name>
  <files>packages/ui/src/theme/vetColors.ts, packages/ui/src/theme/index.ts, packages/ui/scripts/generate-css-tokens.ts, packages/ui/src/theme/portal.css</files>
  <read_first>
    - packages/ui/src/theme/colors.ts (the existing token-module export shape — a plain `const` object exported by name)
    - packages/ui/src/theme/index.ts (complete 9-line barrel — `export { colors } from './colors';`, note no `.js` extension in this package)
    - packages/ui/scripts/generate-css-tokens.ts (complete file — the `toKebabCase` helper, the per-module `for (const [key, value] of Object.entries(...))` blocks, and where the file is written)
    - packages/ui/src/theme/portal.css (the current 97-line generated output, to confirm the diff after regeneration)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Vet identity palette — the five exact hexes, their token names, the permitted-use rules, and the solo-vet hide rule)
  </read_first>
  <action>
Create `packages/ui/src/theme/vetColors.ts` exporting:

- `export const vetColors = ['#1565C0', '#6A1B9A', '#00695C', '#AD1457', '#4E342E'] as const;` — the exact five hues from UI-SPEC § Vet identity palette, in that order (vet-1 through vet-5).
- `export type VetColor = (typeof vetColors)[number];`
- `export function vetColorForIndex(index: number): VetColor` — `vetColors[((index % vetColors.length) + vetColors.length) % vetColors.length]`, so it cycles after 5 and tolerates negative input.
- `export function vetColorForId(vetId: string, sortedVetIds: readonly string[]): VetColor | null` — returns `null` when `sortedVetIds.length <= 1` (UI-SPEC: solo-vet clinics hide the rail and initials chip entirely), otherwise `vetColorForIndex(sortedVetIds.indexOf(vetId))`. Callers pass the clinic's vet ids sorted ascending by `id` so the assignment is deterministic across mobile, web and reloads.
- A file-header comment stating: these five hues are deliberately NOT semantic tokens so vet identity can never be mistaken for status, warning or CTA; permitted uses are a 4px left rail, an 8px legend dot, and the border of the vet-initials chip; never a fill, background, text colour, badge or CTA.

Add `export { vetColors, vetColorForIndex, vetColorForId } from './vetColors';` and `export type { VetColor } from './vetColors';` to `packages/ui/src/theme/index.ts`.

In `packages/ui/scripts/generate-css-tokens.ts`, add `import { vetColors } from '../src/theme/vetColors';` and, after the existing Colors block, emit a `/* Vet identity (D-23) */` comment followed by one property per entry: `--vet-hue-1` … `--vet-hue-5` (1-based index, matching UI-SPEC's token names). Do not run the array through `toKebabCase` — it is an array, not a keyed object, so use `vetColors.forEach((hex, i) => lines.push(\`  --vet-hue-${i + 1}: ${hex};\`))`.

Then regenerate: `pnpm --filter @breeyo/ui generate:css-tokens`. Commit the regenerated `portal.css` — it is checked in and `apps/web` imports it directly.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/ui generate:css-tokens &amp;&amp; grep -c -- '--vet-hue-' packages/ui/src/theme/portal.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--vet-hue-' packages/ui/src/theme/portal.css` returns exactly `5`.
    - `grep -Ec '#1565C0|#6A1B9A|#00695C|#AD1457|#4E342E' packages/ui/src/theme/portal.css` returns `5`.
    - `grep -Ev '^\s*(/\*|\*|//)' packages/ui/src/theme/vetColors.ts | grep -c 'vetColorForId' ` returns at least `1` (the helper exists outside comments).
    - Re-running `pnpm --filter @breeyo/ui generate:css-tokens` a second time produces no further git diff in `portal.css` (generation is idempotent).
    - `grep -c 'vetColors' packages/ui/src/theme/index.ts` returns at least `1`.
    - `pnpm --filter @breeyo/ui exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>`vetColors` is a first-class theme module, exported from the theme barrel, and emitted into `portal.css` as five `--vet-hue-N` custom properties available to the web grid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend StatusBadge with expected, checkedIn, cancelled and completed variants and add the scheduling i18n namespace</name>
  <files>packages/ui/src/atoms/StatusBadge/StatusBadge.ts, packages/ui/src/atoms/StatusBadge/StatusBadge.test.ts, packages/ui/src/i18n/locales/en/common.json, packages/ui/src/i18n/locales/hi/common.json</files>
  <read_first>
    - packages/ui/src/atoms/StatusBadge/StatusBadge.ts (complete file — the `StatusVariant` union at lines 8-16, the `STATUS_CONFIG: Record<StatusVariant, StatusConfigEntry>` table, and the render-time resolution `colors[config.bgColor] || config.bgColor` which is why `bgColor`/`textColor` must be theme-token KEY NAMES, never hex literals)
    - packages/ui/src/atoms/StatusBadge/StatusBadge.test.ts (the existing assertions — a `Record<StatusVariant, ...>` addition surfaces here immediately)
    - packages/ui/src/theme/colors.ts (confirm `secondaryContainer`, `onSecondaryContainer`, `primaryContainer`, `onPrimaryContainer`, `surfaceVariant`, `onSurfaceVariant` all exist as keys before referencing them)
    - packages/ui/src/i18n/locales/en/common.json and packages/ui/src/i18n/locales/hi/common.json (the existing namespace structure, e.g. the `queue` and `portal` namespaces, so `scheduling` is added at the same nesting level)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ StatusBadge extension table for the exact four variants, labels and token pairs; § Copywriting Contract for every string that goes into the i18n namespace)
  </read_first>
  <behavior>
    - Test "every StatusVariant has a config entry": `Object.keys(STATUS_CONFIG).sort()` equals the sorted `StatusVariant` union members, now including `expected`, `checkedIn`, `cancelled` and `completed`.
    - Test "expected uses the calm secondary container": `STATUS_CONFIG.expected.bgColor` is `'secondaryContainer'`, `.textColor` is `'onSecondaryContainer'`, `.defaultLabel` is `'Expected'`.
    - Test "checkedIn uses the primary container pair": `bgColor` `'primaryContainer'`, `textColor` `'onPrimaryContainer'`, label `'Checked in'`.
    - Test "cancelled and completed both use the neutral surface pair": both have `bgColor` `'surfaceVariant'` and `textColor` `'onSurfaceVariant'`, with labels `'Cancelled'` and `'Completed'`.
    - Test "no config entry uses a hex literal": for every entry, neither `bgColor` nor `textColor` matches `/^#/`.
  </behavior>
  <action>
**`StatusBadge.ts`** — append `| 'expected' | 'checkedIn' | 'cancelled' | 'completed'` to the `StatusVariant` union and add the four matching `STATUS_CONFIG` entries exactly as UI-SPEC § StatusBadge extension specifies:

- `expected: { defaultLabel: 'Expected', bgColor: 'secondaryContainer', textColor: 'onSecondaryContainer' }` — D-08/D-13. The calm brown container is deliberate: `expected` must read as quieter than `waiting` (tertiary orange, "act on me now") and must not read as an error.
- `checkedIn: { defaultLabel: 'Checked in', bgColor: 'primaryContainer', textColor: 'onPrimaryContainer' }` — D-20.
- `cancelled: { defaultLabel: 'Cancelled', bgColor: 'surfaceVariant', textColor: 'onSurfaceVariant' }`.
- `completed: { defaultLabel: 'Completed', bgColor: 'surfaceVariant', textColor: 'onSurfaceVariant' }`.

Zero new colours are introduced — all four reuse existing container pairs. Do not add a variant for `SCHEDULED`: UI-SPEC's status-badge rule is that `SCHEDULED` renders no badge at all, because presence on the calendar already communicates "scheduled".

Extend `StatusBadge.test.ts` with the five behaviors above.

**i18n** — add a `scheduling` namespace to both `packages/ui/src/i18n/locales/en/common.json` and `.../hi/common.json`, at the same nesting level as the existing namespaces. Populate it from UI-SPEC § Copywriting Contract, using these key groups and the UI-SPEC copy verbatim as the `en` values:
- `actions`: `bookAppointment` ("Book Appointment"), `newAppointment` ("New Appointment"), `confirmBooking` ("Confirm Booking"), `discardBooking` ("Discard Booking"), `moveAppointment` ("Move Appointment"), `checkInNow` ("Check In Now"), `editAvailability` ("Edit Availability"), `blockTime` ("Block Time"), `turnOnAlerts` ("Turn On Alerts"), `tryAgain` ("Try Again"), `keepAppointment` ("Keep Appointment"), `cancelAppointment` ("Cancel Appointment"), `keepInQueue` ("Keep in Queue"), `markNoShow` ("Mark No-show"), `bookAnyway` ("Book Anyway"), `pickAnotherTime` ("Pick Another Time"), `removeBlockedTime` ("Remove"), `keepBlockedTime` ("Keep Blocked Time").
- `empty`: one key per row of UI-SPEC § Empty states, each with `title` and `body`, using `{{name}}`, `{{date}}`, `{{service}}` i18next interpolation placeholders where UI-SPEC shows `{name}`/`{date}`/`{service}`.
- `errors`: one key per row of UI-SPEC § Error states.
- `warnings`: `doubleBooking`, `recurrenceTruncated`, `pastSlot`.
- `toasts`: one key per row of UI-SPEC § In-app toasts.
- `push`: `startingSoon.title`/`.body`, `ownerCancelled.title`/`.body`, `moveRequested.title`/`.body`, `queueBacklog.title`/`.body`.
- `labels`: `screenTitle` ("Schedule"), `allVets` ("All Vets"), `today` ("Today"), `morning`/`afternoon`/`evening`, `expectedAt` ("Expected {{time}}").

Hindi values may initially mirror the English strings (UI-SPEC § Accessibility explicitly permits this) but every key MUST exist in `hi/common.json` with the identical key path — a missing key is a runtime fallback bug, not a translation gap.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/ui test -- --run src/atoms/StatusBadge/StatusBadge.test.ts &amp;&amp; node -e "const en=require('./packages/ui/src/i18n/locales/en/common.json'),hi=require('./packages/ui/src/i18n/locales/hi/common.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&amp;&amp;v?flat(v,p+k+'.'):[p+k]);const a=flat(en.scheduling||{}).sort(),b=flat(hi.scheduling||{}).sort();if(a.length===0){console.error('no scheduling namespace');process.exit(1)}if(JSON.stringify(a)!==JSON.stringify(b)){console.error('key mismatch');process.exit(1)}console.log('i18n scheduling keys:',a.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/ui test -- --run src/atoms/StatusBadge/StatusBadge.test.ts` passes and includes the five new assertions.
    - `grep -Ec "expected:|checkedIn:|cancelled:|completed:" packages/ui/src/atoms/StatusBadge/StatusBadge.ts` returns at least `4`.
    - `grep -c "'scheduled'" packages/ui/src/atoms/StatusBadge/StatusBadge.ts` returns `0` — `SCHEDULED` deliberately has no badge variant.
    - `node -e "const c=require('./packages/ui/src/atoms/StatusBadge/StatusBadge.ts')" ` is NOT used; instead the vitest assertion above proves `STATUS_CONFIG` completeness.
    - The i18n verify command prints a non-zero key count and exits 0, proving `en` and `hi` have identical `scheduling` key paths.
    - `grep -c 'bgColor: .#' packages/ui/src/atoms/StatusBadge/StatusBadge.ts` returns `0` — every colour is a theme-token key name, never a hex.
    - `pnpm --filter @breeyo/ui test` passes in full (no regression in the other 25 components).
  </acceptance_criteria>
  <done>`StatusBadge` supports the four new lifecycle variants using only existing container pairs, and a complete `scheduling` i18n namespace exists with matching key paths in both locales.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Fix the mobile and web dependency manifests, import portal.css, and verify the three registry packages</name>
  <files>apps/mobile/package.json, apps/web/package.json, apps/web/app/layout.tsx, pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/package.json (confirm for yourself that neither `@breeyo/ui` nor `react-native-paper` nor `react-native-safe-area-context` is declared, and record the current dependency list in the summary)
    - apps/web/package.json (complete file — today exactly `next`, `react`, `react-dom`, `@breeyo/types`)
    - apps/web/app/layout.tsx (complete 16-line file — it currently imports nothing)
    - packages/ui/package.json (confirm `react-native-paper@^5.15.1` and `react-native-safe-area-context@^5.7.0` are the versions already declared there, so `apps/mobile` pins the same versions rather than diverging)
    - apps/mobile/src/features/queue/screens/QueueScreen.tsx (lines 1-10 — the `react-native-paper` and `@breeyo/ui` imports that currently cannot resolve)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Blocking prerequisite, § Registry Safety)
    - .planning/phases/08-scheduling-calendar/08-RESEARCH.md (§ Package Legitimacy Audit — `socket.io-client@^4.8.3` disposition and rationale)
  </read_first>
  <action>
Automate everything first, then pause for the human gate.

From the repo root run `pnpm --filter @breeyo/mobile add @breeyo/ui@workspace:* react-native-paper@^5.15.1 react-native-safe-area-context@^5.7.0` and `pnpm --filter @breeyo/web add @breeyo/ui@workspace:* socket.io-client@^4.8.3`. Pin `react-native-paper` and `react-native-safe-area-context` at the exact versions already declared in `packages/ui/package.json`, and `socket.io-client` at the exact version already declared in `apps/mobile/package.json`, so no new supplier and no new version is introduced.

Add `import '@breeyo/ui/src/theme/portal.css';` as the FIRST line of `apps/web/app/layout.tsx`. Next.js App Router permits a global CSS import only from a layout or a page, and the root layout is the single correct place so it loads exactly once for the whole app. Keep the existing `metadata` export and the `html` and `body` structure unchanged, and do not add `'use client'` to that file.

Then run `pnpm --filter @breeyo/mobile exec tsc --noEmit` and `pnpm --filter @breeyo/web build` and record their output, plus `git diff pnpm-lock.yaml` for the human to inspect.

Do NOT add any calendar library, icon package, date library or data-layer library. UI-SPEC section Registry Safety and RESEARCH section Alternatives Considered both forbid it, and D-25 locks the web approach to plain React plus `portal.css`. If you believe one is needed, stop and escalate rather than installing it.

Then pause for approval.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/web build &amp;&amp; test -d apps/mobile/node_modules/@breeyo/ui &amp;&amp; test -d apps/mobile/node_modules/react-native-paper</automated>
    <human-check>The five registry and runtime checks in how-to-verify below</human-check>
  </verify>
  <done>`apps/mobile` resolves `@breeyo/ui` and `react-native-paper` from its own `node_modules`, `apps/web` resolves `socket.io-client` and `@breeyo/ui`, `portal.css` is imported once from the web root layout, the web build exits 0, and a human has approved the lockfile diff.</done>
  <what-built>
Ran, from the repo root:

```
pnpm --filter @breeyo/mobile add @breeyo/ui@workspace:* react-native-paper@^5.15.1 react-native-safe-area-context@^5.7.0
pnpm --filter @breeyo/web add @breeyo/ui@workspace:* socket.io-client@^4.8.3
```

Added `import '@breeyo/ui/src/theme/portal.css';` as the first line of `apps/web/app/layout.tsx` (Next.js App Router permits a global CSS import only from a layout or page; the root layout is the single correct place, and it is imported exactly once for the whole app).

Confirmed `pnpm --filter @breeyo/mobile exec tsc --noEmit` resolves `react-native-paper` and `@breeyo/ui` from `apps/mobile/node_modules`, and that `pnpm --filter @breeyo/web build` succeeds with the CSS import present.

Did NOT add any calendar library, icon package or date library to `apps/web` — UI-SPEC § Registry Safety and RESEARCH § Alternatives Considered both forbid it (D-25 locks plain React plus `portal.css`).
  </what-built>
  <how-to-verify>
This checkpoint is blocking because it installs packages from a public registry. `workflow.auto_advance` does not apply — a human must approve before the phase continues.

1. Two of the three registry packages are already load-bearing in this monorepo at the exact pinned versions, so they are pre-vetted, not new suppliers:
   - `react-native-paper@^5.15.1` — already declared in `packages/ui/package.json`.
   - `react-native-safe-area-context@^5.7.0` — already declared in `packages/ui/package.json`.
   - `socket.io-client@^4.8.3` — already declared in `apps/mobile/package.json` at the identical version; RESEARCH § Package Legitimacy Audit disposition is **Approved**.
   Confirm each of the three by opening its registry page and checking the publisher and weekly download count:
   - https://www.npmjs.com/package/react-native-paper
   - https://www.npmjs.com/package/react-native-safe-area-context
   - https://www.npmjs.com/package/socket.io-client
2. Run `git diff pnpm-lock.yaml | grep -E '^\+ +[a-z@]' | head -40` and confirm no unexpected new top-level package appears beyond these three and their transitive dependencies.
3. Run `pnpm --filter @breeyo/web build` and confirm it exits 0 and the build output includes a CSS asset.
4. Run `pnpm --filter @breeyo/web dev` and open http://localhost:3001 — confirm the page background renders as the warm white `#FFFBF5` (proving `portal.css` is actually loaded, not merely imported).
5. Confirm `apps/mobile/node_modules/@breeyo/ui` and `apps/mobile/node_modules/react-native-paper` now exist.

Reply `approved` if all five checks pass, or describe what failed.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -Ec '"@breeyo/ui"|"react-native-paper"|"react-native-safe-area-context"' apps/mobile/package.json` returns `3`.
    - `grep -Ec '"@breeyo/ui"|"socket.io-client"' apps/web/package.json` returns `2`.
    - `grep -c 'portal.css' apps/web/app/layout.tsx` returns `1`.
    - `test -d apps/mobile/node_modules/@breeyo/ui && test -d apps/mobile/node_modules/react-native-paper` exits 0.
    - `pnpm --filter @breeyo/web build` exits 0.
    - `pnpm --filter @breeyo/mobile exec tsc --noEmit` produces no "Cannot find module 'react-native-paper'" and no "Cannot find module '@breeyo/ui'" errors (pre-existing unrelated type errors, if any, are recorded in the summary rather than fixed here).
    - `grep -Ec 'fullcalendar|react-big-calendar|date-fns|luxon|moment|lucide|react-icons' apps/web/package.json` returns `0` — no forbidden UI or date dependency was introduced.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public npm registry → repo dependency graph | Three registry packages are installed into two app manifests |
| generated `portal.css` → web browser | A checked-in generated stylesheet is served to every web client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | `pnpm add` of `react-native-paper`, `react-native-safe-area-context`, `socket.io-client` | mitigate | All three are already load-bearing elsewhere in this monorepo at the identical pinned versions (`packages/ui/package.json`, `apps/mobile/package.json`), so no new supplier is introduced. Task 3 is a blocking human checkpoint that is never auto-approvable; the lockfile diff is inspected for unexpected additions and the manifests are grep-gated against any calendar, icon or date library |
| T-08-05 | Information Disclosure | vet identity hues used as a data channel | accept | `vetColors` encodes only "which of this clinic's vets" by index within an already-authorized clinic view; the palette carries no cross-tenant information and UI-SPEC pairs every hue with visible initials so it is never colour-alone |
| T-08-06 | Tampering | `portal.css` regenerated by hand instead of by the generator | mitigate | Task 1 requires `pnpm --filter @breeyo/ui generate:css-tokens` and asserts a second run produces no further diff; the file header already states "do not edit manually" |
</threat_model>

<verification>
- `pnpm --filter @breeyo/ui test` passes in full.
- `pnpm --filter @breeyo/ui generate:css-tokens` is idempotent (second run yields no git diff).
- `pnpm --filter @breeyo/web build` exits 0.
- `pnpm install --frozen-lockfile` succeeds from a clean `node_modules` (proves the lockfile is consistent with the three manifest edits).
</verification>

<success_criteria>
- `--vet-hue-1` through `--vet-hue-5` are present in `portal.css` with the five UI-SPEC hexes.
- `StatusBadge` accepts `expected`, `checkedIn`, `cancelled` and `completed` and every config entry uses theme-token key names.
- `apps/mobile` resolves `@breeyo/ui` and `react-native-paper` from its own `node_modules`, unblocking every Phase 8 mobile plan.
- `apps/web` resolves `socket.io-client` and `@breeyo/ui`, and `portal.css` is imported exactly once from the root layout.
- `scheduling` i18n keys exist with identical paths in `en` and `hi`.
- No calendar library, icon package or date library was added anywhere.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-calendar/08-02-SUMMARY.md` when done. Record the pre-fix `apps/mobile` dependency list, the exact installed versions of the three registry packages, and any pre-existing mobile type errors observed (so plans 08-08, 08-12 and 08-13 know what is theirs versus inherited).
</output>
