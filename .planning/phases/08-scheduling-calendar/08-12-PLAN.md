---
phase: 08-scheduling-calendar
plan: "12"
type: execute
wave: 7
depends_on: ["08-02", "08-11"]
files_modified:
  - apps/mobile/src/features/scheduling/hooks/useSchedule.ts
  - apps/mobile/src/features/scheduling/hooks/useAppointmentActions.ts
  - apps/mobile/src/features/scheduling/hooks/useScheduleSocket.ts
  - apps/mobile/src/features/scheduling/store/scheduleUIStore.ts
  - apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx
  - apps/mobile/src/features/scheduling/components/DateNavigator.tsx
  - apps/mobile/src/features/scheduling/components/VetFilterBar.tsx
  - apps/mobile/src/features/scheduling/components/AppointmentRow.tsx
  - apps/mobile/src/features/scheduling/components/NowIndicator.tsx
  - apps/mobile/src/features/scheduling/components/AppointmentQuickSheet.tsx
  - apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx
  - apps/mobile/src/features/scheduling/lib/agenda-utils.ts
  - apps/mobile/src/features/scheduling/lib/__tests__/agenda-utils.test.ts
  - apps/mobile/app/(app)/(tabs)/schedule.tsx
  - apps/mobile/app/(app)/(tabs)/_layout.tsx
autonomous: true
requirements: [SCH-01, SCH-03, SCH-04]

must_haves:
  truths:
    - "A vet can open a Schedule tab and see the day's appointments grouped into Morning, Afternoon and Evening with date navigation"
    - "A vet can book an appointment from the phone by entering a mobile number, picking pets, a service, a vet, a date and a slot -- the same owner/pet mobile-number lookup used at check-in (D-19)"
    - "An already-taken slot is shown with a warning rather than hidden, and booking it requires an explicit override"
    - "A remote appointment change updates the agenda without moving the scroll position or closing an open sheet"
    - "Blocked periods appear on the agenda as non-interactive bands"
    - "Moving a single occurrence of a recurring series shows the vet that it has detached from the series, matching the server-side D-31 behavior"
  artifacts:
    - path: "apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx"
      provides: "The D-24 mobile day agenda"
      contains: "Schedule"
    - path: "apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx"
      provides: "The seven-step booking flow mirroring CheckInSheet"
      contains: "Confirm Booking"
    - path: "apps/mobile/src/features/scheduling/lib/agenda-utils.ts"
      provides: "Pure time-of-day grouping and slot formatting helpers"
      exports: ["groupAppointmentsByTimeOfDay"]
    - path: "apps/mobile/app/(app)/(tabs)/schedule.tsx"
      provides: "The Schedule tab route"
      contains: "DayAgendaScreen"
  key_links:
    - from: "apps/mobile/src/features/scheduling/hooks/useScheduleSocket.ts"
      to: "apps/mobile/src/features/scheduling/hooks/useSchedule.ts"
      via: "socket events invalidate the schedule query key by clinic prefix"
      pattern: "invalidateQueries"
    - from: "apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx"
      to: "apps/api POST /api/v1/scheduling/appointments"
      via: "useAppointmentActions createAppointment mutation with allowDoubleBook retry"
      pattern: "allowDoubleBook"
---

<objective>
Build the mobile scheduling surface: the data layer, the D-24 day agenda, and the booking flow.

Purpose: D-24 locks the mobile primary view as a scrollable day agenda rather than a dense week grid, because PROJECT.md's core constraint is one-handed mobile-first use by a solo vet. D-19 requires booking to reuse the exact owner-and-pet mobile-number lookup the Phase 3 check-in sheet already implements, which `08-PATTERNS.md` identifies as a near-verbatim analog with one deliberate divergence (multi-select for D-21). D-14's allow-with-warning behaviour is a direct structural copy of Phase 3's `SAME_DAY_RECHECK` retry pattern.
Output: three hooks, a Zustand UI store, six components, a pure agenda-utils module with tests, and the Schedule tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduling-calendar/08-CONTEXT.md
@.planning/phases/08-scheduling-calendar/08-PATTERNS.md
@.planning/phases/08-scheduling-calendar/08-UI-SPEC.md
@.planning/phases/08-scheduling-calendar/08-02-SUMMARY.md
@.planning/phases/08-scheduling-calendar/08-08-SUMMARY.md
@.planning/phases/08-scheduling-calendar/08-11-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the scheduling data layer — query hook, mutation hooks, socket hook, UI store and pure agenda utilities</name>
  <files>apps/mobile/src/features/scheduling/hooks/useSchedule.ts, apps/mobile/src/features/scheduling/hooks/useAppointmentActions.ts, apps/mobile/src/features/scheduling/hooks/useScheduleSocket.ts, apps/mobile/src/features/scheduling/store/scheduleUIStore.ts, apps/mobile/src/features/scheduling/lib/agenda-utils.ts, apps/mobile/src/features/scheduling/lib/__tests__/agenda-utils.test.ts</files>
  <read_first>
    - apps/mobile/src/features/queue/hooks/useQueue.ts (complete 29-line file — the `useQuery` shape with `queryKey: ['queue', activeClinicId, today]`, `enabled: !!accessToken && !!activeClinicId`, `staleTime: 30_000`, `refetchOnWindowFocus`, `refetchOnReconnect`, `select: (response) => response.data`, and the local `formatDate` helper)
    - apps/mobile/src/features/queue/hooks/useQueueSocket.ts (lines 9-31 and 48-66 — the `io(API_URL, { auth: { token: accessToken }, transports: ['websocket'], reconnection: true, reconnectionAttempts: Infinity, ... })` config, prefix invalidation on `queryKey: ['queue', activeClinicId]`, the connect/connect_error/disconnect handlers driving `setOffline`, and the deep subpath import `'@breeyo/types/constants/socket-events'`)
    - apps/mobile/src/features/queue/hooks/useQueueActions.ts (lines 26-78 — the optimistic `onMutate`/`onError`/`onSettled` sequence and the deliberate 300ms `onSettled` invalidation delay that avoids a flicker race with the Socket.IO broadcast)
    - apps/mobile/src/features/queue/store/queueUIStore.ts (complete 19-line file — the minimal Zustand store shape; server data never goes here)
    - apps/mobile/src/lib/api.ts (lines 1-46 — `apiClient` with its `token` option and the `ApiClientError` carrying `code` and `status`)
    - .planning/phases/08-scheduling-calendar/08-11-SUMMARY.md (the endpoint table — exact paths, methods and response envelopes)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Mobile day agenda for the Morning/Afternoon/Evening boundaries; § Real-time sync for the non-disruptive remote-update rules)
  </read_first>
  <behavior>
    - Test "groupAppointmentsByTimeOfDay splits at the right boundaries": an 11:59 appointment is Morning, 12:00 is Afternoon, 16:59 is Afternoon, 17:00 is Evening.
    - Test "empty groups are omitted": with only afternoon appointments, the result contains exactly one group titled `Afternoon`.
    - Test "appointments are ordered ascending within a group".
    - Test "groups are ordered Morning then Afternoon then Evening" regardless of input order.
    - Test "formatSlotRange renders the UI-SPEC time format": a 14:30 start with a 15-minute duration renders `2:30 – 2:45 PM` using `en-IN` with `hour12`.
    - Test "isPastOnToday marks only past appointments on today's date": an appointment earlier today is past, the same clock time on a future date is not, and any appointment on a past date is not marked (the whole day is historical, so dimming individual rows adds nothing).
    - Test "splitIndexForNowIndicator returns the boundary between the last past and first future appointment", and returns null when the selected date is not today.
  </behavior>
  <action>
**`agenda-utils.ts`** — pure functions, no React, no React Native imports, so they are unit-testable exactly the way `apps/mobile/src/lib/wizard-utils.ts` already is in this repo:
- `export function groupAppointmentsByTimeOfDay(appointments: AppointmentWithDetails[]): Array<{ title: 'Morning' | 'Afternoon' | 'Evening'; data: AppointmentWithDetails[] }>` — boundaries from UI-SPEC: Morning is before 12:00, Afternoon is 12:00 to before 17:00, Evening is 17:00 onward, evaluated in IST. Omit empty groups entirely.
- `export function formatSlotRange(scheduledFor: Date, durationMinutes: number): string` — `en-IN`, `hour12: true`, en dash with surrounding spaces, matching `QueueCardItem.formatTime` and the shared `formatMinutesRange` in `@breeyo/types`. Prefer importing the shared helper over reimplementing it; only wrap it if the argument shapes differ.
- `export function isPastOnToday(scheduledFor: Date, selectedDate: Date): boolean`
- `export function splitIndexForNowIndicator(appointments: AppointmentWithDetails[], selectedDate: Date, now: Date): number | null`

**`useSchedule.ts`** — copy `useQueue.ts` exactly in shape. `useSchedule(selectedDate: Date, vetId?: string)` with `queryKey: ['schedule', activeClinicId, isoDate, vetId ?? 'all']` calling `GET /api/v1/scheduling/appointments?from=&to=&vetId=`, where `from` is the selected IST day start and `to` is the next day start. Keep `staleTime: 30_000`, `refetchOnWindowFocus`, `refetchOnReconnect`, `enabled` on both token and clinic, and `select: (r) => r.data`. The clinic-then-date key order matters because the socket hook invalidates by the `['schedule', activeClinicId]` prefix.
Also export `useResolvedAvailability(date, vetId)` hitting `GET /api/v1/scheduling/availability/resolved` (the agenda renders blocked bands from it), `useOfferableSlots(vetId, date, serviceCatalogId)` hitting `GET /api/v1/scheduling/slots` with `enabled` only once all three are set, and `useClinicVets()` hitting `GET /api/v1/scheduling/vets` with a long `staleTime` since the vet list rarely changes.

**`useAppointmentActions.ts`** — React Query mutations copying `useQueueActions.ts`'s structure:
- `useCreateAppointment()` — `POST /api/v1/scheduling/appointments`. No optimistic update (the server assigns the id and may return warnings). On success invalidate the `['schedule', activeClinicId]` prefix and return the full `{ appointments, warnings }` payload so the sheet can render the D-22 truncation toast.
- `useRescheduleAppointment()`, `useCancelAppointment()`, `useUpdateAppointmentStatus()` — optimistic where the change is a simple field flip, each with the `onError` rollback and the `setTimeout(..., 300)` `onSettled` invalidation. That 300ms delay is a hard-won fix for the optimistic-write-versus-broadcast flicker race — carry it over rather than reinventing it.
- Every mutation surfaces `ApiClientError.code` to its caller unchanged, because the booking sheet branches on `SLOT_DOUBLE_BOOKED` to offer the override.

**`useScheduleSocket.ts`** — port `useQueueSocket.ts` line for line, changing only the event subscriptions to `APPOINTMENT_CREATED`, `APPOINTMENT_UPDATED`, `APPOINTMENT_CANCELLED` and `AVAILABILITY_UPDATED`, each invalidating the `['schedule', activeClinicId]` prefix. Keep `auth: { token: accessToken }`, `transports: ['websocket']`, the reconnection options, the `setOffline` handlers, and the deep subpath import for `SOCKET_EVENTS`. Drive `isOffline` on the new `scheduleUIStore` rather than the queue store.

**`scheduleUIStore.ts`** — a Zustand store with `isOffline`, `selectedDate: Date`, `vetFilter: string | null` (D-23, null meaning All Vets), and their setters plus a `goToToday()` helper. Server data never goes in Zustand — React Query owns it.

Write `agenda-utils.test.ts` covering the seven behaviors with plain object fixtures.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/mobile test -- --run src/features/scheduling/lib/__tests__/agenda-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/mobile test -- --run src/features/scheduling/lib/__tests__/agenda-utils.test.ts` reports at least 7 passing tests.
    - `grep -Ec 'react|react-native' apps/mobile/src/features/scheduling/lib/agenda-utils.ts` returns `0` — the utils are framework-free.
    - `grep -c "queryKey: \['schedule', activeClinicId" apps/mobile/src/features/scheduling/hooks/useSchedule.ts` returns at least `1`.
    - `grep -c "auth: { token: accessToken }" apps/mobile/src/features/scheduling/hooks/useScheduleSocket.ts` returns `1` and `grep -c "transports: \['websocket'\]" apps/mobile/src/features/scheduling/hooks/useScheduleSocket.ts` returns `1`.
    - `grep -Ec 'APPOINTMENT_CREATED|APPOINTMENT_UPDATED|APPOINTMENT_CANCELLED|AVAILABILITY_UPDATED' apps/mobile/src/features/scheduling/hooks/useScheduleSocket.ts` returns at least `4`.
    - `grep -c "@breeyo/types/constants/socket-events" apps/mobile/src/features/scheduling/hooks/useScheduleSocket.ts` returns `1` (the deep subpath import that avoids pulling the barrel into RN).
    - `grep -c '300' apps/mobile/src/features/scheduling/hooks/useAppointmentActions.ts` returns at least `1` — the flicker-race delay was carried over.
    - `grep -Ec 'appointments|data\[' apps/mobile/src/features/scheduling/store/scheduleUIStore.ts` returns `0` — no server data in Zustand.
    - `pnpm --filter @breeyo/mobile exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The mobile scheduling data layer mirrors the proven queue layer: one query hook family keyed for prefix invalidation, mutations that preserve the anti-flicker delay and surface error codes, a socket hook on the same authenticated connection, a UI-only store, and pure grouping helpers under test.</done>
</task>

<task type="auto">
  <name>Task 2: Build the day agenda screen with date navigation, vet filter, appointment rows, blocked bands and the now indicator</name>
  <files>apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx, apps/mobile/src/features/scheduling/components/DateNavigator.tsx, apps/mobile/src/features/scheduling/components/VetFilterBar.tsx, apps/mobile/src/features/scheduling/components/AppointmentRow.tsx, apps/mobile/src/features/scheduling/components/NowIndicator.tsx, apps/mobile/app/(app)/(tabs)/schedule.tsx, apps/mobile/app/(app)/(tabs)/_layout.tsx</files>
  <read_first>
    - apps/mobile/src/features/queue/screens/QueueScreen.tsx (all 230 lines — the hook order with the socket hook called first for its side effect, the three explicit render states, `useCallback` on every handler, the FAB with `customSize={56}` and its `fabDisabled` offline treatment, the `showToast('success', ...)` feedback, and the hard-coded hex palette at lines 194-229: `#FFFBF5` background, `#2E7D32` FAB, `#1C1B1F` title, `#49454F` secondary text, `#CAC4D0` disabled)
    - apps/mobile/src/features/queue/components/QueueBoard.tsx (the `SectionList` with `stickySectionHeadersEnabled={false}`, `contentContainerStyle: { paddingBottom: 100 }` for FAB clearance, and `onRefresh`/`refreshing` wiring)
    - apps/mobile/src/features/queue/components/QueueCardItem.tsx (lines 47-111 and 194-229 — the 44px species avatar with `SPECIES_ICONS`, the flex-1 info column, the right-side rail, `minHeight: 80`, the emergency left border, and the full `accessibilityLabel` construction)
    - apps/mobile/src/features/queue/components/QueueSectionHeader.tsx (the section-header component to reuse for Morning/Afternoon/Evening)
    - apps/mobile/app/(app)/(tabs)/_layout.tsx (complete file, two tabs today — the `Tabs.Screen` shape with `title` and `tabBarIcon` using `MaterialCommunityIcons`)
    - apps/mobile/app/(app)/(tabs)/patients.tsx (the one-line tab route that just renders a screen component)
    - packages/ui/src/theme/vetColors.ts (as written by plan 08-02 — `vetColorForId(vetId, sortedVetIds)` returns null for a solo-vet clinic)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Appointment card/block anatomy for the fixed nine-element order; § Mobile day agenda for the screen order, section boundaries, past-row dimming, blocked-period bands and swipe actions; § Color for the accent and vet-hue rules; § Typography for the three permitted sizes; § Copywriting for every label and empty state; § Accessibility for the 44px targets and label format)
    - .planning/phases/08-scheduling-calendar/08-08-SUMMARY.md (the route string the queue board's View Appointment action pushes to — this task must register a matching route)
  </read_first>
  <action>
**`DateNavigator.tsx`** — `‹ Tue, 18 Aug ›` with 44px `IconButton` arrows (`accessibilityLabel` `Previous day` and `Next day`), the centre label tappable to open the native date picker, and a `Today` chip shown only when the selected date is not today. Dates render as `Tue, 18 Aug` per UI-SPEC. Today's date number renders in `#2E7D32`.

**`VetFilterBar.tsx`** — a horizontal `Chip` row from `@breeyo/ui` with `All Vets` first and selected by default (D-23). Each vet chip carries an 8px dot in `vetColorForId(vetId, sortedVetIds)` plus the vet's name. Render nothing at all when the clinic has one vet — `vetColorForId` returns null in that case and UI-SPEC hides the rail and chip entirely for the solo-vet majority.

**`AppointmentRow.tsx`** — the nine-element anatomy from UI-SPEC in fixed order: 4px vet rail (hidden for solo-vet), time as `2:30 – 2:45 PM` in 12px `#49454F`, pet name in 16px weight 500 `#1C1B1F` with `numberOfLines={1}`, owner name in 16px weight 400 `#5D4037`, service line as `Vaccination · 15 min` in 12px, a `+{n} more pets` caption with a `paw` icon when the appointment covers more than one pet (D-21, names live in the sheet not the row), the vet-initials chip (hidden for solo-vet), the right-aligned `StatusBadge` OMITTED for `SCHEDULED` (UI-SPEC's noise-reduction rule — presence on the calendar already says scheduled), and a 12px `repeat` icon next to the time when `recurringSeriesId` is set. Card is `#FFFBF5`, `borderRadius: 12`, 16px padding, `minHeight: 80`, matching `QueueCardItem`. A `CANCELLED` appointment renders at `opacity: 0.5` with the pet name struck through. `accessibilityLabel` is `{time}, {pet}, {owner}, {service}, with Dr. {vet}, {status}`.

**`NowIndicator.tsx`** — a 2px `#2E7D32` rule with an 8px dot, rendered only when the selected date is today, positioned using `splitIndexForNowIndicator`.

**`DayAgendaScreen.tsx`** — call `useScheduleSocket()` first for its side effect, then `useSchedule(selectedDate, vetFilter)` and `useResolvedAvailability(selectedDate, vetFilter)`. Render in the UI-SPEC order: `Schedule` title (28px, 16px horizontal and top padding, matching `QueueScreen.title`), `OfflineBanner`, `DateNavigator`, `VetFilterBar`, the agenda `SectionList`, then the `Book Appointment` FAB (`plus-circle` icon, `customSize={56}`, `#2E7D32`, `borderRadius: 16`, 16px offset, disabled with `#CAC4D0` when offline exactly like `QueueScreen`'s `fabDisabled`).
- Sections come from `groupAppointmentsByTimeOfDay`; headers reuse `QueueSectionHeader`, none collapsible.
- Past appointments on today's date render at `opacity: 0.6`.
- Blocked periods from `useResolvedAvailability` render as full-width 44px `#F5F0EB` bands with caption text `Lunch · 1:00 – 2:00 PM` and no interaction.
- Pull-to-refresh via `onRefresh`/`refreshing`, `contentContainerStyle: { paddingBottom: 100 }`.
- Implement the three explicit render states exactly as `QueueScreen` does: loading shows a `SkeletonLoader` with four 80px rows, error shows `Could not load the calendar. Pull down to try again.`, and empty shows the `EmptyState` `No appointments today` / `Tap Book Appointment to schedule one, or open Queue for walk-ins.` Use the filtered-empty and vet-not-working variants from UI-SPEC when a vet filter is active or the resolved hours are null.
- Tapping a row opens `AppointmentQuickSheet` (Task 3). Swipe left reveals `Cancel Appointment` (destructive) and swipe right reveals `Check In`; both actions must also be reachable through the tap sheet, never gesture-only (UI-SPEC § Accessibility).
- Add a header `IconButton` (`calendar-clock`) navigating to the availability route plan 08-13 creates. Use a plain path string so it resolves once that plan lands in the same wave.
- Every handler is `useCallback`-wrapped. Every destructive action goes through `Alert.alert` with the safe option first and named (`Keep Appointment`, never a bare `Cancel`).
- Wrap all user-facing strings in the `scheduling` i18n namespace added by plan 08-02.

**`apps/mobile/app/(app)/(tabs)/schedule.tsx`** — a one-line route rendering `<DayAgendaScreen />`, mirroring `patients.tsx`. Accept and forward the `appointmentId` and `date` query parameters so the push deep links from plan 08-09 and the queue board's View Appointment action from plan 08-08 land correctly.

**`apps/mobile/app/(app)/(tabs)/_layout.tsx`** — add a `schedule` tab with title `Schedule` and the `calendar-month` `MaterialCommunityIcons` glyph. Place it after the existing Queue tab: PROJECT.md and ROADMAP's cross-cutting constraints keep the walk-in queue as the mobile home surface, with Scheduling linked but separate.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/mobile exec tsc --noEmit &amp;&amp; pnpm --filter @breeyo/mobile test -- --run src/features/scheduling</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/mobile exec tsc --noEmit` exits 0 and `pnpm --filter @breeyo/mobile test -- --run src/features/scheduling` passes.
    - `grep -c 'useScheduleSocket()' apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx` returns `1` and it appears before the `useSchedule(` call (verify by line number).
    - `grep -Ec 'isLoading|isError' apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx` returns at least `2` — all three render states are explicit.
    - `grep -c 'paddingBottom: 100' apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx` returns `1` (FAB clearance, matching the queue board).
    - `grep -c 'Book Appointment' apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx` returns at least `1` and `grep -Ec '>Add<|>New<|>Submit<' apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx` returns `0`.
    - `grep -c 'Keep Appointment' apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx` returns at least `1` and `awk "/Alert.alert/,/\]\)/" apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx | grep -c "text: 'Cancel'"` returns `0`.
    - `grep -c 'vetColorForId' apps/mobile/src/features/scheduling/components/AppointmentRow.tsx` returns at least `1` and `grep -Ec '#1565C0|#6A1B9A|#00695C|#AD1457|#4E342E' apps/mobile/src/features/scheduling/components/AppointmentRow.tsx` returns `0` — vet hues come from the token module, never inlined.
    - `awk "/StatusBadge/,/\/>/" apps/mobile/src/features/scheduling/components/AppointmentRow.tsx | grep -Ec "SCHEDULED"` returns at least `1` (the omit-for-scheduled guard is present).
    - `grep -c 'minHeight: 80' apps/mobile/src/features/scheduling/components/AppointmentRow.tsx` returns `1` and `grep -c 'borderRadius: 12' apps/mobile/src/features/scheduling/components/AppointmentRow.tsx` returns at least `1`.
    - `grep -c 'calendar-month' apps/mobile/app/\(app\)/\(tabs\)/_layout.tsx` returns `1`, and the `schedule` `Tabs.Screen` appears after the queue tab.
    - `grep -c 'accessibilityLabel' apps/mobile/src/features/scheduling/components/AppointmentRow.tsx` returns at least `1` and `grep -c 'accessibilityLabel' apps/mobile/src/features/scheduling/components/DateNavigator.tsx` returns at least `2`.
  </acceptance_criteria>
  <done>A Schedule tab shows the day's appointments grouped by time of day with date navigation, an optional vet filter that disappears for solo-vet clinics, blocked-time bands, a now indicator on today, all four screen states, and a primary Book Appointment FAB that disables offline.</done>
</task>

<task type="auto">
  <name>Task 3: Build the booking sheet and the appointment quick-action sheet</name>
  <files>apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx, apps/mobile/src/features/scheduling/components/AppointmentQuickSheet.tsx, apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx</files>
  <read_first>
    - apps/mobile/src/features/queue/components/CheckInSheet.tsx (all 304 lines — this IS the D-19 lookup flow booking must reuse: `formatMobile`/`extractDigits` at lines 19-27, the `/^[6-9]\d{9}$/` validation, `useLookupOwner(mobile)` wiring and the `ownerNotFound` derivation at lines 29-49, the reset-on-close `useEffect` at 51-58, the `BottomSheet` with `visible={visible && !showReasonPicker}` sibling-sheet trick at 153-159 and 232-238, the single-pet `handlePetTap` at 64-67, and the `error.code === 'SAME_DAY_RECHECK'` retry-with-override Alert at 83-114)
    - apps/mobile/src/features/patient/hooks/usePatientRegister.ts (the `useLookupOwner` hook and the inline-registration mutation the sheet reuses when the owner is not found)
    - apps/mobile/src/features/scheduling/hooks/useSchedule.ts and useAppointmentActions.ts (as written in Task 1 — `useOfferableSlots`, `useClinicVets`, `useCreateAppointment` and the `ApiClientError.code` surfacing)
    - apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx (as written in Task 2 — the FAB and row-tap handlers this task wires to the two sheets)
    - packages/types/src/constants/scheduling.constants.ts (`BOOKING_HORIZON_DAYS`, `RecurrenceInterval`, `RECURRENCE_MIN_OCCURRENCES`, `RECURRENCE_MAX_OCCURRENCES`)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Booking flow — all eight steps with their exact field configuration, the progressive-disclosure rule, the taken-slot styling, the collapsed recurrence accordion and its live preview; § Warnings for the double-booking strip copy and its two buttons; § Copywriting for `Confirm Booking` and `Discard Booking`; § Destructive confirmations for the cancel dialogs; § In-app toasts for every success message)
    - .planning/phases/08-scheduling-calendar/08-11-SUMMARY.md (the create endpoint's response envelope including the `warnings` array)
    - .planning/phases/08-scheduling-calendar/08-CONTEXT.md (D-02, D-07, D-14, D-19, D-21, D-22)
  </read_first>
  <action>
**`BookAppointmentSheet.tsx`** — copy `CheckInSheet.tsx`'s structure and chain the steps with its sibling-sheet trick. Progressive disclosure: each step appears only once the previous resolves, and the slot grid is never rendered before a service is chosen (show the caption `Pick a service to see open slots.`).

1. **Owner lookup (D-19)** — reuse `formatMobile`, `extractDigits`, the `/^[6-9]\d{9}$/` test and `useLookupOwner` verbatim from `CheckInSheet`. Field config: `keyboardType="phone-pad"`, `maxLength={11}` for the formatted value, `phone` left icon, placeholder `Enter 10-digit mobile number`. Not found offers a `New patient` row driving the same inline registration `CheckInSheet` uses.
2. **Pet multi-select (D-21)** — this is the one deliberate divergence from the analog. Hold a `Set<string>` of pet ids instead of a single `selectedPet`, render each of the owner's pets with its species icon and a checkbox affordance, and require at least one. Update the confirm-enabled condition accordingly.
3. **Service (D-02)** — `ListItem` rows from the service catalog showing `{service} · {n} min`. Selecting one sets the slot duration; the duration is not separately editable, because the server snapshots it from the catalog.
4. **Vet (D-04)** — a `Chip` row from `useClinicVets()`, pre-selected when the agenda's vet filter is active, hidden entirely for a solo-vet clinic.
5. **Date** — the native date picker capped at today plus `BOOKING_HORIZON_DAYS`. Dates past the cap are disabled, not hidden, so the constraint is legible.
6. **Slot** — a chip grid from `useOfferableSlots(vetId, date, serviceCatalogId)`. Already-taken slots are SHOWN, not hidden (D-14, RESEARCH anti-pattern A4): style them `#F5F0EB` with a 12px `alert-circle-outline` in `#E65100` and `accessibilityLabel` `{time}, already booked`. Selecting one reveals the warning strip in step 8. While loading show an `ActivityIndicator` with the caption `Finding open slots…`.
7. **Repeat (D-22, collapsed by default)** — an accordion titled `Repeat this appointment` containing `Every week` / `Every 2 weeks` / `Every 4 weeks` and a `Number of times` stepper bounded by `RECURRENCE_MIN_OCCURRENCES` and `RECURRENCE_MAX_OCCURRENCES`, with a live caption `{n} appointments through {last date}`.
8. **Confirm** — `Confirm Booking`, full-width `#2E7D32`, disabled until owner, at least one pet, service, vet, date and slot are all set. The paired dismiss button is `Discard Booking`, a text button in `#49454F` to its left — never a bare `Cancel`, because in a sheet that is itself about an appointment, "Cancel" is ambiguous. Enters a loading state with the label unchanged; the sheet stays open until success, then closes and toasts.

Double-booking behaviour, structurally identical to `CheckInSheet`'s `SAME_DAY_RECHECK` handling: submit without `allowDoubleBook`; on `error.code === 'SLOT_DOUBLE_BOOKED'` render the inline `#FFE0B2` warning strip with `#BF360C` text, a 16px `alert-circle-outline` in `#E65100`, the copy `Dr. {name} already has {pet} at {time}. You can still book this slot.`, and two buttons `Book Anyway` (which re-submits with `allowDoubleBook: true`) and `Pick Another Time` (which clears the slot selection). Never a blocking modal and never a hard block.

On success show `showToast('success', ...)` with the UI-SPEC copy: `{pet} booked for {time}, {date}` for a single booking, `{n} appointments booked for {pet}` for a series, and additionally an `info` toast `Only {n} of {total} repeats fit within 90 days. The rest were not created.` when the response `warnings` contains `RECURRENCE_TRUNCATED`. Interpolate the 90 from `BOOKING_HORIZON_DAYS` rather than hard-coding it (UI-SPEC Open Item 6).

Render other server errors inline using the message the API returned — `SLOT_TAKEN`, `BOOKING_HORIZON_EXCEEDED`, `SLOT_BLOCKED` and `VET_NOT_AVAILABLE` all already carry UI-SPEC copy from the server, so do not restate them client-side.

**`AppointmentQuickSheet.tsx`** — a `BottomSheet` opened by tapping an agenda row, titled with the pet name. Shows the time range, owner, service, vet, every pet on the appointment (D-21 names live here, not on the row), and the recurrence marker when present. Actions: `Check In Now` (primary, calls the status mutation with `CHECKED_IN`), `Move Appointment` (opens the date-and-slot steps of the booking sheet in reschedule mode), `Cancel Appointment` (destructive, guarded by `Alert.alert` titled `Cancel this appointment?` with body `{pet} at {time} on {date} will be cancelled. {owner} gets a WhatsApp update.` and buttons `Keep Appointment` then `Cancel Appointment`), and for a series member the three-way dialog `Keep All` / `Cancel This One` / `Cancel All {n}`. When the appointment being moved is a member of a recurring series (`recurringSeriesId` is non-null), `Move Appointment` does not offer a "move the whole series" choice — per D-22/D-31 that capability does not exist, only single-occurrence moves are supported from this sheet — and after a successful reschedule show an informational toast noting the occurrence has been detached from its series, e.g. `showToast('info', 'This visit is now separate from its repeating series.')`, since D-31 makes moving a single occurrence detach it from the series server-side and the UI must make that consequence visible rather than silent. When `recurringSeriesId` is null, no such toast appears and the move behaves exactly as already described.

Wire both sheets into `DayAgendaScreen`: the FAB opens the booking sheet, a row tap opens the quick sheet, and both are rendered as siblings with reset-on-close effects.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/mobile exec tsc --noEmit &amp;&amp; pnpm --filter @breeyo/mobile test -- --run src/features/scheduling</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/mobile exec tsc --noEmit` exits 0 and `pnpm --filter @breeyo/mobile test -- --run src/features/scheduling` passes.
    - `grep -c 'useLookupOwner' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `1` — the D-19 lookup is reused, not re-derived.
    - `grep -Ec 'Set<string>|new Set' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `1` (D-21 multi-select).
    - `grep -c 'SLOT_DOUBLE_BOOKED' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `1` and `grep -c 'allowDoubleBook: true' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `1`.
    - `grep -Ec 'Book Anyway|Pick Another Time' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns `2`.
    - `grep -c 'Confirm Booking' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `1` and `grep -c 'Discard Booking' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `1`.
    - `grep -Ec '>Submit<|>OK<|>Save<|text: .Cancel.' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx apps/mobile/src/features/scheduling/components/AppointmentQuickSheet.tsx` returns `0`.
    - `grep -c 'BOOKING_HORIZON_DAYS' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `2` (the date cap and the truncation copy interpolation) and `grep -c '90' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns `0`.
    - `grep -c 'already booked' apps/mobile/src/features/scheduling/components/BookAppointmentSheet.tsx` returns at least `1` — taken slots are rendered and labelled, not hidden.
    - `grep -c 'Keep Appointment' apps/mobile/src/features/scheduling/components/AppointmentQuickSheet.tsx` returns at least `1` and `grep -c 'Cancel All' apps/mobile/src/features/scheduling/components/AppointmentQuickSheet.tsx` returns at least `1`.
    - `grep -Ec 'BookAppointmentSheet|AppointmentQuickSheet' apps/mobile/src/features/scheduling/screens/DayAgendaScreen.tsx` returns at least `4` (both imported and both rendered).
    - `grep -Ec 'now separate from its repeating series|detached from its series' apps/mobile/src/features/scheduling/components/AppointmentQuickSheet.tsx` returns at least `1`.
  </acceptance_criteria>
  <done>A vet can book an appointment for several of an owner's pets from the phone using the same lookup as check-in, sees taken slots flagged rather than hidden and can override them explicitly, can set a bounded weekly repeat with a live preview, and can check in, move or cancel an existing appointment from a quick sheet with named safe options on every destructive dialog. Moving a series member additionally surfaces an informational toast noting it has detached from its series (D-31).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → scheduling API | Every booking, reschedule and cancel originates from a staff phone |
| Socket.IO connection → clinic room | The mobile access token authorizes a live feed of a clinic's appointments |
| local optimistic cache → rendered schedule | A locally applied change is shown before the server confirms it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-61 | Elevation of Privilege | client-side validation being treated as the booking constraint | mitigate | Every rule the sheet renders (horizon cap, taken-slot flag, blocked bands, pet ownership) is re-enforced server-side by plan 08-07 and asserted by plan 08-11's integration tests; the client's date cap and disabled confirm button are affordances, not controls |
| T-08-62 | Information Disclosure | the schedule query returning another clinic's appointments | mitigate | The request carries only the access token and a date range; `clinicId` comes from the JWT server-side, and the React Query key is namespaced by `activeClinicId` so a clinic switch cannot serve a stale cross-clinic cache |
| T-08-63 | Spoofing | the Socket.IO connection joining a room it should not | mitigate | The client passes `auth: { token: accessToken }` and the server derives the room from the verified JWT (`socket.ts:34-60`), rejecting non-access tokens; the client never names a room |
| T-08-64 | Repudiation | an accidental swipe cancelling an appointment with no confirmation | mitigate | Every destructive action routes through `Alert.alert` with the safe option first and named (`Keep Appointment`), swipe actions are duplicated in the tap sheet so no destructive action is gesture-only, and the server audit-logs the cancellation |
| T-08-65 | Tampering | a stale optimistic write persisting after a failed mutation | mitigate | The `onError` rollback restores the previous cache and the 300ms `onSettled` invalidation reconciles with the server; the socket hook additionally invalidates on every remote change |
| T-08-66 | Availability | a remote update yanking the viewport or closing a half-completed booking form | mitigate | UI-SPEC's rule is carried into the socket hook: remote changes invalidate the query but never reset scroll position, close a sheet or clear an in-progress form; a remotely cancelled appointment open in a sheet shows an inline notice and disables its actions rather than auto-closing |
</threat_model>

<verification>
- `pnpm --filter @breeyo/mobile test -- --run src/features/scheduling` passes.
- `pnpm --filter @breeyo/mobile test` passes in full (no regression in the queue feature from the shared type changes).
- `pnpm --filter @breeyo/mobile exec tsc --noEmit` exits 0.
- Visual and interaction verification is deferred to plan 08-15's end-to-end human checkpoint.
</verification>

<success_criteria>
- A Schedule tab exists after the Queue tab and shows the day's appointments grouped Morning, Afternoon, Evening with date navigation and an optional vet filter.
- All four screen states (empty, loading, populated, error) are implemented with UI-SPEC copy.
- Booking reuses the Phase 3 mobile-number lookup, supports multiple pets, derives duration from the service, caps the date at the shared horizon constant, shows taken slots flagged rather than hidden, and requires an explicit override to double-book.
- A bounded weekly repeat can be set with a live preview and reports truncation via an info toast.
- Every destructive dialog names what its safe option preserves; no generic button label appears anywhere.
- Remote changes refresh the agenda without disturbing scroll position or an open sheet.
- Moving a single occurrence of a recurring series surfaces an informational toast that it has detached from the series, matching the server-side D-31 behavior.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-calendar/08-12-SUMMARY.md` when done. Record: the exported hook names and their query keys, the `scheduleUIStore` shape, the route path registered for the Schedule tab and how deep-link parameters are consumed, the availability route path the header button navigates to (plan 08-13 must match it), and any UI-SPEC element that could not be implemented with the available `@breeyo/ui` components.
</output>
