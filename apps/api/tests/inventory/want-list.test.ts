import { describe, it } from 'vitest';

// INV-07: Want-list generation and WhatsApp sharing
describe('Want-list (INV-07)', () => {
  it.todo('generates a want-list of items below par level');
  it.todo('excludes items without a par level from the want-list (D-06)');
  it.todo('computes deficit (parLevel - currentStock) per item');
  it.todo('generates a plain-text WhatsApp-formatted want-list (D-24, D-28)');
});
