---
phase: 08-scheduling-calendar
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/scheduling.ts
  - packages/types/src/constants/scheduling.constants.ts
  - packages/types/src/constants/queue-status.ts
  - packages/types/src/constants/socket-events.ts
  - packages/types/src/constants/index.ts
  - packages/types/src/notification.ts
  - packages/types/src/queue.ts
  - packages/types/src/index.ts
  - packages/validators/src/scheduling.ts
  - packages/validators/src/queue.ts
  - packages/validators/src/index.ts
  - packages/types/src/constants/__tests__/scheduling.constants.test.ts
  - packages/validators/src/__tests__/scheduling.test.ts
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03, SCH-04, SCH-05]

must_haves:
  truths:
    - "Appointment lifecycle SCHEDULED to CHECKED_IN to COMPLETED plus CANCELLED and NO_SHOW is expressed as one shared transition table that both API and both client surfaces import"
    - "EXPECTED is a first-class queue status in the shared enum, the shared transition table and the shared queue status validator"
    - "The booking horizon, no-show grace window, starting-soon push lead, queue backlog threshold and backlog debounce window each exist exactly once as a named shared constant"
    - "HH:MM to minutes-from-midnight conversion and weekday label to index conversion exist exactly once and are unit tested"
    - "Every scheduling write request shape is a Zod schema in @breeyo/validators with an inferred type export"
  artifacts:
    - path: "packages/types/src/constants/scheduling.constants.ts"
      provides: "AppointmentStatus, APPOINTMENT_TRANSITIONS, BlockedPeriodReason, tunable scheduling constants, time and weekday converters"
      contains: "APPOINTMENT_TRANSITIONS"
    - path: "packages/types/src/scheduling.ts"
      provides: "Appointment, AppointmentPet, AppointmentWithDetails, VetAvailabilityTemplate, AvailabilityOverride, BlockedPeriod, SlotOption entity types"
      contains: "export interface Appointment"
    - path: "packages/validators/src/scheduling.ts"
      provides: "Zod schemas for appointment create/reschedule/cancel, availability template/override, blocked period, slot and range queries"
      contains: "createAppointmentSchema"
    - path: "packages/types/src/constants/queue-status.ts"
      provides: "EXPECTED queue status plus its transitions"
      contains: "EXPECTED"
  key_links:
    - from: "packages/types/src/constants/queue-status.ts"
      to: "packages/validators/src/queue.ts"
      via: "the queue status z.enum must list every QueueStatus value including EXPECTED"
      pattern: "'EXPECTED'"
    - from: "packages/types/src/queue.ts QueueBoard"
      to: "apps/api getQueueBoard response"
      via: "new expected group consumed by the mobile queue board"
      pattern: "expected:"
---

<objective>
Create the shared contract layer every other Phase 8 plan imports: appointment lifecycle types and transition table, the `EXPECTED` queue status extension, new Socket.IO event keys, the `MOVE_REQUEST` notification type, all tunable scheduling constants, the single source of truth for HH:MM/minutes and weekday conversions, and every scheduling Zod request schema.

Purpose: PATTERNS.md is explicit that the appointment transition table belongs in `@breeyo/types`, not in the API module, so mobile and web render lifecycle state from the same table the server validates against (D-20). UI-SPEC Open Item 6 requires the `90 days`, `15 min`, `5 patients` and `30 min` copy values to be interpolated from named constants rather than hard-coded twice. Every downstream plan (API services, mobile hooks, web grid) fails to compile without this plan.
Output: two new files in `@breeyo/types`, one new file in `@breeyo/validators`, five modified shared files, and unit tests for the converters and schemas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduling-calendar/08-CONTEXT.md
@.planning/phases/08-scheduling-calendar/08-RESEARCH.md
@.planning/phases/08-scheduling-calendar/08-PATTERNS.md
@.planning/phases/08-scheduling-calendar/08-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scheduling.constants.ts with the appointment transition table, blocked-period reasons, tunable constants and the single set of time/weekday converters</name>
  <files>packages/types/src/constants/scheduling.constants.ts, packages/types/src/constants/index.ts, packages/types/src/constants/__tests__/scheduling.constants.test.ts</files>
  <read_first>
    - packages/types/src/constants/queue-status.ts (complete file — this is the exact structural template: enum, `Record<Enum, Enum[]>` transition table, `isValidTransition`, `*_LABELS` record)
    - packages/types/src/constants/index.ts (the barrel; note the ESM `.js` extension on every re-export)
    - apps/mobile/src/lib/wizard-utils.ts (the existing `DAYS_OF_WEEK` Monday-first string convention and `"09:00"` time-string format that must be reconciled with the new 0=Sunday integer weekday convention — PATTERNS.md § AvailabilitySettingsScreen "Caution — two conflicting conventions")
    - .planning/phases/08-scheduling-calendar/08-CONTEXT.md (D-06 reason catalog, D-07 horizon, D-09 grace, D-20 lifecycle, D-22 recurrence)
    - .planning/phases/08-scheduling-calendar/08-RESEARCH.md (§ Assumptions Log A1-A4 for the exact recommended constant values)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Open Items for the Planner item 6 — the constants that appear verbatim in user-facing copy)
  </read_first>
  <behavior>
    - Test "appointment transitions": `isValidAppointmentTransition('SCHEDULED','CHECKED_IN')` is true; `('SCHEDULED','CANCELLED')` true; `('SCHEDULED','NO_SHOW')` true; `('CHECKED_IN','COMPLETED')` true; `('CHECKED_IN','CANCELLED')` false; `('COMPLETED','CANCELLED')` false; `('CANCELLED','SCHEDULED')` false; `('NO_SHOW','CHECKED_IN')` false.
    - Test "every status has a label": `Object.keys(APPOINTMENT_STATUS_LABELS)` equals `Object.values(AppointmentStatus)`.
    - Test "hhmmToMinutes": `'09:00'` is 540, `'00:00'` is 0, `'23:59'` is 1439, `'12:30'` is 750; `'24:00'` and `'9:0'` and `'abc'` each throw.
    - Test "minutesToHHMM": 540 is `'09:00'`, 0 is `'00:00'`, 1439 is `'23:59'`, 750 is `'12:30'`; -1 and 1440 each throw.
    - Test "round trip": for every multiple of 15 in 0..1425, `hhmmToMinutes(minutesToHHMM(m)) === m`.
    - Test "weekday conversion": `weekdayIndexFromLabel('Sunday')` is 0, `('Saturday')` is 6, `('Monday')` is 1; `WEEKDAY_LABELS[0]` is `'Sunday'`; `WEEKDAY_LABELS.length` is 7.
    - Test "formatMinutesRange": `formatMinutesRange(780, 795)` returns `'1:00 PM – 1:15 PM'` (en-IN, hour12, en dash) matching the UI-SPEC time format.
  </behavior>
  <action>
Create `packages/types/src/constants/scheduling.constants.ts` copying the structure of `queue-status.ts` exactly (plain enums, `Record`-typed tables, no classes, no imports from outside `@breeyo/types`).

Export, in this order:

1. `export enum AppointmentStatus { SCHEDULED = 'SCHEDULED', CHECKED_IN = 'CHECKED_IN', COMPLETED = 'COMPLETED', CANCELLED = 'CANCELLED', NO_SHOW = 'NO_SHOW' }` — D-20 exactly, with no `CONFIRMED` value (D-20 states `SCHEDULED` already implies confirmed).
2. `export const APPOINTMENT_TRANSITIONS: Record<AppointmentStatus, AppointmentStatus[]>` with `SCHEDULED -> [CHECKED_IN, CANCELLED, NO_SHOW]`, `CHECKED_IN -> [COMPLETED, NO_SHOW]`, `COMPLETED -> []`, `CANCELLED -> []`, `NO_SHOW -> []`.
3. `export function isValidAppointmentTransition(from: AppointmentStatus, to: AppointmentStatus): boolean` using `APPOINTMENT_TRANSITIONS[from]?.includes(to) ?? false`.
4. `export const APPOINTMENT_STATUS_LABELS: Record<AppointmentStatus, string>` — `Scheduled`, `Checked in`, `Completed`, `Cancelled`, `No show` (UI-SPEC § StatusBadge extension labels).
5. `export enum BlockedPeriodReason { LUNCH = 'LUNCH', BREAK = 'BREAK', PERSONAL = 'PERSONAL', OFF_SITE = 'OFF_SITE', MEETING = 'MEETING', OTHER = 'OTHER' }` plus `BLOCKED_PERIOD_REASON_LABELS` (`Lunch`, `Break`, `Personal`, `Off-site`, `Meeting`, `Other`) — D-06.
6. `export enum AppointmentSource { STAFF = 'STAFF', WHATSAPP = 'WHATSAPP' }` — D-12 needs to distinguish a staff booking from a formalized WhatsApp booking.
7. `export enum RecurrenceInterval { WEEKLY = 'WEEKLY', FORTNIGHTLY = 'FORTNIGHTLY', FOUR_WEEKLY = 'FOUR_WEEKLY' }` plus `RECURRENCE_INTERVAL_DAYS: Record<RecurrenceInterval, number>` = 7 / 14 / 28, and `RECURRENCE_MAX_OCCURRENCES = 12`, `RECURRENCE_MIN_OCCURRENCES = 2` — D-22 and UI-SPEC § Booking flow step 7 stepper range 2–12.
8. Tunable constants, each with a one-line comment naming its decision and its RESEARCH assumption id:
   - `export const BOOKING_HORIZON_DAYS = 90;` (D-07, RESEARCH discretion recommendation)
   - `export const NO_SHOW_GRACE_MINUTES = 20;` (D-09, RESEARCH A1)
   - `export const STARTING_SOON_LEAD_MINUTES = 15;` (D-27 trigger 1, RESEARCH A3)
   - `export const QUEUE_BACKLOG_THRESHOLD = 5;` (D-27 trigger 3, RESEARCH Open Question 2)
   - `export const QUEUE_BACKLOG_DEBOUNCE_MINUTES = 30;` (RESEARCH A4)
   - `export const SCHEDULING_SWEEP_CRON = '*/5 * * * *';` and `export const SCHEDULING_TIMEZONE = 'Asia/Kolkata';` (RESEARCH Pattern 2, A2)
   - `export const DEFAULT_SERVICE_DURATION_MINUTES = 15;` (the `ServiceCatalog.durationMinutes` default that plan 08-03 writes into the schema — keep the two in sync)
   - `export const APPOINTMENT_REMINDER_TOUCHES = ['ADVANCE', 'ON_DATE'] as const;` (D-18)
9. `export const WEEKDAY_LABELS = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'] as const;` and `export type WeekdayLabel = (typeof WEEKDAY_LABELS)[number];` and `export function weekdayIndexFromLabel(label: WeekdayLabel): number`. Index 0 is Sunday, matching the `Date.getUTCDay()`/Postgres convention the availability model uses.
10. `export function hhmmToMinutes(value: string): number` — accept only `/^([01]\d|2[0-3]):([0-5]\d)$/`; throw `new Error('Invalid HH:MM time: ' + value)` otherwise. `export function minutesToHHMM(minutes: number): string` — throw for `!Number.isInteger(minutes) || minutes < 0 || minutes > 1439`; zero-pad both parts.
11. `export function formatMinutesRange(startMinutes: number, endMinutes: number, date?: Date): string` — render `h:mm A – h:mm A` using `toLocaleTimeString('en-IN', { hour: 'numeric', minute: '2-digit', hour12: true })` against a date anchored at IST midnight plus the given minutes, joined by an en dash with surrounding spaces. This is the format `QueueCardItem.formatTime` already uses and UI-SPEC § Copywriting mandates.

Add `export * from './scheduling.constants.js';` to `packages/types/src/constants/index.ts`.

Do not add any date library. Do not duplicate `hhmmToMinutes` logic anywhere else in the monorepo — plan 08-13's settings screen and plan 08-05's availability service both import from here.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/types test -- --run src/constants/__tests__/scheduling.constants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/types test -- --run src/constants/__tests__/scheduling.constants.test.ts` reports at least 7 passing tests covering every behavior above.
    - `grep -c 'BOOKING_HORIZON_DAYS = 90' packages/types/src/constants/scheduling.constants.ts` returns `1`.
    - `grep -Ec 'NO_SHOW_GRACE_MINUTES|STARTING_SOON_LEAD_MINUTES|QUEUE_BACKLOG_THRESHOLD|QUEUE_BACKLOG_DEBOUNCE_MINUTES|SCHEDULING_SWEEP_CRON|SCHEDULING_TIMEZONE' packages/types/src/constants/scheduling.constants.ts` returns at least `6`.
    - `grep -c "from './scheduling.constants.js'" packages/types/src/constants/index.ts` returns `1`.
    - `grep -c 'CONFIRMED' packages/types/src/constants/scheduling.constants.ts` returns `0` (D-20 has no confirmed state).
    - `pnpm --filter @breeyo/types exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A single shared module holds the appointment lifecycle table, the blocked-period reason catalog, every tunable scheduling constant, and the only HH:MM/weekday converters in the monorepo, all unit tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EXPECTED to the queue status contract, add scheduling socket events, add MOVE_REQUEST notification type, and add scheduling entity types</name>
  <files>packages/types/src/constants/queue-status.ts, packages/types/src/constants/socket-events.ts, packages/types/src/notification.ts, packages/types/src/queue.ts, packages/types/src/scheduling.ts, packages/types/src/index.ts</files>
  <read_first>
    - packages/types/src/constants/queue-status.ts (complete file — the enum, `QUEUE_TRANSITIONS`, `isValidTransition`, `QUEUE_STATUS_LABELS`)
    - packages/types/src/constants/socket-events.ts (complete 7-line file)
    - packages/types/src/notification.ts (complete file — `NotificationType` already has `APPOINTMENT_REMINDER` and `QUEUE_CHANGE`, `NotificationModule` already has `SCHEDULING`; do NOT re-add either)
    - packages/types/src/queue.ts (complete file — `QueueEntry`, `QueueEntryWithPet`, `QueueBoard`, `CheckInInput`; note nullables are `| null`, not `?`)
    - packages/types/src/index.ts (the barrel)
    - .planning/phases/08-scheduling-calendar/08-PATTERNS.md (§ `appointment.state.ts` for the three synchronized EXPECTED edit sites; § `packages/types/src/scheduling.ts` for the entity-type conventions)
    - .planning/phases/08-scheduling-calendar/08-CONTEXT.md (D-08, D-09, D-11, D-13, D-21)
  </read_first>
  <behavior>
    - Test "EXPECTED transitions": `isValidTransition(QueueStatus.EXPECTED, QueueStatus.WAITING)` is true (D-11 early check-in); `(EXPECTED, NO_SHOW)` is true (D-09 grace expiry); `(EXPECTED, IN_CONSULT)` is false (a patient must become WAITING first); `(EXPECTED, DONE)` is false; `(WAITING, EXPECTED)` is false (never goes backwards).
    - Test "EXPECTED has a label": `QUEUE_STATUS_LABELS[QueueStatus.EXPECTED]` equals `'Expected'`.
    - Test "existing transitions unchanged": `(WAITING, IN_CONSULT)`, `(IN_CONSULT, DONE)`, `(WAITING, NO_SHOW)`, `(IN_CONSULT, NO_SHOW)` all remain true and `(DONE, *)` remains false for every target.
  </behavior>
  <action>
**`packages/types/src/constants/queue-status.ts`** — add `EXPECTED = 'EXPECTED'` as the first member of `QueueStatus` (D-08). Add `[QueueStatus.EXPECTED]: [QueueStatus.WAITING, QueueStatus.NO_SHOW]` to `QUEUE_TRANSITIONS` and leave every existing entry untouched. Add `[QueueStatus.EXPECTED]: 'Expected'` to `QUEUE_STATUS_LABELS`. Add `export const CLOSED_QUEUE_STATUSES = [QueueStatus.DONE, QueueStatus.NO_SHOW] as const;` and `export const ACTIVE_QUEUE_STATUSES = [QueueStatus.EXPECTED, QueueStatus.WAITING, QueueStatus.IN_CONSULT] as const;` so plan 08-04 replaces its inline `status: { in: [...] }` string arrays with these named lists instead of scattering literals across `queue.repository.ts`.

**`packages/types/src/constants/socket-events.ts`** — append `APPOINTMENT_CREATED: 'appointment:created'`, `APPOINTMENT_UPDATED: 'appointment:updated'`, `APPOINTMENT_CANCELLED: 'appointment:cancelled'`, `AVAILABILITY_UPDATED: 'availability:updated'`. Keep the `as const` and the existing five keys unchanged.

**`packages/types/src/notification.ts`** — add `MOVE_REQUEST = 'MOVE_REQUEST'` to `NotificationType` (D-16 staff task; PATTERNS.md § push-trigger.service records that `Notification.type` is a plain `String` column so this is a type-only change). Do not touch `NotificationModule` — `SCHEDULING` already exists. Do not touch `NotificationEvent`.

**`packages/types/src/queue.ts`** — add `queuePriorityAt: Date;` and `appointmentId: string | null;` to `QueueEntry`, and add `expected: QueueEntryWithPet[];` as the FIRST field of `QueueBoard` (D-13 orders the Expected section first). Every consumer of `QueueBoard` must be updated in the same wave chain — plan 08-04 owns the API side, plan 08-08 owns the mobile side.

**`packages/types/src/scheduling.ts`** (new) — mirror `queue.ts` conventions exactly (`import type` from `./constants/scheduling.constants.js`, nullables as `| null`, no Zod, no Prisma types). Export:
- `interface VetAvailabilityTemplate { id; clinicId; vetId; weekday: number; isClosed: boolean; openMinutes: number | null; closeMinutes: number | null }`
- `interface AvailabilityOverride { id; clinicId; vetId; date: Date; isClosed: boolean; openMinutes: number | null; closeMinutes: number | null; reason: string | null }`
- `interface BlockedPeriod { id; clinicId; vetId; date: Date; startMinutes: number; endMinutes: number; reason: BlockedPeriodReason; reasonText: string | null }`
- `interface ResolvedDayHours { openMinutes: number; closeMinutes: number }` and `type ResolvedDayAvailability = ResolvedDayHours | null` (null means closed).
- `interface SlotOption { startMinutes: number; endMinutes: number; isDoubleBooked: boolean }` — the `isDoubleBooked` flag is the D-14 warning signal; slots are flagged, never omitted.
- `interface AppointmentPetRef { id; petId; queueEntryId: string | null; pet: { id: string; name: string; species: string } }`
- `interface Appointment { id; clinicId; vetId; ownerId; serviceCatalogId: string | null; status: AppointmentStatus; source: AppointmentSource; scheduledFor: Date; durationMinutes: number; recurringSeriesId: string | null; recurrenceIndex: number | null; notes: string | null; createdById; whatsappBookingRequestId: string | null; checkedInAt: Date | null; cancelledAt: Date | null; cancelledById: string | null; cancelReason: string | null; completedAt: Date | null; createdAt: Date; updatedAt: Date }`
- `interface AppointmentWithDetails extends Appointment { pets: AppointmentPetRef[]; owner: { id: string; name: string; mobile: string }; vet: { id: string; name: string }; service: { id: string; name: string; durationMinutes: number } | null }` — this is the exact shape both the mobile day agenda and the web week grid render, per RESEARCH Pattern 4 (one read API, two views).
- `interface ScheduleRange { from: Date; to: Date; vetId?: string }`

Add `export * from './scheduling.js';` to `packages/types/src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/types test -- --run src/constants/__tests__ &amp;&amp; pnpm --filter @breeyo/types exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'EXPECTED' packages/types/src/constants/queue-status.ts` returns at least `4` (enum member, transition key, transition target from nothing, label, ACTIVE list).
    - `grep -Ec "APPOINTMENT_CREATED|APPOINTMENT_UPDATED|APPOINTMENT_CANCELLED|AVAILABILITY_UPDATED" packages/types/src/constants/socket-events.ts` returns `4`.
    - `grep -c "MOVE_REQUEST = 'MOVE_REQUEST'" packages/types/src/notification.ts` returns `1`, and `grep -c "SCHEDULING = 'scheduling'" packages/types/src/notification.ts` still returns `1` (not duplicated).
    - `grep -c 'expected:' packages/types/src/queue.ts` returns `1` and `grep -c 'queuePriorityAt' packages/types/src/queue.ts` returns `1`.
    - `grep -Ec 'interface Appointment\b|interface AppointmentWithDetails|interface VetAvailabilityTemplate|interface AvailabilityOverride|interface BlockedPeriod|interface SlotOption' packages/types/src/scheduling.ts` returns `6`.
    - `grep -c "from './scheduling.js'" packages/types/src/index.ts` returns `1`.
    - `pnpm --filter @breeyo/types exec tsc --noEmit` exits 0 and the queue-status test file reports the three new EXPECTED behaviors passing.
  </acceptance_criteria>
  <done>`EXPECTED` is a first-class queue status across enum, transitions and labels; scheduling socket events, the `MOVE_REQUEST` notification type, the `expected` queue-board group and all scheduling entity types are exported from `@breeyo/types`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the scheduling Zod validators and widen the queue status validator to include EXPECTED</name>
  <files>packages/validators/src/scheduling.ts, packages/validators/src/queue.ts, packages/validators/src/index.ts, packages/validators/src/__tests__/scheduling.test.ts</files>
  <read_first>
    - packages/validators/src/queue.ts (complete file — `checkInSchema`, `queueStatusUpdateSchema` with its `z.enum(['WAITING','IN_CONSULT','DONE','NO_SHOW'])`, and the trailing `z.infer` type exports)
    - packages/validators/src/emr.ts (the bounded-int idiom `z.number().int().min(0).max(...)` and the rich nested-schema style)
    - packages/validators/src/index.ts (the barrel, ESM `.js` extensions)
    - packages/types/src/constants/scheduling.constants.ts (as written in Task 1 — reuse `BOOKING_HORIZON_DAYS`, `RECURRENCE_MIN_OCCURRENCES`, `RECURRENCE_MAX_OCCURRENCES`, `BlockedPeriodReason`, `RecurrenceInterval` rather than re-declaring literals)
    - .planning/phases/08-scheduling-calendar/08-CONTEXT.md (D-02, D-05, D-06, D-07, D-14, D-21, D-22)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Error states — the exact validation failures that need to be representable: OTHER-with-no-text, end-before-start, overlapping block, past-horizon)
  </read_first>
  <behavior>
    - Test "createAppointmentSchema accepts a minimal valid booking": one `petIds` entry, uuid `vetId`, uuid `ownerId`, uuid `serviceCatalogId`, ISO `scheduledFor`, no recurrence, `allowDoubleBook` defaulting to `false`.
    - Test "createAppointmentSchema rejects an empty petIds array" (D-21 requires at least one pet) and "rejects more than 6 petIds".
    - Test "createAppointmentSchema recurrence": supplying `recurrence: { interval: 'WEEKLY', occurrences: 4 }` parses; `occurrences: 1` and `occurrences: 13` both reject.
    - Test "createBlockedPeriodSchema requires reasonText when reason is OTHER" — `{ reason: 'OTHER' }` with no `reasonText` rejects; with `reasonText: 'Vet conference'` parses; `{ reason: 'LUNCH' }` with no `reasonText` parses.
    - Test "createBlockedPeriodSchema rejects endMinutes <= startMinutes".
    - Test "upsertAvailabilityTemplateSchema": a 7-entry array with weekdays 0..6 parses; a duplicate weekday rejects; `isClosed: false` with null `openMinutes` rejects; `closeMinutes <= openMinutes` rejects; weekday `7` rejects.
    - Test "queueStatusUpdateSchema accepts EXPECTED".
    - Test "appointmentRangeQuerySchema": `from`/`to` coerce from ISO strings; `to` before `from` rejects; a range wider than 62 days rejects.
  </behavior>
  <action>
Create `packages/validators/src/scheduling.ts` following `queue.ts`/`emr.ts` conventions: plain `z.object`, `.default()` for optional booleans, a trailing `export type X = z.infer<typeof xSchema>` for every schema, and cross-field rules expressed with `.superRefine`/`.refine` so the message text matches UI-SPEC § Error states.

Schemas to export:

- `createAppointmentSchema` — `{ ownerId: z.string().uuid(), petIds: z.array(z.string().uuid()).min(1).max(6), vetId: z.string().uuid(), serviceCatalogId: z.string().uuid(), scheduledFor: z.coerce.date(), notes: z.string().max(500).optional(), allowDoubleBook: z.boolean().default(false), recurrence: recurrenceSchema.optional() }`. `allowDoubleBook` is the D-14 override flag — a direct structural copy of Phase 3's `reCheckIn`. Server-side horizon enforcement lives in plan 08-07's service (it needs "now" in IST); this schema only checks shape.
- `recurrenceSchema` — `{ interval: z.nativeEnum(RecurrenceInterval), occurrences: z.number().int().min(RECURRENCE_MIN_OCCURRENCES).max(RECURRENCE_MAX_OCCURRENCES) }` (D-22).
- `rescheduleAppointmentSchema` — `{ scheduledFor: z.coerce.date(), vetId: z.string().uuid().optional(), allowDoubleBook: z.boolean().default(false), applyToSeries: z.boolean().default(false) }`.
- `cancelAppointmentSchema` — `{ reason: z.string().max(300).optional(), scope: z.enum(['ONE','SERIES']).default('ONE') }` (UI-SPEC § Destructive confirmations offers Cancel This One / Cancel All n).
- `appointmentStatusUpdateSchema` — `{ status: z.nativeEnum(AppointmentStatus) }`.
- `appointmentRangeQuerySchema` — `{ from: z.coerce.date(), to: z.coerce.date(), vetId: z.string().uuid().optional() }` with a `.refine` that `to >= from` and a `.refine` capping the span at 62 days (message: `Date range cannot exceed 62 days.`). One schema serves both the mobile single-day agenda and the web 7-day grid per RESEARCH Pattern 4.
- `slotQuerySchema` — `{ vetId: z.string().uuid(), date: z.coerce.date(), serviceCatalogId: z.string().uuid().optional(), durationMinutes: z.number().int().min(5).max(480).optional() }` with a `.refine` requiring at least one of `serviceCatalogId` or `durationMinutes` (D-02 derives duration from the service, but the settings preview needs a raw duration).
- `availabilityTemplateDaySchema` — `{ weekday: z.number().int().min(0).max(6), isClosed: z.boolean(), openMinutes: z.number().int().min(0).max(1439).nullable(), closeMinutes: z.number().int().min(1).max(1440).nullable() }` with a `.superRefine`: when `isClosed` is false both minute fields must be non-null and `closeMinutes > openMinutes`; when `isClosed` is true both must be null.
- `upsertAvailabilityTemplateSchema` — `{ vetId: z.string().uuid(), days: z.array(availabilityTemplateDaySchema).length(7) }` with a `.refine` that the seven `weekday` values are the distinct set 0..6 (message: `Provide exactly one entry per weekday.`).
- `upsertAvailabilityOverrideSchema` — `{ vetId: z.string().uuid(), date: z.coerce.date(), isClosed: z.boolean().default(true), openMinutes: ..., closeMinutes: ..., reason: z.string().max(120).optional() }` with the same open/close superRefine (D-01 half-day overrides).
- `createBlockedPeriodSchema` — `{ vetId: z.string().uuid(), date: z.coerce.date(), startMinutes: z.number().int().min(0).max(1439), endMinutes: z.number().int().min(1).max(1440), reason: z.nativeEnum(BlockedPeriodReason), reasonText: z.string().min(1).max(120).optional() }` with a `.superRefine` producing `End time must be after start time.` when `endMinutes <= startMinutes` and `Add a short reason.` when `reason === 'OTHER'` and `reasonText` is missing or blank (D-06, UI-SPEC error copy verbatim).
- `ownerAppointmentActionSchema` — `{ action: z.enum(['KEEP','MOVE','CANCEL']), appointmentId: z.string().uuid() }` (D-16; consumed by plan 08-10's inbound bridge).

**`packages/validators/src/queue.ts`** — change the `queueStatusUpdateSchema` enum to `z.enum(['EXPECTED','WAITING','IN_CONSULT','DONE','NO_SHOW'])`. Leave `checkInSchema` untouched.

Add `export * from './scheduling.js';` to `packages/validators/src/index.ts`.

`packages/validators` may import from `@breeyo/types` — check whether it already declares that dependency in its `package.json`; if it does not, add `"@breeyo/types": "workspace:*"` rather than re-declaring the enums locally.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/validators test -- --run src/__tests__/scheduling.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/validators test -- --run src/__tests__/scheduling.test.ts` reports at least 8 passing tests covering every behavior above.
    - `grep -c "'EXPECTED'" packages/validators/src/queue.ts` returns `1`.
    - `grep -Ec 'createAppointmentSchema|rescheduleAppointmentSchema|cancelAppointmentSchema|appointmentRangeQuerySchema|slotQuerySchema|upsertAvailabilityTemplateSchema|upsertAvailabilityOverrideSchema|createBlockedPeriodSchema|ownerAppointmentActionSchema' packages/validators/src/scheduling.ts` returns at least `9`.
    - `grep -c 'z.infer' packages/validators/src/scheduling.ts` returns at least `9` (one inferred type export per schema).
    - `grep -c "from './scheduling.js'" packages/validators/src/index.ts` returns `1`.
    - `grep -c 'allowDoubleBook' packages/validators/src/scheduling.ts` returns at least `2` (create and reschedule) — D-14 is allow-with-warning, never a hard block.
    - `pnpm --filter @breeyo/validators exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every scheduling write shape is a tested Zod schema with an inferred type export, `EXPECTED` is accepted by the queue status validator, and both barrels re-export the new modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request body → scheduling Zod schemas | Untrusted booking, availability and blocked-period input crosses here before any service logic runs |
| shared enum → database enum | A status value that exists in TypeScript but not in Postgres (or vice versa) is a silent data-integrity hole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `createBlockedPeriodSchema`, `availabilityTemplateDaySchema` — out-of-range or inverted minute values reaching the slot-generation arithmetic | mitigate | Bounded-int Zod constraints (`min(0).max(1439)` / `max(1440)`) plus `.superRefine` cross-field rules rejecting `end <= start` and closed-day-with-hours; unit tested in Task 3 |
| T-08-02 | Elevation of Privilege | `createAppointmentSchema` — a client supplying `clinicId` to book into another tenant | mitigate | No scheduling schema in this plan declares a `clinicId` field at all; `clinicId` is taken exclusively from `request.user.activeClinicId` in plan 08-11's controller, matching `queue.controller.ts:28-29` |
| T-08-03 | Tampering | `queueStatusUpdateSchema` widened to accept `EXPECTED` — a client force-setting an arbitrary entry to `EXPECTED` | mitigate | Shape validation only; `QUEUE_TRANSITIONS` has no path into `EXPECTED` from any status, so plan 08-04's `isValidTransition` guard rejects every client-driven move into `EXPECTED`. Only the server-side sweep creates `EXPECTED` rows |
| T-08-04 | Denial of Service | `appointmentRangeQuerySchema` — an unbounded `from`/`to` range forcing a full-table calendar scan | mitigate | `.refine` caps the span at 62 days (covers the widest legitimate view, a two-month web grid) and requires `to >= from` |
</threat_model>

<verification>
- `pnpm --filter @breeyo/types test` and `pnpm --filter @breeyo/validators test` both pass in full.
- `pnpm --filter @breeyo/types exec tsc --noEmit` and `pnpm --filter @breeyo/validators exec tsc --noEmit` both exit 0.
- `pnpm build` succeeds for both shared packages (no consumer breakage from the `QueueBoard` field addition is expected yet — the API and mobile consumers are updated in plans 08-04 and 08-08 of later waves; if `pnpm build` fails on an existing consumer, record which file and hand it to the owning plan rather than patching it here).
</verification>

<success_criteria>
- `AppointmentStatus`, `APPOINTMENT_TRANSITIONS`, `isValidAppointmentTransition` and `APPOINTMENT_STATUS_LABELS` are importable from `@breeyo/types` and match D-20 exactly.
- `QueueStatus.EXPECTED` exists with transitions to `WAITING` and `NO_SHOW` only, and no transition into `EXPECTED`.
- Every tunable value that appears in user-facing copy (90 days, 20 minutes, 15 minutes, 5 patients, 30 minutes) exists as exactly one named constant.
- `hhmmToMinutes`/`minutesToHHMM`/`weekdayIndexFromLabel` exist in exactly one file and are unit tested including their throw cases.
- Nine scheduling Zod schemas with inferred type exports are re-exported from `@breeyo/validators`.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-calendar/08-01-SUMMARY.md` when done. Record the exact constant values chosen, the final `QUEUE_TRANSITIONS` table, and the list of exported schema names so downstream plans can import without re-reading the files.
</output>
