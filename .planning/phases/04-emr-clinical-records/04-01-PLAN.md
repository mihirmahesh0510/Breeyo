---
phase: 04-emr-clinical-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/emr.types.ts
  - packages/shared/src/types/drug.types.ts
  - packages/shared/src/types/vaccination.types.ts
  - packages/shared/src/types/attachment.types.ts
  - packages/shared/src/validators/emr.validators.ts
  - packages/shared/src/validators/prescription.validators.ts
  - packages/shared/src/validators/vitals.validators.ts
  - packages/shared/src/constants/vitals-ranges.ts
  - packages/shared/src/constants/quick-pick-chips.ts
  - packages/shared/src/constants/vaccination-intervals.ts
  - packages/shared/src/constants/frequencies.ts
  - packages/shared/src/constants/routes-of-admin.ts
  - packages/shared/src/constants/durations.ts
  - packages/shared/src/constants/body-systems.ts
  - packages/shared/src/constants/care-instructions.ts
  - packages/shared/src/constants/specialist-types.ts
  - prisma/schema.prisma
  - apps/api/src/modules/emr/__tests__/emr.fixtures.ts
  - packages/shared/src/validators/__tests__/emr.validators.test.ts
  - packages/shared/src/validators/__tests__/vitals.validators.test.ts
autonomous: true
requirements:
  - EMR-01
  - EMR-02
  - EMR-03
  - EMR-04
  - EMR-06
  - EMR-07

must_haves:
  truths:
    - "Shared TypeScript types exist for Consultation, VitalsData, SubjectiveData, ObjectiveData, PlanData, PrescriptionItem, DrugEntry, DrugFormulation, SpeciesDosage, VaccinationRecord, DewormingRecord, ConsultationAttachment"
    - "Zod validators enforce consultation creation rules, vitals species-aware range checks, prescription entry structure, and file attachment constraints"
    - "Prisma schema defines Consultation, Vitals, Drug, DrugFormulation, SpeciesDosage, Prescription, VaccinationRecord, DewormingRecord, ConsultationAttachment, and ConsultationDraft models with RLS-ready clinicId columns"
    - "Species-aware vitals normal ranges exist for dog, cat, rabbit, and bird"
    - "Constants exist for body systems (8 systems per D-19), quick-pick chips (template-specific per D-16), frequencies (per D-31), durations (per D-32), routes of administration (per D-30), care instructions (per D-50), and specialist types (per D-49)"
    - "Vaccination interval constants cover Indian veterinary vaccination schedules with age-dependent rules"
  artifacts:
    - path: "packages/shared/src/types/emr.types.ts"
      provides: "Consultation, VitalsData, SubjectiveData, ObjectiveData, PlanData, ConsultationDraftState, ReviewData types"
      exports: ["Consultation", "VitalsData", "SubjectiveData", "ObjectiveData", "PlanData", "ConsultationDraftState", "ConsultationStatus", "VisitType", "BodySystemExam", "ReferralData", "AddendumEntry"]
    - path: "packages/shared/src/types/drug.types.ts"
      provides: "DrugEntry, DrugFormulation, SpeciesDosage, PrescriptionItem, DosageWarning types"
      exports: ["DrugEntry", "DrugFormulation", "SpeciesDosage", "PrescriptionItem", "DosageWarning", "DrugCategory", "FormulationType", "RouteOfAdmin"]
    - path: "packages/shared/src/types/vaccination.types.ts"
      provides: "VaccinationRecord, DewormingRecord, PreventiveCareStatus, VaccinationInterval types"
      exports: ["VaccinationRecord", "DewormingRecord", "PreventiveCareStatus", "VaccinationInterval", "PreventiveCareStatusLevel"]
    - path: "prisma/schema.prisma"
      provides: "EMR database models"
      contains: "model Consultation"
  key_links:
    - from: "packages/shared/src/validators/vitals.validators.ts"
      to: "packages/shared/src/constants/vitals-ranges.ts"
      via: "validator uses VITALS_NORMAL_RANGES for species-aware checks"
      pattern: "VITALS_NORMAL_RANGES"
    - from: "packages/shared/src/validators/prescription.validators.ts"
      to: "packages/shared/src/types/drug.types.ts"
      via: "validator enforces PrescriptionItem structure"
      pattern: "PrescriptionItem"
---

<objective>
Establish the shared foundation for Phase 4: TypeScript types for all EMR domains (consultation, SOAP, prescriptions, drugs, vaccination, attachments), zod validators with species-aware vitals range checking, all clinical constants (body systems, quick-pick chips, vitals ranges, frequencies, durations, routes, vaccination intervals), Prisma schema extension for EMR tables with audit triggers and RLS, and test fixtures.

Purpose: Every subsequent Phase 4 plan imports from these shared contracts. The drug database, consultation API, and mobile screens all depend on these types and constants existing first. Building them first prevents type mismatches and ensures consistent clinical data modeling across the stack.

Output: Shared packages populated with EMR types/validators/constants, Prisma migration with all EMR tables, audit trigger SQL, and test fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-emr-clinical-records/04-CONTEXT.md
@.planning/phases/04-emr-clinical-records/04-RESEARCH.md
@.planning/phases/04-emr-clinical-records/04-UI-SPEC.md
@.planning/phases/04-emr-clinical-records/04-VALIDATION.md

<interfaces>
<!-- From Phase 3 shared types (already exist) -->
From packages/shared/src/types/patient.types.ts:
- Owner, Pet, PetWithOwner, Species

From packages/shared/src/types/queue.types.ts:
- QueueEntry, QueueEntryWithPet, QueueStatus

From packages/shared/src/constants/queue-status.ts:
- QueueStatus enum (WAITING, IN_CONSULT, DONE, NO_SHOW)

From packages/shared/src/constants/species.ts:
- Species type ('DOG' | 'CAT' | 'BIRD' | 'RABBIT' | 'FISH' | 'REPTILE' | 'OTHER')

From prisma/schema.prisma (Phase 3):
- Clinic, User, Owner, Pet, QueueEntry models already exist
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared EMR types, constants, and zod validators with tests</name>
  <files>
    packages/shared/src/types/emr.types.ts,
    packages/shared/src/types/drug.types.ts,
    packages/shared/src/types/vaccination.types.ts,
    packages/shared/src/types/attachment.types.ts,
    packages/shared/src/validators/emr.validators.ts,
    packages/shared/src/validators/prescription.validators.ts,
    packages/shared/src/validators/vitals.validators.ts,
    packages/shared/src/constants/vitals-ranges.ts,
    packages/shared/src/constants/quick-pick-chips.ts,
    packages/shared/src/constants/vaccination-intervals.ts,
    packages/shared/src/constants/frequencies.ts,
    packages/shared/src/constants/routes-of-admin.ts,
    packages/shared/src/constants/durations.ts,
    packages/shared/src/constants/body-systems.ts,
    packages/shared/src/constants/care-instructions.ts,
    packages/shared/src/constants/specialist-types.ts,
    packages/shared/src/validators/__tests__/emr.validators.test.ts,
    packages/shared/src/validators/__tests__/vitals.validators.test.ts
  </files>
  <read_first>
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/index.ts,
    packages/shared/src/types/patient.types.ts,
    packages/shared/src/types/queue.types.ts,
    packages/shared/src/constants/species.ts,
    packages/shared/src/constants/queue-status.ts,
    packages/shared/src/schemas/patient.schema.ts
  </read_first>
  <behavior>
    - createConsultationSchema accepts { petId: cuid, queueEntryId?: cuid, visitType: 'general'|'surgery'|'vaccination' }
    - createConsultationSchema rejects unknown visitType 'emergency'
    - vitalsSchema accepts { weightKg: 25.5, temperatureC: 38.5, heartRateBpm: 80, respiratoryRate: 20 }
    - vitalsSchema rejects negative weight: { weightKg: -1 }
    - vitalsSchema rejects temperature above 50: { temperatureC: 55 }
    - vitalsSchema allows all fields to be null/undefined (partial vitals entry)
    - checkVitalRange('dog', 'temperatureC', 38.5) returns { status: 'normal' }
    - checkVitalRange('dog', 'temperatureC', 39.5) returns { status: 'slightlyAbnormal' } (within 10% of 39.2 boundary)
    - checkVitalRange('dog', 'temperatureC', 40.5) returns { status: 'criticallyAbnormal' } (beyond 10% of boundary)
    - checkVitalRange('cat', 'heartRateBpm', 180) returns { status: 'normal' } (cat range 140-220)
    - checkVitalRange('cat', 'heartRateBpm', 100) returns { status: 'criticallyAbnormal' }
    - prescriptionItemSchema accepts { drugName: 'Amoxicillin', formulation: 'tablet', strength: '250mg', dosage: '250mg', route: 'Oral', frequency: 'Twice daily', duration: '7 days', dispensed: true, sortOrder: 1 }
    - prescriptionItemSchema rejects missing drugName
    - prescriptionItemSchema rejects empty frequency
    - attachmentMetaSchema rejects file size > 10485760 (10MB)
    - attachmentMetaSchema rejects mimeType 'application/exe'
    - attachmentMetaSchema accepts mimeType 'image/jpeg', 'image/png', 'application/pdf', 'application/dicom'
    - VITALS_NORMAL_RANGES has entries for 'dog', 'cat', 'rabbit', 'bird'
    - BODY_SYSTEMS has exactly 8 entries matching D-19: Eyes, Ears, Skin/Coat, Oral, Lymph Nodes, Abdomen, Heart/Lungs, Musculoskeletal
    - VACCINATION_INTERVALS includes 'DHPPi' for dog with puppy interval (21 days, minAgeDays 42, maxAgeDays 120) and adult booster (365 days, minAgeDays 120)
    - VACCINATION_INTERVALS includes 'Anti-Rabies' for dog and cat
    - QUICK_PICK_CHIPS.general.subjective includes 'Vomiting', 'Diarrhea', 'Lethargy'
    - QUICK_PICK_CHIPS.surgery.subjective includes 'Incision', 'Sutures'
    - QUICK_PICK_CHIPS.vaccination.subjective includes vaccine names
  </behavior>
  <action>
    **1. Create `packages/shared/src/types/emr.types.ts`:**

    ```typescript
    export type VisitType = 'general' | 'surgery' | 'vaccination';
    export type ConsultationStatus = 'draft' | 'finalized';
    export type VitalRangeStatus = 'normal' | 'slightlyAbnormal' | 'criticallyAbnormal';

    export interface VitalsData {
      weightKg: number | null;
      temperatureC: number | null;
      heartRateBpm: number | null;
      respiratoryRate: number | null;
    }

    export interface SubjectiveData {
      ownerReports: string;
      history: string;
      chips: string[];
    }

    export interface BodySystemExam {
      system: string;           // 'Eyes', 'Ears', etc.
      status: 'normal' | 'abnormal';
      findings: string[];       // Sub-finding checkboxes selected
      notes: string;            // Free-text notes for abnormal findings
    }

    export interface ObjectiveData {
      bodySystems: BodySystemExam[];
      notes: string;            // Additional notes beyond body systems
    }

    export interface PlanData {
      actionItems: string[];    // 'Follow-up', 'Lab Retest', etc.
      freeText: string;
    }

    export interface ReferralData {
      specialistType: string;
      reason: string;
      urgency: 'routine' | 'urgent';
    }

    export interface AddendumEntry {
      id: string;
      text: string;
      addedBy: string;          // vet user ID
      addedByName: string;      // vet display name
      addedAt: Date;
    }

    export interface Consultation {
      id: string;
      clinicId: string;
      petId: string;
      vetId: string;
      queueEntryId: string | null;
      visitType: VisitType;
      status: ConsultationStatus;
      startedAt: Date;
      finalizedAt: Date | null;
      durationMinutes: number | null;
      vitals: VitalsData | null;
      subjective: SubjectiveData | null;
      objective: ObjectiveData | null;
      assessment: string | null;
      plan: PlanData | null;
      careInstructions: string | null;
      referral: ReferralData | null;
      rxNotes: string | null;
      followUpDate: Date | null;
      followUpReason: string | null;
      addenda: AddendumEntry[];
      createdAt: Date;
      updatedAt: Date;
    }

    export interface ConsultationDraftState {
      consultationId: string;
      visitType: VisitType;
      expandedSections: string[];
      vitals: VitalsData;
      subjective: SubjectiveData;
      objective: ObjectiveData;
      assessment: string;
      plan: PlanData;
      careInstructions: string;
      referral: ReferralData | null;
      rxNotes: string;
      isDirty: boolean;
      isFinalizing: boolean;
      lastSavedAt: Date | null;
    }

    export interface ConsultationSummary {
      id: string;
      visitType: VisitType;
      status: ConsultationStatus;
      startedAt: Date;
      finalizedAt: Date | null;
      durationMinutes: number | null;
      assessment: string | null;
      vetId: string;
      vetName: string;
      prescriptionCount: number;
      attachmentCount: number;
    }

    export interface CreateConsultationInput {
      petId: string;
      queueEntryId?: string;
      visitType: VisitType;
    }

    export interface SaveDraftInput {
      vitals?: Partial<VitalsData>;
      subjective?: Partial<SubjectiveData>;
      objective?: Partial<ObjectiveData>;
      assessment?: string;
      plan?: Partial<PlanData>;
      careInstructions?: string;
      referral?: ReferralData | null;
      rxNotes?: string;
    }

    export interface FinalizeInput {
      followUpDate?: string;      // ISO date string
      followUpReason?: string;
    }

    export interface VitalRangeCheck {
      vital: string;
      value: number;
      status: VitalRangeStatus;
      normalMin: number;
      normalMax: number;
      unit: string;
    }
    ```

    **2. Create `packages/shared/src/types/drug.types.ts`:**

    ```typescript
    export type DrugCategory = 'antibiotic' | 'nsaid' | 'antiparasitic' | 'vaccine' | 'antifungal' | 'corticosteroid' | 'antiemetic' | 'cardiac' | 'supplement' | 'other';
    export type FormulationType = 'tablet' | 'suspension' | 'injectable' | 'drops' | 'ointment' | 'powder' | 'capsule' | 'spray';
    export type RouteOfAdmin = 'Oral' | 'Injectable (IV)' | 'Injectable (IM)' | 'Injectable (SC)' | 'Topical' | 'Eye Drops' | 'Ear Drops' | 'Inhalation' | 'Rectal';

    export interface DrugEntry {
      id: string;
      name: string;
      genericName: string;
      category: DrugCategory;
      isActive: boolean;
      formulations: DrugFormulation[];
      dosageRanges: SpeciesDosage[];
    }

    export interface DrugFormulation {
      id: string;
      drugId: string;
      form: FormulationType;
      strength: string;             // e.g., "250mg", "50mg/ml"
      strengthValue: number;        // numeric value for calculation
      strengthUnit: string;         // "mg", "mg/ml", "IU"
    }

    export interface SpeciesDosage {
      id: string;
      drugId: string;
      species: string;              // 'dog', 'cat', 'bird', 'rabbit'
      minDoseMgPerKg: number;
      maxDoseMgPerKg: number;
      isFixedDose: boolean;
      fixedDoseMin: number | null;
      fixedDoseMax: number | null;
      notes: string | null;
    }

    export interface PrescriptionItem {
      id?: string;
      drugId: string | null;
      drugName: string;
      formulationId: string | null;
      formulation: string;
      strength: string;
      dosage: string;               // "250mg" or "5ml"
      dosageMg: number | null;
      route: RouteOfAdmin;
      frequency: string;
      duration: string;
      durationDays: number | null;
      clinicalInstructions: string | null;
      ownerInstructions: string | null;
      dispensed: boolean;
      inventoryItemId: string | null;   // nullable for Phase 5 linking (D-34)
      sortOrder: number;
    }

    export interface DosageWarning {
      level: 'warning';
      message: string;
      enteredDose: number;
      enteredDosePerKg?: number;
      recommendedMinMg: number;
      recommendedMaxMg: number;
    }

    export interface DrugSearchResult {
      id: string;
      name: string;
      genericName: string;
      category: DrugCategory;
      formulations: DrugFormulation[];
    }
    ```

    **3. Create `packages/shared/src/types/vaccination.types.ts`:**

    ```typescript
    export type PreventiveCareStatusLevel = 'upToDate' | 'dueSoon' | 'overdue';

    export interface VaccinationRecord {
      id: string;
      clinicId: string;
      petId: string;
      consultationId: string | null;
      vaccineName: string;
      batchNumber: string | null;
      manufacturer: string | null;
      expiryDate: Date | null;
      administeredAt: Date;
      administeredBy: string;
      nextDueDate: Date | null;
    }

    export interface DewormingRecord {
      id: string;
      clinicId: string;
      petId: string;
      consultationId: string | null;
      drugName: string;
      administeredAt: Date;
      administeredBy: string;
      nextDueDate: Date | null;
    }

    export interface VaccinationInterval {
      vaccineName: string;
      species: string;
      intervalDays: number;
      minAgeDays: number;
      maxAgeDays: number | null;
      isBooster: boolean;
    }

    export interface PreventiveCareStatus {
      vaccinationStatus: PreventiveCareStatusLevel;
      vaccinationNextDue: Date | null;
      vaccinationOverdueItems: string[];
      dewormingStatus: PreventiveCareStatusLevel;
      dewormingNextDue: Date | null;
    }
    ```

    **4. Create `packages/shared/src/types/attachment.types.ts`:**

    ```typescript
    export type AttachmentFileType = 'lab_report' | 'xray' | 'ultrasound' | 'ecg' | 'photo' | 'other';
    export const ALLOWED_MIME_TYPES = ['image/jpeg', 'image/png', 'application/pdf', 'application/dicom'] as const;
    export const MAX_FILE_SIZE_BYTES = 10 * 1024 * 1024; // 10MB (D-61)
    export const MAX_FILES_PER_CONSULTATION = 10;         // D-61
    export const COMPRESS_THRESHOLD_BYTES = 5 * 1024 * 1024; // 5MB (D-61)

    export interface ConsultationAttachment {
      id: string;
      consultationId: string;
      fileType: AttachmentFileType;
      fileName: string;
      mimeType: string;
      fileSizeBytes: number;
      s3Key: string;
      s3Url: string | null;
      thumbnailS3Key: string | null;
      description: string | null;
      uploadedBy: string;
      uploadedAt: Date;
    }

    export interface AttachmentUploadRequest {
      fileName: string;
      mimeType: string;
      fileSizeBytes: number;
      fileType: AttachmentFileType;
      description?: string;
    }

    export interface PresignedUrlResponse {
      uploadUrl: string;
      s3Key: string;
      expiresIn: number;
    }
    ```

    **5. Create constants files:**

    `packages/shared/src/constants/vitals-ranges.ts`:
    Export `VITALS_NORMAL_RANGES` as a `Record<string, VitalRanges>` with entries for dog, cat, rabbit, bird. Values:
    - dog: temperatureC { min: 38.0, max: 39.2 }, heartRateBpm { min: 60, max: 140 }, respiratoryRate { min: 10, max: 30 }
    - cat: temperatureC { min: 38.0, max: 39.5 }, heartRateBpm { min: 140, max: 220 }, respiratoryRate { min: 20, max: 30 }
    - rabbit: temperatureC { min: 38.5, max: 40.0 }, heartRateBpm { min: 120, max: 150 }, respiratoryRate { min: 30, max: 60 }
    - bird: temperatureC { min: 40.0, max: 42.0 }, heartRateBpm { min: 200, max: 600 }, respiratoryRate { min: 15, max: 30 }

    Export `checkVitalRange(species: string, vital: string, value: number): VitalRangeCheck` function:
    - Get range from VITALS_NORMAL_RANGES[species.toLowerCase()][vital]
    - If value is within range: status = 'normal'
    - If value is outside range but within 10% of boundary: status = 'slightlyAbnormal'
    - If value is beyond 10% of boundary: status = 'criticallyAbnormal'
    - Return { vital, value, status, normalMin, normalMax, unit }

    `packages/shared/src/constants/body-systems.ts`:
    Export `BODY_SYSTEMS` array with exactly 8 entries per D-19:
    ```typescript
    export const BODY_SYSTEMS = [
      { id: 'eyes', label: 'Eyes', subFindings: ['Discharge', 'Redness', 'Opacity', 'Third eyelid prolapse'] },
      { id: 'ears', label: 'Ears', subFindings: ['Discharge', 'Odor', 'Inflammation', 'Mites', 'Hematoma'] },
      { id: 'skin_coat', label: 'Skin/Coat', subFindings: ['Hair loss', 'Lesions', 'Parasites', 'Dry/flaky', 'Hot spots', 'Lumps'] },
      { id: 'oral', label: 'Oral', subFindings: ['Tartar', 'Gingivitis', 'Broken teeth', 'Ulcers', 'Bad breath'] },
      { id: 'lymph_nodes', label: 'Lymph Nodes', subFindings: ['Enlarged submandibular', 'Enlarged prescapular', 'Enlarged popliteal', 'Generalized lymphadenopathy'] },
      { id: 'abdomen', label: 'Abdomen', subFindings: ['Distension', 'Pain on palpation', 'Mass', 'Organomegaly', 'Fluid wave'] },
      { id: 'heart_lungs', label: 'Heart/Lungs', subFindings: ['Murmur', 'Arrhythmia', 'Crackles', 'Wheezing', 'Cough', 'Dyspnea'] },
      { id: 'musculoskeletal', label: 'Musculoskeletal', subFindings: ['Lameness', 'Joint swelling', 'Crepitus', 'Muscle atrophy', 'Pain on manipulation'] },
    ] as const;
    ```

    `packages/shared/src/constants/quick-pick-chips.ts`:
    Export `QUICK_PICK_CHIPS` organized by visitType (general, surgery, vaccination) and SOAP section (subjective, objective, plan) per D-16:
    - general.subjective: ['Vomiting', 'Diarrhea', 'Lethargy', 'Loss of appetite', 'Coughing', 'Sneezing', 'Itching', 'Limping', 'Eye discharge', 'Ear discharge', 'Weight loss', 'Increased thirst', 'Frequent urination', 'Behavioral change']
    - surgery.subjective: ['Pre-surgical assessment', 'Post-operative check', 'Incision check', 'Suture removal', 'Wound assessment', 'Drain check']
    - vaccination.subjective: ['Routine vaccination', 'Booster due', 'Puppy/kitten series', 'Rabies vaccination', 'Travel vaccination', 'Adverse reaction follow-up']
    - general.plan: ['Follow-up', 'Lab Retest', 'Diet Change', 'Exercise Restriction', 'Referral', 'Other']
    - surgery.plan: ['Follow-up', 'Suture Removal', 'Post-op Recheck', 'Drain Removal', 'Exercise Restriction', 'Wound Care']
    - vaccination.plan: ['Next Booster', 'Reaction Monitoring', 'Follow-up', 'Other']

    `packages/shared/src/constants/frequencies.ts`:
    Export `MEDICATION_FREQUENCIES` per D-31:
    ```typescript
    export const MEDICATION_FREQUENCIES = [
      { value: 'Once daily', label: 'Once daily' },
      { value: 'Twice daily', label: 'Twice daily' },
      { value: 'Three times daily', label: 'Three times daily' },
      { value: 'Every 8 hours', label: 'Every 8 hours' },
      { value: 'As needed (PRN)', label: 'As needed (PRN)' },
      { value: 'Before meals', label: 'Before meals' },
      { value: 'After meals', label: 'After meals' },
    ] as const;
    ```

    `packages/shared/src/constants/routes-of-admin.ts`:
    Export `ROUTES_OF_ADMINISTRATION` per D-30:
    ```typescript
    export const ROUTES_OF_ADMINISTRATION = [
      'Oral', 'Injectable (IV)', 'Injectable (IM)', 'Injectable (SC)',
      'Topical', 'Eye Drops', 'Ear Drops', 'Inhalation', 'Rectal',
    ] as const;
    ```

    `packages/shared/src/constants/durations.ts`:
    Export `DURATION_OPTIONS` per D-32:
    ```typescript
    export const DURATION_OPTIONS = [
      { value: '3 days', label: '3 days', days: 3 },
      { value: '5 days', label: '5 days', days: 5 },
      { value: '7 days', label: '7 days', days: 7 },
      { value: '10 days', label: '10 days', days: 10 },
      { value: '14 days', label: '14 days', days: 14 },
      { value: '30 days', label: '30 days', days: 30 },
      { value: 'Until finished', label: 'Until finished', days: null },
      { value: 'As needed (PRN)', label: 'As needed (PRN)', days: null },
      { value: 'Ongoing/Chronic', label: 'Ongoing/Chronic', days: null },
      { value: 'Custom', label: 'Custom', days: null },
    ] as const;
    ```

    `packages/shared/src/constants/vaccination-intervals.ts`:
    Export `VACCINATION_INTERVALS` and `DEWORMING_INTERVALS` with the exact data from RESEARCH.md (see code example in 04-RESEARCH.md, lines 903-936). Include all dog and cat vaccination entries. Export a `calculateNextDueDate(vaccineName: string, species: string, petAgeDays: number, administeredDate: Date): Date | null` helper function.

    `packages/shared/src/constants/care-instructions.ts`:
    Export `CARE_INSTRUCTION_CHIPS` per D-50:
    ```typescript
    export const CARE_INSTRUCTION_CHIPS = [
      'Soft food', 'NPO (nothing by mouth)', 'Exercise restriction',
      'Cone/collar', 'Wound care', 'Keep dry', 'Rest',
    ] as const;
    ```

    `packages/shared/src/constants/specialist-types.ts`:
    Export `SPECIALIST_TYPES` per D-49:
    ```typescript
    export const SPECIALIST_TYPES = [
      'Surgeon', 'Dermatologist', 'Ophthalmologist', 'Cardiologist',
      'Oncologist', 'Radiologist', 'Neurologist', 'Other',
    ] as const;
    ```

    **6. Create zod validators:**

    `packages/shared/src/validators/emr.validators.ts`:
    - `createConsultationSchema`: z.object({ petId: z.string().min(1), queueEntryId: z.string().optional(), visitType: z.enum(['general', 'surgery', 'vaccination']) })
    - `saveDraftSchema`: z.object with all SOAP sections optional (deep partial)
    - `finalizeConsultationSchema`: z.object({ followUpDate: z.string().datetime().optional(), followUpReason: z.string().max(500).optional() })
    - `addendumSchema`: z.object({ text: z.string().min(1).max(5000) })

    `packages/shared/src/validators/vitals.validators.ts`:
    - `vitalsSchema`: z.object({ weightKg: z.number().positive().max(500).nullable().optional(), temperatureC: z.number().min(30).max(50).nullable().optional(), heartRateBpm: z.number().int().positive().max(1000).nullable().optional(), respiratoryRate: z.number().int().positive().max(200).nullable().optional() })

    `packages/shared/src/validators/prescription.validators.ts`:
    - `prescriptionItemSchema`: z.object({ drugId: z.string().nullable().optional(), drugName: z.string().min(1).max(200), formulationId: z.string().nullable().optional(), formulation: z.string().min(1), strength: z.string().min(1), dosage: z.string().min(1), dosageMg: z.number().positive().nullable().optional(), route: z.enum(ROUTES_OF_ADMINISTRATION), frequency: z.string().min(1), duration: z.string().min(1), durationDays: z.number().int().positive().nullable().optional(), clinicalInstructions: z.string().max(1000).nullable().optional(), ownerInstructions: z.string().max(1000).nullable().optional(), dispensed: z.boolean().default(false), inventoryItemId: z.string().nullable().optional(), sortOrder: z.number().int().min(0) })
    - `attachmentMetaSchema`: z.object({ fileName: z.string().min(1).max(255), mimeType: z.enum(ALLOWED_MIME_TYPES), fileSizeBytes: z.number().int().positive().max(MAX_FILE_SIZE_BYTES), fileType: z.enum(['lab_report', 'xray', 'ultrasound', 'ecg', 'photo', 'other']), description: z.string().max(500).optional() })

    **7. Create test files:**

    `packages/shared/src/validators/__tests__/emr.validators.test.ts` -- test createConsultationSchema, saveDraftSchema, addendumSchema
    `packages/shared/src/validators/__tests__/vitals.validators.test.ts` -- test vitalsSchema + checkVitalRange function

    **8. Update `packages/shared/src/index.ts`** to re-export all new type, constant, and validator modules.
  </action>
  <verify>
    <automated>npx vitest run packages/shared/src/validators/__tests__/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/types/emr.types.ts contains `export type VisitType`
    - packages/shared/src/types/emr.types.ts contains `export interface Consultation`
    - packages/shared/src/types/emr.types.ts contains `export interface ConsultationDraftState`
    - packages/shared/src/types/emr.types.ts contains `export interface BodySystemExam`
    - packages/shared/src/types/emr.types.ts contains `export interface AddendumEntry`
    - packages/shared/src/types/drug.types.ts contains `export interface DrugEntry`
    - packages/shared/src/types/drug.types.ts contains `export interface PrescriptionItem`
    - packages/shared/src/types/drug.types.ts contains `export interface DosageWarning`
    - packages/shared/src/types/drug.types.ts contains `export type RouteOfAdmin`
    - packages/shared/src/types/vaccination.types.ts contains `export interface VaccinationRecord`
    - packages/shared/src/types/vaccination.types.ts contains `export interface VaccinationInterval`
    - packages/shared/src/types/vaccination.types.ts contains `export interface PreventiveCareStatus`
    - packages/shared/src/types/attachment.types.ts contains `MAX_FILE_SIZE_BYTES`
    - packages/shared/src/types/attachment.types.ts contains `ALLOWED_MIME_TYPES`
    - packages/shared/src/constants/vitals-ranges.ts contains `VITALS_NORMAL_RANGES`
    - packages/shared/src/constants/vitals-ranges.ts contains `checkVitalRange`
    - packages/shared/src/constants/body-systems.ts contains `BODY_SYSTEMS`
    - packages/shared/src/constants/body-systems.ts contains `eyes` and `musculoskeletal`
    - packages/shared/src/constants/quick-pick-chips.ts contains `QUICK_PICK_CHIPS`
    - packages/shared/src/constants/quick-pick-chips.ts contains `Vomiting`
    - packages/shared/src/constants/vaccination-intervals.ts contains `VACCINATION_INTERVALS`
    - packages/shared/src/constants/vaccination-intervals.ts contains `DHPPi`
    - packages/shared/src/constants/vaccination-intervals.ts contains `Anti-Rabies`
    - packages/shared/src/constants/vaccination-intervals.ts contains `calculateNextDueDate`
    - packages/shared/src/constants/frequencies.ts contains `MEDICATION_FREQUENCIES`
    - packages/shared/src/constants/routes-of-admin.ts contains `ROUTES_OF_ADMINISTRATION`
    - packages/shared/src/constants/durations.ts contains `DURATION_OPTIONS`
    - packages/shared/src/constants/care-instructions.ts contains `CARE_INSTRUCTION_CHIPS`
    - packages/shared/src/constants/specialist-types.ts contains `SPECIALIST_TYPES`
    - packages/shared/src/validators/emr.validators.ts contains `createConsultationSchema`
    - packages/shared/src/validators/vitals.validators.ts contains `vitalsSchema`
    - packages/shared/src/validators/prescription.validators.ts contains `prescriptionItemSchema`
    - packages/shared/src/validators/prescription.validators.ts contains `attachmentMetaSchema`
    - packages/shared/src/validators/__tests__/emr.validators.test.ts exits 0
    - packages/shared/src/validators/__tests__/vitals.validators.test.ts exits 0
  </acceptance_criteria>
  <done>All shared EMR types, constants, and validators exist with passing tests. Species-aware vitals range checking works for dog, cat, rabbit, bird. Body systems constant has exactly 8 entries. Quick-pick chips are template-specific. Vaccination intervals include age-dependent rules.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Prisma schema with EMR models, audit triggers, and drug seed migration</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/YYYYMMDDHHMMSS_add_emr_models/migration.sql,
    prisma/migrations/YYYYMMDDHHMMSS_add_emr_audit_triggers/migration.sql,
    apps/api/src/modules/emr/__tests__/emr.fixtures.ts
  </files>
  <read_first>
    prisma/schema.prisma,
    prisma/migrations/,
    apps/api/src/lib/prisma.ts,
    packages/shared/src/types/emr.types.ts,
    packages/shared/src/types/drug.types.ts,
    packages/shared/src/types/vaccination.types.ts,
    packages/shared/src/types/attachment.types.ts
  </read_first>
  <action>
    **1. Add EMR models to `prisma/schema.prisma`** (append to existing schema from Phases 1-3):

    Add `ConsultationDraft` model (NO audit triggers -- per RESEARCH.md Pitfall 7):
    ```prisma
    model ConsultationDraft {
      id              String   @id @default(cuid())
      consultationId  String   @unique @map("consultation_id")
      clinicId        String   @map("clinic_id")
      data            Json     // Full draft state as JSON
      updatedAt       DateTime @updatedAt @map("updated_at")

      clinic          Clinic   @relation(fields: [clinicId], references: [id])

      @@index([clinicId])
      @@map("consultation_drafts")
    }
    ```

    Add `Consultation` model (from RESEARCH.md Prisma schema, lines 722-761):
    ```prisma
    model Consultation {
      id              String   @id @default(cuid())
      clinicId        String   @map("clinic_id")
      petId           String   @map("pet_id")
      vetId           String   @map("vet_id")
      queueEntryId    String?  @map("queue_entry_id")
      visitType       String   @default("general")
      status          String   @default("draft")
      startedAt       DateTime @default(now()) @map("started_at")
      finalizedAt     DateTime? @map("finalized_at")
      durationMinutes Int?     @map("duration_minutes")
      subjective      Json?
      objective       Json?
      assessment      String?
      plan            Json?
      careInstructions String? @map("care_instructions")
      referral        Json?
      rxNotes         String?  @map("rx_notes")
      followUpDate    DateTime? @map("follow_up_date")
      followUpReason  String?   @map("follow_up_reason")
      addenda         Json?     @default("[]")

      clinic          Clinic   @relation(fields: [clinicId], references: [id])
      pet             Pet      @relation(fields: [petId], references: [id])
      vet             User     @relation("ConsultationVet", fields: [vetId], references: [id])
      vitals          Vitals?
      prescriptions   Prescription[]
      attachments     ConsultationAttachment[]
      vaccinationRecords VaccinationRecord[]
      dewormingRecords   DewormingRecord[]

      createdAt       DateTime @default(now()) @map("created_at")
      updatedAt       DateTime @updatedAt @map("updated_at")

      @@index([clinicId])
      @@index([clinicId, petId])
      @@index([clinicId, vetId])
      @@index([clinicId, status])
      @@map("consultations")
    }
    ```

    Add `Vitals` model (lines 763-774):
    ```prisma
    model Vitals {
      id              String   @id @default(cuid())
      consultationId  String   @unique @map("consultation_id")
      weightKg        Decimal? @map("weight_kg") @db.Decimal(6,2)
      temperatureC    Decimal? @map("temperature_c") @db.Decimal(4,1)
      heartRateBpm    Int?     @map("heart_rate_bpm")
      respiratoryRate Int?     @map("respiratory_rate")

      consultation    Consultation @relation(fields: [consultationId], references: [id])

      @@map("vitals")
    }
    ```

    Add `Drug`, `DrugFormulation`, `SpeciesDosage` models (lines 776-817):
    Use exact schema from RESEARCH.md. Drug model has `name`, `genericName`, `category`, `isActive`. DrugFormulation has `form`, `strength`, `strengthValue` (Decimal), `strengthUnit`. SpeciesDosage has `minDoseMgPerKg`, `maxDoseMgPerKg` (Decimal), `isFixedDose`, `fixedDoseMin`, `fixedDoseMax`, `notes`, with `@@unique([drugId, species])`.

    Add `Prescription` model (lines 819-842):
    Use exact schema from RESEARCH.md. Includes `drugId` (nullable), `drugName` (denormalized), `formulationId`, `formulation`, `strength`, `dosage`, `dosageMg` (Decimal), `route`, `frequency`, `duration`, `durationDays`, `clinicalInstructions`, `ownerInstructions`, `dispensed`, `inventoryItemId` (nullable for Phase 5 D-34), `sortOrder`.

    Add `VaccinationRecord` model (lines 844-861):
    Use exact schema from RESEARCH.md with `clinicId`, `petId`, `consultationId` (nullable), `vaccineName`, `batchNumber`, `manufacturer`, `expiryDate`, `administeredAt`, `administeredBy`, `nextDueDate`.

    Add `DewormingRecord` model (lines 863-877):
    Use exact schema from RESEARCH.md with `clinicId`, `petId`, `consultationId` (nullable), `drugName`, `administeredAt`, `administeredBy`, `nextDueDate`.

    Add `ConsultationAttachment` model (lines 879-896):
    Use exact schema from RESEARCH.md with `fileType`, `fileName`, `mimeType`, `fileSizeBytes`, `s3Key`, `s3Url`, `thumbnailS3Key`, `description`, `uploadedBy`, `uploadedAt`.

    Add `ConsultationLock` model for D-06:
    ```prisma
    model ConsultationLock {
      id              String   @id @default(cuid())
      consultationId  String   @unique @map("consultation_id")
      vetId           String   @map("vet_id")
      vetName         String   @map("vet_name")
      acquiredAt      DateTime @default(now()) @map("acquired_at")
      lastHeartbeat   DateTime @default(now()) @map("last_heartbeat")
      expiresAt       DateTime @map("expires_at")

      @@map("consultation_locks")
    }
    ```

    Add reverse relations to existing models:
    - Clinic: add `consultations Consultation[]`, `consultationDrafts ConsultationDraft[]`, `vaccinationRecords VaccinationRecord[]`, `dewormingRecords DewormingRecord[]`
    - Pet: add `consultations Consultation[]`, `vaccinationRecords VaccinationRecord[]`, `dewormingRecords DewormingRecord[]`
    - User: add `consultationsAsVet Consultation[] @relation("ConsultationVet")`

    **2. Enable RLS on EMR tables.** Create custom SQL migration with:
    ```sql
    ALTER TABLE "consultations" ENABLE ROW LEVEL SECURITY;
    ALTER TABLE "vitals" ENABLE ROW LEVEL SECURITY;
    ALTER TABLE "prescriptions" ENABLE ROW LEVEL SECURITY;
    ALTER TABLE "consultation_attachments" ENABLE ROW LEVEL SECURITY;
    ALTER TABLE "vaccination_records" ENABLE ROW LEVEL SECURITY;
    ALTER TABLE "deworming_records" ENABLE ROW LEVEL SECURITY;

    CREATE POLICY consultation_tenant ON "consultations"
      USING ("clinic_id" = current_setting('app.current_clinic_id', true));
    CREATE POLICY vitals_tenant ON "vitals"
      USING ("consultation_id" IN (SELECT id FROM "consultations" WHERE "clinic_id" = current_setting('app.current_clinic_id', true)));
    CREATE POLICY prescription_tenant ON "prescriptions"
      USING ("consultation_id" IN (SELECT id FROM "consultations" WHERE "clinic_id" = current_setting('app.current_clinic_id', true)));
    CREATE POLICY attachment_tenant ON "consultation_attachments"
      USING ("consultation_id" IN (SELECT id FROM "consultations" WHERE "clinic_id" = current_setting('app.current_clinic_id', true)));
    CREATE POLICY vaccination_tenant ON "vaccination_records"
      USING ("clinic_id" = current_setting('app.current_clinic_id', true));
    CREATE POLICY deworming_tenant ON "deworming_records"
      USING ("clinic_id" = current_setting('app.current_clinic_id', true));
    ```

    **3. Create audit triggers.** Use the exact pattern from RESEARCH.md Pattern 5 (lines 564-629). Apply `audit_trigger_func()` to: consultations, vitals, prescriptions, consultation_attachments, vaccination_records, deworming_records. Do NOT apply to consultation_drafts (Pitfall 7).

    **4. Run migrations:**
    ```bash
    npx prisma migrate dev --name add_emr_models
    npx prisma migrate dev --name add_emr_rls_and_audit
    npx prisma generate
    ```

    **5. Create `apps/api/src/modules/emr/__tests__/emr.fixtures.ts`:**
    Export shared test fixtures:
    - `mockClinic`: { id: 'clinic_test_1', name: 'Test Vet Clinic' }
    - `mockVet`: { id: 'vet_test_1', name: 'Dr. Test', clinicId: 'clinic_test_1' }
    - `mockPet`: { id: 'pet_test_1', name: 'Buddy', species: 'DOG', ownerId: 'owner_test_1', clinicId: 'clinic_test_1', weight: 25.0 }
    - `mockPetCat`: { id: 'pet_test_2', name: 'Whiskers', species: 'CAT', ownerId: 'owner_test_1', clinicId: 'clinic_test_1', weight: 4.5 }
    - `mockOwner`: { id: 'owner_test_1', name: 'Rahul Kumar', mobile: '9876543210', clinicId: 'clinic_test_1' }
    - `mockConsultation`: full Consultation object with all fields populated
    - `mockVitals`: { weightKg: 25.5, temperatureC: 38.5, heartRateBpm: 80, respiratoryRate: 20 }
    - `mockPrescriptionItem`: full PrescriptionItem for Amoxicillin 250mg tablet
    - `mockDrugEntry`: full DrugEntry for Amoxicillin with 2 formulations and dog/cat dosage ranges
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `model Consultation`
    - prisma/schema.prisma contains `model Vitals`
    - prisma/schema.prisma contains `model Drug`
    - prisma/schema.prisma contains `model DrugFormulation`
    - prisma/schema.prisma contains `model SpeciesDosage`
    - prisma/schema.prisma contains `model Prescription`
    - prisma/schema.prisma contains `model VaccinationRecord`
    - prisma/schema.prisma contains `model DewormingRecord`
    - prisma/schema.prisma contains `model ConsultationAttachment`
    - prisma/schema.prisma contains `model ConsultationDraft`
    - prisma/schema.prisma contains `model ConsultationLock`
    - prisma/schema.prisma contains `@@unique([drugId, species])` on SpeciesDosage
    - prisma/schema.prisma contains `inventoryItemId` (nullable FK for Phase 5, D-34)
    - Migration SQL contains `ALTER TABLE "consultations" ENABLE ROW LEVEL SECURITY`
    - Migration SQL contains `audit_trigger_func`
    - Migration SQL contains `consultation_audit`
    - Migration SQL does NOT contain trigger on `consultation_drafts` (Pitfall 7)
    - `npx prisma validate` exits 0
    - `npx prisma generate` exits 0
    - apps/api/src/modules/emr/__tests__/emr.fixtures.ts contains `mockClinic`
    - apps/api/src/modules/emr/__tests__/emr.fixtures.ts contains `mockPet`
    - apps/api/src/modules/emr/__tests__/emr.fixtures.ts contains `mockPrescriptionItem`
    - apps/api/src/modules/emr/__tests__/emr.fixtures.ts contains `mockDrugEntry`
  </acceptance_criteria>
  <done>Prisma schema includes all EMR models (Consultation, Vitals, Drug, DrugFormulation, SpeciesDosage, Prescription, VaccinationRecord, DewormingRecord, ConsultationAttachment, ConsultationDraft, ConsultationLock). Migrations applied with RLS policies and audit triggers on all finalized EMR tables (not drafts). Test fixtures ready for all subsequent plans.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/shared/src/validators/__tests__/ --reporter=verbose` -- all EMR validator tests pass
- `npx prisma validate` -- schema is valid
- `npx prisma generate` -- client generated
- `npx tsc --noEmit -p packages/shared/tsconfig.json` -- shared package compiles
</verification>

<success_criteria>
1. All shared EMR types exist (emr, drug, vaccination, attachment) with correct interfaces
2. All constants match CONTEXT.md decisions (8 body systems, template-specific chips, vitals ranges for 4 species)
3. Zod validators pass for consultation creation, vitals, prescriptions, and attachments
4. checkVitalRange correctly classifies normal/slightlyAbnormal/criticallyAbnormal
5. Prisma schema has all EMR models with correct indexes and relationships
6. Audit triggers applied to finalized tables only (NOT consultation_drafts)
7. RLS policies applied to all EMR tables
8. Test fixtures exist for downstream plans
</success_criteria>

<output>
After completion, create `.planning/phases/04-emr-clinical-records/04-01-SUMMARY.md`
</output>
