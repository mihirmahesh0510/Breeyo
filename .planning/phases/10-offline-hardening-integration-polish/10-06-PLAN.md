---
phase: 10-offline-hardening-integration-polish
plan: 06
type: execute
wave: 4
depends_on:
  - "10-05"
files_modified:
  - apps/api/tests/integration/offline-recovery.e2e.test.ts
  - apps/api/tests/integration/walkin-to-payment.e2e.test.ts
  - apps/api/tests/integration/whatsapp-triggered-flow.e2e.test.ts
  - apps/web/tests/integration/reconnect-live-sync.test.ts
  - apps/mobile/.maestro/offline-recovery.yaml
  - apps/mobile/.maestro/multi-drop-recovery.yaml
  - apps/mobile/.maestro/whatsapp-triggered-flow.yaml
autonomous: false
requirements:
  - PLT-03
must_haves:
  truths:
    - "Offline recovery is proven through repeated disconnect/reconnect drills, not only mocked automation."
    - "The walk-in-to-payment path and a WhatsApp-triggered clinic path both survive reconnect and replay without integrity loss."
    - "Phase 10 is not signed off until automated proof passes and a human approves the real-device drill."
  artifacts:
    - path: "apps/api/tests/integration/offline-recovery.e2e.test.ts"
      provides: "Automated reconnect/replay integrity proof"
      exports: []
    - path: "apps/mobile/.maestro/offline-recovery.yaml"
      provides: "Real-device disconnect/reconnect drill script"
      exports: []
    - path: "apps/mobile/.maestro/whatsapp-triggered-flow.yaml"
      provides: "WhatsApp-triggered clinic flow proof script"
      exports: []
  key_links:
    - from: "apps/api/tests/integration/walkin-to-payment.e2e.test.ts"
      to: "apps/web/tests/integration/reconnect-live-sync.test.ts"
      via: "Golden-path replay proof spans mobile action, API reconciliation, and web visibility"
      pattern: "walkin-to-payment"
    - from: "apps/mobile/.maestro/offline-recovery.yaml"
      to: "apps/mobile/.maestro/multi-drop-recovery.yaml"
      via: "Single and repeated disconnect/reconnect drills both exist before human sign-off"
      pattern: "airplaneMode"
---

<objective>
Build and run the final automated proof harnesses for Phase 10, then pause for real-device human verification.

Purpose: D-25 through D-33 define a proof bar, not just an implementation bar. Phase 10 is only done when offline recovery, golden-path integration, and WhatsApp-triggered recovery have all been proven through automation and a real-device drill.

Output: Integration tests, Maestro disconnect/reconnect flows, and a blocking human verification checkpoint.
</objective>

<execution_context>
@/Users/mihirmahesh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mihirmahesh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-offline-hardening-integration-polish/10-CONTEXT.md
@.planning/phases/10-offline-hardening-integration-polish/10-RESEARCH.md
@.planning/phases/07-whatsapp-communication/07-CONTEXT.md
@.planning/phases/09-web-dashboard-owner-portal/09-CONTEXT.md
@.planning/phases/10-offline-hardening-integration-polish/10-05-PLAN.md

<interfaces>
Mandatory proof paths for this plan:

- Offline recovery with multiple drop/recover cycles per D-26, D-27, and D-33
- Walk-in → consultation → inventory dispense → invoice/payment golden path per roadmap success criteria
- WhatsApp-triggered clinic flow per D-29
- Mid-range Android real-device drill per D-31 and D-32
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create automated integration and Maestro proof harnesses for offline recovery, golden path, and WhatsApp-triggered flow</name>
  <files>apps/api/tests/integration/offline-recovery.e2e.test.ts, apps/api/tests/integration/walkin-to-payment.e2e.test.ts, apps/api/tests/integration/whatsapp-triggered-flow.e2e.test.ts, apps/web/tests/integration/reconnect-live-sync.test.ts, apps/mobile/.maestro/offline-recovery.yaml, apps/mobile/.maestro/multi-drop-recovery.yaml, apps/mobile/.maestro/whatsapp-triggered-flow.yaml</files>
  <read_first>.planning/phases/10-offline-hardening-integration-polish/10-CONTEXT.md, .planning/phases/10-offline-hardening-integration-polish/10-RESEARCH.md, .planning/phases/07-whatsapp-communication/07-CONTEXT.md, .planning/phases/09-web-dashboard-owner-portal/09-CONTEXT.md</read_first>
  <behavior>
    - Proof suite covers more than one happy path.
    - Offline recovery includes repeated drop/recover cycles.
    - WhatsApp-triggered clinic flow exists as a first-class proof path.
  </behavior>
  <action>Create `offline-recovery.e2e.test.ts` to assert queue-first replay, consultation draft replay, inventory replay, conflict record creation, and subtle caught-up state after reconnect per D-25 to D-28. Create `walkin-to-payment.e2e.test.ts` to cover walk-in registration/check-in through consultation, dispense, invoice generation, payment confirmation, and replay-safe completion across mobile/web visibility. Create `whatsapp-triggered-flow.e2e.test.ts` to prove a WhatsApp-triggered clinic action still reconciles correctly when connectivity drops and returns per D-29. In `reconnect-live-sync.test.ts`, verify replayed queue/inventory/billing changes surface on the browser with stale-state prompts rather than silent overwrite. Add Maestro scripts `offline-recovery.yaml`, `multi-drop-recovery.yaml`, and `whatsapp-triggered-flow.yaml` that explicitly toggle connectivity, perform queue/note/barcode work offline, reconnect multiple times, and exercise the WhatsApp-triggered path on-device per D-27 and D-33.</action>
  <acceptance_criteria>
    - `apps/api/tests/integration/offline-recovery.e2e.test.ts` contains repeated disconnect/reconnect assertions and replay integrity expectations.
    - `apps/api/tests/integration/walkin-to-payment.e2e.test.ts` covers the full clinic golden path through payment confirmation.
    - `apps/api/tests/integration/whatsapp-triggered-flow.e2e.test.ts` covers the WhatsApp-triggered clinic path required by D-29.
    - `apps/mobile/.maestro/multi-drop-recovery.yaml` contains more than one drop/recover cycle.
    - `apps/web/tests/integration/reconnect-live-sync.test.ts` asserts stale-state review rather than silent overwrite.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/api/tests/integration/offline-recovery.e2e.test.ts apps/api/tests/integration/walkin-to-payment.e2e.test.ts apps/api/tests/integration/whatsapp-triggered-flow.e2e.test.ts apps/web/tests/integration/reconnect-live-sync.test.ts && npx maestro test apps/mobile/.maestro/offline-recovery.yaml && npx maestro test apps/mobile/.maestro/whatsapp-triggered-flow.yaml</automated>
  </verify>
  <done>The Phase 10 proof harness covers offline recovery, golden-path integration, and WhatsApp-triggered recovery through automated tests and device scripts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the real-device Phase 10 proof bar</name>
  <files>apps/mobile/.maestro/offline-recovery.yaml, apps/mobile/.maestro/multi-drop-recovery.yaml, apps/mobile/.maestro/whatsapp-triggered-flow.yaml</files>
  <what-built>Automated integration tests and Maestro drills now cover offline recovery, multiple reconnect cycles, walk-in-to-payment, WhatsApp-triggered flow, and browser replay visibility.</what-built>
  <action>Run the real-device proof checklist below only after Task 1's automated suite is green. Approval is blocked until the repeated disconnect/reconnect drill, golden path, and WhatsApp-triggered clinic path all pass on real hardware; if any issue appears, capture it and resume execution instead of approving, per D-28 and D-30.</action>
  <how-to-verify>
    1. Run the mobile app on a real mid-range Android 8+ device and sign in to a clinic account with queue, EMR, inventory, billing, WhatsApp, and web dashboard data available.
    2. Put the device offline. Perform a real queue check-in, add/update consultation notes, and scan/queue an inventory stock action. Verify each action remains usable and visible locally while the sync badge stays calm but visible.
    3. Restore connectivity, confirm queue replay happens before the other backlog work, and verify any conflict/failure items appear in the failure center rather than disappearing.
    4. Repeat the drop/recover cycle at least two more times using the `multi-drop-recovery` drill to confirm the app remains usable for several hours offline and does not depend on one clean reconnect.
    5. Execute the full walk-in → consultation → dispense → invoice → payment path and verify the final state is consistent on both mobile and web after replay.
    6. Trigger the WhatsApp-based clinic flow, disconnect during the relevant state change, reconnect, and verify the replay lands correctly without breaking thread/log state.
    7. If any integrity or polish issue appears, do not approve. Have the executor fix it before sign-off, per D-28 and D-30.
  </how-to-verify>
  <verify>
    <automated>MISSING — Task 1 automated proof harness must pass before this blocking human verification can be approved</automated>
  </verify>
  <done>Human approves only after the real-device proof bar passes; otherwise the reported issue list becomes the resume input for the executor.</done>
  <resume-signal>Type "approved" or describe the issue(s) that blocked the real-device proof.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated proof harness → sign-off decision | Passing automation informs whether the phase is safe to approve |
| Real-device drill → human approval | Human observation validates multi-drop resilience and real hardware/network behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Repudiation | Phase sign-off | mitigate | Require automated proof plus blocking human verification before approval |
| T-10-11 | Denial of Service | Emulator-only confidence | mitigate | Include Maestro device drills and a real mid-range Android checkpoint per D-31 to D-33 |
| T-10-12 | Integrity | Unfixed proof failures | mitigate | Instruct the executor to stop approval and fix all discovered integrity/polish issues before sign-off per D-28 and D-30 |
</threat_model>

<verification>
- Integration tests and Maestro proof harnesses pass
- Proof suite covers offline recovery, golden path, and WhatsApp-triggered flow
- Human real-device checkpoint approves the repeated disconnect/reconnect drill
</verification>

<success_criteria>
- Phase 10 has credible automated proof beyond a single happy path
- Real-device offline recovery has been verified under repeated drop/recover conditions
- Phase sign-off is blocked until discovered proof issues are fixed and the human approves
</success_criteria>

<output>
After completion, create `.planning/phases/10-offline-hardening-integration-polish/10-06-SUMMARY.md`
</output>
