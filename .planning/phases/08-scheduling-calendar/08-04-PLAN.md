---
phase: 08-scheduling-calendar
plan: "04"
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - apps/api/src/lib/ist-date.ts
  - apps/api/src/modules/queue/queue.repository.ts
  - apps/api/src/modules/queue/queue.service.ts
  - apps/api/src/jobs/midnight-archive.ts
  - apps/api/src/lib/__tests__/ist-date.test.ts
  - apps/api/src/modules/queue/__tests__/queue.service.test.ts
  - apps/api/tests/queue/queue-priority-ordering.test.ts
autonomous: true
requirements: [SCH-02]

must_haves:
  truths:
    - "A checked-in scheduled patient whose slot was due earlier sorts ahead of a walk-in who arrived later, on both the visible board and call-next"
    - "EXPECTED entries are returned as their own group by the queue board API so the mobile board can show them first"
    - "An EXPECTED entry can only become WAITING or NO_SHOW, and an early check-in flips it to WAITING immediately rather than waiting for the slot time, without changing its queue priority time (D-11)"
    - "All IST date arithmetic in the API lives in one module and is unit tested"
    - "EXPECTED entries are excluded from the same-pet duplicate-check and from midnight archiving until they resolve"
  artifacts:
    - path: "apps/api/src/lib/ist-date.ts"
      provides: "getTodayIST, addDaysIST, istDateOnly, istDayBounds, istMinutesOfDay, minutesToIstDate, weekdayIST"
      exports: ["getTodayIST", "addDaysIST", "istDateOnly", "istDayBounds", "minutesToIstDate", "weekdayIST"]
    - path: "apps/api/src/modules/queue/queue.repository.ts"
      provides: "queuePriorityAt ordering on both the board and call-next, and an expected group on the board"
      contains: "queuePriorityAt"
    - path: "apps/api/tests/queue/queue-priority-ordering.test.ts"
      provides: "End-to-end proof of D-10 ordering across a scheduled and a walk-in entry"
      contains: "queuePriorityAt"
  key_links:
    - from: "apps/api/src/modules/queue/queue.repository.ts findNextWaiting"
      to: "apps/api/src/modules/queue/queue.repository.ts getQueueBoard"
      via: "both orderBy clauses must use queuePriorityAt or call-next disagrees with the visible board"
      pattern: "queuePriorityAt: 'asc'"
    - from: "apps/api/src/lib/ist-date.ts"
      to: "apps/api/src/modules/queue/queue.repository.ts"
      via: "getTodayIST delegates to the shared module instead of duplicating the arithmetic"
      pattern: "ist-date.js"
---

<objective>
Deliver D-10's queue ordering and D-08/D-11/D-13's `EXPECTED` queue semantics inside the existing Phase 3 queue module, and extract the one piece of IST date arithmetic in the codebase into a shared module every Phase 8 service will import.

Purpose: SCH-02 promises a scheduled appointment "appears in the walk-in queue at its time slot". RESEARCH Pattern 3 shows that promise reduces to one new column plus two `orderBy` changes — but `08-PATTERNS.md` finding 4 and 5 record that the surface area is wider than RESEARCH's single citation: three status-enumerating queries, a fourth board group, and `createEntry`'s explicit field list all need to change or `EXPECTED` entries silently vanish and call-next disagrees with the visible board. This plan owns every edit inside `apps/api/src/modules/queue/` so no later plan has to touch it.
Output: `apps/api/src/lib/ist-date.ts`, a queue repository and service that understand `EXPECTED` and `queuePriorityAt`, and an integration test that proves the ordering promise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduling-calendar/08-CONTEXT.md
@.planning/phases/08-scheduling-calendar/08-RESEARCH.md
@.planning/phases/08-scheduling-calendar/08-PATTERNS.md
@.planning/phases/08-scheduling-calendar/08-01-SUMMARY.md
@.planning/phases/08-scheduling-calendar/08-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract IST date arithmetic into apps/api/src/lib/ist-date.ts and route the two existing call sites through it</name>
  <files>apps/api/src/lib/ist-date.ts, apps/api/src/modules/queue/queue.repository.ts, apps/api/src/jobs/midnight-archive.ts, apps/api/src/lib/__tests__/ist-date.test.ts</files>
  <read_first>
    - apps/api/src/modules/queue/queue.repository.ts (lines 14-25 — the `static getTodayIST(date?: Date)` implementation using `toLocaleDateString('en-CA', { timeZone: 'Asia/Kolkata' })` and `Date.UTC(y, m-1, d, -5, -30, 0, 0)`; this is the only IST date math in the repo and the arithmetic must be preserved exactly, not re-derived)
    - apps/api/src/jobs/midnight-archive.ts (complete file — line 30's `{ timezone: 'Asia/Kolkata' }` and the `QueueRepository.getTodayIST()` call at line ~16)
    - apps/api/src/lib/audit-log.ts and apps/api/src/lib/prisma-rls.ts (the only two files in `apps/api/src/lib/` today — match their export style, ESM `.js` relative imports, and file-header comment convention)
    - .planning/phases/08-scheduling-calendar/08-PATTERNS.md (§ IST date handling and § `appointment.repository.ts` — the excerpt of `getTodayIST` and the instruction "Do not duplicate the arithmetic inline")
    - packages/types/src/constants/scheduling.constants.ts (as written by plan 08-01 — `SCHEDULING_TIMEZONE` is the single source of the `'Asia/Kolkata'` literal)
  </read_first>
  <behavior>
    - Test "getTodayIST returns IST midnight as a UTC instant": for a fixed input of `2026-08-13T02:00:00Z` (07:30 IST on 13 Aug), `getTodayIST` returns the instant `2026-08-12T18:30:00Z` (IST midnight of 13 Aug).
    - Test "getTodayIST rolls over at IST midnight, not UTC midnight": `2026-08-13T18:29:00Z` (23:59 IST on 13 Aug) and `2026-08-13T18:31:00Z` (00:01 IST on 14 Aug) return different days.
    - Test "istDateOnly": returns a `Date` at IST midnight for the IST calendar day containing the input, and is idempotent (applying it twice returns the same value).
    - Test "addDaysIST": `addDaysIST(getTodayIST(ref), 1)` equals the IST midnight of the following IST calendar day, and `addDaysIST(x, 90)` lands exactly 90 IST days later including across a month boundary.
    - Test "istDayBounds": returns `{ start, end }` where `start` is IST midnight and `end` is IST midnight of the next day, so a `gte: start, lt: end` filter selects exactly one IST calendar day.
    - Test "minutesToIstDate": `minutesToIstDate(istMidnightOf13Aug, 540)` returns the instant equal to 09:00 IST on 13 Aug; `minutesToIstDate(x, 0)` returns `x`.
    - Test "istMinutesOfDay": for the instant 09:00 IST returns 540; for 23:59 IST returns 1439; round-trips with `minutesToIstDate`.
    - Test "weekdayIST": for a known Sunday instant returns 0, for the following Saturday returns 6, matching the `WEEKDAY_LABELS` index convention in `scheduling.constants.ts`.
  </behavior>
  <action>
Create `apps/api/src/lib/ist-date.ts` exporting these plain functions (no class, no side effects, no Prisma import, so it is unit-testable with zero setup):

- `getTodayIST(date?: Date): Date` — move the body of `QueueRepository.getTodayIST` here verbatim. Do not rewrite the arithmetic; the `Date.UTC(year, month - 1, day, -5, -30, 0, 0)` form is the shipped behavior every Phase 3 query already depends on.
- `istDateOnly(date: Date): Date` — alias semantics of `getTodayIST(date)`, named for the "which IST calendar day is this instant in" intent that availability and appointment queries express.
- `addDaysIST(istMidnight: Date, days: number): Date` — add `days * 86400000` to an IST-midnight instant. Because India has no DST, fixed-millisecond arithmetic is exact; state that in a comment so nobody later reaches for a date library.
- `istDayBounds(date: Date): { start: Date; end: Date }` — `start = istDateOnly(date)`, `end = addDaysIST(start, 1)`. Every day-scoped query uses `gte: start, lt: end` rather than a same-day equality test.
- `minutesToIstDate(istMidnight: Date, minutes: number): Date` — `new Date(istMidnight.getTime() + minutes * 60000)`. This is the bridge between the minutes-from-midnight availability model and an absolute `scheduledFor` timestamp.
- `istMinutesOfDay(date: Date): number` — `Math.floor((date.getTime() - istDateOnly(date).getTime()) / 60000)`, the inverse of `minutesToIstDate`.
- `weekdayIST(date: Date): number` — 0=Sunday..6=Saturday for the IST calendar day containing the instant; derive it from `istDateOnly(date).getUTCDay()` after shifting, or from `toLocaleDateString('en-CA', { timeZone: SCHEDULING_TIMEZONE })` parsed into a `Date.UTC` — whichever you choose, the test for a known Sunday and Saturday must pass.

Import `SCHEDULING_TIMEZONE` from `@breeyo/types` rather than repeating the `'Asia/Kolkata'` string literal.

Then change `QueueRepository.getTodayIST` to `static getTodayIST(date?: Date): Date { return getTodayIST(date); }` with an import of `../../lib/ist-date.js`. Keep the static method — `midnight-archive.ts` and existing tests call it, and removing it would be an unrelated breaking change. Also change `midnight-archive.ts` to import `getTodayIST` directly from `../lib/ist-date.js` instead of reaching through `QueueRepository`, and to use `SCHEDULING_TIMEZONE` for its `{ timezone: ... }` option. Do not convert `midnight-archive.ts` off `node-cron` — RESEARCH § State of the Art explicitly scopes that fix out of Phase 8.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/api test -- --run src/lib/__tests__/ist-date.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/api test -- --run src/lib/__tests__/ist-date.test.ts` reports at least 8 passing tests covering every behavior above.
    - `grep -Ec 'export function getTodayIST|export function istDateOnly|export function addDaysIST|export function istDayBounds|export function minutesToIstDate|export function istMinutesOfDay|export function weekdayIST' apps/api/src/lib/ist-date.ts` returns `7`.
    - `grep -c "Asia/Kolkata" apps/api/src/lib/ist-date.ts` returns `0` (the timezone comes from `SCHEDULING_TIMEZONE`).
    - `grep -c 'ist-date.js' apps/api/src/modules/queue/queue.repository.ts` returns `1` and `grep -c 'ist-date.js' apps/api/src/jobs/midnight-archive.ts` returns `1`.
    - `grep -v '^\s*\*' apps/api/src/modules/queue/queue.repository.ts | grep -c 'toLocaleDateString' ` returns `0` — the arithmetic no longer lives in the repository.
    - `grep -Ec 'date-fns|luxon|dayjs|moment' apps/api/package.json` returns `0` — no date library was added.
    - `pnpm --filter @breeyo/api test -- --run tests/queue` passes (the existing queue suites still work through the delegating static method).
  </acceptance_criteria>
  <done>All IST date arithmetic lives in `apps/api/src/lib/ist-date.ts`, is unit tested including the IST-vs-UTC midnight rollover, and the two pre-existing call sites delegate to it with no behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Set queuePriorityAt at entry creation and order both the board and call-next by it</name>
  <files>apps/api/src/modules/queue/queue.repository.ts, apps/api/src/modules/queue/queue.service.ts, apps/api/tests/queue/queue-priority-ordering.test.ts</files>
  <read_first>
    - apps/api/src/modules/queue/queue.repository.ts (lines 74-95 `createEntry` — note it lists every field explicitly and does NOT spread its argument, so a new column must be added by hand; lines 129-134 `findNextWaiting`'s `orderBy`; lines 154-166 `getQueueBoard`'s waiting branch `orderBy`)
    - apps/api/src/modules/queue/queue.service.ts (lines 64-84 `checkIn` — the position assignment and the `createEntry` call whose argument list must gain the new field)
    - apps/api/src/modules/queue/queue.types.ts (complete file — `CheckInParams`, `UpdateStatusParams`; add any new param field here rather than inline)
    - apps/api/tests/queue/queue-board.test.ts (the integration idiom this plan's new test file copies)
    - .planning/phases/08-scheduling-calendar/08-RESEARCH.md (§ Architecture Patterns Pattern 3 — the exact before/after `orderBy` diff and the worked 2:00pm-slot-versus-2:05pm-walk-in example)
    - .planning/phases/08-scheduling-calendar/08-PATTERNS.md (finding under § `apps/api/prisma/schema.prisma` — "Both sites must change, or call next and the visible board disagree")
    - .planning/phases/08-scheduling-calendar/08-CONTEXT.md (D-10, D-11)
  </read_first>
  <behavior>
    - Test "walk-in check-in sets queuePriorityAt to now": after `checkIn`, the created entry's `queuePriorityAt` equals its `checkedInAt` to within one second.
    - Test "scheduled patient checking in later still sorts first": create an `EXPECTED` entry with `queuePriorityAt` at 14:00 and a walk-in `WAITING` entry with `queuePriorityAt` at 14:05; transition the `EXPECTED` entry to `WAITING` at 14:07; `getQueueBoard`'s waiting list returns the scheduled patient at index 0 (D-10).
    - Test "emergency still outranks priority time": an emergency walk-in with `queuePriorityAt` 14:30 sorts ahead of a non-emergency scheduled entry with `queuePriorityAt` 14:00, because `isEmergency: 'desc'` remains the first sort key.
    - Test "call-next agrees with the board": `findNextWaiting` returns the same entry that `getQueueBoard().waiting[0]` returns, for the same fixture set.
    - Test "early check-in does not change priority": transitioning an `EXPECTED` entry to `WAITING` before its slot time leaves `queuePriorityAt` unchanged and sets `checkedInAt` to now (D-11).
  </behavior>
  <action>
**`queue.repository.ts`**
- `createEntry`: add `queuePriorityAt` to the explicit field list and to its input type. Also add `appointmentId` (nullable) so plan 08-09's handoff pass can create appointment-derived entries through the same method rather than reaching for `prisma.queueEntry.create` directly.
- `findNextWaiting` (around line 129-134): change `{ checkedInAt: 'asc' }` to `{ queuePriorityAt: 'asc' }`, leaving `{ isEmergency: 'desc' }` as the first key.
- `getQueueBoard` waiting branch (around line 162-165): the identical change.
- Add `async updateEntryPriority(...)` only if a later plan actually needs it — otherwise do not add a mutator. `queuePriorityAt` is set once at creation and never mutated; that invariant is what makes D-10 hold for early check-ins (D-11).

**`queue.service.ts`**
- `checkIn`: pass `queuePriorityAt: new Date()` in the `createEntry` argument. For an organic walk-in, priority time and physical arrival time are the same value, which is exactly why the backfill in plan 08-03 used `checked_in_at`.
- `updateStatus`: when transitioning to `WAITING` from `EXPECTED`, set `checkedInAt = new Date()` in the `updateData` object (the entry was created by the sweep, so its `checkedInAt` default is the creation instant, not the arrival instant) and do NOT touch `queuePriorityAt`. Add a comment naming D-10 and D-11 and stating the invariant explicitly, because the natural instinct on reading this code later is to "fix" the priority to the arrival time, which would silently delete the feature.
- Do not change the `position` computation in this task; `EXPECTED` position handling is Task 3.

Create `apps/api/tests/queue/queue-priority-ordering.test.ts` implementing the five behaviors above with `app.inject` for the transitions and direct `prisma` reads for the ordering assertions. Use fixed, explicit timestamps rather than relative offsets so failures are diagnosable.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/api test -- --run tests/queue/queue-priority-ordering.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/api test -- --run tests/queue/queue-priority-ordering.test.ts` reports at least 5 passing tests.
    - `grep -c "queuePriorityAt: 'asc'" apps/api/src/modules/queue/queue.repository.ts` returns exactly `2` — both `findNextWaiting` and `getQueueBoard`.
    - `grep -c "checkedInAt: 'asc'" apps/api/src/modules/queue/queue.repository.ts` returns `0`.
    - `awk '/async createEntry/,/^  }/' apps/api/src/modules/queue/queue.repository.ts | grep -c 'queuePriorityAt'` returns at least `1`, and the same range grepped for `appointmentId` returns at least `1`.
    - `grep -c 'queuePriorityAt: new Date()' apps/api/src/modules/queue/queue.service.ts` returns `1`.
    - `awk '/async updateStatus/,/^  }/' apps/api/src/modules/queue/queue.service.ts | grep -c 'queuePriorityAt'` returns `0` outside comments — verify with `awk '/async updateStatus/,/^  }/' apps/api/src/modules/queue/queue.service.ts | grep -v '//' | grep -c 'queuePriorityAt ='` returning `0`.
    - `pnpm --filter @breeyo/api test -- --run src/modules/queue` and `pnpm --filter @breeyo/api test -- --run tests/queue` both pass.
  </acceptance_criteria>
  <done>`queuePriorityAt` is written once at entry creation, both ordering sites read it, an emergency still outranks it, and an integration test proves a scheduled patient who arrives after a later walk-in still sorts ahead.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make EXPECTED a real status across every queue query, add the expected board group, and gate its transitions</name>
  <files>apps/api/src/modules/queue/queue.repository.ts, apps/api/src/modules/queue/queue.service.ts, apps/api/src/modules/queue/queue.types.ts, apps/api/src/modules/queue/__tests__/queue.service.test.ts</files>
  <read_first>
    - apps/api/src/modules/queue/queue.repository.ts (all of it, and specifically the three status-enumerating queries: `findTodayActiveEntryForPet` around line 36 with `status: { in: ['WAITING','IN_CONSULT'] }`, `getQueueBoard`'s done branch around line 170 with `{ in: ['DONE','NO_SHOW'] }`, and `archiveEntries` around line 215 with `{ in: ['WAITING','DONE','NO_SHOW'] }` — verify the exact current line numbers yourself, they may have shifted after Task 2)
    - apps/api/src/modules/queue/queue.service.ts (lines 41-46 for the error throw shape, lines 101-133 for the `isValidTransition` guard and the status-derived `updateData` writes, lines 192-199 for `broadcast`)
    - apps/api/src/modules/queue/__tests__/queue.service.test.ts (lines 1-83 — the hand-rolled `vi.fn()` repository mock, the `createMockIO` helper, and the fixed sequential UUID constants)
    - packages/types/src/queue.ts (as modified by plan 08-01 — `QueueBoard` now has `expected` as its first field)
    - packages/types/src/constants/queue-status.ts (as modified by plan 08-01 — `QueueStatus.EXPECTED`, its transitions, and the new `ACTIVE_QUEUE_STATUSES` / `CLOSED_QUEUE_STATUSES` lists)
    - .planning/phases/08-scheduling-calendar/08-PATTERNS.md (§ `apps/api/prisma/schema.prisma` — the paragraph listing the three affected queries and the fourth board group requirement)
    - .planning/phases/08-scheduling-calendar/08-CONTEXT.md (D-08, D-09, D-11, D-13)
  </read_first>
  <behavior>
    - Test "getQueueBoard returns four groups": the response object has `expected`, `inConsult`, `waiting` and `done` keys, with `expected` containing only `EXPECTED` entries ordered by `queuePriorityAt` ascending.
    - Test "EXPECTED blocks a duplicate same-day check-in": a pet with an `EXPECTED` entry today is treated as already in the queue, so `checkIn` for that pet throws `ALREADY_IN_QUEUE` with `statusCode` 409 (the front desk should check the scheduled patient in, not create a second entry).
    - Test "EXPECTED is not archived at midnight": `archiveEntries` leaves an `EXPECTED` entry untouched and archives a `WAITING` one from the same prior day.
    - Test "EXPECTED to IN_CONSULT is rejected": `updateStatus` throws `INVALID_TRANSITION` with `statusCode` 400.
    - Test "EXPECTED to WAITING is accepted and broadcasts": the transition succeeds, sets `checkedInAt`, and emits `QUEUE_UPDATED` to `clinic:{clinicId}`.
    - Test "EXPECTED to NO_SHOW is accepted and sets completedAt": the D-09 grace-expiry path.
    - Test "WAITING to EXPECTED is rejected": nothing may move backwards into `EXPECTED`.
    - Test "EXPECTED entries have no position": an `EXPECTED` entry is created with `position: 0` and does not shift the `WAITING` position sequence.
  </behavior>
  <action>
**`queue.repository.ts`** — replace the three inline status string arrays with the named lists from `@breeyo/types`, so the next status addition cannot miss a site:
- `findTodayActiveEntryForPet`: use `ACTIVE_QUEUE_STATUSES` (`EXPECTED`, `WAITING`, `IN_CONSULT`). Including `EXPECTED` is deliberate — D-13 puts the scheduled patient on the board, so a second walk-in check-in for the same pet is a duplicate, and the correct staff action is to check the existing `EXPECTED` entry in.
- `getQueueBoard` done branch: use `CLOSED_QUEUE_STATUSES`.
- `archiveEntries`: keep `WAITING`, `DONE`, `NO_SHOW` and deliberately do NOT add `EXPECTED`. An unresolved `EXPECTED` entry means the sweep has not yet flipped it to `NO_SHOW`; archiving it would strand the appointment in `SCHEDULED` forever. Add a comment stating this and naming D-09.
- `getQueueBoard`: add a fourth query returning `status: 'EXPECTED'` entries for the requested IST day, ordered `[{ isEmergency: 'desc' }, { queuePriorityAt: 'asc' }]`, and return it as the `expected` field of the board object. Use `istDayBounds` from `../../lib/ist-date.js` for the day filter rather than a bare `gte: today`.

**`queue.types.ts`** — add `queuePriorityAt?: Date` and `appointmentId?: string | null` to whatever param interface `createEntry` consumes, so plan 08-09 passes them through typed params rather than casting.

**`queue.service.ts`** — in `updateStatus`, extend the status-derived `updateData` switch:
- transitioning to `WAITING`: set `checkedInAt = new Date()` (already added in Task 2) and assign a real `position` by re-reading `countWaiting` for the clinic's IST day and using `waitingCount + 1`, because an `EXPECTED` entry entered the board with `position: 0`.
- transitioning to `NO_SHOW`: `completedAt = new Date()` (existing behavior, unchanged).
Leave the `isValidTransition` guard exactly as it is — plan 08-01's `QUEUE_TRANSITIONS` already encodes every legal `EXPECTED` edge, so no special-casing belongs here. Keep the existing `broadcast(entry.clinicId, SOCKET_EVENTS.QUEUE_UPDATED, ...)` call unchanged.

Add a `createExpectedEntry`-shaped path only if it does not already fall out of `createEntry`: plan 08-09's handoff needs to create an entry with `status: 'EXPECTED'`, `position: 0`, `queuePriorityAt: appointment.scheduledFor`, `appointmentId`, and `checkedInBy` set to the appointment's `createdById`. If `createEntry` can express all of that through its existing parameters, do not add a second method — record that decision in the summary so plan 08-09 knows which call to make.

Extend `apps/api/src/modules/queue/__tests__/queue.service.test.ts` with the eight behaviors above using the existing `vi.fn()` mock-repository style and fixed UUID constants.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/api test -- --run src/modules/queue/__tests__/queue.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/api test -- --run src/modules/queue/__tests__/queue.service.test.ts` reports at least 8 passing tests covering every behavior above.
    - `grep -Ec "ACTIVE_QUEUE_STATUSES|CLOSED_QUEUE_STATUSES" apps/api/src/modules/queue/queue.repository.ts` returns at least `2`.
    - `grep -v '^\s*//' apps/api/src/modules/queue/queue.repository.ts | grep -c "in: \['WAITING', 'IN_CONSULT'\]"` returns `0` (the inline literal was replaced by the named list).
    - `awk '/async archiveEntries/,/^  }/' apps/api/src/modules/queue/queue.repository.ts | grep -c 'EXPECTED'` returns `0` outside comments — an unresolved `EXPECTED` entry is never archived.
    - `awk '/async getQueueBoard/,/^  }/' apps/api/src/modules/queue/queue.repository.ts | grep -c 'EXPECTED'` returns at least `1`, and the same range grepped for `istDayBounds` returns at least `1`.
    - `grep -c 'expected' apps/api/src/modules/queue/queue.repository.ts` returns at least `1` (the returned board field).
    - `pnpm --filter @breeyo/api test -- --run tests/queue` passes in full, including the pre-existing `queue-board.test.ts` (which must be updated to expect the fourth `expected` key if it asserts the board's exact shape — list any such change in the summary).
    - `pnpm --filter @breeyo/api exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>`EXPECTED` is honoured by every queue query, the board returns it as its own group ordered by scheduled time, only `WAITING` and `NO_SHOW` transitions out of it are permitted, and it neither collides with walk-in positions nor gets archived before it resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client status-update request → `queue.service.updateStatus` | Staff-supplied status transitions cross into a privileged queue-state change |
| appointment-derived entry → walk-in queue ordering | A server-created row is inserted into the same ordered list as human-created walk-ins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Elevation of Privilege | a client PATCHing an arbitrary entry to `EXPECTED` to jump the queue | mitigate | `QUEUE_TRANSITIONS` has no inbound edge into `EXPECTED`, so the existing `isValidTransition` guard rejects it with `400 INVALID_TRANSITION`; Task 3 asserts the `WAITING -> EXPECTED` rejection explicitly |
| T-08-14 | Tampering | a client rewriting `queuePriorityAt` to seize the front of the queue | mitigate | No endpoint, service method or param interface accepts `queuePriorityAt` from a request body; it is written only by `checkIn` (as `new Date()`) and by plan 08-09's sweep (as `appointment.scheduledFor`), and `updateStatus` is grep-gated against touching it |
| T-08-15 | Information Disclosure | `getQueueBoard`'s new fourth query omitting the `clinicId` filter | mitigate | The new `expected` query copies the existing branches' `where` clause including `clinicId` and `archivedAt: null`; the pre-existing tenant-isolation integration suite covers the endpoint, and Task 3's board test runs against a seeded single clinic |
| T-08-16 | Denial of Service | an unresolved `EXPECTED` entry accumulating forever and bloating the board | mitigate | Deliberately excluded from `archiveEntries` (so it is never silently hidden) but plan 08-09's no-show pass flips it to `NO_SHOW` after `NO_SHOW_GRACE_MINUTES`, after which normal archiving applies; the comment in `archiveEntries` names D-09 so the exclusion reads as intentional |
</threat_model>

<verification>
- `pnpm --filter @breeyo/api test` passes in full, including every pre-existing Phase 3 queue suite.
- `pnpm --filter @breeyo/api exec tsc --noEmit` exits 0.
- `grep -rn "toLocaleDateString" apps/api/src --include=*.ts | grep -v 'lib/ist-date.ts'` returns no queue or scheduling file (IST arithmetic is not duplicated).
</verification>

<success_criteria>
- `apps/api/src/lib/ist-date.ts` is the only place in `apps/api` performing IST date arithmetic, and it is unit tested across the IST-midnight rollover.
- Both `findNextWaiting` and `getQueueBoard` order by `queuePriorityAt`, so call-next and the visible board can never disagree.
- An integration test proves a 14:00-slot patient checking in at 14:07 sorts ahead of a 14:05 walk-in.
- `getQueueBoard` returns an `expected` group; `EXPECTED` blocks duplicate same-pet check-ins, is never archived while unresolved, and can only transition to `WAITING` or `NO_SHOW`.
- `queuePriorityAt` is provably never mutated after creation.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-calendar/08-04-SUMMARY.md` when done. Record: the exact signature of `createEntry` after this plan (so 08-09 calls it correctly), whether a separate `createExpectedEntry` method was added, the final `getQueueBoard` return shape, and every pre-existing test file that had to be updated for the fourth board group.
</output>
