---
phase: 07-whatsapp-communication
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/src/providers/ThemeProvider.tsx
  - apps/mobile/app/(app)/ui-spike.tsx
  - pnpm-lock.yaml
autonomous: false
requirements:
  - WHA-05

must_haves:
  truths:
    - "apps/mobile declares @breeyo/ui and react-native-paper (plus the peer deps react-native-safe-area-context, react-native-reanimated, react-native-gesture-handler) or a documented deviation records why plain React Native components are used instead (D-17)"
    - "The mobile app bundles successfully with the chosen approach — `expo export` completes without an unresolved-module error"
    - "The root layout wraps the app in a Paper provider with the Breeyo MD3 theme (primary #2E7D32, secondary #5D4037, tertiary #E65100, background #FFFBF5) when the Paper path is taken"
    - "A spike screen renders at minimum a Paper Button, Card, Chip and one @breeyo/ui component, proving the integration end-to-end on Expo SDK 52"
    - "The outcome is written into the plan summary as a binding instruction for plans 07-06, 07-14, 07-15 and 07-16: either 'Paper path' or 'plain RN path with UI-SPEC token values'"
  artifacts:
    - path: "apps/mobile/package.json"
      provides: "Mobile dependency set resolved for the Phase 7 UI approach"
      contains: "react-native-paper"
    - path: "apps/mobile/app/(app)/ui-spike.tsx"
      provides: "Throwaway spike route proving Paper + @breeyo/ui render under Expo SDK 52"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "react-native-paper PaperProvider"
      via: "root provider wrapping with the Breeyo theme"
      pattern: "PaperProvider|Provider as PaperProvider"
---

<objective>
Resolve the mobile UI library question before any Phase 7 screen is written (D-17). `apps/mobile/package.json` declares neither `@breeyo/ui` nor `react-native-paper`, yet 19 Phase 3 files import `react-native-paper` and 15 import `@breeyo/ui`, and 07-UI-SPEC.md mandates React Native Paper v5 (MD3). Phase 4 silently worked around this with plain React Native components.

Purpose: four downstream mobile plans (07-06 components, 07-14 hooks and send sheet, 07-15 inbox/thread screens, 07-16 config and cross-module entry points) need to know which component vocabulary to use. D-17 locks the process: spike the Paper integration first; if it proves fiddly on Expo SDK 52, fall back to plain React Native while honoring UI-SPEC's token values (4px spacing multiples, exact hexes, four type roles, 44pt targets, WCAG AA). This must not be an implementer's silent mid-phase improvisation.

Output: a resolved and bundling mobile dependency set, a Paper theme provider (or a recorded deviation), a spike route, and a summary that states the binding path for downstream plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-whatsapp-communication/07-CONTEXT.md
@.planning/phases/07-whatsapp-communication/07-RESEARCH.md
@.planning/phases/07-whatsapp-communication/07-PATTERNS.md
@.planning/phases/07-whatsapp-communication/07-UI-SPEC.md

<interfaces>
Verified current state (07-RESEARCH § Pitfall 11, § Pitfall 12; 07-PATTERNS § R):
- apps/mobile/package.json dependencies: @breeyo/types, @breeyo/validators, @expo/vector-icons, @tanstack/react-query, expo ~52.0.0, expo-haptics, expo-image-picker, expo-linking, expo-notifications, expo-router, expo-secure-store, react ^18.3.0, react-native ^0.76.0, socket.io-client, zustand
- packages/ui declares react-native-paper ^5.15.1 and react-native-safe-area-context ^5.7.0 as dependencies, and peerDepends on react-native-reanimated >=3 and react-native-gesture-handler >=2
- apps/mobile/app/_layout.tsx currently wraps only QueryProvider + AuthProvider + Slot — there is no PaperProvider anywhere
- apps/mobile has no babel.config.js; Expo SDK 52's babel-preset-expo already bundles the react-native-reanimated plugin
- Paper analog to copy from: apps/mobile/src/features/queue/screens/QueueScreen.tsx (imports Text/FAB/ActivityIndicator from react-native-paper and showToast from @breeyo/ui)
- Plain-RN fallback analog: apps/mobile/src/features/consultation/screens/ConsultationScreen.tsx (react-native primitives only)
- Theme token source: packages/ui/src/theme/ (colors #2E7D32 / #5D4037 / #E65100 / #FFFBF5)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike the React Native Paper v5 + @breeyo/ui integration on Expo SDK 52 (D-17)</name>
  <files>
    apps/mobile/package.json,
    apps/mobile/src/providers/ThemeProvider.tsx,
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/(app)/ui-spike.tsx,
    pnpm-lock.yaml
  </files>
  <read_first>
    apps/mobile/package.json,
    apps/mobile/app/_layout.tsx,
    apps/mobile/app.config.ts,
    apps/mobile/tsconfig.json,
    apps/mobile/src/providers/QueryProvider.tsx,
    apps/mobile/src/features/queue/screens/QueueScreen.tsx,
    apps/mobile/src/features/consultation/screens/ConsultationScreen.tsx,
    packages/ui/package.json,
    packages/ui/src/index.ts,
    packages/ui/src/theme/index.ts
  </read_first>
  <action>
    WHA-05 / D-17: Prove or disprove the Paper path before Phase 7 screens exist.

    1. Add to `apps/mobile/package.json` dependencies: `"@breeyo/ui": "workspace:*"` and `"react-native-paper": "^5.15.1"`. Install the native peers with the Expo-pinned versions rather than hand-picking them: run `cd apps/mobile && npx expo install react-native-safe-area-context react-native-reanimated react-native-gesture-handler` so SDK 52-compatible versions are selected. Then `pnpm install` from the repo root.
    2. Verify Phase 4's already-committed imports also resolve, since Phase 7 screens live in the same bundle: `apps/mobile/src/features/pdf/hooks/useGeneratePdf.ts` imports `expo-print`, `expo-file-system` and `expo-sharing`, and voice code imports `expo-av`, none of which are declared. If the bundle fails on these, install them with `npx expo install expo-print expo-file-system expo-sharing expo-av`. This is a dependency conflict blocking Phase 7 verification, not new scope — note it in the summary.
    3. Create `apps/mobile/src/providers/ThemeProvider.tsx`: wrap children in Paper's `Provider` (imported as `PaperProvider`) with an MD3 theme built from `MD3LightTheme` overriding colors.primary `#2E7D32`, colors.secondary `#5D4037`, colors.tertiary `#E65100`, colors.background `#FFFBF5`, colors.error `#BA1A1A`. Reuse the token values from `packages/ui/src/theme/` rather than re-typing hexes if that module exports a consumable theme. Also wrap in `SafeAreaProvider` from react-native-safe-area-context and `GestureHandlerRootView` (flex: 1) from react-native-gesture-handler, in that nesting order: GestureHandlerRootView > SafeAreaProvider > PaperProvider.
    4. Insert `ThemeProvider` into `apps/mobile/app/_layout.tsx` between `QueryProvider` and `AuthProvider` so every route including existing Phase 3/4 screens gets the Paper context they already assume.
    5. Create the throwaway spike route `apps/mobile/app/(app)/ui-spike.tsx` rendering, in one ScrollView: Paper `Button` (mode="contained"), Paper `Card` with `Card.Title` and `Card.Content`, Paper `Chip` (selected and unselected), Paper `ActivityIndicator`, Paper `Snackbar`, and at least one `@breeyo/ui` export (e.g. `StatusBadge` and `showToast`). Add a 44x44 minimum-height Pressable to confirm the touch-target rule is expressible. Label the screen "Phase 7 UI spike (D-17) — delete before merge".
    6. Prove it bundles: `cd apps/mobile && npx expo export --platform android --output-dir /tmp/breeyo-ui-spike` must complete without an unresolved-module error. Also run `pnpm --filter @breeyo/mobile exec tsc --noEmit`.
    7. If step 6 fails after a reasonable attempt (reanimated babel plugin missing, gesture-handler native module error under Expo Go, or a peer-version conflict that pnpm cannot resolve), take D-17's documented fallback: revert the `react-native-paper` / `@breeyo/ui` additions, keep the spike route as a plain-RN rendering of the same elements using UI-SPEC token values, and record the failure output verbatim in the summary. Do not leave the repo in a half-migrated state.
  </action>
  <verify>
    <automated>cd apps/mobile && npx expo export --platform android --output-dir /tmp/breeyo-ui-spike >/dev/null 2>&1 && pnpm --filter @breeyo/mobile exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx expo export --platform android --output-dir /tmp/breeyo-ui-spike` exits 0
    - `pnpm --filter @breeyo/mobile exec tsc --noEmit` exits 0
    - `apps/mobile/app/(app)/ui-spike.tsx` exists and, on the Paper path, `grep -c "react-native-paper" apps/mobile/app/\(app\)/ui-spike.tsx` returns >= 1
    - On the Paper path: `node -e "const p=require('./apps/mobile/package.json');process.exit(p.dependencies['react-native-paper']&&p.dependencies['@breeyo/ui']?0:1)"` exits 0
    - On the Paper path: `grep -qE "PaperProvider|Provider as PaperProvider" apps/mobile/app/_layout.tsx` exits 0
    - On the fallback path: `node -e "const p=require('./apps/mobile/package.json');process.exit(p.dependencies['react-native-paper']?1:0)"` exits 0 AND the summary contains a "UI-SPEC deviation" section naming the exact bundler error
  </acceptance_criteria>
  <done>The mobile app bundles with a decided UI approach. Either Paper v5 + @breeyo/ui are installed, themed at the root, and proven by a rendering spike route; or the documented fallback is taken with the failure output recorded. Downstream mobile plans have an unambiguous component vocabulary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the mobile UI foundation</name>
  <read_first>
    .planning/phases/07-whatsapp-communication/07-UI-SPEC.md,
    apps/mobile/package.json,
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/(app)/ui-spike.tsx
  </read_first>
  <action>
    **What was built:** The Phase 7 mobile UI foundation: either React Native Paper v5 + @breeyo/ui wired into apps/mobile with a Breeyo MD3 theme at the root layout, or the D-17 fallback to plain React Native with UI-SPEC token values. A throwaway `/ui-spike` route renders Button, Card, Chip, ActivityIndicator, Snackbar, a @breeyo/ui component and a 44pt touch target.

    **How to verify:**


    1. Start the app: `pnpm --filter @breeyo/mobile dev` and open it in Expo Go or a simulator.
    2. Navigate to `/ui-spike`.
    3. Confirm visually: the Button is filled green `#2E7D32`; the screen background is warm white `#FFFBF5`; the Chip shows a visible selected/unselected difference; the Snackbar appears when triggered; text is legible at 16px; no red error overlay appears.
    4. Confirm existing screens did not regress: open the Walk-in Queue tab and the patient list. Both already import react-native-paper and @breeyo/ui, so they should now render rather than crash.
    5. Increase the device font size to 1.5x (iOS Settings > Display & Brightness > Text Size, or Android Settings > Display > Font size) and re-open `/ui-spike`. Labels must wrap rather than truncate.
    6. State which path was taken: "Paper path approved" or "plain RN fallback approved".

    **Resume signal:** Type "approved: paper" or "approved: plain-rn", or describe what rendered incorrectly.
  </action>
  <verify>
    <human-check>Type "approved: paper" or "approved: plain-rn", or describe what rendered incorrectly.</human-check>
  </verify>
  <done>Human confirms the /ui-spike screen renders with Breeyo tokens, existing Paper-importing screens no longer crash, labels wrap at 1.5x font scale, and states which D-17 path was taken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → apps/mobile bundle | New third-party packages enter the client bundle shipped to clinic devices |
| workspace package → app | @breeyo/ui code executes with full app privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | new mobile dependencies (react-native-paper, safe-area-context, reanimated, gesture-handler) | mitigate | All four are already declared dependencies or peer dependencies of the in-repo `packages/ui` (react-native-paper ^5.15.1 verified in packages/ui/package.json) and are installed with `npx expo install` so versions come from the Expo SDK 52 compatibility set, not arbitrary latest. No new vendor is introduced. |
| T-07-03-02 | Tampering | expo-print / expo-file-system / expo-sharing / expo-av installs | mitigate | First-party Expo modules installed via `npx expo install` (SDK-pinned). Only installed if the existing Phase 4 imports break the bundle. |
| T-07-03-03 | Information Disclosure | throwaway spike route reachable in a shipped build | mitigate | `ui-spike.tsx` renders only static component samples with no clinic data and no API calls; it is labelled for deletion and 07-16's final task removes it before phase completion |
| T-07-03-04 | Denial of Service | half-migrated dependency state breaking every mobile screen | mitigate | Task action mandates a full revert on the fallback path rather than leaving partial Paper wiring; `expo export` + `tsc --noEmit` are the gates |
| T-07-03-SC | Tampering | npm/pip/cargo installs | mitigate | 07-RESEARCH § Package Legitimacy Audit covers this phase's package decisions (zero new external vendors; all four candidates were rejected). The packages added here are Expo-SDK-managed or already vendored by packages/ui, so no `[ASSUMED]`/`[SUS]` package is introduced. If a genuinely new external package becomes necessary, run `slopcheck install -e npm <pkg>` (the `-e npm` flag is mandatory in this repo — slopcheck defaults to PyPI and returns false `[SLOP]`) and gate on a blocking human checkpoint. |
</threat_model>

<verification>
- `cd apps/mobile && npx expo export --platform android --output-dir /tmp/breeyo-ui-spike` exits 0
- `pnpm --filter @breeyo/mobile exec tsc --noEmit` exits 0
- `pnpm --filter @breeyo/mobile test` exits 0 (existing logic-level tests unaffected)
- Human checkpoint confirms the spike screen renders with Breeyo tokens and existing screens do not regress
</verification>

<success_criteria>
1. D-17 is resolved with evidence, not assumption
2. The mobile app bundles (`expo export`) and typechecks
3. On the Paper path: @breeyo/ui + react-native-paper + safe-area-context + reanimated + gesture-handler are declared and the root layout provides the Breeyo MD3 theme
4. On the fallback path: the exact bundler failure is recorded and the repo is fully reverted to plain RN
5. The summary states the binding UI path for plans 07-06, 07-14, 07-15, 07-16
6. Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/07-whatsapp-communication/07-03-SUMMARY.md` — it MUST contain a section headed "UI path decision (D-17)" stating either "Paper path" or "plain RN path", because downstream plans read it as a binding instruction.
</output>
