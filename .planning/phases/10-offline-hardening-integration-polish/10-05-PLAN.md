---
phase: 10-offline-hardening-integration-polish
plan: 05
type: execute
wave: 3
depends_on:
  - "10-02"
  - "10-03"
  - "10-04"
files_modified:
  - apps/mobile/src/features/offline-sync/components/SyncStatusBadge.tsx
  - apps/mobile/src/features/offline-sync/screens/SyncFailureCenterScreen.tsx
  - apps/mobile/src/features/offline-sync/hooks/useSyncStatus.ts
  - apps/mobile/src/features/offline-sync/store/syncUiStore.ts
  - apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx
  - apps/api/src/modules/sync/services/retryEscalation.service.ts
  - apps/api/src/modules/sync/services/replayBroadcast.service.ts
  - apps/api/src/modules/sync/__tests__/retryEscalation.service.test.ts
  - apps/web/src/features/dashboard/hooks/useReplayStaleState.ts
  - apps/web/src/features/queue/hooks/useQueueReplayRealtime.ts
  - apps/web/src/features/inventory/hooks/useInventoryReplayRealtime.ts
  - apps/web/src/features/dashboard/__tests__/replayStaleState.test.tsx
autonomous: true
requirements:
  - PLT-03
must_haves:
  truths:
    - "Staff can always see sync state through a calm persistent badge during normal work."
    - "Failed replay items escalate into an actionable failure center with guided retry and ownership tracking."
    - "Browser and mobile clients receive scoped replay updates and show stale-state review instead of silently overwriting active views."
  artifacts:
    - path: "apps/mobile/src/features/offline-sync/components/SyncStatusBadge.tsx"
      provides: "Always-visible calm sync badge"
      exports: ["SyncStatusBadge"]
    - path: "apps/mobile/src/features/offline-sync/screens/SyncFailureCenterScreen.tsx"
      provides: "Actionable failure center with retry/escalation UX"
      exports: ["SyncFailureCenterScreen"]
    - path: "apps/api/src/modules/sync/services/replayBroadcast.service.ts"
      provides: "Scoped replay broadcasts for mobile and web consumers"
      exports: ["ReplayBroadcastService"]
  key_links:
    - from: "apps/mobile/src/features/offline-sync/hooks/useSyncStatus.ts"
      to: "apps/api/src/modules/sync/services/retryEscalation.service.ts"
      via: "Mobile sync UI reflects retry ownership and escalation state from persisted failure tasks"
      pattern: "guidedRetryCount"
    - from: "apps/api/src/modules/sync/services/replayBroadcast.service.ts"
      to: "apps/web/src/features/dashboard/hooks/useReplayStaleState.ts"
      via: "Scoped replay events trigger stale-state prompts on overtaken web views"
      pattern: "REPLAY_APPLIED"
---

<objective>
Deliver the sync visibility UX, failure ownership workflow, and replay broadcasts that make offline recovery trustworthy across mobile and web.

Purpose: D-18 through D-24 define a specific trust posture: calm visibility for pending work, actionable failure handling, subtle recovery cues, originating-user retry first, and explicit escalation. Phase 9 also requires stale-state prompts on cross-device overtakes.

Output: Sync badge, failure center, sync UI hooks/store, retry-escalation + replay-broadcast services, and mobile/web replay visibility tests.
</objective>

<execution_context>
@/Users/mihirmahesh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mihirmahesh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-offline-hardening-integration-polish/10-CONTEXT.md
@.planning/phases/10-offline-hardening-integration-polish/10-RESEARCH.md
@.planning/phases/09-web-dashboard-owner-portal/09-CONTEXT.md
@.planning/phases/09-web-dashboard-owner-portal/09-RESEARCH.md
@.planning/phases/10-offline-hardening-integration-polish/10-01-PLAN.md

<interfaces>
Visibility and ownership rules for this plan:

- Pending work uses a calm persistent badge per D-19.
- Failed items route into an actionable failure center per D-20.
- Successful recovery uses a subtle cue per D-21.
- Originating user owns first guided retry, then escalation applies per D-22 and D-23.
- Browser/mobile stale-state review must remain visible rather than silent per Phase 9 D-40.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the sync badge, failure center, and retry-ownership mobile UX</name>
  <files>apps/mobile/src/features/offline-sync/components/SyncStatusBadge.tsx, apps/mobile/src/features/offline-sync/screens/SyncFailureCenterScreen.tsx, apps/mobile/src/features/offline-sync/hooks/useSyncStatus.ts, apps/mobile/src/features/offline-sync/store/syncUiStore.ts, apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx</files>
  <read_first>.planning/phases/10-offline-hardening-integration-polish/10-CONTEXT.md, .planning/phases/10-offline-hardening-integration-polish/10-RESEARCH.md, .planning/phases/09-web-dashboard-owner-portal/09-CONTEXT.md</read_first>
  <behavior>
    - Pending work shows a calm persistent badge, not repeated blocking banners.
    - Failed items open an actionable failure center with retry, review, and escalation affordances.
    - Successful recovery shows a subtle caught-up cue.
  </behavior>
  <action>Create `SyncStatusBadge.tsx` as an always-visible but calm status element that summarizes pending count, replaying state, conflict count, and failed count per D-18 to D-21. Create `useSyncStatus.ts` and `syncUiStore.ts` to aggregate local replay state, failure ownership, and subtle recovery transitions from the shared sync tables. Create `SyncFailureCenterScreen.tsx` as the actionable failure center required by D-20: group items into `Needs your retry`, `Escalated to clinician`, and `Operational review` sections, surface the originating user and current owner, show explicit guided retry steps, and keep unresolved conflicts visible until cleared per D-11. Replace the Wave 0 test with failing coverage that proves calm badge copy, failure-center grouping, persistent unresolved visibility, and subtle recovery cue rendering.</action>
  <acceptance_criteria>
    - `apps/mobile/src/features/offline-sync/components/SyncStatusBadge.tsx` contains pending/conflict/failed summaries and `CAUGHT_UP` recovery handling.
    - `apps/mobile/src/features/offline-sync/screens/SyncFailureCenterScreen.tsx` contains sections for originating-user retry and escalated items.
    - `apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx` contains assertions for persistent unresolved visibility and subtle recovery cue behavior.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx apps/api/src/modules/sync/__tests__/retryEscalation.service.test.ts</automated>
  </verify>
  <done>Staff can tell whether the app is pending, replaying, conflicted, failed, or caught up without mystery or banner fatigue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement guided retry/escalation and scoped replay broadcasts for mobile + web stale-state review</name>
  <files>apps/api/src/modules/sync/services/retryEscalation.service.ts, apps/api/src/modules/sync/services/replayBroadcast.service.ts, apps/api/src/modules/sync/__tests__/retryEscalation.service.test.ts, apps/web/src/features/dashboard/hooks/useReplayStaleState.ts, apps/web/src/features/queue/hooks/useQueueReplayRealtime.ts, apps/web/src/features/inventory/hooks/useInventoryReplayRealtime.ts, apps/web/src/features/dashboard/__tests__/replayStaleState.test.tsx</files>
  <read_first>apps/api/src/modules/sync/services/replayIngest.service.ts, .planning/phases/09-web-dashboard-owner-portal/09-RESEARCH.md, .planning/phases/10-offline-hardening-integration-polish/10-RESEARCH.md</read_first>
  <behavior>
    - First guided retry stays with the originating user.
    - After guided retry fails, safety-critical conflicts escalate to the assigned clinician and remain visible.
    - Replay broadcasts invalidate only affected views and surface stale-state prompts where active browser views were overtaken.
  </behavior>
  <action>Create `retryEscalation.service.ts` to transition failure tasks from `OPEN` to `GUIDED_RETRY` to `ESCALATED`, keeping the originating user as first owner per D-22 and moving safety-critical EMR items to the assigned clinician after guided retry fails per D-23 and D-24. Create `replayBroadcast.service.ts` to emit scoped `REPLAY_APPLIED`, `REPLAY_CONFLICT_OPENED`, and `REPLAY_FAILURE_ESCALATED` events containing clinic id, domain, affected entity ids, and date-window hints for web/mobile invalidation. In `useReplayStaleState.ts`, `useQueueReplayRealtime.ts`, and `useInventoryReplayRealtime.ts`, consume those scoped events so active browser views prompt `Refresh` / `Review changes` instead of silently overwriting overtaken state, preserving Phase 9 stale-state philosophy. Replace the Wave 0 tests with failing cases proving guided retry ownership, clinician escalation, scoped replay events, and browser stale-state prompt behavior.</action>
  <acceptance_criteria>
    - `apps/api/src/modules/sync/services/retryEscalation.service.ts` contains `assignOriginatingUserRetry` and clinician escalation handling for safety-critical items.
    - `apps/api/src/modules/sync/services/replayBroadcast.service.ts` emits `REPLAY_APPLIED` and `REPLAY_FAILURE_ESCALATED` with scoped payload data.
    - `apps/web/src/features/dashboard/hooks/useReplayStaleState.ts` exposes stale-state prompt state instead of auto-overwriting active browser views.
    - `apps/web/src/features/dashboard/__tests__/replayStaleState.test.tsx` contains overtaken-view prompt assertions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/api/src/modules/sync/__tests__/retryEscalation.service.test.ts apps/web/src/features/dashboard/__tests__/replayStaleState.test.tsx</automated>
  </verify>
  <done>Sync failures now have explicit ownership and replayed changes become visible across mobile/web without silent stale-state overwrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sync failure state → user-facing recovery UI | Internal retry/escalation state becomes visible and actionable to staff |
| Replay broadcasts → mobile/web clients | Scoped replay events update live views across devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Repudiation | Retry/escalation ownership | mitigate | Persist owner transitions and guided retry counts in `retryEscalation.service.ts` so accountability remains explicit |
| T-10-09 | Information Disclosure | Replay broadcast scope | mitigate | Emit only scoped clinic/domain/entity payloads and let clients invalidate relevant windows only |
| T-10-10 | Tampering | Browser stale-state handling | mitigate | Expose stale-state prompts in `useReplayStaleState.ts` instead of silently applying replayed data to active edits |
</threat_model>

<verification>
- Sync failure-center and replay-stale-state tests pass
- Pending/failure/caught-up states are visible without intrusive banner spam
- Guided retry ownership and clinician escalation are explicit and persisted
- Web consumers show stale-state prompts instead of silent overwrite
</verification>

<success_criteria>
- Sync state remains visible through normal clinic use
- Failed replay items have clear retry/escalation ownership
- Replay updates reach web/mobile while preserving stale-state trust
</success_criteria>

<output>
After completion, create `.planning/phases/10-offline-hardening-integration-polish/10-05-SUMMARY.md`
</output>
