---
phase: 08-scheduling-calendar
plan: "15"
type: execute
wave: 8
depends_on: ["08-08", "08-12", "08-13", "08-14"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-scheduling-calendar/08-VALIDATION.md
autonomous: false
requirements: [SCH-01, SCH-02, SCH-03, SCH-04, SCH-05]

must_haves:
  truths:
    - "A vet can schedule a future appointment and that appointment appears in the walk-in queue at its scheduled time"
    - "A vet can view a calendar in a day view on mobile and a week view on web, and a change on one appears on the other without a refresh"
    - "Staff receive a push notification for an upcoming appointment and for a queue backlog"
    - "The full monorepo test suite is green and no requirement is left unmarked"
  artifacts:
    - path: ".planning/phases/08-scheduling-calendar/08-VALIDATION.md"
      provides: "Completed per-task verification map and Wave 0 record with the observed suite runtime"
      contains: "nyquist_compliant: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCH-01 through SCH-05 marked complete"
      contains: "SCH-01"
  key_links:
    - from: "mobile day agenda"
      to: "web week grid"
      via: "the shared clinic Socket.IO room, verified by two live clients"
      pattern: "clinic:"
---

<objective>
Close the phase: prove the full suite is green across every package, then human-verify the three things no automated test in this repo can prove — real-time sync across two live clients, real push delivery to a device, and the end-to-end appointment-to-queue handoff on a real clock.

Purpose: `08-VALIDATION.md` § Manual-Only Verifications names exactly two behaviours as unmockable (multi-client realtime sync and device push delivery), and the queue-handoff sweep is a third: every automated test drives `runSchedulingSweep` with an injected `now`, so nothing so far proves the BullMQ scheduler actually fires on its own five-minute cadence. This plan is where the phase's three ROADMAP success criteria are confirmed against a running system rather than against mocks.
Output: a green full suite, a completed validation document, updated requirement checkboxes, and three signed-off human verifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-scheduling-calendar/08-CONTEXT.md
@.planning/phases/08-scheduling-calendar/08-VALIDATION.md
@.planning/phases/08-scheduling-calendar/08-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full monorepo suite, complete 08-VALIDATION.md, and mark the requirements</name>
  <files>.planning/phases/08-scheduling-calendar/08-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/08-scheduling-calendar/08-VALIDATION.md (the template with its `{N}` and `{command}` placeholders in § Test Infrastructure, § Sampling Rate, § Per-Task Verification Map and § Wave 0 Requirements, plus the `nyquist_compliant: false` and `wave_0_complete: false` frontmatter flags)
    - Every `08-NN-SUMMARY.md` in `.planning/phases/08-scheduling-calendar/` (the authoritative record of what each task actually built, its test file and its verify command — the verification map is populated from these, not from the plans)
    - .planning/REQUIREMENTS.md (lines 93-97 for the SCH checkboxes and lines 204-208 for the traceability table)
    - .planning/phases/08-scheduling-calendar/08-RESEARCH.md (§ Validation Architecture — the Phase Requirements to Test Map and the Sampling Rate the completed document must reflect)
  </read_first>
  <action>
Run, from the repo root, and record the exact wall-clock duration of each:
- `pnpm --filter @breeyo/types test`
- `pnpm --filter @breeyo/validators test`
- `pnpm --filter @breeyo/ui test`
- `pnpm --filter @breeyo/api test`
- `pnpm --filter @breeyo/mobile test`
- `pnpm --filter @breeyo/web test`
- `pnpm test` (the full turbo run)
- `pnpm --filter @breeyo/api exec tsc --noEmit`, and the same for `@breeyo/mobile`, `@breeyo/web`, `@breeyo/ui`, `@breeyo/types` and `@breeyo/validators`
- `pnpm build`

Every one must exit 0. If any fails, fix it or, when the failure is genuinely outside this phase's scope, record it as a blocker in the summary with the exact failing test and file — do not weaken an assertion or skip a test to get green.

Also re-run `pnpm --filter @breeyo/api test` a second time and confirm identical results, which is the standing check that the scheduling sweep worker is not firing during vitest.

Then complete `08-VALIDATION.md`, replacing every placeholder:
- **§ Test Infrastructure** — set the real quick-run and full-suite commands and the observed estimated runtime.
- **§ Sampling Rate** — set the observed max feedback latency in seconds (the slowest single-file scoped run).
- **§ Per-Task Verification Map** — one row per task across all fifteen plans, with: task id in `{plan}-{task}` form, plan number, wave, the requirement ids that task serves, its threat ref from that plan's `<threat_model>` (or a dash), the secure behaviour asserted (or `N/A`), test type, the exact automated command from that task's `<verify>`, whether the test file exists, and status `✅ passing`. Populate this from the summaries, not the plans, so it records what shipped.
- **§ Wave 0 Requirements** — replace the two template bullets with what actually landed: the Prisma scheduling schema and blocking migration (plan 08-03), the shared contracts in `@breeyo/types` and `@breeyo/validators` (plan 08-01), the test factories and cleanup order (plan 08-03), `apps/api/src/lib/ist-date.ts` (plan 08-04), the design tokens and dependency fixes (plan 08-02), and the `apps/web` vitest setup (plan 08-14). Check each box.
- **§ Manual-Only Verifications** — keep both existing rows and add a third for the BullMQ five-minute sweep cadence, since every automated test injects `now`. Correct the requirement id on the realtime-sync row from SCH-02 to SCH-04, which is the requirement it actually covers.
- **§ Validation Sign-Off** — check every box and set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, plus `status: complete` and `approved`.

Finally, in `.planning/REQUIREMENTS.md`, tick `SCH-01` through `SCH-05` in the checklist and change their traceability-table status from `Pending` to `Complete`. Do this only after Tasks 2, 3 and 4 have been approved — if any is not, leave the affected requirement unticked and record why. State in the summary which order was actually followed.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 and `pnpm build` exits 0.
    - `pnpm --filter @breeyo/api test` exits 0 on two consecutive runs with the same test count (proving no worker fires under vitest).
    - `grep -c '{N}' .planning/phases/08-scheduling-calendar/08-VALIDATION.md` returns `0` and `grep -c '{command}' .planning/phases/08-scheduling-calendar/08-VALIDATION.md` returns `0`.
    - `grep -c 'nyquist_compliant: true' .planning/phases/08-scheduling-calendar/08-VALIDATION.md` returns `1` and `grep -c 'wave_0_complete: true' .planning/phases/08-scheduling-calendar/08-VALIDATION.md` returns `1`.
    - `grep -c '⬜ pending' .planning/phases/08-scheduling-calendar/08-VALIDATION.md` returns `0`.
    - The § Per-Task Verification Map has at least 40 data rows (fifteen plans of two to three tasks each): `awk '/Per-Task Verification Map/,/Wave 0 Requirements/' .planning/phases/08-scheduling-calendar/08-VALIDATION.md | grep -c '^| 08-'` returns at least `40`.
    - Every row in that map has a non-empty automated command column: the same awk range grepped for `` | `pnpm `` or `` | `cd `` returns a count equal to the row count.
    - `awk '/Manual-Only Verifications/,/Validation Sign-Off/' .planning/phases/08-scheduling-calendar/08-VALIDATION.md | grep -c '^| ' ` returns at least `4` (a header plus three manual rows).
    - `grep -Ec '\[x\] \*\*SCH-0[1-5]\*\*' .planning/REQUIREMENTS.md` returns `5`, and `grep -Ec 'SCH-0[1-5] \| Phase 8 \| Complete' .planning/REQUIREMENTS.md` returns `5`.
    - The summary records the wall-clock duration of every command listed in the action.
  </acceptance_criteria>
  <done>Every package's tests, type checks and builds are green, the API suite is proven stable across two runs, the validation document is fully populated with a per-task map and no placeholders, and all five requirements are ticked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the appointment-to-queue handoff on a real clock with the real BullMQ scheduler</name>
  <files>.planning/phases/08-scheduling-calendar/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-scheduling-calendar/08-09-SUMMARY.md (the sweep cadence, the three passes, the grace window used, and the Redis debounce key format)
    - .planning/phases/08-scheduling-calendar/08-04-SUMMARY.md (the queue board's four-group shape)
    - packages/types/src/constants/scheduling.constants.ts (`SCHEDULING_SWEEP_CRON`, `NO_SHOW_GRACE_MINUTES`, `STARTING_SOON_LEAD_MINUTES` — the verifier needs the actual configured values, not the recommended ones)
    - .planning/ROADMAP.md (Phase 8 Success Criteria 1 — this checkpoint is its direct verification)
  </read_first>
  <action>
Nothing further is built here. Automate the setup, then pause for the human observation.

Bring the stack up: `docker compose up -d` for Postgres and Redis, then `pnpm --filter @breeyo/api dev`, then `pnpm --filter @breeyo/mobile start`. Confirm the API booted without error.

Read the CONFIGURED values from `packages/types/src/constants/scheduling.constants.ts` and report them to the verifier before they begin: `SCHEDULING_SWEEP_CRON`, `NO_SHOW_GRACE_MINUTES` and `STARTING_SOON_LEAD_MINUTES`. Do not let the verifier assume the recommended values — plan 08-01 may have chosen differently.

Confirm the scheduler registered by running `redis-cli --scan --pattern 'bull:scheduling-sweep*'` and reporting the keys found. An empty result means the sweep was never registered and ROADMAP Success Criterion 1 cannot pass; report that as a failure rather than triggering the sweep manually.

Do NOT manually invoke `runSchedulingSweep`, do NOT restart the API before the observation window, and do NOT shorten the cron cadence for convenience. The entire purpose of this checkpoint is that every automated test injected `now`, so only an unprompted firing proves the scheduler works.

Then pause for the thirteen-step verification below.
  </action>
  <verify>
    <automated>redis-cli --scan --pattern 'bull:scheduling-sweep*' | grep -c 'scheduling-sweep'</automated>
    <human-check>The thirteen handoff steps in how-to-verify below, in particular steps 1, 7, 10, 11 and 13</human-check>
  </verify>
  <done>The BullMQ scheduler is registered in Redis, an `EXPECTED` entry per pet appears unprompted within one sweep interval of the slot time, a scheduled patient checking in later sorts above an earlier walk-in, an unattended entry flips to `NO_SHOW` after the configured grace window, and an API restart produces no duplicates.</done>
  <what-built>
By this point a staff booking creates an `Appointment`; a BullMQ `upsertJobScheduler` job runs every five minutes IST; its first pass creates one `EXPECTED` `QueueEntry` per pet with `queuePriorityAt` set to the appointment's `scheduledFor`; the mobile queue board shows those entries in an `Expected` section ordered first; checking in flips the entry to `WAITING` while keeping its scheduled-time priority; and a second pass flips an unattended entry to `NO_SHOW` on both the entry and the appointment after the grace window.

Every automated test drives `runSchedulingSweep` with an injected `now`, so nothing so far proves the scheduler itself fires on its own cadence in a running process. That is what this checkpoint verifies.
  </what-built>
  <how-to-verify>
Start the stack: Postgres and Redis via `docker compose up -d`, then `pnpm --filter @breeyo/api dev`, then `pnpm --filter @breeyo/mobile start`.

1. Confirm the scheduler registered: `redis-cli --scan --pattern 'bull:scheduling-sweep*'` returns at least one key. If it returns nothing, the sweep was never registered and criterion 1 fails.
2. On mobile, open Settings then Edit Availability and set the signed-in vet's hours to cover the current time of day. Save and confirm the `Weekly hours saved` toast.
3. On the Schedule tab, tap Book Appointment. Enter a test owner's 10-digit mobile number, select TWO of that owner's pets, pick a service, pick today, and pick the slot closest to two or three minutes from now. Confirm the booking and note the toast.
4. Confirm the appointment appears in the day agenda immediately, in the correct time-of-day group, with a `+1 more pets` caption.
5. Switch to the Queue tab. Confirm the appointment does NOT yet appear — the slot time has not arrived, so there should be no `Expected` section for it.
6. Wait for the slot time to pass, then wait for the next five-minute sweep boundary (up to five minutes). Do NOT restart the API — the point is that the scheduler fires unprompted.
7. Confirm the Queue tab now shows an `Expected` section ABOVE `In Consult`, containing TWO rows (one per pet), each showing `Expected {slot time}` with a clock glyph, a calm brown `Expected` badge, and NO position number and NO wait estimate.
8. Check a walk-in patient in through the normal Check In flow, noting the current time.
9. Tap one of the `Expected` rows, tap `Check In Now`, and confirm the toast shows a position number.
10. **The core assertion for criterion 1:** confirm the just-checked-in scheduled patient appears in the `Waiting` section ABOVE the walk-in you checked in at step 8, even though the walk-in physically arrived first. That is D-10 working through `queuePriorityAt`.
11. Leave the second `Expected` row alone. Wait `NO_SHOW_GRACE_MINUTES` past the slot time (read the configured value from `scheduling.constants.ts` — do not assume 20). Confirm that row disappears from `Expected` and appears in `Done` with a `No Show` badge.
12. Open the appointment from the Schedule tab and confirm its status. Because one pet attended, the appointment should NOT be `NO_SHOW` — record what you observe against what `08-09-SUMMARY.md` says the multi-pet rule is.
13. Restart the API and confirm no duplicate `Expected` rows appear for the already-handed-off appointment on the next sweep — this is the idempotency guarantee under a real restart.

Reply `approved` if 1, 7, 10, 11 and 13 all pass, or describe exactly which step diverged and what you saw.
  </how-to-verify>
  <acceptance_criteria>
    - `redis-cli --scan --pattern 'bull:scheduling-sweep*'` returns at least one key (step 1).
    - The `Expected` section appears without any manual trigger or restart, within one sweep interval of the slot time (steps 6 and 7).
    - Two `Expected` rows appear for a two-pet appointment (step 7, D-21).
    - `Expected` rows show no position number and no wait estimate (step 7).
    - A scheduled patient checked in at 14:07 for a 14:00 slot sorts above a walk-in checked in at 14:05 (step 10, D-10 — this is ROADMAP Success Criterion 1).
    - An unattended `Expected` row flips to `NO_SHOW` after the configured grace window with no manual action (step 11, D-09).
    - An API restart produces no duplicate entries (step 13).
    - The observed sweep latency and the actual configured grace window are recorded in `08-VALIDATION.md` § Manual-Only Verifications.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which step diverged</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real-time calendar sync across two live clients, mobile and web</name>
  <files>.planning/phases/08-scheduling-calendar/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-scheduling-calendar/08-06-SUMMARY.md (the web login flow and where the token is stored)
    - .planning/phases/08-scheduling-calendar/08-14-SUMMARY.md (the week grid's geometry decisions, including the no-sub-row-sizing rule and the overflow-chip threshold, so the verifier knows what correct looks like)
    - .planning/phases/08-scheduling-calendar/08-12-SUMMARY.md (the mobile agenda's grouping and the socket invalidation behaviour)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Real-time sync — the non-disruptive rules this checkpoint verifies; § Web week grid for the expected measurements)
    - .planning/ROADMAP.md (Phase 8 Success Criteria 2 — this checkpoint is its direct verification)
  </read_first>
  <action>
Nothing further is built here. Automate the setup, then pause for the human observation.

Start the API, the mobile app, and `pnpm --filter @breeyo/web dev` on port 3001. Confirm `apps/api/src/app.ts` lines 39-45 allow the web origin and that `socket.ts:15` permits the socket origin, so no CORS surprise interrupts the verification.

Seed or identify two staff accounts in the SAME clinic, and report their credentials to the verifier along with the clinic id, so both surfaces can be signed in and both are provably in the same `clinic:{clinicId}` room.

Report to the verifier what correct looks like for the measurements they will check, taken from `08-14-SUMMARY.md`: the 56px time gutter, the 64px 30-minute row height, the deliberate no-sub-row-proportional-sizing rule, and the three-block overflow threshold. Without those numbers the verifier cannot distinguish a bug from a locked design decision.

Do NOT refresh the browser or pull-to-refresh on mobile during steps 7 through 12 — the whole assertion is that the socket delivers the change.

Then pause for the fourteen-step verification below.
  </action>
  <verify>
    <automated>curl -sf http://localhost:3000/health &amp;&amp; curl -sf http://localhost:3001/schedule -o /dev/null &amp;&amp; redis-cli ping</automated>
    <human-check>The fourteen cross-surface steps in how-to-verify below, in particular steps 7, 8, 9, 10, 11 and 14</human-check>
  </verify>
  <done>A change made on either surface appears on the other without a refresh, a remote change disturbs neither scroll position nor a part-filled form, an open drawer for a remotely cancelled appointment shows an inline notice instead of auto-closing, connection loss and recovery are surfaced quietly on both surfaces, and a double-booking is warned about rather than blocked.</done>
  <what-built>
Both mobile and web join the same `clinic:{clinicId}` Socket.IO room, using the same authenticated access token. Appointment create, reschedule and cancel, and availability writes, each broadcast a distinct event; both clients invalidate and refetch on receipt. The web surface additionally has its first login page and its first Socket.IO client.

Two live clients observing one socket event cannot be asserted in CI — `08-VALIDATION.md` § Manual-Only Verifications names this explicitly — so it is verified here.
  </what-built>
  <how-to-verify>
Start the API, the mobile app and `pnpm --filter @breeyo/web dev` (port 3001). Have the mobile Schedule tab and a browser at http://localhost:3001/schedule open side by side, both signed in as staff of the SAME clinic.

**Web surface (ROADMAP criterion 2, week view):**
1. Confirm signing in at `/login` lands on `/schedule` and the grid renders.
2. Confirm the grid shows seven day columns and a 56px time gutter with hour-only labels, that today's column header carries a green underline, and that a green now-line with a dot crosses today's column only.
3. Confirm non-working hours and any blocked period render as greyed, non-clickable cells, and that hovering an open cell shows a plus glyph.
4. Confirm a 15-minute appointment occupies a full 30-minute row height rather than a half row — that is the deliberate v1 rule, not a bug.
5. Tab into the grid and confirm arrow keys move cell focus with a visible focus ring and that Enter opens the booking drawer. Press Escape and confirm it closes.
6. Book an appointment from the web drawer and confirm it appears in the grid.

**Cross-surface sync (the core assertion):**
7. On mobile, book an appointment for today. Confirm it appears in the web grid within a couple of seconds **without refreshing the browser**.
8. On web, cancel that appointment. Confirm the mobile agenda updates without a pull-to-refresh, and that the row renders dimmed with a struck-through pet name.
9. Scroll the mobile agenda to the bottom, then create an appointment from the web. Confirm the mobile list updates but the scroll position does NOT jump.
10. Open the booking sheet on mobile and part-fill it. From the web, create an appointment. Confirm the mobile sheet stays open with the entered values intact.
11. Open the appointment drawer on web for a specific appointment. From mobile, cancel that same appointment. Confirm the web drawer shows the inline notice `This appointment was just cancelled by someone else.` with its actions disabled, and does NOT auto-close.
12. On mobile, change the vet's weekly hours. Confirm the web grid's row range or greyed cells update without a refresh.
13. Stop the API. Confirm the web header shows `Live updates paused. Reconnecting…` and the mobile screen shows its offline banner with the Book Appointment FAB greyed. Restart the API and confirm both clear silently with no toast and no spinner.

**Double-booking (D-14):**
14. From web, click a cell that already holds an appointment for that vet and complete a booking into the same slot. Confirm an orange warning strip appears reading that the vet already has a patient at that time and that you can still book, with `Book Anyway` and `Pick Another Time`. Confirm `Book Anyway` succeeds and both blocks render side by side at half width.

Reply `approved` if 7, 8, 9, 10, 11 and 14 all pass, or describe what diverged.
  </how-to-verify>
  <acceptance_criteria>
    - A mobile booking appears on the web grid without a browser refresh, and a web cancellation appears on mobile without a pull-to-refresh (steps 7 and 8 — ROADMAP Success Criterion 2).
    - A remote change does not move the mobile scroll position and does not disturb a part-filled booking sheet (steps 9 and 10, UI-SPEC § Real-time sync).
    - An open web drawer for a remotely cancelled appointment shows the inline notice with disabled actions and does not auto-close (step 11).
    - Losing and regaining the API connection produces the reconnecting caption on web and the offline banner on mobile, both clearing silently (step 13).
    - A double-booking attempt shows the warning strip with `Book Anyway` and `Pick Another Time`, is never hard-blocked, and renders side by side after the override (step 14, D-14).
    - The web grid is keyboard navigable with Enter-to-book and Escape-to-close (step 5).
    - The observed sync latency is recorded in `08-VALIDATION.md`.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which step diverged</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify staff push notification delivery on a real device</name>
  <files>.planning/phases/08-scheduling-calendar/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-scheduling-calendar/08-09-SUMMARY.md (the three triggers, the Redis debounce key format, and the configured lead time and backlog threshold)
    - .planning/phases/08-scheduling-calendar/08-10-SUMMARY.md (the gate outcome — if Phase 7 was not merged, the owner-reply trigger cannot be verified and that must be recorded rather than glossed over)
    - packages/types/src/constants/scheduling.constants.ts (`STARTING_SOON_LEAD_MINUTES`, `QUEUE_BACKLOG_THRESHOLD`, `QUEUE_BACKLOG_DEBOUNCE_MINUTES` — the actual configured values)
    - .planning/phases/08-scheduling-calendar/08-UI-SPEC.md (§ Push notification copy — the four exact title and body templates and their deep links)
    - .planning/ROADMAP.md (Phase 8 Success Criteria 3 — this checkpoint is its direct verification)
  </read_first>
  <action>
Nothing further is built here. Automate the setup, then pause for the human observation.

Start the API, Redis and the mobile app on a physical device or a simulator with push enabled. Confirm a device token was registered: `psql "$DATABASE_URL" -c "SELECT user_id, left(token, 20) FROM device_tokens"` must return at least one row for the signed-in user. If it returns none, push cannot be verified at all — report that first rather than working through the steps.

Read and report the CONFIGURED values from `packages/types/src/constants/scheduling.constants.ts`: `STARTING_SOON_LEAD_MINUTES`, `QUEUE_BACKLOG_THRESHOLD` and `QUEUE_BACKLOG_DEBOUNCE_MINUTES`. The verifier must use these, not the recommended values.

Read `08-10-SUMMARY.md` and report the Phase 7 gate outcome. If it was `blocked`, tell the verifier to skip steps 12 through 14 and record trigger 2 as unverified pending Phase 7, so it becomes a gap-closure item rather than a silent omission.

If the gate was approved, prepare the inbound simulation the verifier needs for steps 12 through 14 using Phase 7's existing simulator entry point, including one payload deliberately targeting an appointment owned by a DIFFERENT owner for the security assertion in step 14.

Then pause for the sixteen-step verification below.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT count(*) FROM device_tokens" &amp;&amp; redis-cli ping</automated>
    <human-check>The sixteen push steps in how-to-verify below, in particular steps 3, 6, 8, 9, 11, 14 and 16</human-check>
  </verify>
  <done>A starting-soon push reaches a real device with the contract copy and a working deep link and does not repeat, a backlog push fires once per debounce window with a live Redis TTL, a check-in still succeeds with Redis stopped, a forged owner action changes nothing, and the web notification fires only while the tab is open.</done>
  <what-built>
Three push triggers, each of which creates an in-app `Notification` row and sends an Expo push to every registered staff `DeviceToken`: an appointment starting soon, an owner MOVE or CANCEL reply, and the queue backing up with a per-clinic-per-day Redis debounce.

Device-level push delivery cannot be asserted in CI — `08-VALIDATION.md` § Manual-Only Verifications names this explicitly — so it is verified here on a real device or simulator with a registered token.
  </what-built>
  <how-to-verify>
Sign in on a physical device or a simulator with push enabled and confirm a `DeviceToken` row exists for that user (`psql "$DATABASE_URL" -c "SELECT user_id, left(token, 20) FROM device_tokens"`).

**Trigger 1, appointment starting soon (SCH-05):**
1. Read the configured `STARTING_SOON_LEAD_MINUTES` value. Book an appointment for slightly more than that many minutes from now.
2. Background the app. Wait for the sweep to cross the lead-time boundary.
3. Confirm a push arrives with the title `Appointment in {n} min` and a body naming the pet, owner, time and vet, with the title fitting on one line.
4. Tap it and confirm the app opens on the Schedule tab at that appointment's date.
5. Confirm the in-app notification list also shows the entry with an unread badge.
6. Wait through two further sweep cycles and confirm NO second push for the same appointment arrives (the `startingSoonNotifiedAt` marker).

**Trigger 3, queue backing up (SCH-05):**
7. Read the configured `QUEUE_BACKLOG_THRESHOLD`. Check in that many walk-in patients.
8. Confirm one push arrives with the title `{n} patients waiting` and a body naming the longest wait, and that tapping it opens the Queue tab.
9. Check in one more patient. Confirm NO second push arrives, because the debounce window is still open.
10. Confirm the debounce key exists: `redis-cli --scan --pattern 'scheduling:backlog-alert:*'` returns a key containing the clinic id and today's date, and `redis-cli ttl <that key>` returns a positive value consistent with `QUEUE_BACKLOG_DEBOUNCE_MINUTES`.
11. Confirm a check-in still succeeds normally with Redis stopped — a notification failure must never fail a check-in. Stop Redis, check a patient in, confirm the queue updates, then restart Redis.

**Trigger 2, owner reply (D-16):**
12. If `08-10-SUMMARY.md` records the Phase 7 gate as approved: simulate an inbound `appointment:cancel:<id>` for an appointment owned by that thread's owner. Confirm the appointment becomes `CANCELLED`, a push arrives titled `Appointment cancelled`, and the calendar updates on both surfaces.
13. Simulate `appointment:move:<id>`. Confirm the appointment does NOT change, a push arrives titled `Reschedule requested`, and a `MOVE_REQUEST` entry appears in the existing in-app notification list.
14. Simulate `appointment:cancel:<id>` for an appointment belonging to a DIFFERENT owner. Confirm nothing changes and no push is sent — this is the security assertion.
15. If the gate was recorded as `blocked`, skip steps 12 through 14 and record in the summary that trigger 2 is unverified pending Phase 7, so it can be picked up in a gap-closure plan.

**Web foreground notification:**
16. On the web calendar, click `Turn On Alerts` and grant permission. With the tab open, trigger a starting-soon appointment and confirm a browser notification appears. Close the tab entirely and confirm no notification arrives — there is deliberately no service worker.

Reply `approved` if 3, 6, 8, 9, 11 and 16 all pass (plus 12 through 14 when applicable), or describe what diverged.
  </how-to-verify>
  <acceptance_criteria>
    - A starting-soon push is delivered to a real device with UI-SPEC copy and a working deep link (steps 3 and 4 — ROADMAP Success Criterion 3).
    - No duplicate starting-soon push arrives across subsequent sweeps (step 6).
    - A backlog push fires once at the threshold and not again inside the debounce window, with a live Redis key carrying a positive TTL (steps 8, 9 and 10).
    - A check-in succeeds with Redis stopped (step 11) — no notification failure can fail a check-in.
    - An owner CANCEL auto-applies and an owner MOVE does not change the appointment but does create a `MOVE_REQUEST` notification (steps 12 and 13), OR the Phase 7 gate outcome is recorded as blocking these and the gap is written up.
    - A forged owner action for another owner's appointment changes nothing and sends no push (step 14).
    - A web foreground notification fires with the tab open and does not fire with the tab closed (step 16).
    - The configured lead time, backlog threshold and debounce window are recorded in `08-VALIDATION.md` alongside the observed delivery latency.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which step diverged</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running system → phase sign-off | Verification claims must reflect a real system, not a mocked one |
| inbound WhatsApp payload → appointment state | The forged-owner-action refusal is asserted here against a live stack |
| Redis availability → patient check-in | A cache dependency must not be able to block clinical throughput |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-79 | Repudiation | a phase signed off green while a behaviour only ever worked under an injected clock | mitigate | Task 2 explicitly forbids manual sweep triggering and API restarts before the observation, and requires the live `bull:scheduling-sweep*` Redis key as proof the scheduler registered; Task 1 additionally runs the API suite twice to prove no worker fires under vitest |
| T-08-80 | Spoofing | the forged owner-action refusal passing in unit tests but failing against real rows | mitigate | Task 4 step 14 exercises a cross-owner `appointment:cancel` against the live stack and requires that nothing change and no push be sent, complementing plan 08-10's integration test |
| T-08-81 | Availability | a Redis outage blocking patient check-in through the notification hook | mitigate | Task 4 step 11 stops Redis and requires a check-in to still succeed, verifying the try/catch isolation plan 08-09 added to `queue.service.checkIn` |
| T-08-82 | Information Disclosure | a background web push channel existing beyond D-26's staff-Expo-only scope | mitigate | Task 4 step 16 requires that closing the tab stops notifications entirely, confirming the absence of a service worker that plan 08-14's grep gates also assert |
| T-08-83 | Repudiation | requirements marked complete while a manual verification was skipped | mitigate | Task 1 sequences the requirement ticks after Tasks 2 through 4 and requires any unapproved checkpoint to leave the affected requirement unticked with a written reason |
</threat_model>

<verification>
- `pnpm test` and `pnpm build` both exit 0.
- `pnpm --filter @breeyo/api test` exits 0 on two consecutive runs with identical test counts.
- Every package's `tsc --noEmit` exits 0.
- `08-VALIDATION.md` contains no `{N}` or `{command}` placeholder and no `⬜ pending` row.
- All five SCH requirements are ticked in `.planning/REQUIREMENTS.md` and marked `Complete` in its traceability table.
- Tasks 2, 3 and 4 are each approved, or the specific unverified behaviour is recorded as a gap.
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 is confirmed: a booked appointment appears in the walk-in queue at its slot time and, once checked in, sorts ahead of a later-arriving walk-in.
- ROADMAP Success Criterion 2 is confirmed: a day view on mobile and a week view on web stay in sync across two live clients without a refresh and without disturbing scroll position or an open form.
- ROADMAP Success Criterion 3 is confirmed: staff receive real device pushes for an upcoming appointment and for a queue backlog, debounced, with no duplicate for a single appointment.
- The full monorepo suite, every type check and every build are green, with the API suite proven stable across two runs.
- `08-VALIDATION.md` is complete with a per-task verification map and three manual-only rows, and all five requirements are ticked.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-calendar/08-15-SUMMARY.md` when done. Record: the wall-clock duration of every command run, the observed sweep latency and sync latency, the actual configured values for the grace window, lead time, backlog threshold and debounce window, the outcome of each of the three checkpoints step by step, any behaviour left unverified (particularly if the Phase 7 gate blocked plan 08-10) written up clearly enough to become a gap-closure plan, and any deviation between what UI-SPEC specified and what shipped.
</output>
