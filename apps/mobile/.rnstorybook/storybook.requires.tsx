// Placeholder for @storybook/react-native's generated story registry.
// The real file is produced by the storybook CLI's story-requires generator,
// which hasn't been wired into this project yet. This stub keeps the /storybook
// route (app/storybook.tsx) resolvable so it doesn't break the main app bundle.
export const view = {
  getStorybookUI: () => function StorybookNotConfigured() {
    return null;
  },
};
