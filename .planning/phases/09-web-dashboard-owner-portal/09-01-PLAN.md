---
phase: 09-web-dashboard-owner-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/web-dashboard/types.ts
  - packages/shared/src/web-dashboard/schemas.ts
  - packages/shared/src/web-dashboard/__tests__/schemas.test.ts
  - packages/shared/src/owner-portal/types.ts
  - packages/shared/src/owner-portal/schemas.ts
  - packages/shared/src/owner-portal/__tests__/schemas.test.ts
  - apps/api/src/modules/web-dashboard/__tests__/access-policy.service.test.ts
  - apps/api/src/modules/owner-portal/__tests__/magic-link.service.test.ts
  - apps/api/src/modules/owner-portal/__tests__/portal-session.service.test.ts
  - apps/api/prisma/schema.prisma
autonomous: true
requirements:
  - PLT-01
  - PLT-02
  - OWN-04
  - OWN-06
must_haves:
  truths:
    - "Dashboard and owner-portal code share one canonical contract layer for browser access, token session state, deep links, and checkout snapshots."
    - "Magic-link persistence stores only hashed tokens with 7-day expiry and explicit owner/pet/invoice scope before any portal API is implemented."
    - "Later Phase 9 plans can build against concrete schema-backed models instead of inventing access or token rules ad hoc."
  artifacts:
    - path: "packages/shared/src/web-dashboard/types.ts"
      provides: "Browser access policy, cockpit panel, and stale-state contracts"
      exports: ["BrowserRoleCode", "DashboardPanelId", "StaleStateEnvelope"]
    - path: "packages/shared/src/owner-portal/types.ts"
      provides: "Portal session, overview, deep-link, and checkout contracts"
      exports: ["OwnerPortalSessionState", "OwnerPortalDeepLinkTarget", "OwnerPortalCheckoutSelection"]
    - path: "apps/api/prisma/schema.prisma"
      provides: "Browser-access and owner-portal persistence models"
      contains: "model OwnerPortalMagicLink"
  key_links:
    - from: "packages/shared/src/owner-portal/schemas.ts"
      to: "packages/shared/src/owner-portal/types.ts"
      via: "zod schemas mirror the exported portal contracts"
      pattern: "OwnerPortalSessionState"
    - from: "apps/api/prisma/schema.prisma"
      to: "packages/shared/src/web-dashboard/types.ts"
      via: "database access-policy fields align with browser role/module contracts"
      pattern: "FRONT_DESK"
---

<objective>
Create the shared Phase 9 contracts, Wave 0 tests, and Prisma models for browser access and owner magic-link scope.

Purpose: Phase 9 adds browser-only orchestration plus a no-login owner portal. These flows are security-sensitive and cross-module by default, so contracts and persistence must land first per D-15 to D-24 and D-64 to D-72.

Output: Shared `web-dashboard` and `owner-portal` packages, Wave 0 test scaffolds, Prisma models for browser access and portal state, and a blocking schema push step.
</objective>

<execution_context>
@/Users/mihirmahesh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mihirmahesh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-owner-portal/09-CONTEXT.md
@.planning/phases/09-web-dashboard-owner-portal/09-RESEARCH.md
@.planning/phases/09-web-dashboard-owner-portal/09-VALIDATION.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/06-invoicing-payments/06-CONTEXT.md

<interfaces>
Use these Phase 9 contracts as the baseline for all later plans:

- `BrowserRoleCode`: `ADMIN | FRONT_DESK | CLINICIAN`
- `DashboardPanelId`: `ALERTS | QUEUE | SCHEDULING | BILLING | INVENTORY | USERS | OWNER_EXCEPTIONS`
- `OwnerPortalSessionState`: `VALIDATING | READY | EXPIRED | INVALID`
- `OwnerPortalDeepLinkType`: `OVERVIEW | INVOICE | VISIT`
- `OwnerPortalTabId`: `OVERVIEW | RECORDS | INVOICES`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared dashboard and owner-portal contracts plus Wave 0 tests</name>
  <files>packages/shared/src/web-dashboard/types.ts, packages/shared/src/web-dashboard/schemas.ts, packages/shared/src/web-dashboard/__tests__/schemas.test.ts, packages/shared/src/owner-portal/types.ts, packages/shared/src/owner-portal/schemas.ts, packages/shared/src/owner-portal/__tests__/schemas.test.ts, apps/api/src/modules/web-dashboard/__tests__/access-policy.service.test.ts, apps/api/src/modules/owner-portal/__tests__/magic-link.service.test.ts, apps/api/src/modules/owner-portal/__tests__/portal-session.service.test.ts</files>
  <read_first>.planning/phases/09-web-dashboard-owner-portal/09-CONTEXT.md, .planning/phases/09-web-dashboard-owner-portal/09-RESEARCH.md, .planning/phases/09-web-dashboard-owner-portal/09-VALIDATION.md, .planning/phases/01-foundation-authentication/01-CONTEXT.md, .planning/phases/06-invoicing-payments/06-CONTEXT.md</read_first>
  <behavior>
    - Browser access defaults are `ADMIN` enabled, `FRONT_DESK` disabled until configured, and `CLINICIAN` denied per D-15, D-16, and D-19.
    - Owner-portal session schemas encode `VALIDATING`, `READY`, `EXPIRED`, and `INVALID` states with no data payload allowed in `INVALID` per D-64, D-67, OWN-04, and OWN-06.
    - Portal checkout selection supports one or many invoices while keeping invoice browsing pet-scoped per D-59, D-69, and D-70.
    - Wave 0 tests fail first for access-policy defaults, token hash validation, 7-day expiry, and scope mismatch handling before service implementation begins.
  </behavior>
  <action>Create `packages/shared/src/web-dashboard/` and `packages/shared/src/owner-portal/` as the canonical shared Phase 9 contract packages. In `packages/shared/src/web-dashboard/types.ts`, export `BrowserRoleCode`, `BrowserModuleCode`, `ClinicBrowserAccessPolicy`, `DashboardPanelId`, `DashboardPanelSummary`, `DashboardQuickAction`, `UserManagementMiniPanelSummary`, and `StaleStateEnvelope`; hard-code the locked panel order `ALERTS`, `QUEUE`, `SCHEDULING`, `BILLING`, `INVENTORY`, `USERS`, `OWNER_EXCEPTIONS` per D-06, D-11, D-12, and D-13, and model hidden unauthorized modules instead of lock placeholders per D-20. In `packages/shared/src/web-dashboard/schemas.ts`, add zod schemas for browser access updates, panel-order preference payloads, cockpit responses, and stale metadata payloads used by later web workbenches per D-14 and D-40. In `packages/shared/src/owner-portal/types.ts`, export `OwnerPortalSessionState`, `OwnerPortalTabId`, `OwnerPortalDeepLinkTarget`, `OwnerPortalPetSummary`, `OwnerPortalVisitSummary`, `OwnerPortalPrescriptionCard`, `OwnerPortalInvoiceSummary`, `OwnerPortalCheckoutSelection`, and `OwnerPortalCheckoutReturnState`; keep records read-only and diagnosis/prescription-only per OWN-01 and D-73 to D-77. In `packages/shared/src/owner-portal/schemas.ts`, add zod schemas for magic-link validation results, deep-link request params, session-restore state, combined checkout submission, and reissue requests; encode the exact `7 * 24 * 60 * 60` expiry window for D-64 and OWN-04. Replace the Wave 0 files named in `09-VALIDATION.md` with failing tests that assert access-policy defaults, hidden-module semantics, token hash lookup, expired-link state, invalid-link no-data behavior, and multi-invoice checkout selection constraints.</action>
  <acceptance_criteria>
    - `packages/shared/src/web-dashboard/types.ts` contains `export type BrowserRoleCode` and `export type DashboardPanelId`.
    - `packages/shared/src/web-dashboard/schemas.ts` contains `browserAccessPolicySchema` and `dashboardPanelOrderSchema`.
    - `packages/shared/src/owner-portal/types.ts` contains `export type OwnerPortalSessionState` and `export interface OwnerPortalCheckoutSelection`.
    - `packages/shared/src/owner-portal/schemas.ts` contains `ownerPortalSessionSchema` and `ownerPortalCheckoutSchema`.
    - `apps/api/src/modules/owner-portal/__tests__/magic-link.service.test.ts` contains `EXPIRED` and `INVALID` assertions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run packages/shared/src/web-dashboard/__tests__/schemas.test.ts packages/shared/src/owner-portal/__tests__/schemas.test.ts apps/api/src/modules/web-dashboard/__tests__/access-policy.service.test.ts apps/api/src/modules/owner-portal/__tests__/magic-link.service.test.ts apps/api/src/modules/owner-portal/__tests__/portal-session.service.test.ts</automated>
  </verify>
  <done>Later Phase 9 plans can import one shared contract surface for browser access, portal state, deep links, and checkout selection without redefining scope or security rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Prisma models for browser access, magic links, session restore, and checkout snapshots</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>apps/api/prisma/schema.prisma, .planning/phases/09-web-dashboard-owner-portal/09-CONTEXT.md, .planning/phases/09-web-dashboard-owner-portal/09-RESEARCH.md, .planning/phases/06-invoicing-payments/06-CONTEXT.md</read_first>
  <behavior>
    - Browser-access policy is clinic-scoped and role-scoped, not per-user ACL drift.
    - Magic-link persistence stores token hash, explicit scope, deep-link target, expiry, and reissue lineage only.
    - Session restore remembers last tab, last pet, last invoice, and last return state during a valid link window.
  </behavior>
  <action>Append the Phase 9 models to `apps/api/prisma/schema.prisma`. Add `model ClinicBrowserAccessPolicy` with fields `id`, `clinicId`, `roleCode`, `browserEnabled`, `queueEnabled`, `schedulingEnabled`, `billingEnabled`, `inventoryEnabled`, `inventoryWriteEnabled`, `usersEnabled`, `updatedByUserId`, `updatedAt`, and `createdAt`; enforce one row per clinic+role with `@@unique([clinicId, roleCode])` per D-19 and D-21. Add `model UserDashboardPreference` with `userId`, `clinicId`, `panelOrderJson`, `collapsedPanelsJson`, `lastViewedDashboardAt`, and `updatedAt`; store order only, never removable core panel state, per D-14. Add `model OwnerPortalMagicLink` with `clinicId`, `ownerId`, `tokenHash`, `defaultTab`, `deepLinkType`, `deepLinkEntityId`, `allowedPetIdsJson`, `allowedInvoiceIdsJson`, `issuedAt`, `expiresAt`, `revokedAt`, `reissuedFromLinkId`, `latestReissueLinkId`, and `lastViewedAt`; do not persist raw tokens. Add `model OwnerPortalSessionState` with `magicLinkId`, `lastTab`, `lastPetId`, `lastInvoiceId`, `lastVisitId`, `lastCheckoutSessionId`, `lastReturnState`, and `updatedAt` per D-53. Add `model OwnerPortalCheckoutSession` with `magicLinkId`, `selectedInvoiceIdsJson`, `petBreakdownJson`, `amountDuePaise`, `razorpayPaymentLinkId`, `returnState`, `createdAt`, `updatedAt`, and `completedAt`. Add indexes exactly on `[clinicId, roleCode]`, `[clinicId, browserEnabled]`, `[tokenHash]`, `[ownerId, expiresAt]`, `[magicLinkId, updatedAt]`, and `[magicLinkId, returnState]`.</action>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` contains `model ClinicBrowserAccessPolicy`, `model UserDashboardPreference`, `model OwnerPortalMagicLink`, `model OwnerPortalSessionState`, and `model OwnerPortalCheckoutSession`.
    - `apps/api/prisma/schema.prisma` contains `tokenHash`, `allowedPetIdsJson`, `allowedInvoiceIdsJson`, and `expiresAt` on `OwnerPortalMagicLink`.
    - `apps/api/prisma/schema.prisma` contains `browserEnabled`, `inventoryWriteEnabled`, and `usersEnabled` on `ClinicBrowserAccessPolicy`.
    - `apps/api/prisma/schema.prisma` contains `@@unique([clinicId, roleCode])` and `@@index([tokenHash])`.
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate --schema apps/api/prisma/schema.prisma</automated>
  </verify>
  <done>The database can persist role-shaped browser access, hashed owner links, valid-link session restore, and combined-checkout snapshots before API work begins.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push the Phase 9 schema before API and web plans begin</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>apps/api/prisma/schema.prisma</read_first>
  <action>After Task 2 is complete, run `npx prisma db push --schema apps/api/prisma/schema.prisma` and then `npx prisma generate --schema apps/api/prisma/schema.prisma`. If Prisma requires an interactive destructive-confirmation path that cannot be suppressed safely, stop and surface the blocking prompt instead of guessing. This task is mandatory because Phase 9 APIs and generated types will otherwise verify against stale local schema state, creating a false-positive completion signal.</action>
  <acceptance_criteria>
    - `npx prisma db push --schema apps/api/prisma/schema.prisma` exits 0.
    - `npx prisma generate --schema apps/api/prisma/schema.prisma` exits 0.
    - No later Phase 9 plan starts before the pushed schema exists locally.
  </acceptance_criteria>
  <verify>
    <automated>npx prisma db push --schema apps/api/prisma/schema.prisma && npx prisma generate --schema apps/api/prisma/schema.prisma</automated>
  </verify>
  <done>The local database and Prisma client match the Phase 9 access and portal models, removing schema drift before Wave 2 work starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser and portal payloads → shared schemas | Untrusted role-toggle, token, and checkout payloads cross into trusted business logic |
| Prisma schema → live database | Stale DB state can invalidate later security assumptions around token scope and role toggles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | Browser-access policy contracts | mitigate | Encode canonical role/module fields in `packages/shared/src/web-dashboard/types.ts` and require later services to derive access from server-side clinic role state only |
| T-09-02 | Information Disclosure | Owner magic-link contracts | mitigate | Encode `INVALID` state with no data payload in `packages/shared/src/owner-portal/schemas.ts` and later enforce token-hash lookups against DB scope |
| T-09-03 | Denial of Service | Schema drift before API implementation | mitigate | Keep the blocking Prisma push task and verify with `prisma validate` plus `prisma db push` before Wave 2 |
</threat_model>

<verification>
- Shared contract tests and Wave 0 service tests exist and run
- Prisma schema validates successfully
- Blocking schema push completes before any browser-access or owner-portal API work starts
</verification>

<success_criteria>
- Phase 9 has one canonical contract layer for browser access and portal state
- Magic-link storage is hashed, scoped, and expiry-aware before routes are implemented
- Later plans can build against pushed Prisma models instead of inferring persistence ad hoc
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-dashboard-owner-portal/09-01-SUMMARY.md`
</output>
