---
phase: 10-offline-hardening-integration-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - apps/mobile/src/features/queue/hooks/useOfflineQueueActions.ts
  - apps/mobile/src/features/queue/store/queueOfflineStore.ts
  - apps/mobile/src/features/queue/screens/QueueBoardScreen.tsx
  - apps/api/src/modules/queue/services/queueOfflineReplay.service.ts
  - apps/api/src/modules/queue/services/queuePreemption.service.ts
  - apps/api/src/modules/queue/controllers/queueSync.controller.ts
  - apps/api/src/modules/queue/__tests__/queueOfflineReplay.service.test.ts
  - apps/api/src/modules/queue/__tests__/queuePreemption.service.test.ts
autonomous: true
requirements:
  - PLT-03
must_haves:
  truths:
    - "Offline queue check-ins are operationally real on-device and appear in the queue board immediately."
    - "Queue replay always runs before lower-priority backlog work when connectivity returns."
    - "Queue mismatches resolve through a lightweight operational review path rather than silent overwrite."
  artifacts:
    - path: "apps/mobile/src/features/queue/hooks/useOfflineQueueActions.ts"
      provides: "Queue check-in/status mutation hook that queues offline operations"
      exports: ["useOfflineQueueActions"]
    - path: "apps/api/src/modules/queue/services/queueOfflineReplay.service.ts"
      provides: "Server-side queue replay reconciliation"
      exports: ["QueueOfflineReplayService"]
    - path: "apps/api/src/modules/queue/services/queuePreemption.service.ts"
      provides: "Queue-first replay ordering and preemption rules"
      exports: ["QueuePreemptionService"]
  key_links:
    - from: "apps/mobile/src/features/queue/hooks/useOfflineQueueActions.ts"
      to: "apps/mobile/src/features/offline-sync/services/syncCoordinator.ts"
      via: "Queue mutations enqueue `QUEUE_HIGH` replay envelopes"
      pattern: "QUEUE_HIGH"
    - from: "apps/api/src/modules/queue/controllers/queueSync.controller.ts"
      to: "apps/api/src/modules/queue/services/queueOfflineReplay.service.ts"
      via: "Queue-specific replay endpoint applies offline queue mutations through one reconciliation service"
      pattern: "replayQueueOperation"
---

<objective>
Make the walk-in queue fully operational offline, with immediate local queue truth and queue-first reconnect replay.

Purpose: Queue operations are the clinic's most time-sensitive workflow. D-03 and D-12 through D-14 require local queue truth plus first-priority replay before any consultation or inventory backlog drains.

Output: Offline queue mutation hook/store, optimistic queue board integration, queue replay services/controllers, and queue replay tests.
</objective>

<execution_context>
@/Users/mihirmahesh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mihirmahesh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-offline-hardening-integration-polish/10-CONTEXT.md
@.planning/phases/10-offline-hardening-integration-polish/10-RESEARCH.md
@.planning/phases/03-patient-registration-walk-in-queue/03-CONTEXT.md
@.planning/phases/10-offline-hardening-integration-polish/10-01-PLAN.md

<interfaces>
Queue replay rules for this plan:

- Offline-created queue entries are operationally real per D-03.
- Reconnect priority is `QUEUE_HIGH` before every other tier per D-12.
- Replay can preempt lower-priority backlog work per D-14.
- Queue conflicts use lightweight operational review, not the clinical resolution sheet per D-10.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add offline queue actions, local queue state, and optimistic queue-board rendering</name>
  <files>apps/mobile/src/features/queue/hooks/useOfflineQueueActions.ts, apps/mobile/src/features/queue/store/queueOfflineStore.ts, apps/mobile/src/features/queue/screens/QueueBoardScreen.tsx</files>
  <read_first>.planning/phases/03-patient-registration-walk-in-queue/03-CONTEXT.md, .planning/phases/10-offline-hardening-integration-polish/10-CONTEXT.md, .planning/phases/10-offline-hardening-integration-polish/10-01-PLAN.md</read_first>
  <behavior>
    - Offline-created queue entries show up immediately on the local board and are not marked as draft placeholders.
    - Returning-patient check-in, status transitions, and call-next enqueue `QUEUE_HIGH` operations.
    - Queue board distinguishes locally pending replay state without demoting the entry out of the operational queue.
  </behavior>
  <action>Create `useOfflineQueueActions.ts` so check-in, status transition, no-show, and call-next all write `QUEUE_HIGH` envelopes into the offline sync ledger from Plan 10-01 instead of hard-failing on connectivity loss, per D-01 to D-03 and D-12. In `queueOfflineStore.ts`, keep a local queue projection keyed by queue entry id, preserving check-in time, emergency flag, scheduled metadata when present, and local replay status. Update `QueueBoardScreen.tsx` so locally created or updated entries render in their real status sections immediately, with a quiet pending marker rather than a blocking modal per D-19; do not degrade them to placeholders or require reconnect before staff can proceed.</action>
  <acceptance_criteria>
    - `apps/mobile/src/features/queue/hooks/useOfflineQueueActions.ts` contains `QUEUE_HIGH` and `enqueueOfflineOperation`.
    - `apps/mobile/src/features/queue/store/queueOfflineStore.ts` contains `pendingReplayState` and `applyLocalQueueOperation`.
    - `apps/mobile/src/features/queue/screens/QueueBoardScreen.tsx` renders pending/local entries without moving them out of the main queue sections.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/api/src/modules/queue/__tests__/queueOfflineReplay.service.test.ts apps/api/src/modules/queue/__tests__/queuePreemption.service.test.ts</automated>
  </verify>
  <done>Queue actions continue working offline and the local queue board treats them as real clinic work, not deferred placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement queue replay reconciliation, lower-tier preemption, and lightweight queue review handling</name>
  <files>apps/api/src/modules/queue/services/queueOfflineReplay.service.ts, apps/api/src/modules/queue/services/queuePreemption.service.ts, apps/api/src/modules/queue/controllers/queueSync.controller.ts, apps/api/src/modules/queue/__tests__/queueOfflineReplay.service.test.ts, apps/api/src/modules/queue/__tests__/queuePreemption.service.test.ts</files>
  <read_first>apps/api/src/modules/sync/services/replayIngest.service.ts, .planning/phases/10-offline-hardening-integration-polish/10-RESEARCH.md, .planning/phases/03-patient-registration-walk-in-queue/03-CONTEXT.md</read_first>
  <behavior>
    - Duplicate or flapping replay of the same queue mutation stays idempotent.
    - Queue replay is serviced before lower tiers and can interrupt unsent lower-tier work.
    - Queue mismatches create operational review items instead of silent last-write-wins.
  </behavior>
  <action>Create `queueOfflineReplay.service.ts` to reconcile offline queue check-ins, status transitions, and same-day re-check-ins against the live queue model while preserving the Phase 3 queue rules. Treat `deviceId + operationId` receipts from Plan 10-01 as authoritative idempotency guards. Create `queuePreemption.service.ts` to expose explicit `canRunQueueReplayNow` and `pauseLowerTierReplayForQueue` helpers so D-12 to D-14 are enforced centrally instead of by ad hoc timers. Add `queueSync.controller.ts` routes that accept queue replay envelopes, return operational review tasks for mismatches such as already-archived entries or conflicting status changes, and never downgrade queue replay into a generic background tier. Replace the Wave 0 tests with failing cases proving idempotent duplicate rejection, queue-first replay, lower-tier preemption, and operational review creation instead of silent overwrite.</action>
  <acceptance_criteria>
    - `apps/api/src/modules/queue/services/queueOfflineReplay.service.ts` contains `replayQueueOperation` and `createOperationalReviewTask`.
    - `apps/api/src/modules/queue/services/queuePreemption.service.ts` contains `pauseLowerTierReplayForQueue`.
    - `apps/api/src/modules/queue/controllers/queueSync.controller.ts` accepts queue replay payloads and returns review-task metadata on mismatch.
    - `apps/api/src/modules/queue/__tests__/queuePreemption.service.test.ts` contains preemption assertions for non-queue tiers.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/api/src/modules/queue/__tests__/queueOfflineReplay.service.test.ts apps/api/src/modules/queue/__tests__/queuePreemption.service.test.ts</automated>
  </verify>
  <done>Queue reconnect behavior is explicitly first-priority, idempotent, and reviewable when server state disagrees with local queue actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Offline queue UI/store → sync ledger | Local queue mutations become authoritative replay requests |
| Queue replay endpoint → live queue data | Replayed queue actions can alter real clinic order if validation is weak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Queue replay payloads | mitigate | Use shared replay schemas plus idempotent `deviceId + operationId` guards before applying queue mutations |
| T-10-05 | Denial of Service | Replay ordering | mitigate | Centralize queue-first preemption in `queuePreemption.service.ts` so lower-tier backlog cannot starve live queue recovery |
| T-10-06 | Repudiation | Queue mismatch handling | mitigate | Create explicit operational review tasks instead of silent overwrite so staff can see and resolve mismatches |
</threat_model>

<verification>
- Queue replay and preemption tests pass
- Offline queue board renders pending/local entries as operationally real
- Queue mismatches create lightweight review items rather than silent overwrite
</verification>

<success_criteria>
- Staff can continue queue operations offline without losing operational truth
- Queue replay always runs before lower-priority work
- Queue conflicts remain visible and reviewable
</success_criteria>

<output>
After completion, create `.planning/phases/10-offline-hardening-integration-polish/10-02-SUMMARY.md`
</output>
