---
phase: 10-offline-hardening-integration-polish
plan: 05
subsystem: sync-visibility-ux-retry-escalation-replay-broadcasts-and-browser-optimistic-concurrency
tags: [zustand, react-native-paper, fastify, zod, socket.io, TDD, guided-retry, escalation, stale-state, optimistic-concurrency]
dependency_graph:
  requires: [10-01, 10-02, 10-03, 10-04]
  provides: [sync-status-badge, sync-failure-center, retry-escalation-service, replay-broadcast-service, web-replay-stale-state, browser-write-version-check]
  affects: [10-06]
tech_stack:
  added: []
  patterns:
    - calm-badge-plus-actionable-failure-center (D-18 to D-21)
    - originating-user-then-clinician-then-any-on-duty-clinician escalation ladder (D-22 to D-24, D-36)
    - ownership-only-escalation-never-touches-replay-priority (D-37)
    - scoped-per-clinic-room realtime broadcast (mirrors BrowserSyncService)
    - browser-write-side optimistic-concurrency check reusing the existing read-side knownVersion/staleVersion epoch-ms scheme
key_files:
  created:
    - apps/mobile/src/features/offline-sync/lib/sync-status.ts
    - apps/mobile/src/features/offline-sync/store/syncUiStore.ts
    - apps/mobile/src/features/offline-sync/hooks/useSyncStatus.ts
    - apps/mobile/src/features/offline-sync/components/SyncStatusBadge.tsx
    - apps/mobile/src/features/offline-sync/screens/SyncFailureCenterScreen.tsx
    - apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx
    - apps/api/src/modules/sync/services/retryEscalation.service.ts
    - apps/api/src/modules/sync/services/replayBroadcast.service.ts
    - apps/api/src/modules/sync/__tests__/retryEscalation.service.test.ts
    - apps/web/src/features/dashboard/hooks/useReplayStaleState.ts
    - apps/web/src/features/queue/hooks/useQueueReplayRealtime.ts
    - apps/web/src/features/inventory/hooks/useInventoryReplayRealtime.ts
    - apps/web/src/features/dashboard/__tests__/replayStaleState.test.tsx
  modified:
    - apps/mobile/src/features/offline-sync/db/offlineDb.ts (added read helpers for the sync-visibility aggregate)
    - apps/api/src/realtime/browser-sync.service.ts (added checkWriteVersion + staleWriteConflictError)
    - apps/api/src/realtime/__tests__/browser-sync.service.test.ts
    - apps/api/src/modules/queue/queue.schema.ts (statusUpdateBodySchema + expectedVersion)
    - apps/api/src/modules/queue/web-queue.service.ts (updateEntryStatus version check)
    - apps/api/src/modules/queue/web-queue.controller.ts
    - apps/api/src/modules/queue/__tests__/web-queue.service.test.ts
    - apps/api/src/modules/billing/billing.schema.ts (collectPaymentBodySchema + webRefundBodySchema + webVoidBodySchema)
    - apps/api/src/modules/billing/billing-workbench.service.ts (assertInvoiceVersionCurrent on collect/refund/void)
    - apps/api/src/modules/billing/workbench.controller.ts
    - apps/api/src/modules/billing/__tests__/billing-workbench.service.test.ts
    - apps/api/src/modules/inventory/inventory.schema.ts (webStockAdjustmentBodySchema)
    - apps/api/src/modules/inventory/inventory-web.service.ts (adjustStock version check)
    - apps/api/src/modules/inventory/inventory-web.controller.ts
    - apps/api/src/modules/inventory/__tests__/inventory-web.service.test.ts
    - vitest.config.ts (repo root; appended Plan 10-05 mobile offline-sync .tsx test glob)
metrics:
  duration: ~1 session, fully TDD (RED confirmed before every implementation file)
  completed: 2026-08-24
  tasks_completed: 2
  tasks_total: 2
---

# Phase 10 Plan 05: Sync Visibility UX, Retry Escalation, Replay Broadcasts, and Browser Optimistic-Concurrency Enforcement Summary

Built the cross-cutting layer that sits on top of Plans 10-02/10-03/10-04's queue/EMR/inventory offline-replay work: a calm, always-visible mobile sync badge plus an actionable failure center with explicit guided-retry/escalation ownership (D-18 to D-24, D-36, D-11), a server-side `retryEscalation.service.ts` that moves a stuck safety-critical conflict from the originating clinician to another on-duty clinician without ever touching replay priority (D-37), scoped `replayBroadcast.service.ts` events so mobile replays surface stale/conflict prompts on browser tabs (D-40) through the **existing** `StaleStateBanner`, and — per the plan's extended Task 2 scope — closed a real, `no-mistakes`-flagged gap in Phase 9's shipped code: browser mutation endpoints (queue status update, billing collect/refund/void, inventory adjust) now verify an `expectedVersion` against the row's live version and REJECT a stale write with a 409 `STALE_WRITE_CONFLICT` instead of silently applying it.

Built strictly test-first: every acceptance criterion had a failing test confirmed RED before its implementation was written.

## What Was Built

### Task 1: Mobile sync badge, failure center, and retry-ownership UX (TDD)

- **`apps/mobile/src/features/offline-sync/lib/sync-status.ts`** (new, RN-free — same pattern as `queue-offline-utils.ts`/`clinical-conflict-resolution.ts`, since `apps/mobile`'s vitest runs in plain `node` with no Metro/Babel transform): `deriveVisibilityState` mirrors `ReplayIngestService.deriveVisibilityState`'s precedence (FAILED > CONFLICT > REPLAYING > PENDING > CAUGHT_UP) for the on-device aggregate; `badgeCopy` is deliberately calm prose (never `!`, never the word "error") per D-19; `shouldShowRecoveryCue` fires only on a genuine transition INTO `CAUGHT_UP` from a real prior state (D-21 — never on cold start, never repeatedly); `isUnresolved` gates D-11's "stays visible until cleared"; `toFailureCenterItemFromTask`/`toFailureCenterItemFromConflict` normalize `SyncFailureTaskRecord` and `SyncConflictEnvelope` into one `FailureCenterItem` shape; `groupFailureCenterItems` buckets into `needsYourRetry` (owned by viewer, not escalated), `escalatedToClinician` (ESCALATED **and** `SAFETY_CRITICAL` only — D-24), and `operationalReview` (everything else unresolved, including a non-safety-critical ESCALATED item, per D-10's lighter review with no clinician hand-off).
- **`apps/mobile/src/features/offline-sync/store/syncUiStore.ts`** (zustand, RN-free, same convention as `queueOfflineStore.ts`) — holds `counts`, `visibilityState`, `failureItems`, and derives `showRecoveryCue` from the previous→current state transition on every `setSummary` call.
- **`apps/mobile/src/features/offline-sync/hooks/useSyncStatus.ts`** — polls the on-device `sync_operations`/`sync_conflicts`/`sync_failure_tasks` tables (via three new read helpers added to `offlineDb.ts`: `countPendingSyncOperations`, `listUnresolvedSyncFailureTasks`, `listUnresolvedSyncConflicts`) every 5s and writes the aggregate into `syncUiStore` — the single shared source both UI pieces below read from.
- **`apps/mobile/src/features/offline-sync/components/SyncStatusBadge.tsx`** — an always-visible, non-blocking element (never `Alert`/modal) that renders `badgeCopy` or the subtle `RECOVERY_CUE_COPY` when `showRecoveryCue` is true, and exposes `onPress` only for the two actionable states (CONFLICT/FAILED) to route into the failure center.
- **`apps/mobile/src/features/offline-sync/screens/SyncFailureCenterScreen.tsx`** — groups every unresolved item via `groupFailureCenterItems` into the three named sections ("Needs your retry", "Escalated to clinician", "Operational review"), shows both `originatingUserId` and `currentOwnerUserId` per row (via an injected `resolveUserName`, matching `ClinicalConflictResolutionSheet.tsx`'s convention), and offers explicit Retry/Escalate actions rather than a generic toast. D-11 is enforced structurally by the shared grouping function (a `RESOLVED` item never reaches any group), not by anything screen-local.
- Tests: `SyncFailureCenterScreen.test.tsx` (41) — direct unit coverage of every `sync-status.ts` function (precedence, calm-copy assertions, recovery-cue transition matrix, grouping including the safety-critical-vs-operational ESCALATED split), `syncUiStore.ts` behavior, and component-source-assertion checks (same technique `ClinicalConflictResolutionSheet.test.tsx` established, since `react-native`/`react-native-paper` cannot be rendered under this repo's plain-`node` mobile vitest environment) for `SyncStatusBadge.tsx`, `SyncFailureCenterScreen.tsx`, and `useSyncStatus.ts`.

### Task 2: Guided retry/escalation, scoped replay broadcasts, web stale-state hooks, and the browser optimistic-concurrency gap (TDD)

- **`apps/api/src/modules/sync/services/retryEscalation.service.ts`** (new) — an ownership-only state machine over `SyncFailureTask`/`SyncConflictRecord` rows (`OPEN → GUIDED_RETRY → ESCALATED`), reading/writing through a minimal injected Prisma-delegate interface (matches `ReplayIngestPrismaClient`'s convention). `assignOriginatingUserRetry` only advances the state — ownership was already resolved correctly at creation time by `replayIngest.service.ts` (originating user for a plain failure task or an `OPERATIONAL` conflict; the assigned clinician for a `SAFETY_CRITICAL` conflict per D-09). `recordGuidedRetryFailure` moves everything to `ESCALATED` (D-23 is explicit that escalation is automatic after a failed guided retry), but only reassigns ownership for a `SAFETY_CRITICAL` conflict — to a **different** on-duty clinician via an injected `OnDutyRosterProvider`, never the same one whose retry just failed (D-24). `reassignUnreachableEscalatedOwner` implements D-36's "escalates further" half: if the already-escalated clinician is also unreachable, it reassigns to yet another on-duty clinician — the roster-provider contract explicitly documents it must never include Admin, and the service throws (`NO_ON_DUTY_CLINICIAN_AVAILABLE`) rather than silently stalling or falling back to Admin when nobody else is available.
- **D-37 proof**: neither `RetryEscalationConflictRow`/`RetryEscalationTaskRow` nor any `update()` call this service makes has a `priority`/`replayPriority` field at all — enforced structurally, not just by convention. Tests assert (a) the locked `REPLAY_PRIORITIES` ladder keeps `QUEUE_HIGH` ahead of `CLINICAL_MEDIUM` independent of any conflict severity/state, and (b) every escalation `update()` call's `data` payload never carries a `priority`/`replayPriority` key.
- **`apps/api/src/modules/sync/services/replayBroadcast.service.ts`** (new) — `REPLAY_BROADCAST_EVENTS` (`replay:applied`, `replay:conflict-opened`, `replay:failure-escalated`), mirroring `BrowserSyncService`'s `io: Server | null` no-op-when-null convention and its `clinic:${clinicId}` room scoping (T-10-09: never a global/unscoped broadcast). Payload carries only `clinicId`/`domain`/`entityIds`/optional `dateWindow` — never the replayed data itself.
- **`apps/web/src/features/dashboard/hooks/useReplayStaleState.ts`** (new) — the missing realtime half of Phase 9's `StaleStateBanner`/`resolveStaleStatus`: `onReplayApplied`/`onReplayConflictOpened` (fed by the domain-scoped realtime hooks below) flip a `watchedEntityIds`-filtered `'fresh' | 'stale' | 'conflict'` status, `conflict` never downgrades back to `stale` on a later apply for the same entity, and `acknowledge()` is the `StaleStateBanner` "Refresh" action. Proven against the **real** `StaleStateBanner` component (a render test drives the actual banner, not a mock).
- **`apps/web/src/features/queue/hooks/useQueueReplayRealtime.ts`** / **`apps/web/src/features/inventory/hooks/useInventoryReplayRealtime.ts`** (new) — ported from `useQueueRealtime.ts`'s own socket setup (same auth handshake, transport, reconnection policy), each filtering the shared `replay:applied`/`replay:conflict-opened` events down to their own domain (defense in depth beyond the server's room scoping) before forwarding to `useReplayStaleState`'s handlers.
- **Closing the browser optimistic-concurrency gap** (the `no-mistakes` finding cited in `10-CONTEXT.md`'s Reusable Assets note): `knownVersion` was read-only — checked on every board/workbench read to decide `fresh`/`stale`, but no mutation endpoint ever verified a caller's claimed version before writing, so `StaleStateBanner`'s `conflict` state was purely decorative and a stale browser tab could silently clobber a row a mobile replay or another session had just changed. This violates D-05 ("review before overwrite, not silent last-write-wins") on the browser side of exactly the offline-recovery race this phase exists to harden.
  - `BrowserSyncService` gained `checkWriteVersion(currentVersion, expectedVersion?)` (`'ok' | 'stale'`, always `'ok'` when `expectedVersion` is absent — strictly additive) and a `staleWriteConflictError(...)` factory producing a 409 `STALE_WRITE_CONFLICT` whose `.conflict` mirrors `SyncConflictEnvelope`'s domain/entityType/entityId/severity shape (`currentVersion`/`expectedVersion` stand in for `localPayload`/`serverPayload` since the write is rejected *before* anything is applied — there is no local payload to snapshot).
  - `queue.schema.ts`'s `statusUpdateBodySchema` (shared with mobile via `@breeyo/validators`'s `queueStatusUpdateSchema`) gained an optional `expectedVersion`; `web-queue.service.ts`'s `updateEntryStatus` checks it against the live `queueEntry.updatedAt` before calling `QueueService.updateStatus`.
  - `billing.schema.ts` gained `expectedVersion` on `collectPaymentBodySchema` directly (already web-only) and two new **web-only wrapper schemas**, `webRefundBodySchema`/`webVoidBodySchema` (extending the shared mobile `refundInputSchema`/`voidInvoiceSchema` with the same field) so the mobile refund/void forms are untouched; `billing-workbench.service.ts`'s `collectPayment`/`refundInvoice`/`voidInvoice` all run `assertInvoiceVersionCurrent` against the live `invoice.updatedAt` before delegating to `PaymentService`/`RefundService`/`InvoiceService`.
  - `inventory.schema.ts` gained a new web-only `webStockAdjustmentBodySchema` (extending the shared mobile `stockAdjustmentSchema`, keeping D-37's mobile-first barcode-scan flow untouched); `inventory-web.service.ts`'s `adjustStock` checks the live `inventoryItem.updatedAt` before delegating to `StockAdjustmentService`.
  - Every check is a no-op when `expectedVersion` is omitted (verified by an explicit test on each of the three services), so no existing caller (nothing sent this field before this plan) is a breaking change.
- Tests: `retryEscalation.service.test.ts` (20 — guided retry, clinician escalation with on-duty reassignment, D-36 further-reassignment, D-37's two structural proofs, plus `ReplayBroadcastService`'s scoped-emission/no-op-when-null behavior in the same file per this plan's file list), `replayStaleState.test.tsx` (12 — the stale/conflict state machine, a render test against the real `StaleStateBanner`, and the two domain-scoped realtime hooks via a mocked `socket.io-client`), `browser-sync.service.test.ts` (+4), `web-queue.service.test.ts` (+3), `billing-workbench.service.test.ts` (+5), `inventory-web.service.test.ts` (+3).

## Deviations From the Plan (flagged, not silently made)

1. **`retryEscalation.service.test.ts` also contains `ReplayBroadcastService`'s tests.** The plan's `files_modified` list names only one API sync test file for this task (no separate `replayBroadcast.service.test.ts`), and the `<verify>` command only runs that one file — both services are exercised there as two `describe` blocks rather than splitting into a second test file the plan never names.
2. **`groupFailureCenterItems` gates "Escalated to clinician" on `severity === SAFETY_CRITICAL`, not merely `resolutionState === ESCALATED`.** The plan's acceptance criteria say the section is for "escalated items" without spelling out the non-safety-critical case explicitly. Since `SyncFailureTask` has no severity column at all and D-10 requires a *lighter* review for operational items (no clinician hand-off), routing a non-safety-critical `ESCALATED` item into "Operational review" instead keeps the three sections mutually exclusive and honest about who is actually expected to act — covered by an explicit test.
3. **`useQueueReplayRealtime`/`useInventoryReplayRealtime` take `accessToken`/`activeClinicId` as explicit parameters** rather than calling `useAuth()` internally the way `useQueueRealtime.ts` does. This keeps them trivially composable from `useReplayStaleState`'s call site (which already has those values from its own page-level `useAuth()` call) and easier to unit test in isolation; the underlying socket setup (auth handshake, transport, reconnection policy) is otherwise identical.
4. **Neither `useReplayStaleState`/`useQueueReplayRealtime`/`useInventoryReplayRealtime` nor `SyncStatusBadge`/`SyncFailureCenterScreen` were wired into a live screen/page.** The plan's files list and acceptance criteria scope this plan to the hooks/components/services and their tests, not to editing `QueueBoard.tsx`/`BillingWorkbench.tsx`/a mobile navigator to mount them — matching the same class of "built, tested, ready to adopt" scope note Plan 10-04 flagged for `useOfflineStockActions.ts` not being wired into every stock screen.

   **Review-Fix Follow-Up (second review round, F1-F7, resolved):** the web half (`useQueueReplayRealtime`/`useInventoryReplayRealtime`/`useReplayStaleState` into `QueueBoard.tsx`/`BillingWorkbench.tsx`) was wired by verify-fix 10.3 (see below). The mobile half was wired by this second round: `SyncStatusBadge` now renders in `apps/mobile/app/(app)/_layout.tsx`'s header (reading `syncUiStore`, kept current by `useSyncStatus()`), and a new `apps/mobile/app/(app)/sync-failures.tsx` route mounts `SyncFailureCenterScreen` as the badge's real `onPress` target. See `10-01-SUMMARY.md`'s own "Second Review Round Follow-Up" for the related, more fundamental gap this same round closed: `syncCoordinator.ts`'s `runReplayCycle` (Plan 10-01) had no live caller at all until this round added `ConnectivityReplayProvider`.
5. **`offlineDb.ts` was modified** (three new read helpers) even though it is not in this plan's `files_modified` list. `useSyncStatus.ts` needs to read the local `sync_operations`/`sync_conflicts`/`sync_failure_tasks` tables Plan 10-01 already created — adding the read counterparts to the same file that owns every other read/write helper for those tables (rather than duplicating raw SQL elsewhere) is the same reasoning Plan 10-03 used when it added `readWorkingSetSnapshot` to this file for the first time.

## Task Commits

Both tasks were implemented and verified together in one session (the mobile UX and the API/web services share the same `ResolutionState`/`ConflictSeverity`/`SyncVisibilityState` vocabulary and were developed in the same TDD loop); the version-check gap closure is committed separately from the UX/escalation/broadcast work, since it touches Phase 9 modules (queue/billing/inventory web services) rather than Phase 10's own sync module.

_All TDD tasks followed the iron law: the failing test was written and run first (confirmed RED), then the minimal implementation was added until it passed._

## Verification

```
npx vitest run apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx apps/api/src/modules/sync/__tests__/retryEscalation.service.test.ts apps/web/src/features/dashboard/__tests__/replayStaleState.test.tsx apps/api/src/modules/queue/__tests__/web-queue.service.test.ts apps/api/src/modules/billing/__tests__/billing-workbench.service.test.ts apps/api/src/modules/inventory/__tests__/inventory-web.service.test.ts
```
111/111 passing (41 + 20 + 12 + 9 + 15 + 14).

Additional verification run beyond the plan's gate:
- `apps/api` full suite (real Postgres 16 + Redis 7): **172 test files (163 passed, 9 skipped — pre-existing DB-fixture-dependent files), 2142 tests (2062 passed, 80 todo — pre-existing)**, 0 failed. (Grew from 10-04's 171 files/2107 tests by exactly this plan's 35 new test cases: 20 in `retryEscalation.service.test.ts` + 3/5/3/4 added to `web-queue`/`billing-workbench`/`inventory-web`/`browser-sync`.)
- `apps/mobile` full suite: **52 test files, 847 tests passed**, 0 failed (grew from 10-04's 806 by exactly this plan's 41 new `SyncFailureCenterScreen.test.tsx` cases).
- Root aggregate `npx vitest run` (the cross-package sweep `vitest.config.ts` defines): **69 test files, 1004 tests passed**, 0 failed (grew from 10-04's 916).
- `npx tsc --noEmit` in `apps/api`: only the same 5 pre-existing errors 10-04 documented (`replayIngest.service.test.ts`'s own module-resolution issue) — zero new errors from any file this plan touched or created.
- `npx tsc --noEmit` in `apps/web`: only pre-existing `@testing-library/jest-dom` matcher-typing gaps (`toBeInTheDocument`/`toHaveTextContent`/`toHaveAttribute` not augmented onto `Assertion<T>` in this package's `tsconfig`) that affect every jest-dom-using test file in this package, including several this plan did not touch (`portal-shell.test.tsx`, `upcoming-care.test.tsx`, `queue-board.test.tsx`, `visit-timeline.test.tsx`) and one pre-existing `TS2493` in `use-portal-receipt-url.test.ts` — this plan's own `replayStaleState.test.tsx` shows the same pre-existing jest-dom pattern (4 lines) and nothing else; neither package's `build` script runs `tsc` as a gate.

## Verify-Fix Follow-Up

Per `.planning/PHASE-10-VERIFY-FIX-PLAN.md` finding **10.3**: a `no-mistakes --verify phase 10` pass found that `ReplayBroadcastService` (built and unit-tested by this plan's own Task 2) was never actually called from any of the four domain replay services (`replayIngest`, `queueOfflineReplay`, `consultationOfflineReplay`, `inventoryOfflineReplay`) -- the browser stale-state push it exists to drive was dead in production. On the web side, `QueueBoard.tsx`/`BillingWorkbench.tsx` hardcoded `<StaleStateBanner status="stale" .../>` regardless of what actually happened, `apiClient` silently dropped a raw error response's `.conflict` field instead of forwarding it (mirroring the existing `.details` forwarding), and `useBillingWorkbench.ts`'s `collectPayment` 409 handler only set a generic `mutationError` string instead of driving the conflict prompt.

Fixed by: (a) each domain replay service now takes an injected `ReplayBroadcastService` (defaulting to a no-op instance when `io` is null, matching `BrowserSyncService`'s own convention) and calls `emitReplayApplied`/`emitReplayConflictOpened`/`emitReplayFailureEscalated` at every apply/conflict-creation/failure-task-creation point, scoped to `{ clinicId, domain, entityIds }`; every route file (`sync/routes.ts`, `queue.routes.ts`, `emr.routes.ts`, `dispense.routes.ts`) now constructs a real `ReplayBroadcastService(fastify.io)` and threads it through the corresponding `build*OfflineReplayService` factory. (b) `apiClient` (`apps/web/src/lib/api.ts`) forwards `error.conflict` onto a new `ApiClientError.conflict` field, typed as `ApiConflictInfo` (mirroring `BrowserWriteConflictInfo`). (c) `QueueBoard.tsx` now mounts `useAuth`/`useReplayStaleState`/`useQueueReplayRealtime` directly and drives `StaleStateBanner`'s `status` from the real `'fresh' | 'stale' | 'conflict'` state instead of a hardcoded string. (d) `useBillingWorkbench.ts` mounts `useReplayStaleState` (not `BillingWorkbench.tsx` itself, since the state has to be reachable from `collectPayment`'s catch block, which lives in the hook) and exposes `replayStatus`, which `BillingWorkbench.tsx` now consumes as a prop to drive the same banner; `collectPayment`'s catch calls `onReplayConflictOpened([err.conflict.entityId])` when the caught error is an `ApiClientError` carrying a `.conflict` payload.

TDD: failing tests were added first to each of the four replay-service test files (spying on an injected mock `ReplayBroadcastService`, asserting the right event/payload after a real call to the service's own public replay method -- not just that DB state changed), to a new `apps/web/src/lib/__tests__/api.test.ts` (raw 409 response with `.conflict` surfaces on the thrown error), and to the real `queue-board.test.tsx`/`billing-workbench.test.tsx` (rendering the real `QueuePage`/`BillingPage`, simulating a scoped `replay:conflict-opened` socket event / a real 409 response, asserting `StaleStateBanner` shows the conflict copy) -- confirmed RED against the pre-fix code before implementing.

Verify: `npx vitest run apps/api/src/modules/sync/__tests__/replayIngest.service.test.ts apps/api/src/modules/queue/__tests__/queueOfflineReplay.service.test.ts apps/api/src/modules/emr/__tests__/consultationOfflineReplay.service.test.ts apps/api/src/modules/inventory/__tests__/inventoryOfflineReplay.service.test.ts apps/web/src/lib/__tests__/api.test.ts apps/web/src/features/queue/__tests__/queue-board.test.tsx apps/web/src/features/billing/__tests__/billing-workbench.test.tsx` -- see `10-06-SUMMARY.md`'s Verify-Fix Follow-Up for the full pass/fail counts at every suite level.

---

Per `.planning/PHASE-10-VERIFY-FIX-PLAN.md` finding **10.4**: a `no-mistakes --verify phase 10` pass found that `ClinicalConflictResolutionSheet.tsx` (correctly built by Plan 10-03 Task 2, D-08) was never mounted anywhere reachable -- `SyncFailureCenterScreen.tsx` (this plan's own Task 1) rendered only the generic domain-agnostic row/Retry/Escalate buttons for every item, including an EMR `SAFETY_CRITICAL` clinical conflict, so the structured local-vs-server comparison D-08 requires was unreachable from the failure center.

Fixed by: (a) `lib/sync-status.ts` gained `isClinicalConflictItem(item)` (true exactly when `item.domain === 'emr'`, `item.severity === ConflictSeverity.SAFETY_CRITICAL`, and both `localPayload`/`serverPayload` are present -- the last check is what naturally excludes `FAILURE_TASK`-kind items without a separate `kind` check) and `resolveItemPressAction(item)`, a real, directly-testable routing decision (`OPEN_CLINICAL_CONFLICT_SHEET` vs `NONE`) kept RN-free per this file's existing pattern; `toFailureCenterItemFromConflict` now also carries `localPayload`/`serverPayload` through (previously dropped) so the sheet has data to render. (b) `clinical-conflict-resolution.ts` gained `buildClinicalConflictSummaryFromEnvelope`, a conservative two-way diff (any top-level `SaveDraftInput` field that differs between local and server is treated as conflicting) since the on-device `sync_conflicts` table has no pre-offline-edit baseline to run the server-side three-way `classifyClinicalConflict` diff -- `safeMergeFields` is always empty on-device as a result, so `MERGE_SAFE_FIELDS` is correctly never offered as a false affordance. (c) `SyncFailureCenterScreen.tsx`'s `ItemRow` wraps only a clinical-conflict row in a `Pressable` (testID `sync-failure-clinical-trigger-<id>`) that opens the sheet on tap; every other domain's row keeps its exact pre-existing `View`-only markup, untouched. The screen mounts `ClinicalConflictResolutionSheet` conditionally on `selectedConflictItem`, wiring `onRetry`/`onEscalate` to the screen's existing item-level callbacks and `onKeepLocal`/`onKeepServer`/`onMergeSafeFields` to a new optional `onResolveClinicalConflict(conflict, action)` prop -- left decoupled from any specific endpoint shape since finding 10.5 (a separate, not-yet-done fix) is what adds the real `POST .../conflicts/:conflictId/resolve` route this would eventually call.

TDD: `apps/mobile` runs vitest in a plain `node` environment with no Metro/Babel transform (documented in both test files' header comments) -- `import 'react-native'` fails at parse time and no RN renderer (`react-test-renderer`) is installed, so a literal render-and-tap test is not possible here, matching every other `.tsx` component in this feature. Followed the same established substitute this codebase already uses twice (`SyncStatusBadge.tsx`/`SyncFailureCenterScreen.tsx`'s own contract tests): the actual tap-routing decision was extracted into `resolveItemPressAction`, a pure function exercised with real assertions (an EMR `SAFETY_CRITICAL` item resolves to `OPEN_CLINICAL_CONFLICT_SHEET`; a non-EMR domain, an `OPERATIONAL`-severity EMR item, and a raw `FAILURE_TASK` all resolve to `NONE`), plus component-source-contract checks proving `SyncFailureCenterScreen.tsx` actually imports/mounts `ClinicalConflictResolutionSheet` gated by that same function and only wraps a clinical row in the `sync-failure-clinical-trigger-` pressable. Confirmed RED first (13 failing tests: missing exports/imports) before implementing.

Verify: `npx vitest run apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx apps/mobile/src/features/consultation/__tests__/ClinicalConflictResolutionSheet.test.tsx` -- 70/70 passing (50 + 20). Root aggregate `npx vitest run`: 76 files, 1058 tests passing, 0 failed. `pnpm --filter @breeyo/mobile test -- --run`: 52 files, 860 tests passing, 0 failed. `pnpm --filter @breeyo/mobile exec tsc --noEmit -p .`: same 61 pre-existing `packages/ui` react-native-paper `Text`/theme typing errors present before this change (confirmed via `git stash`), zero new errors from any file this fix touched.

---

Per `.planning/PHASE-10-VERIFY-FIX-PLAN.md` finding **10.6**: a `no-mistakes --verify phase 10` pass found that `retryEscalation.service.ts` (correctly built by this plan's own Task 2, D-22 to D-24, D-36) had no live HTTP caller at all, and its `OnDutyRosterProvider` interface had no concrete implementation -- both were an explicit, disclosed deferral in this plan's own notes and in **Deviation 4** above (`SyncFailureCenterScreen.tsx`'s `onRetry`/`onEscalate` props were never wired to anything real). Finding 10.5's own `ESCALATE` action (`ConsultationConflictResolutionService`, documented in `10-03-SUMMARY.md`) also deliberately left `currentOwnerUserId` untouched pending this fix.

Fixed by: (a) `apps/api/src/modules/sync/services/onDutyRoster.service.ts`'s `ClinicVetRosterProvider` implements `OnDutyRosterProvider` over Phase 8's `AvailabilityRepository.listClinicVets(clinicId)`, minus the unreachable clinician (D-39, resolved via the plan's "on-duty roster source" decision -- no new shift-tracking concept). A second review round (F1-F7) tightened this further: `ClinicVetRosterProvider` now also excludes every Admin-role member itself, via a new `AvailabilityRepository.listAdminUserIds(clinicId)`, rather than relying only on `resolveNextOnDutyClinicianId`'s own never-Admin filtering downstream -- `listClinicVets` deliberately still includes Admins (other callers need every vet-capable id regardless of role), so the roster-provider contract's own "every real implementation MUST exclude Admin" requirement needed enforcing at this layer too. (b) `retryEscalation.service.ts` gained a shared, exported `resolveNextOnDutyClinicianId` helper (the D-24/D-36 roster-exhaustion/never-Admin logic, factored out of the class so it has exactly one implementation) and a new `escalate(kind, id)` dispatcher that picks the right ESCALATED-bound transition (`recordGuidedRetryFailure` from `GUIDED_RETRY`, `reassignUnreachableEscalatedOwner` from `ESCALATED`) based on the row's current state; every thrown error now carries a real `statusCode` (404/409/500) instead of collapsing into a generic 500. (c) `apps/api/src/modules/sync/routes.ts` adds `POST /sync/failures/:failureTaskId/retry` and `POST /sync/failures/:failureTaskId/escalate` (new `controllers/retryEscalation.controller.ts`), tenant-scoped via `request.db` (RLS) so a foreign clinic's id resolves as a genuine 404, never a leak. (d) `ConsultationConflictResolutionService`'s `ESCALATE` branch (finding 10.5) now calls the same `resolveNextOnDutyClinicianId` to set a real `currentOwnerUserId` before transitioning to `ESCALATED`, via an optional constructor-injected `OnDutyRosterProvider` wired from `emr.routes.ts`. (e) `apps/mobile/src/features/offline-sync/lib/syncFailureActions.ts` (RN-free, matching this feature's established decision-layer pattern) adds `buildRetryRequest`/`buildEscalateRequest` -- retry always targets the generic `/sync/failures/:id/retry` route; escalate routes a clinical (EMR, SAFETY_CRITICAL) conflict through the EMR `.../resolve` endpoint with `action: 'ESCALATE'`, and everything else through the generic `/sync/failures/:id/escalate` route -- and a new `hooks/useSyncFailureActions.ts` thin `apiClient` wrapper gives `SyncFailureCenterScreen.tsx`'s `onRetry`/`onEscalate` props a real, live implementation to be composed with wherever the screen is eventually mounted (Deviation 4's "not wired into a live screen/page" gap is about screen/navigation composition, which remains out of this finding's scope exactly as it was out of this plan's -- what changed is that the callbacks now have a genuine implementation behind them instead of nothing).

TDD: failing tests were added first for the roster provider (`onDutyRoster.service.test.ts`, a realistic fake `AvailabilityRepository` proving the real method name/signature is called and correctly filtered, not a hand-waved stub of `OnDutyRosterProvider` itself), for `RetryEscalationService.escalate`'s dispatch behavior, for `ConsultationConflictResolutionService`'s `ESCALATE` branch (real reassignment, the `ROSTER_PROVIDER_REQUIRED`/`NO_ON_DUTY_CLINICIAN_AVAILABLE` throws), and for both new routes via real HTTP + real Postgres (`apps/api/tests/sync/retry-escalation-routes.test.ts`, new -- every `SyncFailureTask`/`SyncConflictRecord` produced through the real `/sync/replay` endpoint, never seeded directly), covering D-36's further-escalation chain, the "throw rather than fall back to Admin" behavior, and tenant isolation. The mobile side follows the same test-substitute pattern `SyncFailureCenterScreen.test.tsx`/`ClinicalConflictResolutionSheet.test.tsx` already established: `syncFailureActions.test.ts` exercises the pure request builders directly; the hook itself stays untested the same way `useOfflineStockActions.ts`/`useFifoDispense.ts` are (no working RN renderer in this test environment). The pre-existing `consultation-conflict-resolve.test.ts` ESCALATE test (finding 10.5) was updated to add a second real clinician and assert genuine reassignment, plus a new test for the roster-exhausted 409 case, since the old assertion ("ownership untouched") was finding 10.5's own documented deferral to this fix.

Verify: `npx vitest run apps/api/src/modules/sync/__tests__/retryEscalation.service.test.ts apps/api/src/modules/sync/__tests__/onDutyRoster.service.test.ts apps/api/src/modules/emr/__tests__/consultationConflictResolution.service.test.ts apps/api/tests/emr/consultation-conflict-resolve.test.ts apps/api/tests/sync/retry-escalation-routes.test.ts apps/mobile/src/features/offline-sync/__tests__/SyncFailureCenterScreen.test.tsx apps/mobile/src/features/offline-sync/__tests__/syncFailureActions.test.ts` -- 121/121 passing (25 + 5 + 13 + 9 + 12 + 50 + 7). Root aggregate `npx vitest run`: 81 files, 1109 tests passing, 0 failed. `pnpm --filter @breeyo/api test -- --run`: 172 files passed / 9 skipped (181), 2137 passed / 80 todo (2217), 0 failed. `pnpm --filter @breeyo/mobile test -- --run`: 53 files, 867 tests passing, 0 failed. `apps/api/tests/sync/*.test.ts` is a new directory not previously covered by root `vitest.config.ts`'s include list -- added alongside the existing `apps/api/tests/emr/*.test.ts` line, and the root count was re-verified to actually rise (not just the targeted command passing) before treating this as done.

---

Per `.planning/PHASE-10-VERIFY-FIX-PLAN.md` finding **10.10** (the last of Phase 10's 11 verify-fix findings): a `no-mistakes --verify phase 10` pass found that `BrowserSyncService.checkWriteVersion`'s three callers (`WebQueueService.updateEntryStatus`, `BillingWorkbenchService.collectPayment`/`refundInvoice`/`voidInvoice` via `assertInvoiceVersionCurrent`, `InventoryWebService.adjustStock`) each ran the version check as a separate `findUnique` read, with the real write (`QueueService.updateStatus` / `PaymentService.recordCashPayment` / `RefundService.createRefund` / `InvoiceService.voidInvoice` / `StockAdjustmentService.adjust`) executing several `await`s later -- no atomicity between the check and the write, so two genuinely concurrent callers sharing the same stale `expectedVersion` could both read the row before either had written and both proceed. Every pre-existing test for this path (`web-queue.service.test.ts`, `billing-workbench.service.test.ts`, `inventory-web.service.test.ts`) mocked the Prisma delegate, so this was never provable against real concurrent behavior.

Fixed by converting each write path's version check from a separate read into a single atomic conditional UPDATE via Prisma's `updateMany` (`WHERE id = ? AND updated_at = ?`, with `updatedAt` doubling as the optimistic-lock's version stamp, and the returned affected-row count as the compare-and-swap result): `WebQueueService.updateEntryStatus` claims `QueueEntry.updatedAt`, `BillingWorkbenchService.assertInvoiceVersionCurrent` (shared by `collectPayment`/`refundInvoice`/`voidInvoice`) claims `Invoice.updatedAt`, and `InventoryWebService.adjustStock` claims `InventoryItem.updatedAt`. When `count === 1` the caller has genuinely claimed the row and the delegated write proceeds; when `count !== 1`, a follow-up `findUnique` distinguishes a real version conflict (row exists with a different `updatedAt` -> throw `staleWriteConflictError`, 409 `STALE_WRITE_CONFLICT`) from a not-found row (skip the check and let the delegated service raise its own 404). This is a genuine compare-and-swap: Postgres locks the row for the duration of whichever concurrent `UPDATE` reaches it first, and the second statement only proceeds once that lock releases, at which point its `WHERE updated_at = expectedVersion` no longer matches the winner's already-committed (newer) `updatedAt`.

`FifoDispenseService`'s `$transaction` + `SELECT ... FOR UPDATE` row-lock pattern (this codebase's other established approach to exactly this class of race) was considered and rejected for these three paths specifically: the real write in each case is delegated to a separately-constructed service (`QueueService`, `PaymentService`/`RefundService`/`InvoiceService`, `StockAdjustmentService`) holding its own `TenantPrismaClient` reference distinct from a `db.$transaction(tx => ...)` callback's `tx`. Holding a `SELECT ... FOR UPDATE` lock open on one Postgres connection while `await`ing a write on a *different* connection to the same row self-deadlocks (the second connection's write blocks on the first connection's still-open lock, and the first connection is waiting on the second connection's promise to resolve) -- closing that gap the transactional way would have required threading a shared `tx` client through all five delegated services' constructors, a much larger blast radius than this finding's severity warranted. The `updateMany` claim needs no such refactor: it is a single, self-contained atomic statement that happens strictly before the delegated write, on the service's own existing `db` reference.

TDD: two new real-Postgres, real-HTTP integration test files were added first -- `apps/api/tests/queue/queue-web-optimistic-concurrency.test.ts` and `apps/api/tests/billing/billing-web-optimistic-concurrency.test.ts` -- using `supertest` against `buildTestApp()`'s genuinely listening server (not `app.inject`, which several existing suites use but which does not exercise real per-request socket/connection concurrency) and `Promise.allSettled` to fire two truly concurrent requests sharing one stale `expectedVersion` at the same row, matching the established `finalize-stock.test.ts` "two concurrent finalizes" / `numbering-concurrency.test.ts` convention for proving a race inside one test under this package's `fileParallelism: false`. Confirmed **RED** first: with the pre-fix code restored via `git stash`, the queue race returned `[200, 200]` (both concurrent status updates applied) and the billing race returned zero 409s (both concurrent cash-collection calls applied, double-collecting cash never taken) -- exactly the nondeterministic double-apply the finding describes. Restoring the fix (`git stash pop`) turned both **GREEN**: exactly one 200 and one real 409 whose body carries `.conflict` (`domain`/`entityType`/`entityId`/`currentVersion`/`expectedVersion`/`severity`) on the wire, and the underlying row/payment state confirms only one write actually landed. The three existing mock-based unit test files were updated (not replaced) to mock `updateMany`'s conditional-match semantics instead of a bare `findUnique`, keeping their fast unit-level coverage of the omitted/current/stale branches intact.

Verify: `npx vitest run apps/api/src/modules/queue/__tests__/web-queue.service.test.ts apps/api/src/modules/billing/__tests__/billing-workbench.service.test.ts apps/api/src/modules/inventory/__tests__/inventory-web.service.test.ts apps/api/tests/queue/queue-web-optimistic-concurrency.test.ts apps/api/tests/billing/billing-web-optimistic-concurrency.test.ts` -- 41/41 passing (9 + 15 + 14 + 2 + 1). Root aggregate `npx vitest run`: **85 files passed, 1139 tests passed, 0 failed** (grew from the pre-fix baseline of 1136 by exactly this fix's 3 new integration-test cases; both new files were added to root `vitest.config.ts`'s include list as explicit filenames rather than `apps/api/tests/queue/*.test.ts` / `apps/api/tests/billing/*.test.ts` directory globs, since those two directories already hold many pre-existing suites not otherwise part of the root run -- a directory glob would have silently pulled all of them in). `pnpm --filter @breeyo/api test -- --run`: **175 test files passed, 9 skipped (184 total), 2156 tests passed, 80 todo (2236 total), 0 failed** (grew from the ~2153 pre-fix baseline -- the accumulated total after findings 10.1-10.9/10.11 -- by exactly this fix's 3 new real-concurrency integration-test cases; the three existing unit test files were edited in place, same 9/15/14 test counts as before, just simulating `updateMany`'s conditional-match semantics instead of a bare `findUnique`). `npx tsc --noEmit` in `apps/api`: only the same pre-existing `replayIngest.service.test.ts` module-resolution errors every prior Phase 10 fix has documented -- zero new errors from any file this fix touched or created. This closes the last of Phase 10's 11 verify-fix findings; see `.planning/PHASE-10-VERIFY-FIX-PLAN.md`'s "Execution status" table for the full roster.

---

**Second review round follow-up (F1-F7, resolved) -- superseding the `updateMany`-precheck shape above.** A second `no-mistakes` code-review pass found the verify-fix 10.10 claim (above) always committed a fresh `updatedAt` on its `updateMany` claim even when the delegated write then failed its own validation (an invalid queue status transition, a non-collectable/non-refundable invoice state, a stock-adjustment guard) -- bumping the version with no real change applied, so a second, genuinely-current concurrent caller could see a false 409 against a version nobody legitimately changed. The pre-check-then-delegate shape described above (`updateMany` claim, then a separate call into `QueueService`/`PaymentService`/`RefundService`/`InvoiceService`/`StockAdjustmentService`) no longer exists.

Fixed by folding the version check into the SAME statement/transaction that applies the real change, instead of a separate pre-check: `QueueRepository.updateEntry` and `StockAdjustmentService.adjust` now accept `expectedVersion` directly and run one conditional `updateMany` that both claims the version and applies the real field change in one statement; `PaymentService.recordCashPayment`/`RefundService.reserveLegs`/`InvoiceRepository.voidInvoice` accept `expectedVersion` and check it (`assertLockedInvoiceVersionCurrent`, or an inline check in `voidInvoice`'s own `FOR UPDATE` transaction) against the row already locked by the existing transaction, so the check and the write commit or roll back together. `WebQueueService`, `BillingWorkbenchService`, and `InventoryWebService` no longer run any version check themselves -- `BillingWorkbenchService.assertInvoiceVersionCurrent` (the private pre-check method described above) was deleted -- they just forward `expectedVersion` to the delegated service. The version now only ever advances when a real change actually lands.

Verify: `npx vitest run apps/api/src/modules/queue/__tests__/queue.service.test.ts apps/api/src/modules/billing/__tests__/invoice-state.test.ts apps/api/src/modules/billing/__tests__/invoice.repository.test.ts apps/api/src/modules/billing/__tests__/payment.service.test.ts apps/api/src/modules/billing/__tests__/refund.service.test.ts apps/api/src/modules/inventory/__tests__/stock-adjustment.test.ts`.
