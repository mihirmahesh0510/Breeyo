---
phase: 05-inventory-management
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx
  - apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx
  - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx
  - apps/mobile/src/features/inventory/components/SummaryHeader.tsx
  - apps/mobile/src/features/inventory/components/AttentionCard.tsx
  - apps/mobile/src/features/inventory/components/InventoryItemCard.tsx
  - apps/mobile/src/features/inventory/components/CategoryFilterChips.tsx
  - apps/mobile/src/features/inventory/components/SortSelector.tsx
  - apps/mobile/src/features/inventory/components/ItemProfileHeader.tsx
  - apps/mobile/src/features/inventory/components/BatchList.tsx
  - apps/mobile/src/features/inventory/components/StockMovementTimeline.tsx
  - apps/mobile/src/features/inventory/components/ItemDetailsTab.tsx
  - apps/mobile/src/features/inventory/components/ItemPhotoPicker.tsx
  - apps/mobile/src/features/inventory/hooks/useInventorySearch.ts
  - apps/mobile/src/features/inventory/hooks/useInventoryApi.ts
  - apps/mobile/src/features/inventory/hooks/useItemPhotoUpload.ts
autonomous: true
requirements:
  - INV-01
  - INV-06

must_haves:
  truths:
    - "Inventory list screen shows summary header (Total Items, Low Stock, Expiring Soon, Total Value), Attention card with tabbed alerts, search bar, category filter chips, sort selector, and scrollable item card list with FAB"
    - "Item profile screen shows header with stock level (color-coded), tabbed content (Batches, History, Details), and action buttons (Receive Stock, Dispense, Adjust)"
    - "Item creation form has all required fields (name, category, unit, selling price) and optional fields (par level, Schedule H, notes, photo, barcodes) per D-01"
    - "Category and unit pickers show predefined options merged with the clinic's previously-typed custom options (D-61); typing a new custom value persists it via the item save call for reuse on the next item"
    - "Item photo field actually uploads: camera/gallery pick, compress, request a presigned URL, PUT to S3, then save the resulting photoUrl on the item -- not just a local URI (D-64)"
    - "Search works with debounce (300ms) across name, barcode, and category with minimum 2 characters (D-31)"
    - "Stock movement timeline shows chronological entries with movement type icons, quantity changes, running totals, and CSV export button (D-46, D-47)"
  artifacts:
    - path: "apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx"
      provides: "Main inventory tab screen with summary, alerts, search, filter, sort, item list"
      min_lines: 80
    - path: "apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx"
      provides: "Item profile with tabbed content and action buttons"
      min_lines: 60
    - path: "apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx"
      provides: "Item creation and edit form"
      min_lines: 80
    - path: "apps/mobile/src/features/inventory/components/AttentionCard.tsx"
      provides: "Combined alert card with Low Stock, Expiring Soon, Expired tabs (D-26)"
      min_lines: 40
  key_links:
    - from: "apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx"
      to: "/api/v1/inventory/items"
      via: "React Query useQuery in useInventoryApi hook"
      pattern: "useQuery.*inventory"
    - from: "apps/mobile/src/features/inventory/hooks/useInventorySearch.ts"
      to: "/api/v1/inventory/items?search="
      via: "debounced search query"
      pattern: "useDeferredValue\\|setTimeout.*300"
---

<objective>
Build the mobile inventory list screen, item profile screen, and item creation/edit form. This plan creates the primary inventory browsing experience: the tab landing with summary dashboard, attention alerts, searchable/filterable item list, detailed item profile with batch/history/details tabs, and the item creation form.

Purpose: These screens are the inventory hub that users navigate to for all stock management tasks. The list screen is the entry point from the bottom tab bar. The item profile provides the detailed view with action buttons that launch stock receipt, dispense, and adjustment flows (built in Plan 06).

Output: Three complete screens with all supporting components, hooks, and API integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-inventory-management/05-CONTEXT.md
@.planning/phases/05-inventory-management/05-UI-SPEC.md
@.planning/phases/05-inventory-management/05-02-SUMMARY.md
@.planning/phases/05-inventory-management/05-03-SUMMARY.md

<interfaces>
<!-- From Plans 02+03 API endpoints -->
API Routes:
- GET /api/v1/inventory/items?search=&category=&sort=&page=&limit= -> { items, total, page, limit }
- GET /api/v1/inventory/items/summary -> InventorySummary
- GET /api/v1/inventory/categories -> predefined + clinic-custom categories (D-61)
- GET /api/v1/inventory/units -> predefined + clinic-custom units (D-61)
- GET /api/v1/inventory/items/:itemId -> InventoryItem with barcodes and batches
- POST /api/v1/inventory/items -> create item
- PATCH /api/v1/inventory/items/:itemId -> update item
- POST /api/v1/inventory/items/:itemId/barcodes -> add barcode; 409 with { existingItem } on conflict (D-63)
- POST /api/v1/inventory/items/:itemId/photo-upload-url -> { uploadUrl, photoUrl, expiresIn } (D-64)
- GET /api/v1/inventory/alerts -> { lowStock, expiringSoon, expired, counts }
- GET /api/v1/inventory/items/:itemId/movements?page=&limit= -> { movements, total }

<!-- From Plan 01 shared types -->
From packages/shared/src/types/inventory.types.ts:
- InventoryItem, StockBatch, StockMovement, InventoryBarcode, InventorySummary
- LowStockItem, ExpiringBatchItem, StockLevelStatus, getStockLevelStatus

From packages/shared/src/constants/inventory-categories.ts:
- INVENTORY_CATEGORIES, getCategoryIcon, getCategoryLabel

From packages/shared/src/constants/stock-movement-types.ts:
- STOCK_MOVEMENT_TYPES (with icon and color mappings)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build inventory list screen with summary, alerts, search, filter, sort components</name>
  <files>
    apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx,
    apps/mobile/src/features/inventory/components/SummaryHeader.tsx,
    apps/mobile/src/features/inventory/components/AttentionCard.tsx,
    apps/mobile/src/features/inventory/components/InventoryItemCard.tsx,
    apps/mobile/src/features/inventory/components/CategoryFilterChips.tsx,
    apps/mobile/src/features/inventory/components/SortSelector.tsx,
    apps/mobile/src/features/inventory/hooks/useInventorySearch.ts,
    apps/mobile/src/features/inventory/hooks/useInventoryApi.ts
  </files>
  <read_first>
    apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx,
    apps/mobile/src/navigation/BottomTabNavigator.tsx,
    apps/mobile/src/features/patient/screens/PatientListScreen.tsx,
    apps/mobile/src/features/patient/hooks/usePatientSearch.ts,
    apps/mobile/src/components/SearchBar.tsx,
    apps/mobile/src/components/EmptyState.tsx,
    apps/mobile/src/components/FAB.tsx,
    apps/mobile/src/components/Card.tsx,
    apps/mobile/src/components/Chip.tsx,
    apps/mobile/src/theme/tokens.ts,
    packages/shared/src/types/inventory.types.ts,
    packages/shared/src/constants/inventory-categories.ts,
    packages/shared/src/constants/stock-movement-types.ts
  </read_first>
  <action>
    **1. Create `apps/mobile/src/features/inventory/hooks/useInventoryApi.ts`:**

    React Query hooks for inventory API calls:
    - `useInventoryItems(clinicId, { search, category, sort, page })`: useQuery fetching GET /api/v1/inventory/items with query params. queryKey: ['inventory', 'items', clinicId, search, category, sort, page].
    - `useInventorySummary(clinicId)`: useQuery fetching GET /api/v1/inventory/items/summary. queryKey: ['inventory', 'summary', clinicId].
    - `useInventoryAlerts(clinicId)`: useQuery fetching GET /api/v1/inventory/alerts. queryKey: ['inventory', 'alerts', clinicId].
    - `useInventoryItem(clinicId, itemId)`: useQuery fetching GET /api/v1/inventory/items/:itemId. queryKey: ['inventory', 'item', clinicId, itemId].
    - `useItemMovements(clinicId, itemId, page)`: useQuery fetching GET /api/v1/inventory/items/:itemId/movements. queryKey: ['inventory', 'movements', clinicId, itemId, page].
    - `useCreateItem(clinicId)`: useMutation for POST /api/v1/inventory/items. Invalidates items, summary, categories, and units queries on success (creating an item can register a new custom category/unit, D-61).
    - `useUpdateItem(clinicId, itemId)`: useMutation for PATCH /api/v1/inventory/items/:itemId. Invalidates item, items, categories, and units queries on success.
    - `useInventoryCategories(clinicId)`: useQuery fetching GET /api/v1/inventory/categories. queryKey: ['inventory', 'categories', clinicId]. Returns predefined + clinic-custom categories merged (D-61).
    - `useInventoryUnits(clinicId)`: useQuery fetching GET /api/v1/inventory/units. queryKey: ['inventory', 'units', clinicId]. Returns predefined + clinic-custom units merged (D-61).

    **2. Create `apps/mobile/src/features/inventory/hooks/useInventorySearch.ts`:**

    Follow the same debounce pattern as Phase 3 patient search:
    - Accept search text input
    - Debounce at 300ms (per UI-SPEC Search Behavior)
    - Minimum 2 characters before triggering search
    - Return { debouncedSearch, isSearching }
    - Use useDeferredValue or useRef + setTimeout (match existing pattern)

    **3. Create `apps/mobile/src/features/inventory/components/SummaryHeader.tsx`:**

    Row of 4 summary cards per UI-SPEC (D-32):
    - Card 1: "Total Items" with count. No color. 64px height, 8px internal padding.
    - Card 2: "Low Stock" with count. tertiary color (#E65100) text if count > 0. Tappable -> scrolls to Attention card Low Stock tab.
    - Card 3: "Expiring Soon" with count. tertiary color if count > 0. Tappable -> scrolls to Attention card Expiring Soon tab.
    - Card 4: "Total Value" with "Rs [N]". No color.
    - Use Phase 2 Card component as base. Loading state: 4 skeleton cards.
    - Props: `summary: InventorySummary | undefined`, `isLoading: boolean`, `onLowStockPress: () => void`, `onExpiringPress: () => void`

    **4. Create `apps/mobile/src/features/inventory/components/AttentionCard.tsx`:**

    Combined alert card with 3 tabs per D-26:
    - Card title: "Attention Needed"
    - Tabs: "Low Stock ([N])", "Expiring Soon ([N])", "Expired ([N])"
    - Tab content: FlatList of alert item rows.
    - Low stock row: "[Item Name] -- [currentStock] left (par: [parLevel])" per UI-SPEC
    - Expiring soon row: "[Item Name] -- Batch [lotNumber] expires [DD MMM YYYY]"
    - Expired row: "[Item Name] -- Batch [lotNumber] expired [DD MMM YYYY]"
    - Each row tappable -> navigates to item detail
    - Empty tab messages per UI-SPEC
    - Hidden when all counts are 0 (allClear state)
    - Tab count badges: tertiary for Low Stock and Expiring, error for Expired
    - 150ms cross-fade animation on tab switch
    - Props: `alerts: AlertData`, `onItemPress: (itemId: string) => void`

    **5. Create `apps/mobile/src/features/inventory/components/InventoryItemCard.tsx`:**

    Card per item in list per D-30 and UI-SPEC:
    - 72px height, 12px internal padding (balanced density)
    - Left: category icon from getCategoryIcon(category)
    - Center: item name (bodyLarge), category label (caption)
    - Right column: stock level "In stock: [N] [unit]" (caption, color-coded via getStockLevelStatus), selling price "Rs [N]" (caption)
    - Badges: "Exp: [DD MMM YY]" (nearest expiry, tertiaryContainer bg if expiring, errorContainer bg if expired). "Schedule H" badge (tertiaryContainer bg) if scheduleH=true. "Out of stock" (error color) if currentStock=0.
    - Tappable -> navigates to item detail screen
    - Props: `item: InventoryItem`, `nearestExpiry?: Date`, `onPress: () => void`

    **6. Create `apps/mobile/src/features/inventory/components/CategoryFilterChips.tsx`:**

    Horizontal scroll of Chip components per D-31:
    - "All" chip selected by default
    - 7 predefined category chips from INVENTORY_CATEGORIES
    - Plus clinic-custom categories from `useInventoryCategories(clinicId)` (D-61) -- not derived from the currently-loaded item page, since a custom category used only by items outside the current page/filter would otherwise never show up as a filter option
    - Selected chip: primaryContainer (#C8E6C9) background, 100ms selection animation
    - 8px spacing between chips (spacing.sm)
    - Props: `categories: Array<{ value: string; label: string }>`, `selectedCategory: string | null`, `onSelect: (category: string | null) => void`

    **7. Create `apps/mobile/src/features/inventory/components/SortSelector.tsx`:**

    Bottom sheet with sort options per UI-SPEC Sort Selector Behavior:
    - Trigger: tap sort label/icon near top-right of list
    - Options: "Name (A-Z)", "Stock Level (Low First)", "Recently Added", "Expiring Soon", "Category"
    - Default: "Name (A-Z)"
    - Current sort shown as caption: "SORT: [OPTION]" (overline text)
    - 200ms bottom sheet animation
    - Props: `selectedSort: string`, `onSortChange: (sort: string) => void`

    **8. Create `apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx`:**

    Main screen composition per UI-SPEC:
    - Screen wrapper with SafeAreaView
    - SummaryHeader at top
    - AttentionCard below summary (hidden if no alerts)
    - SearchBar with barcode scanner icon button (scanner navigates to BarcodeScannerScreen, Plan 05)
    - CategoryFilterChips below search
    - SortSelector top-right of list area
    - FlatList of InventoryItemCard items
    - FAB "Add Item" bottom-right -> navigates to ItemFormScreen
    - Pull-to-refresh invalidates React Query cache
    - All 7 screen states per UI-SPEC: empty, loading, populated, searching, searchNoResults, error, offline
    - Empty state: dog + empty shelf illustration, "No inventory items yet", "Tap Add Item to add your first product."
    - Error state: "Could not load inventory. Pull down to try again."
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/mobile/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx contains `SummaryHeader`
    - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx contains `AttentionCard`
    - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx contains `InventoryItemCard`
    - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx contains `CategoryFilterChips`
    - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx contains `SortSelector`
    - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx contains `FAB`
    - apps/mobile/src/features/inventory/screens/InventoryListScreen.tsx contains `useInventoryItems`
    - apps/mobile/src/features/inventory/components/SummaryHeader.tsx contains `Total Items`
    - apps/mobile/src/features/inventory/components/SummaryHeader.tsx contains `Low Stock`
    - apps/mobile/src/features/inventory/components/AttentionCard.tsx contains `Attention Needed`
    - apps/mobile/src/features/inventory/components/AttentionCard.tsx contains `Expiring Soon`
    - apps/mobile/src/features/inventory/components/InventoryItemCard.tsx contains `getStockLevelStatus`
    - apps/mobile/src/features/inventory/components/InventoryItemCard.tsx contains `Schedule H`
    - apps/mobile/src/features/inventory/hooks/useInventorySearch.ts contains `300`
    - apps/mobile/src/features/inventory/hooks/useInventoryApi.ts contains `useInventoryItems`
    - apps/mobile/src/features/inventory/hooks/useInventoryApi.ts contains `useInventorySummary`
    - apps/mobile/src/features/inventory/hooks/useInventoryApi.ts contains `useInventoryAlerts`
  </acceptance_criteria>
  <done>Inventory list screen fully composed with summary header, attention alerts card, debounced search, category filter, sort selector, item cards, FAB, and all 7 screen states. React Query hooks handle API integration with cache invalidation.</done>
</task>

<task type="auto">
  <name>Task 2: Build item profile screen and item creation/edit form</name>
  <files>
    apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx,
    apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx,
    apps/mobile/src/features/inventory/components/ItemProfileHeader.tsx,
    apps/mobile/src/features/inventory/components/BatchList.tsx,
    apps/mobile/src/features/inventory/components/StockMovementTimeline.tsx,
    apps/mobile/src/features/inventory/components/ItemDetailsTab.tsx
  </files>
  <read_first>
    apps/mobile/src/features/inventory/hooks/useInventoryApi.ts,
    apps/mobile/src/features/inventory/components/InventoryItemCard.tsx,
    apps/mobile/src/features/patient/screens/PetProfileScreen.tsx,
    apps/mobile/src/features/patient/components/PetPhotoPicker.tsx,
    apps/mobile/src/features/attachment/hooks/useFileUpload.ts,
    apps/mobile/src/components/FormField.tsx,
    apps/mobile/src/components/BottomSheet.tsx,
    apps/mobile/src/components/Toast.tsx,
    packages/shared/src/types/inventory.types.ts,
    packages/shared/src/validators/inventory.validators.ts,
    packages/shared/src/constants/inventory-categories.ts,
    packages/shared/src/constants/inventory-units.ts,
    packages/shared/src/constants/stock-movement-types.ts
  </read_first>
  <action>
    **1. Create `apps/mobile/src/features/inventory/components/ItemProfileHeader.tsx`:**

    Top section of item detail per UI-SPEC:
    - Item name (headlineMedium, 28px)
    - Category badge with icon from getCategoryIcon()
    - "Schedule H" badge if scheduleH=true (tertiaryContainer bg)
    - Total stock: "In stock: [N] [unit]" (headlineMedium size, color-coded via getStockLevelStatus)
    - "Selling: Rs [N]" (bodyLarge)
    - "Purchase: Rs [N]" caption (latest batch purchase price)
    - Photo thumbnail if photoUrl exists, else category icon placeholder
    - Props: `item: InventoryItem`, `latestPurchasePrice?: number`

    **2. Create `apps/mobile/src/features/inventory/components/BatchList.tsx`:**

    Batches tab content per UI-SPEC:
    - FlatList of batch rows (64px per row, 12px internal padding)
    - Each row: lot number ("Lot: [lotNumber]" or "No lot number"), quantity "[currentQty] / [initialQty] [unit]", expiry "Exp: [DD MMM YYYY]" or "No expiry", purchase price "Rs [purchasePrice] / [unit]", supplier "From: [supplier]", received date "Received: [DD MMM YYYY]"
    - Expired badge: "EXPIRED" (errorContainer bg) if isExpired or expiryDate <= today
    - Empty state: "No active batches. Receive stock to add a batch."
    - Expired batches show "Dispose" action button -> triggers dispose confirmation per UI-SPEC destructive actions
    - Props: `batches: StockBatch[]`, `unit: string`, `onDispose: (batchId: string) => void`

    **3. Create `apps/mobile/src/features/inventory/components/StockMovementTimeline.tsx`:**

    History tab content per D-46 and UI-SPEC:
    - FlatList of movement entries (48px per row, 8px internal padding -- compact density)
    - Each row: movement type icon from STOCK_MOVEMENT_TYPES (with correct color per UI-SPEC Movement Type Icon Map), quantity change "[+/-N] [unit]", reason (for adjusted type), user name, timestamp "DD MMM YYYY, HH:mm AM", running total "Total: [N]" (caption at end of row)
    - Movement type labels: "Received [+N]", "Dispensed [-N]", "Adjusted [+/-N] -- [reason]", "Disposed [-N] -- expired", "Stock-take [+/-N]", "Returned [+N]"
    - "Counter Sale" badge (secondaryContainer bg) when type='dispensed' AND consultationId is null (per UI-SPEC Counter Sale Display)
    - Newest first ordering
    - "Export CSV" outlined button at top-right -> triggers CSV export via papaparse + expo-file-system + expo-sharing (per RESEARCH.md code example lines 728-762)
    - Empty state: "No stock movements yet."
    - Pagination with infinite scroll
    - Props: `movements: StockMovement[]`, `unit: string`, `isLoading: boolean`, `onLoadMore: () => void`, `onExportCSV: () => void`, `hasMore: boolean`

    **4. Create `apps/mobile/src/features/inventory/components/ItemDetailsTab.tsx`:**

    Details tab content per UI-SPEC:
    - Barcodes section: "Barcodes ([N])" with list of "[code] ([format])" entries. Remove button in edit mode per UI-SPEC destructive action.
    - Unit: "Unit: [unit]"
    - Distributor: "Distributor: [supplier]" or "No distributor recorded" (from latest batch)
    - Par level: "Par Level: [N] [unit]" or "No par level set"
    - Notes: "[notes text]" or "No notes"
    - Photo: item photo or category icon placeholder
    - 16px section padding (balanced density)
    - Props: `item: InventoryItem`, `isEditing: boolean`, `onRemoveBarcode: (barcodeId: string) => void`

    **5. Create `apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx`:**

    Composition per UI-SPEC:
    - ItemProfileHeader at top
    - TabView with 3 tabs: "Batches ([N])", "History", "Details" per D-33
    - 200ms cross-fade animation on tab switch
    - Action buttons at bottom: "Receive Stock" (outlined), "Dispense" (filled primary), "Adjust" (text)
    - "Edit" text button in header -> navigates to ItemFormScreen in edit mode
    - Pull-to-refresh
    - 4 screen states: loading (skeleton), populated, editing, error
    - Route params: itemId
    - Uses useInventoryItem and useItemMovements hooks

    **6. Create `apps/mobile/src/features/inventory/hooks/useItemPhotoUpload.ts`** (D-64):

    Mirrors the presigned-URL upload pattern from Phase 4's `useFileUpload.ts` (`apps/mobile/src/features/attachment/hooks/useFileUpload.ts`), scoped to a single item photo instead of a consultation attachment list:
    - `pickAndUpload(itemId): Promise<string>` -- launches image picker (camera or gallery via action sheet, same UX as `PetPhotoPicker.tsx`), compresses via expo-image-manipulator if the file is over ~1MB, requests a presigned URL from `POST /api/v1/inventory/items/:itemId/photo-upload-url`, PUTs the compressed image to `uploadUrl` via `FileSystem.uploadAsync`, and returns the resulting `photoUrl` on success.
    - Exposes `{ pickAndUpload, isUploading, progress, error }`.
    - Unlike Phase 3's `PetPhotoPicker` (which intentionally deferred the S3 upload and stored a local URI), this hook uploads immediately on selection so `photoUrl` is a real, persisted URL by the time the form saves (D-64 closes that gap).

    **7. Create `apps/mobile/src/features/inventory/components/ItemPhotoPicker.tsx`** (D-64):

    Thin UI wrapper around `useItemPhotoUpload`, visually consistent with `PetPhotoPicker.tsx`:
    - Empty state: "Add Photo" outlined button with camera icon
    - Uploading state: same pressable area shows a spinner overlay
    - Has-photo state: 120px thumbnail with a remove (trash) icon overlay; tapping remove clears `photoUrl` on the form (inline confirmation per UI-SPEC destructive actions: "Remove photo?")
    - Error state: inline caption "Could not upload photo. Try again." with retry
    - Props: `photoUrl: string | null`, `itemId: string | undefined` (undefined until the item is first saved in create mode -- see note below), `onPhotoUploaded: (url: string) => void`, `onRemove: () => void`

    Note on create-mode ordering: `photo-upload-url` is item-scoped (needs an itemId), but photos are optional and a new item doesn't have an id until first saved. In create mode, disable `ItemPhotoPicker` until the item exists -- either by auto-saving a draft item on first required-field blur, or (simpler, chosen here) by moving the photo field to appear only after the required fields are filled and the form has silently created the item via `useCreateItem`, then treating the rest of the form as edit mode. This keeps `photo-upload-url` item-scoped everywhere rather than adding a separate itemless upload path.

    **8. Create `apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx`:**

    Item creation and edit form per UI-SPEC:
    - Screen title: "Add Inventory Item" (create) or "Edit Item" (edit)
    - Required fields with red asterisk: Item Name (TextInput), Category (bottom sheet picker sourced from `useInventoryCategories(clinicId)` -- predefined + clinic custom, D-61 -- plus "Add Custom Category" free-text entry that becomes a new clinic-wide option once the item saves), Unit (same pattern via `useInventoryUnits(clinicId)` + "Add Custom Unit", D-61), Selling Price (numeric input with "Rs" prefix)
    - Optional fields: Par Level (numeric, helper "Leave blank for no low-stock alerts"), Schedule H toggle with helper "Mark items requiring special handling", Notes (multiline text, placeholder "Storage instructions, handling warnings..."), Photo (`ItemPhotoPicker` component per D-64 -- see create-mode ordering note above)
    - Barcodes section: "Scan or Enter Barcode" button -> opens inline barcode scanner or manual numeric input. Multiple barcodes supported (D-16). Each scanned barcode shows "[code] ([format])" with remove icon. If `useCreateItem`/`useUpdateItem`'s add-barcode call returns a 409 conflict (D-63), show inline: "This barcode is linked to '[itemName]'" with a "View Item" text link (navigates to that item's detail screen) instead of a generic error toast.
    - "Save Item" / "Save Changes" filled primary button
    - "Cancel" text button
    - Validation with createItemSchema/updateItemSchema (zod). Inline field errors per UI-SPEC.
    - Success toast: "[Item Name] added to inventory" or "Item updated"
    - Uses useCreateItem or useUpdateItem mutation hook
    - When navigated from unknown barcode scan (D-14): barcode pre-filled
    - Route params: itemId? (optional, for edit mode), prefilledBarcode? (optional)
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/mobile/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx contains `Batches`
    - apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx contains `History`
    - apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx contains `Details`
    - apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx contains `Receive Stock`
    - apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx contains `Dispense`
    - apps/mobile/src/features/inventory/screens/InventoryItemDetailScreen.tsx contains `Adjust`
    - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx contains `createItemSchema`
    - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx contains `useInventoryCategories`
    - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx contains `useInventoryUnits`
    - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx contains `Add Inventory Item`
    - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx contains `Save Item`
    - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx contains `ItemPhotoPicker`
    - apps/mobile/src/features/inventory/screens/ItemFormScreen.tsx contains `409`
    - apps/mobile/src/features/inventory/components/ItemPhotoPicker.tsx contains `useItemPhotoUpload`
    - apps/mobile/src/features/inventory/components/ItemPhotoPicker.tsx contains `Add Photo`
    - apps/mobile/src/features/inventory/hooks/useItemPhotoUpload.ts contains `photo-upload-url`
    - apps/mobile/src/features/inventory/hooks/useItemPhotoUpload.ts contains `uploadAsync`
    - apps/mobile/src/features/inventory/hooks/useInventoryApi.ts contains `useInventoryCategories`
    - apps/mobile/src/features/inventory/hooks/useInventoryApi.ts contains `useInventoryUnits`
    - apps/mobile/src/features/inventory/components/BatchList.tsx contains `EXPIRED`
    - apps/mobile/src/features/inventory/components/BatchList.tsx contains `Dispose`
    - apps/mobile/src/features/inventory/components/StockMovementTimeline.tsx contains `Export CSV`
    - apps/mobile/src/features/inventory/components/StockMovementTimeline.tsx contains `Counter Sale`
    - apps/mobile/src/features/inventory/components/StockMovementTimeline.tsx contains `runningTotal`
    - apps/mobile/src/features/inventory/components/ItemDetailsTab.tsx contains `Barcodes`
    - apps/mobile/src/features/inventory/components/ItemDetailsTab.tsx contains `Par Level`
  </acceptance_criteria>
  <done>Item profile screen with tabbed content (batches, history, details) and action buttons. Item creation/edit form with all fields per D-01, category-aware validation, barcode scanning support, and custom category/unit input that persists clinic-wide (D-61). Item photo actually uploads via presigned URL instead of storing a local-only URI (D-64). Add-barcode conflicts surface the existing item instead of a generic error (D-63). Stock movement timeline with CSV export, counter sale badge, and movement type icons.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/mobile/tsconfig.json` -- mobile app compiles
</verification>

<success_criteria>
1. Inventory list screen has all elements per D-32: summary header, attention card, search, filter, sort, item cards, FAB
2. Attention card shows tabbed alerts (Low Stock, Expiring Soon, Expired) per D-26
3. Search debounces at 300ms with 2-char minimum per D-31
4. Item profile has 3 tabs (Batches, History, Details) per D-33
5. Item form validates all required fields and supports custom categories/units per D-05, D-29, and those custom values persist clinic-wide for reuse (D-61)
6. Stock movement timeline shows correct icons, colors, and running totals per D-46
7. CSV export works via papaparse + expo-sharing per D-47
8. Item photo picker uploads to S3 via presigned URL on selection, not just a local URI (D-64)
9. Adding a barcode already linked to a different item shows that item's name with a view link instead of a generic error (D-63)
</success_criteria>

<output>
After completion, create `.planning/phases/05-inventory-management/05-04-SUMMARY.md`
</output>
