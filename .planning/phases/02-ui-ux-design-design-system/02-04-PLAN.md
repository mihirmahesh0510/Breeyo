---
phase: 02-ui-ux-design-design-system
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - packages/ui/src/atoms/NotificationBadge/NotificationBadge.tsx
  - packages/ui/src/atoms/NotificationBadge/NotificationBadge.test.tsx
  - packages/ui/src/atoms/NotificationBadge/NotificationBadge.stories.tsx
  - packages/ui/src/atoms/NotificationBadge/index.ts
  - packages/ui/src/molecules/NotificationItem/NotificationItem.tsx
  - packages/ui/src/molecules/NotificationItem/NotificationItem.test.tsx
  - packages/ui/src/molecules/NotificationItem/NotificationItem.stories.tsx
  - packages/ui/src/molecules/NotificationItem/index.ts
  - packages/ui/src/organisms/NotificationList/NotificationList.tsx
  - packages/ui/src/organisms/NotificationList/NotificationList.test.tsx
  - packages/ui/src/organisms/NotificationList/NotificationList.stories.tsx
  - packages/ui/src/organisms/NotificationList/index.ts
  - packages/ui/src/wireframes/notifications/NotificationScreen.stories.tsx
  - packages/ui/src/wireframes/notifications/fixtures.ts
  - packages/ui/src/atoms/index.ts
  - packages/ui/src/molecules/index.ts
  - packages/ui/src/organisms/index.ts
  - packages/ui/src/index.ts
autonomous: true
requirements:
  - NTF-02

must_haves:
  truths:
    - "NotificationBadge renders an unread count on the navigation bar, using tertiary (#E65100) dot color per UI-SPEC accent rules (NTF-02)"
    - "NotificationBadge hides when count is zero and shows '9+' when count exceeds 9"
    - "NotificationList displays notifications filterable by module (queue, inventory, billing, whatsapp, emr, scheduling)"
    - "NotificationItem renders icon, message, timestamp, and read/unread visual state"
    - "Tapping NotificationBadge in the NavigationBar opens a notification list (NTF-02)"
    - "Wireframe for notification screen shows all 4 states: empty, loading, populated, error"
  artifacts:
    - path: "packages/ui/src/atoms/NotificationBadge/NotificationBadge.tsx"
      provides: "Badge atom showing unread notification count"
      contains: "accessibilityLabel"
    - path: "packages/ui/src/molecules/NotificationItem/NotificationItem.tsx"
      provides: "Single notification row with icon, message, timestamp, read/unread"
      contains: "onPress"
    - path: "packages/ui/src/organisms/NotificationList/NotificationList.tsx"
      provides: "Filterable notification list organism with module Chip filters"
      contains: "activeFilter"
    - path: "packages/ui/src/wireframes/notifications/NotificationScreen.stories.tsx"
      provides: "Notification screen wireframe with all 4 states"
      contains: "Empty"
  key_links:
    - from: "packages/ui/src/atoms/NotificationBadge/NotificationBadge.tsx"
      to: "packages/ui/src/theme/colors.ts"
      via: "tertiary color for badge dot"
      pattern: "tertiary"
    - from: "packages/ui/src/organisms/NotificationList/NotificationList.tsx"
      to: "packages/ui/src/molecules/NotificationItem/NotificationItem.tsx"
      via: "renders list of NotificationItem"
      pattern: "import.*NotificationItem"
    - from: "packages/ui/src/organisms/NotificationList/NotificationList.tsx"
      to: "packages/ui/src/atoms/Chip/Chip.tsx"
      via: "module filter chips"
      pattern: "import.*Chip"
---

<objective>
Create the notification UI components required by NTF-02: NotificationBadge (atom), NotificationItem (molecule), and NotificationList (organism), plus a notification screen wireframe with all 4 states and Storybook stories for every component.

Purpose: NTF-02 requires in-app notification UI -- a badge on the navigation bar showing unread count, and a tappable notification list with per-module filtering. These components slot into the existing design system and are consumed by all subsequent phases that generate notifications (queue changes, low stock alerts, overdue invoices, WhatsApp failures, appointment reminders).

Output: 3 notification components (atom + molecule + organism), wireframe stories for the notification screen, unit tests for all three, barrel export updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ui-ux-design-design-system/02-CONTEXT.md
@.planning/phases/02-ui-ux-design-design-system/02-RESEARCH.md
@.planning/phases/02-ui-ux-design-design-system/02-UI-SPEC.md

<interfaces>
<!-- From Plan 01: Theme and tokens -->
From packages/ui/src/theme/theme.ts:
```typescript
export const breeyoTheme = { ...MD3LightTheme, colors, spacing, elevation, borderRadius, animation, customTypography } as const;
export type AppTheme = typeof breeyoTheme;
```
From packages/ui/src/theme/types.ts:
```typescript
export const useAppTheme = () => useTheme<AppTheme>();
```
From packages/ui/src/theme/colors.ts (notification-relevant colors):
- tertiary: '#E65100' (notification badge dots per UI-SPEC accent rules)
- onTertiary: '#FFFFFF'
- tertiaryContainer: '#FFE0B2'
- onTertiaryContainer: '#BF360C'
- surface: '#FFFBF5'
- surfaceVariant: '#F5F0EB'
- onSurface: '#1C1B1F'
- onSurfaceVariant: '#49454F'
- primary: '#2E7D32'
- primaryContainer: '#C8E6C9'
- outline: '#79747E'

<!-- From Plan 02: Atoms and molecules consumed by this plan -->
From packages/ui/src/atoms/index.ts:
```typescript
export { Typography } from './Typography';
export { Button } from './Button';
export { StatusBadge } from './StatusBadge';
export { Avatar } from './Avatar';
export { Chip } from './Chip';
export { IconButton } from './IconButton';
export { Divider } from './Divider';
```

Key component props consumed:
- Typography: variant ('display'|'heading1'|'heading2'|'subheading'|'body'|'caption'|'overline')
- Chip: label, selected, onPress, mode ('flat'|'outlined')
- IconButton: icon, onPress, accessibilityLabel (required)
- Divider: inset (boolean)
- Avatar: type ('image'|'initials'|'icon'), size ('sm'|'md'|'lg')

From packages/ui/src/molecules/index.ts:
```typescript
export { ListItem } from './ListItem';
export { EmptyState } from './EmptyState';
export { SkeletonLoader } from './SkeletonLoader';
```

ListItem props: title, description, left (React.ReactNode), right (React.ReactNode), onPress, disabled, testID

<!-- From Plan 03: NavigationBar organism (consumes NotificationBadge via actions prop) -->
NavigationBar props: title (string), onBack (() => void | undefined), actions (React.ReactNode[]), testID
- NotificationBadge integrates as one of the actions array elements passed to NavigationBar
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NotificationBadge atom and NotificationItem molecule with tests and stories</name>
  <files>
    packages/ui/src/atoms/NotificationBadge/NotificationBadge.tsx,
    packages/ui/src/atoms/NotificationBadge/NotificationBadge.test.tsx,
    packages/ui/src/atoms/NotificationBadge/NotificationBadge.stories.tsx,
    packages/ui/src/atoms/NotificationBadge/index.ts,
    packages/ui/src/molecules/NotificationItem/NotificationItem.tsx,
    packages/ui/src/molecules/NotificationItem/NotificationItem.test.tsx,
    packages/ui/src/molecules/NotificationItem/NotificationItem.stories.tsx,
    packages/ui/src/molecules/NotificationItem/index.ts,
    packages/ui/src/atoms/index.ts,
    packages/ui/src/molecules/index.ts,
    packages/ui/src/index.ts
  </files>
  <read_first>
    packages/ui/src/theme/theme.ts,
    packages/ui/src/theme/types.ts,
    packages/ui/src/theme/colors.ts,
    packages/ui/src/theme/spacing.ts,
    packages/ui/src/atoms/StatusBadge/StatusBadge.tsx,
    packages/ui/src/atoms/IconButton/IconButton.tsx,
    packages/ui/src/atoms/Typography/Typography.tsx,
    packages/ui/src/atoms/Chip/Chip.tsx,
    packages/ui/src/molecules/ListItem/ListItem.tsx,
    packages/ui/src/atoms/index.ts,
    packages/ui/src/molecules/index.ts,
    packages/ui/src/index.ts,
    packages/ui/tests/setup.ts,
    .planning/phases/02-ui-ux-design-design-system/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: NotificationBadge renders count text when count > 0
    - Test: NotificationBadge renders nothing (returns null) when count is 0
    - Test: NotificationBadge renders "9+" when count exceeds 9
    - Test: NotificationBadge renders "99+" when count exceeds 99
    - Test: NotificationBadge dot uses tertiary color (#E65100) background
    - Test: NotificationBadge has accessibilityLabel "N unread notifications" (or "No unread notifications" when 0)
    - Test: NotificationBadge onPress callback fires when tapped
    - Test: NotificationItem renders message text and timestamp
    - Test: NotificationItem unread state has bold title and surfaceVariant left-border accent
    - Test: NotificationItem read state has normal-weight title and no left-border accent
    - Test: NotificationItem renders module icon on the left (per module type)
    - Test: NotificationItem onPress callback fires when tapped
    - Test: NotificationItem has accessibilityLabel containing the message
  </behavior>
  <action>
    **NotificationBadge (packages/ui/src/atoms/NotificationBadge/NotificationBadge.tsx):**
    - Per NTF-02 and UI-SPEC accent rules: tertiary (#E65100) for notification badge dots
    - Props interface NotificationBadgeProps:
      - count: number (unread notification count)
      - onPress: () => void (navigates to notification list when tapped)
      - testID?: string
    - Wraps IconButton with icon "bell-outline" (MaterialCommunityIcons), size 24, accessibilityLabel required
    - Badge overlay: absolute-positioned View at top-right of the icon container
      - Circular dot: minWidth 18, height 18, borderRadius 9, backgroundColor tertiary (#E65100)
      - Text inside dot: Typography-like Text with color onTertiary (#FFFFFF), fontSize 10, fontWeight '500', textAlign center
      - Display logic:
        - count === 0: render IconButton only (no badge dot), accessibilityLabel="Notifications, no unread"
        - count >= 1 and count <= 9: render count as string, accessibilityLabel="{count} unread notifications"
        - count >= 10 and count <= 99: render count as string, badge minWidth expands to 22
        - count > 99: render "99+", badge minWidth expands to 26
    - Container View: position relative, minimum touch target 44x44pt (per D-28)
    - Use useAppTheme() for all colors (theme.colors.tertiary, theme.colors.onTertiary)
    - Designed to be passed into NavigationBar's actions array:
      Usage pattern: <NavigationBar title="Walk-in Queue" actions={[<NotificationBadge count={3} onPress={openNotifications} />]} />

    **NotificationItem (packages/ui/src/molecules/NotificationItem/NotificationItem.tsx):**
    - Individual notification row for the notification list
    - Props interface NotificationItemProps:
      - id: string
      - module: 'queue' | 'inventory' | 'billing' | 'whatsapp' | 'emr' | 'scheduling' | 'system'
      - title: string (notification headline, e.g. "Low stock alert")
      - message: string (notification body, e.g. "Amoxicillin is below par level (5 remaining)")
      - timestamp: string (relative time, e.g. "5 min ago", "2h ago", "Yesterday")
      - read: boolean
      - onPress: () => void
      - testID?: string
    - Module icon map (MaterialCommunityIcons):
      - queue: 'clipboard-list-outline'
      - inventory: 'package-variant-closed'
      - billing: 'receipt'
      - whatsapp: 'whatsapp'
      - emr: 'stethoscope'
      - scheduling: 'calendar-clock'
      - system: 'cog-outline'
    - Layout: Pressable row, minHeight 72px (balanced density, notifications are scannable)
      - Left: module icon in Avatar component (type 'icon', size 'md' = 40px), icon color varies by module:
        - queue/scheduling: primary (#2E7D32)
        - inventory/billing: secondary (#5D4037)
        - whatsapp: primary (#2E7D32)
        - emr: primary (#2E7D32)
        - system: onSurfaceVariant (#49454F)
      - Center column (flex 1, marginHorizontal spacing.md):
        - Title: Typography variant 'subheading', fontWeight '500' if unread, '400' if read
        - Message: Typography variant 'caption', color onSurfaceVariant, numberOfLines 2
        - Timestamp: Typography variant 'caption', color outline (#79747E)
      - Right: unread indicator dot (8px circle, tertiary color) when read=false, nothing when read=true
    - Unread visual distinction: entire row background is surface (#FFFBF5) when read, surfaceVariant (#F5F0EB) when unread. Left border: 3px solid tertiary (#E65100) when unread, transparent when read.
    - accessibilityLabel: "{read ? '' : 'Unread, '}{title}, {message}, {timestamp}"
    - accessibilityRole: "button"
    - Divider at bottom (inset) using the Divider atom

    **Barrel export updates:**
    - Add NotificationBadge to packages/ui/src/atoms/index.ts
    - Add NotificationItem to packages/ui/src/molecules/index.ts
    - Verify packages/ui/src/index.ts re-exports updated atoms and molecules

    **Storybook stories:**

    NotificationBadge.stories.tsx:
    - "NoNotifications": count=0 (no badge visible, just bell icon)
    - "FewNotifications": count=3 (small dot with "3")
    - "ManyNotifications": count=15 (wider dot with "15")
    - "OverflowNotifications": count=150 (shows "99+")
    - "InNavigationBar": shows NotificationBadge rendered inside NavigationBar actions array (integration example)

    NotificationItem.stories.tsx:
    - "UnreadQueueNotification": module='queue', read=false, title='Patient checked in', message='Tiger (Cat) checked in by Dr. Mehta', timestamp='2 min ago'
    - "ReadInventoryNotification": module='inventory', read=true, title='Low stock alert', message='Amoxicillin is below par level (5 remaining)', timestamp='1h ago'
    - "UnreadBillingNotification": module='billing', read=false, title='Overdue invoice', message='Invoice #INV-0045 for Rs 2,500 is 3 days overdue', timestamp='3h ago'
    - "ReadWhatsAppNotification": module='whatsapp', read=true, title='Message delivery failed', message='Vaccination reminder to +91-98765-43210 could not be delivered', timestamp='Yesterday'
    - "UnreadSchedulingNotification": module='scheduling', read=false, title='Upcoming appointment', message='Tiger (Cat) has an appointment at 3:00 PM today', timestamp='30 min ago'
    - "SystemNotification": module='system', read=true, title='Backup complete', message='Daily database backup completed successfully', timestamp='6h ago'

    **Tests:**
    - NotificationBadge.test.tsx: 7 tests covering count display (0, 3, 15, 150), tertiary color assertion, accessibilityLabel format, onPress callback
    - NotificationItem.test.tsx: 6 tests covering message/timestamp rendering, read vs unread visual state, module icon rendering, onPress callback, accessibilityLabel format
  </action>
  <verify>
    <automated>cd /Users/mihirmahesh/Documents/Projects/Breeyo && pnpm --filter @breeyo/ui test -- --reporter=verbose NotificationBadge NotificationItem</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/atoms/NotificationBadge/NotificationBadge.tsx contains "tertiary" and "count" and "onPress" and "accessibilityLabel" and "bell-outline" and "99+"
    - packages/ui/src/atoms/NotificationBadge/NotificationBadge.test.tsx contains "renders count" and "renders nothing" or "returns null" and "9+" and "accessibilityLabel"
    - packages/ui/src/molecules/NotificationItem/NotificationItem.tsx contains "module" and "title" and "message" and "timestamp" and "read" and "onPress" and "accessibilityLabel" and "queue" and "inventory" and "billing" and "whatsapp"
    - packages/ui/src/molecules/NotificationItem/NotificationItem.test.tsx contains "unread" and "read" and "onPress" and "accessibilityLabel"
    - packages/ui/src/atoms/index.ts contains "NotificationBadge"
    - packages/ui/src/molecules/index.ts contains "NotificationItem"
    - NotificationBadge.test.tsx exits with code 0
    - NotificationItem.test.tsx exits with code 0
  </acceptance_criteria>
  <done>NotificationBadge atom renders unread count with tertiary dot color, hides at zero, shows "9+" overflow, and integrates with NavigationBar actions. NotificationItem molecule renders icon, message, timestamp, and read/unread state with per-module icons. Both have passing tests, Storybook stories for all variants, and are exported from barrel files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NotificationList organism with module filtering, wireframe screen, and stories</name>
  <files>
    packages/ui/src/organisms/NotificationList/NotificationList.tsx,
    packages/ui/src/organisms/NotificationList/NotificationList.test.tsx,
    packages/ui/src/organisms/NotificationList/NotificationList.stories.tsx,
    packages/ui/src/organisms/NotificationList/index.ts,
    packages/ui/src/wireframes/notifications/NotificationScreen.stories.tsx,
    packages/ui/src/wireframes/notifications/fixtures.ts,
    packages/ui/src/organisms/index.ts
  </files>
  <read_first>
    packages/ui/src/atoms/NotificationBadge/NotificationBadge.tsx,
    packages/ui/src/molecules/NotificationItem/NotificationItem.tsx,
    packages/ui/src/atoms/Chip/Chip.tsx,
    packages/ui/src/molecules/EmptyState/EmptyState.tsx,
    packages/ui/src/molecules/SkeletonLoader/SkeletonLoader.tsx,
    packages/ui/src/organisms/NavigationBar/NavigationBar.tsx,
    packages/ui/src/organisms/index.ts,
    packages/ui/src/wireframes/queue/QueueStatusBoard.stories.tsx,
    packages/ui/src/wireframes/queue/fixtures.ts,
    .planning/phases/02-ui-ux-design-design-system/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: NotificationList renders a list of NotificationItem components from the notifications prop
    - Test: NotificationList renders module filter chips (All, Queue, Inventory, Billing, WhatsApp, EMR, Scheduling)
    - Test: NotificationList with activeFilter='inventory' renders only notifications where module='inventory'
    - Test: NotificationList with activeFilter='all' renders all notifications
    - Test: NotificationList calls onFilterChange when a filter chip is tapped
    - Test: NotificationList calls onMarkAllRead when "Mark all read" button is pressed
    - Test: NotificationList shows EmptyState when filtered list is empty
    - Test: NotificationList has accessibilityLabel "Notification list" on the container
  </behavior>
  <action>
    **NotificationList (packages/ui/src/organisms/NotificationList/NotificationList.tsx):**
    - Per NTF-02: tapping notification badge opens a notification list with per-module filtering
    - Props interface NotificationListProps:
      - notifications: NotificationItemData[] (array of notification objects matching NotificationItem props minus onPress)
      - activeFilter: 'all' | 'queue' | 'inventory' | 'billing' | 'whatsapp' | 'emr' | 'scheduling' | 'system'
      - onFilterChange: (filter: string) => void
      - onNotificationPress: (id: string) => void
      - onMarkAllRead: () => void
      - isLoading: boolean
      - error: string | null
      - testID?: string
    - Export a NotificationItemData type from the organism's index.ts:
      - id: string, module: NotificationItem's module type, title: string, message: string, timestamp: string, read: boolean
    - Layout:
      - Header section: row with Typography variant 'heading2' "Notifications" on left, "Mark all read" text Button on right (only visible when any notification is unread)
      - Filter chips section: horizontal ScrollView of Chip components
        - Chips: "All", "Queue", "Inventory", "Billing", "WhatsApp", "EMR", "Scheduling"
        - Active chip: selected=true (mode 'flat')
        - Inactive chips: selected=false (mode 'outlined')
        - Chip labels map: all->"All", queue->"Queue", inventory->"Inventory", billing->"Billing", whatsapp->"WhatsApp", emr->"EMR", scheduling->"Scheduling"
        - Horizontal padding: spacing.md, gap between chips: spacing.xs
      - Content section (flex 1):
        - Loading state: SkeletonLoader type='listRow' count=5
        - Error state: View with error icon (MaterialCommunityIcons 'alert-circle-outline', color error) + Typography body error message + Button "Try Again" (variant 'filled')
        - Empty state (filtered list is empty): EmptyState with title "No notifications", description "You're all caught up!" (when filter is 'all') or "No {filterLabel} notifications" (when a specific filter is active), actionLabel not shown (no action needed for empty notifications)
        - Populated state: FlatList of NotificationItem components
          - Filter logic: if activeFilter === 'all', show all; otherwise filter by module === activeFilter
          - keyExtractor: notification.id
          - ItemSeparatorComponent: none needed (NotificationItem includes its own bottom Divider)
          - Renders each notification as NotificationItem with onPress={() => onNotificationPress(notification.id)}
      - Unread count summary: Typography variant 'caption' above the list: "{N} unread" (only when N > 0), color tertiary
    - accessibilityLabel: "Notification list"
    - accessibilityRole: "list" on the FlatList container

    **Wireframe fixture data (packages/ui/src/wireframes/notifications/fixtures.ts):**
    - Export mockNotifications: array of 8 NotificationItemData objects covering all module types:
      1. { id: 'n1', module: 'queue', title: 'Patient checked in', message: 'Tiger (Cat) checked in by front desk', timestamp: '2 min ago', read: false }
      2. { id: 'n2', module: 'inventory', title: 'Low stock alert', message: 'Amoxicillin 250mg is below par level (5 remaining)', timestamp: '15 min ago', read: false }
      3. { id: 'n3', module: 'billing', title: 'Overdue invoice', message: 'Invoice #INV-0045 for Rs 2,500 is 3 days overdue', timestamp: '1h ago', read: false }
      4. { id: 'n4', module: 'whatsapp', title: 'Message delivery failed', message: 'Vaccination reminder to Rajesh Kumar could not be delivered', timestamp: '2h ago', read: true }
      5. { id: 'n5', module: 'emr', title: 'Consultation finalized', message: 'Dr. Mehta finalized consultation for Bruno (Dog)', timestamp: '3h ago', read: true }
      6. { id: 'n6', module: 'scheduling', title: 'Upcoming appointment', message: 'Max (Labrador) has an appointment at 3:00 PM today', timestamp: '4h ago', read: true }
      7. { id: 'n7', module: 'queue', title: 'Queue status changed', message: 'Bella (Persian Cat) moved to In Consult', timestamp: '5h ago', read: true }
      8. { id: 'n8', module: 'system', title: 'Backup complete', message: 'Daily database backup completed successfully', timestamp: 'Yesterday', read: true }
    - Export mockEmptyNotifications: empty array

    **Wireframe stories (packages/ui/src/wireframes/notifications/NotificationScreen.stories.tsx):**
    - Follow same pattern as QueueStatusBoard.stories.tsx (read that file for reference)
    - Each story renders a full notification screen layout: NavigationBar (title "Notifications", onBack provided) + NotificationList
    - Meta: title='Wireframes/Notifications/NotificationScreen'
    - 4 stories per D-33:
      - "Empty": notifications=[], isLoading=false, error=null, activeFilter='all' -- shows EmptyState "No notifications / You're all caught up!"
      - "Loading": notifications=[], isLoading=true, error=null, activeFilter='all' -- shows 5 skeleton list rows
      - "Populated": notifications=mockNotifications, isLoading=false, error=null, activeFilter='all' -- shows 8 notifications, 3 unread with "3 unread" summary, filter chips with "All" selected
      - "Error": notifications=[], isLoading=false, error='Failed to load notifications. Please try again.', activeFilter='all' -- shows error icon + message + retry button

    **NotificationList Storybook stories (packages/ui/src/organisms/NotificationList/NotificationList.stories.tsx):**
    - "AllNotifications": activeFilter='all', all 8 mock notifications
    - "FilteredByQueue": activeFilter='queue', shows only queue notifications (n1, n7)
    - "FilteredByInventory": activeFilter='inventory', shows only inventory notification (n2)
    - "EmptyFiltered": activeFilter='scheduling', notifications array has no scheduling items (pass subset without n6) -- shows module-specific empty state
    - "AllRead": all notifications with read=true, "Mark all read" button hidden
    - "Loading": isLoading=true
    - "Error": error='Connection failed'

    **Barrel export update:**
    - Add NotificationList and NotificationItemData to packages/ui/src/organisms/index.ts
    - Verify packages/ui/src/index.ts re-exports updated organisms

    **Tests (packages/ui/src/organisms/NotificationList/NotificationList.test.tsx):**
    - 8 tests covering: renders list of NotificationItem, renders filter chips, filters by module, filters by 'all', calls onFilterChange, calls onMarkAllRead, shows EmptyState when filtered empty, accessibilityLabel on container
  </action>
  <verify>
    <automated>cd /Users/mihirmahesh/Documents/Projects/Breeyo && pnpm --filter @breeyo/ui test -- --reporter=verbose NotificationList</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/organisms/NotificationList/NotificationList.tsx contains "activeFilter" and "onFilterChange" and "onNotificationPress" and "onMarkAllRead" and "isLoading" and "error" and "EmptyState" and "SkeletonLoader" and "NotificationItem" and "Chip" and "FlatList"
    - packages/ui/src/organisms/NotificationList/NotificationList.test.tsx contains "filter" and "onFilterChange" and "onMarkAllRead" and "EmptyState" and "NotificationItem"
    - packages/ui/src/organisms/NotificationList/index.ts contains "NotificationList" and "NotificationItemData"
    - packages/ui/src/organisms/index.ts contains "NotificationList"
    - packages/ui/src/wireframes/notifications/NotificationScreen.stories.tsx contains "Empty" and "Loading" and "Populated" and "Error"
    - packages/ui/src/wireframes/notifications/fixtures.ts contains "mockNotifications" and "queue" and "inventory" and "billing" and "whatsapp" and "emr" and "scheduling"
    - NotificationList.test.tsx exits with code 0
  </acceptance_criteria>
  <done>NotificationList organism renders filterable notification list with per-module Chip filters, loading/error/empty states, and "Mark all read" action. Wireframe screen shows all 4 states (empty, loading, populated, error). All tests pass and stories cover all filter combinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in scope | This plan creates UI-only components with no data fetching, no API calls, and no user input processing. All data is passed as props. Trust boundary enforcement happens in the API/service layers built in Phase 1 (NTF-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | NotificationItem | accept | Notification content is passed as props from parent; data scoping is enforced by the notification service (Phase 1 NTF-01) and RLS, not by the UI component |
| T-02-02 | Tampering | NotificationBadge count | accept | Count is a read-only prop rendered as text; no user input or mutation capability in the component |
| T-02-SC | Tampering | npm installs | accept | No new npm packages are installed in this plan; all components use existing dependencies from Plans 02-01 and 02-02 (react-native-paper, @expo/vector-icons) |
</threat_model>

<verification>
1. Run `pnpm --filter @breeyo/ui test -- NotificationBadge NotificationItem NotificationList` -- all notification component tests pass
2. Verify packages/ui/src/atoms/index.ts exports NotificationBadge
3. Verify packages/ui/src/molecules/index.ts exports NotificationItem
4. Verify packages/ui/src/organisms/index.ts exports NotificationList and NotificationItemData
5. Verify packages/ui/src/index.ts re-exports all updated barrels
6. NotificationBadge.stories.tsx has at least 5 stories (NoNotifications, FewNotifications, ManyNotifications, OverflowNotifications, InNavigationBar)
7. NotificationItem.stories.tsx has at least 6 stories covering all module types
8. NotificationList.stories.tsx has at least 7 stories covering filter states
9. NotificationScreen.stories.tsx has exactly 4 stories (Empty, Loading, Populated, Error)
10. NotificationBadge uses theme.colors.tertiary (#E65100) for the badge dot
</verification>

<success_criteria>
1. NotificationBadge atom exists with unread count display, "9+" overflow, tertiary dot color, and NavigationBar integration (NTF-02)
2. NotificationItem molecule exists with module icon, title, message, timestamp, and read/unread visual state
3. NotificationList organism exists with per-module Chip filtering (queue, inventory, billing, whatsapp, emr, scheduling), loading/error/empty states, and "Mark all read" action (NTF-02)
4. Wireframe for notification screen shows all 4 states (empty, loading, populated, error) per D-33
5. All components use theme tokens via useAppTheme (no hardcoded color/spacing values)
6. All interactive components have accessibilityLabel and accessibilityRole per D-24
7. NotificationBadge meets 44pt minimum touch target per D-28
8. All unit tests pass
9. Storybook stories exist for all components showing all variants
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-ux-design-design-system/02-04-SUMMARY.md`
</output>
