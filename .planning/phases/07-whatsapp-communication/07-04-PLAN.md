---
phase: 07-whatsapp-communication
plan: 04
type: execute
wave: 1
depends_on:
  - 07-01
  - 07-02
files_modified:
  - apps/api/tests/helpers/factories.ts
  - apps/api/src/lib/ist-date.ts
  - apps/api/src/lib/__tests__/ist-date.test.ts
  - apps/api/src/lib/audit-log.ts
  - apps/api/src/modules/queue/queue.repository.ts
  - apps/api/src/jobs/midnight-archive.ts
  - .env.example
  - apps/api/tests/whatsapp/send.test.ts
  - apps/api/tests/whatsapp/status-ledger.test.ts
  - apps/api/tests/whatsapp/webhook-idempotency.test.ts
  - apps/api/tests/whatsapp/inbox.test.ts
  - apps/api/tests/whatsapp/reminder-idempotency.test.ts
  - apps/api/tests/whatsapp/opt-out.test.ts
  - apps/api/tests/whatsapp/consent.test.ts
  - apps/api/tests/whatsapp/invoice-delivery.test.ts
  - apps/api/tests/whatsapp/booking.test.ts
  - apps/api/tests/whatsapp/booking-concurrency.test.ts
  - apps/api/tests/whatsapp/booking-authz.test.ts
  - apps/api/tests/whatsapp/authz.test.ts
autonomous: true
requirements:
  - WHA-01
  - WHA-02
  - WHA-03
  - WHA-05

must_haves:
  truths:
    - "Integration tests can build a full Phase 7 fixture graph — clinic, member with a specific role, pet owner, pet, consultation with a follow-up date, vaccination record, deworming record, WhatsApp thread and clinic config — through named factory functions"
    - "cleanupTestData deletes the eight whatsapp_* tables in reverse-dependency order before pets/pet_owners/clinics, so no foreign-key error is possible between suites"
    - "getTodayIST and addDaysIST live in one shared module (apps/api/src/lib/ist-date.ts) and both QueueRepository and midnight-archive delegate to it, so the WhatsApp reminder sweep never imports QueueRepository"
    - "AuditEvent carries the Phase 7 compliance events so consent, opt-out, invalid-number and booking-action writes are typed rather than string-cast (Pitfall 6)"
    - "The root .env.example documents all seven WHATSAPP_* variables with WHATSAPP_PROVIDER defaulting to simulator, so a misconfigured deploy cannot send real messages to real pet owners"
    - "Every integration suite named in 07-VALIDATION.md exists as a file with it.todo placeholders, so later plans have a known target and the sampling map is addressable"
  artifacts:
    - path: "apps/api/src/lib/ist-date.ts"
      provides: "IST-anchored date helpers shared by queue, midnight archive and the WhatsApp reminder sweep"
      exports: ["getTodayIST", "addDaysIST", "IST_TIMEZONE"]
    - path: "apps/api/tests/helpers/factories.ts"
      provides: "Phase 7 test fixture factories plus extended cleanup"
      exports: ["createTestPetOwner", "createTestPet", "createTestConsultation", "createTestVaccinationRecord", "createTestDewormingRecord", "createTestWhatsAppThread", "createTestWhatsAppClinicConfig", "cleanupTestData"]
    - path: "apps/api/src/lib/audit-log.ts"
      provides: "AuditEvent extended with Phase 7 compliance events"
      contains: "WHATSAPP_CONSENT_GRANTED"
    - path: ".env.example"
      provides: "Documented WhatsApp environment variables"
      contains: "WHATSAPP_PROVIDER=simulator"
  key_links:
    - from: "apps/api/src/modules/queue/queue.repository.ts"
      to: "apps/api/src/lib/ist-date.ts"
      via: "getTodayIST delegates to the shared helper"
      pattern: "ist-date"
    - from: "apps/api/src/jobs/midnight-archive.ts"
      to: "apps/api/src/lib/ist-date.ts"
      via: "import of getTodayIST"
      pattern: "ist-date"
---

<objective>
Build the API-side scaffolding every later Phase 7 plan depends on: test data factories for the Phase 3-6 tables that only just became migratable, the shared IST date module, the extended audit event enum, the documented WhatsApp environment variables, and the empty-but-addressable integration suite files from 07-VALIDATION.md.

Purpose: 07-VALIDATION.md lists 30+ automated checks, most of which need a pet, a consultation with a follow-up date, or a vaccination record with a next-due date. None of those factories exist today (`factories.ts` covers only user/clinic/member/tokens). The reminder sweep needs IST date arithmetic that currently lives as a static method on `QueueRepository`, which the WhatsApp module must not import. Doing this once, here, prevents six later plans from each inventing their own fixtures.

Output: extended factories with cleanup ordering, `apps/api/src/lib/ist-date.ts` with tests, extended `AuditEvent`, seven documented `WHATSAPP_*` env vars, and twelve integration test files with `it.todo` placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-whatsapp-communication/07-CONTEXT.md
@.planning/phases/07-whatsapp-communication/07-RESEARCH.md
@.planning/phases/07-whatsapp-communication/07-PATTERNS.md
@.planning/phases/07-whatsapp-communication/07-VALIDATION.md

<interfaces>
From 07-01 (already applied): prisma.petOwner, prisma.pet, prisma.consultation, prisma.vaccinationRecord, prisma.dewormingRecord, prisma.whatsAppThread, prisma.whatsAppMessage, prisma.whatsAppMessageStatusEvent, prisma.whatsAppOwnerPreference, prisma.whatsAppReminderTask, prisma.whatsAppBookingRequest, prisma.whatsAppSlotHold, prisma.whatsAppClinicConfig

From 07-02: WA_TEMPLATE_KEYS, WA_REMINDER_LEAD_DAYS, WA_ESCALATION, WaDeliveryStatus, WaBookingState

Analogs (07-PATTERNS § C, § K, § M):
- apps/api/tests/helpers/factories.ts:35-51 — createTestClinic factory shape (overrides object, randomUUID-suffixed defaults)
- apps/api/tests/helpers/factories.ts:53-56 — createTestClinicMember(userId, clinicId, roleName) for role-specific authz tests
- apps/api/tests/helpers/factories.ts:103-137 — cleanupTestData deletes inside $transaction in reverse-dependency order
- apps/api/src/modules/queue/queue.repository.ts:14-25 — static getTodayIST to extract verbatim
- apps/api/src/jobs/midnight-archive.ts:16 — existing getTodayIST call site
- apps/api/src/lib/audit-log.ts:25-32 — the Phase 4 enum-extension comment convention to copy
- apps/api/tests/queue/queue-checkin.test.ts:1-86 — integration harness boilerplate for the scaffold files
- Stale name to avoid: queue-checkin.test.ts:77 calls prisma.owner.deleteMany() — the model is petOwner
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared IST date module and extend the audit event enum</name>
  <files>
    apps/api/src/lib/ist-date.ts,
    apps/api/src/lib/__tests__/ist-date.test.ts,
    apps/api/src/lib/audit-log.ts,
    apps/api/src/modules/queue/queue.repository.ts,
    apps/api/src/jobs/midnight-archive.ts,
    .env.example
  </files>
  <read_first>
    apps/api/src/modules/queue/queue.repository.ts,
    apps/api/src/jobs/midnight-archive.ts,
    apps/api/src/modules/queue/queue.service.ts,
    apps/api/src/lib/audit-log.ts,
    .env.example,
    apps/api/vitest.config.ts
  </read_first>
  <behavior>
    - getTodayIST(new Date('2026-08-12T20:30:00Z')) returns the Date representing midnight IST on 2026-08-13 (because 20:30 UTC is 02:00 IST the next day)
    - getTodayIST(new Date('2026-08-12T18:29:00Z')) returns midnight IST on 2026-08-12 (18:29 UTC is 23:59 IST the same day)
    - getTodayIST() with no argument returns a Date whose UTC hours are 18 and UTC minutes are 30 on the previous calendar day (i.e. midnight IST)
    - addDaysIST(getTodayIST(new Date('2026-08-12T06:00:00Z')), 3) returns midnight IST on 2026-08-15
    - addDaysIST(d, -1) returns the previous IST midnight
    - addDaysIST crossing a month boundary works: addDaysIST(midnight IST 2026-08-30, 3) returns midnight IST 2026-09-02
    - IST_TIMEZONE === 'Asia/Kolkata'
    - QueueRepository.getTodayIST still returns the same value as the shared getTodayIST for the same input (delegation, not duplication)
  </behavior>
  <action>
    WHA-01 / D-01, D-02: The reminder sweep must do IST-anchored date arithmetic without importing `QueueRepository` into the WhatsApp module.

    Write `apps/api/src/lib/__tests__/ist-date.test.ts` first (RED), then implement.

    1. Create `apps/api/src/lib/ist-date.ts` exporting `IST_TIMEZONE = 'Asia/Kolkata'`, `getTodayIST(date?: Date): Date` and `addDaysIST(date: Date, days: number): Date`. Move the body of `QueueRepository.getTodayIST` (queue.repository.ts:14-25) verbatim — the `toLocaleDateString('en-CA', { timeZone: 'Asia/Kolkata' })` then `Date.UTC(year, month - 1, day, -5, -30, 0, 0)` approach. `addDaysIST` must add whole days to an existing IST-midnight Date without re-deriving from the local clock.
    2. Change `QueueRepository.getTodayIST` to delegate to the shared function (keep the static method so the three existing call sites in `queue.service.ts:32,143,166` keep compiling) and change `apps/api/src/jobs/midnight-archive.ts:16` to import `getTodayIST` from `../lib/ist-date.js` directly. ESM: all intra-API imports need the `.js` extension.
    3. Extend the `AuditEvent` enum in `apps/api/src/lib/audit-log.ts`, appending a block commented `// WhatsApp Communication (Phase 7) — WHA-02/WHA-05 / D-11, D-12, D-13`: `WHATSAPP_CONSENT_GRANTED`, `WHATSAPP_CONSENT_WITHDRAWN`, `WHATSAPP_SENT_WITHOUT_CONSENT`, `WHATSAPP_OPT_OUT`, `WHATSAPP_OPT_IN`, `WHATSAPP_NUMBER_MARKED_INVALID`, `WHATSAPP_BOOKING_CANCELLED`, `WHATSAPP_BOOKING_MOVED`. Do not change `writeAuditLog` itself — it already writes to `prisma.authAuditLog` with a metadata JSON blob. Per Pitfall 6, message-flow logging goes to `WhatsAppMessageStatusEvent`, not here; this enum is for compliance events only.
    4. Append the seven WhatsApp variables to the root `.env.example` following the existing comment style at lines 6-13: `WHATSAPP_PROVIDER=simulator` (with a comment noting `simulator | cloud-api` and that the default must stay `simulator` so a misconfigured deploy cannot message real owners), `WHATSAPP_GRAPH_VERSION=v23.0`, `WHATSAPP_PHONE_NUMBER_ID=`, `WHATSAPP_ACCESS_TOKEN=`, `WHATSAPP_APP_SECRET=`, `WHATSAPP_WEBHOOK_VERIFY_TOKEN=`, `WHATSAPP_SIMULATOR_WEBHOOK_SECRET=`. Leave every secret value empty — never commit a real secret.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/api test -- --run src/lib/__tests__/ist-date.test.ts && pnpm --filter @breeyo/api test -- --run src/modules/queue tests/queue</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/api test -- --run src/lib/__tests__/ist-date.test.ts` exits 0 with every behavior case above passing
    - `pnpm --filter @breeyo/api test -- --run src/modules/queue tests/queue` exits 0 (no Phase 3 regression from the delegation)
    - `grep -q "from '../lib/ist-date.js'" apps/api/src/jobs/midnight-archive.ts` exits 0
    - `grep -q "ist-date" apps/api/src/modules/queue/queue.repository.ts` exits 0
    - `grep -c "WHATSAPP_CONSENT_GRANTED\|WHATSAPP_OPT_OUT\|WHATSAPP_NUMBER_MARKED_INVALID\|WHATSAPP_BOOKING_CANCELLED" apps/api/src/lib/audit-log.ts` returns >= 4
    - `grep -q '^WHATSAPP_PROVIDER=simulator' .env.example` exits 0
    - `grep -cE '^WHATSAPP_' .env.example` returns 7
    - `grep -E '^WHATSAPP_(ACCESS_TOKEN|APP_SECRET|WEBHOOK_VERIFY_TOKEN|SIMULATOR_WEBHOOK_SECRET)=.+' .env.example` matches nothing (no secret values committed)
  </acceptance_criteria>
  <done>One shared IST date module exists, used by queue, midnight archive and (later) the WhatsApp sweep. AuditEvent carries the eight Phase 7 compliance events. The root .env.example documents all seven WHATSAPP_* variables with an empty-secret, simulator-by-default posture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 7 test data factories and extended cleanup ordering</name>
  <files>
    apps/api/tests/helpers/factories.ts,
    apps/api/tests/helpers/__tests__/factories.test.ts
  </files>
  <read_first>
    apps/api/tests/helpers/factories.ts,
    apps/api/tests/helpers/app.ts,
    apps/api/tests/helpers/setup.ts,
    apps/api/tests/queue/queue-checkin.test.ts,
    apps/api/prisma/schema.prisma
  </read_first>
  <behavior>
    - createTestPetOwner(clinicId) creates a PetOwner with a unique +91 mobile and returns it
    - createTestPet(clinicId, ownerId) creates a Pet linked to that owner and returns it
    - createTestConsultation(clinicId, petId, vetUserId, { followUpDate }) creates a Consultation whose followUpDate matches the override (WHA-01 / D-01 source)
    - createTestVaccinationRecord(clinicId, petId, { vaccineName, administeredAt, nextDueDate }) creates a VaccinationRecord with the given nextDueDate (WHA-01 / D-02 source)
    - createTestDewormingRecord(clinicId, petId, { administeredAt, nextDueDate }) creates a DewormingRecord
    - createTestWhatsAppThread(clinicId, ownerId, { waPhone }) creates a WhatsAppThread whose waPhone starts with '+'
    - createTestWhatsAppClinicConfig(clinicId, { deliveryMode }) creates a WhatsAppClinicConfig defaulting to SIMULATOR / NORMAL / autoReplyEnabled true / autoReplyDelaySeconds 10 (D-14, D-16)
    - Calling createTestVaccinationRecord twice for the same pet and vaccineName with different administeredAt values succeeds (append-only history — needed for the superseded-record test in 07-11)
    - cleanupTestData() run after creating a full fixture graph (clinic + owner + pet + consultation + vaccination + thread + message + reminder task + booking + slot hold + config) leaves zero rows in every one of those tables and throws no foreign-key error
    - cleanupTestData() is idempotent — calling it twice in a row succeeds
  </behavior>
  <action>
    WHA-01/WHA-02/WHA-03/WHA-05: Give every later integration suite a one-line way to build fixtures.

    Write `apps/api/tests/helpers/__tests__/factories.test.ts` first (RED) — it exercises each factory against the real database and then asserts cleanup leaves nothing behind. Note `apps/api/vitest.config.ts` sets `fileParallelism: false`, so suites share one database serially; the test must not assume an empty database at start, only that cleanup empties it.

    Extend `apps/api/tests/helpers/factories.ts` following the existing `createTestClinic` shape exactly (an `overrides: Partial<{...}> = {}` second parameter, `randomUUID().slice(0, 8)`-suffixed defaults, `+91` + random 10 digits for mobiles): add `createTestPetOwner`, `createTestPet`, `createTestConsultation`, `createTestVaccinationRecord`, `createTestDewormingRecord`, `createTestWhatsAppThread`, `createTestWhatsAppMessage`, `createTestWhatsAppReminderTask`, `createTestWhatsAppBookingRequest`, `createTestWhatsAppClinicConfig`. Read the actual required columns off `schema.prisma` rather than guessing — species, breed, sex and dateOfBirth requirements on `Pet`, and the visitType/status enums on `Consultation`, must match the schema.

    Extend `cleanupTestData` (factories.ts:103-137). Inside the existing `$transaction`, delete the WhatsApp tables in this order BEFORE the existing `tx.consultation.deleteMany()` line, because they reference threads, messages, bookings, pets and owners: `whatsAppMessageStatusEvent`, `whatsAppMessage`, `whatsAppSlotHold`, `whatsAppBookingRequest`, `whatsAppReminderTask`, `whatsAppThread`, `whatsAppOwnerPreference`, `whatsAppClinicConfig`. Keep the existing explanatory comment style about FK ordering. Also add `tx.dewormingRecord.deleteMany()` and `tx.vaccinationRecord.deleteMany()` before `tx.consultation.deleteMany()` if they are not already present.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/api test -- --run tests/helpers/__tests__/factories.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/api test -- --run tests/helpers/__tests__/factories.test.ts` exits 0 with every behavior case above passing
    - `grep -c "export async function createTest" apps/api/tests/helpers/factories.ts` returns >= 14
    - `grep -c "whatsApp" apps/api/tests/helpers/factories.ts` returns >= 8
    - `grep -q "whatsAppMessageStatusEvent.deleteMany" apps/api/tests/helpers/factories.ts` exits 0
    - `pnpm --filter @breeyo/api test -- --run tests/patient tests/queue tests/clinic` exits 0 (existing suites still pass with the extended cleanup)
  </acceptance_criteria>
  <done>Ten new factories exist and produce valid rows against the migrated schema. cleanupTestData removes all WhatsApp tables in FK-safe order and is idempotent. Existing Phase 1-4 suites still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create the twelve integration suite files from the validation map as addressable scaffolds</name>
  <files>
    apps/api/tests/whatsapp/send.test.ts,
    apps/api/tests/whatsapp/status-ledger.test.ts,
    apps/api/tests/whatsapp/webhook-idempotency.test.ts,
    apps/api/tests/whatsapp/inbox.test.ts,
    apps/api/tests/whatsapp/reminder-idempotency.test.ts,
    apps/api/tests/whatsapp/opt-out.test.ts,
    apps/api/tests/whatsapp/consent.test.ts,
    apps/api/tests/whatsapp/invoice-delivery.test.ts,
    apps/api/tests/whatsapp/booking.test.ts,
    apps/api/tests/whatsapp/booking-concurrency.test.ts,
    apps/api/tests/whatsapp/booking-authz.test.ts,
    apps/api/tests/whatsapp/authz.test.ts
  </files>
  <read_first>
    apps/api/tests/queue/queue-checkin.test.ts,
    apps/api/tests/tenant-isolation.test.ts,
    apps/api/tests/helpers/app.ts,
    apps/api/tests/helpers/factories.ts,
    .planning/phases/07-whatsapp-communication/07-VALIDATION.md
  </read_first>
  <action>
    WHA-01/WHA-02/WHA-03/WHA-05: Create every integration suite file named in 07-VALIDATION.md § Per-Task Verification Map so later plans have a known target path and no plan has to invent a filename.

    Each file gets: the `queue-checkin.test.ts:1-36` harness boilerplate (`buildTestApp()` in `beforeAll`, `cleanupTestData()` + `closeTestApp()` in `afterAll`, a local `setupAuthenticatedUser()` helper composing createTestUser/createTestClinic/createTestClinicMember/createTestTokens), and one `it.todo('...')` per row of the validation map that maps to that file, with the row's exact behavior sentence as the todo text plus its `WHA-0X / D-XX` annotation.

    Map file to todos: send.test.ts — send persists WhatsAppMessage(status=QUEUED) before any dispatch (WHA-05, Pattern 2). status-ledger.test.ts — every status change appends a WhatsAppMessageStatusEvent row (WHA-05). webhook-idempotency.test.ts — duplicate webhook for the same providerMessageId is a no-op (WHA-05, Pitfall 14). inbox.test.ts — inbox list returns threads filtered by all/invoices/reminders/bookings/failed/needs_action (WHA-05, UI-SPEC chips). reminder-idempotency.test.ts — running the sweep twice on the same day creates exactly one task per source/kind/touch (WHA-01, D-03). opt-out.test.ts — invoice_delivery and booking_confirmation send even when remindersOptedOut is true (WHA-02, D-10); reminder-category templates are blocked for all of that owner's pets (WHA-02/03, D-10/D-11). consent.test.ts — send proceeds with missing consent and writes an audit entry (WHA-02, D-13); consent grant appends a ConsentRecord with consentType 'whatsapp_communication' and withdrawal stamps withdrawnAt (WHA-02, D-12). invoice-delivery.test.ts — invoice_delivery sends with contextType INVOICE and contextId and no FK to a nonexistent Invoice, link-only (WHA-02, D-18, Pitfall 8). booking.test.ts — inbound slot pick auto-confirms without staff action and queues booking_confirmation (WHA-03, D-06); a confirmed slot is absent from the next slot-offer list for that clinic/day (WHA-03, D-08). booking-concurrency.test.ts — two concurrent confirmations for the same slot yield one CONFIRMED and one SLOT_TAKEN (WHA-03, D-07). booking-authz.test.ts — no inbound payload can cancel or move a booking; only the authenticated staff endpoints can (WHA-03, D-09). authz.test.ts — requirePermission('SEND_WHATSAPP') returns 403 for a role lacking it (WHA-05, D-20).

    Use `it.todo`, not `it.skip` and not an empty `it` — `it.todo` is reported as pending rather than passing, so an unimplemented behavior can never masquerade as green.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/api test -- --run tests/whatsapp</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/api test -- --run tests/whatsapp` exits 0 and reports 12 test files with todo entries and zero failures
    - `ls apps/api/tests/whatsapp/*.test.ts | wc -l` returns 12
    - `grep -rc 'it.todo' apps/api/tests/whatsapp/ | grep -c ':0'` returns 0 (every file has at least one todo)
    - `grep -rl 'it.skip' apps/api/tests/whatsapp/ | wc -l` returns 0
    - `grep -rc 'WHA-0' apps/api/tests/whatsapp/ | grep -c ':0'` returns 0 (every file carries requirement traceability)
  </acceptance_criteria>
  <done>Twelve integration suite files exist with harness boilerplate and it.todo placeholders carrying the validation map's exact behavior sentences and WHA-0X/D-XX annotations. Later plans replace todos with real tests rather than creating new files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example → deployed configuration | A wrong default here becomes a production misconfiguration |
| test database → developer machine | Fixtures and cleanup run destructive deletes |
| audit log writer → auth_audit_log table | Compliance records must be append-only and typed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Elevation of Privilege | WHATSAPP_PROVIDER defaulting to cloud-api would send real messages to real pet owners from a dev deploy | mitigate | `.env.example` ships `WHATSAPP_PROVIDER=simulator`; acceptance criteria asserts that exact line; the provider registry (07-05) treats any unset value as simulator |
| T-07-04-02 | Information Disclosure | secrets committed in .env.example | mitigate | All four secret-bearing variables ship with empty values; acceptance criteria greps for any non-empty secret assignment and requires zero matches |
| T-07-04-03 | Repudiation | untyped audit event strings polluting auth_audit_log | mitigate | `AuditEvent` enum extended with the eight Phase 7 compliance events; `writeAuditLog` continues to take the enum, so a raw string is a type error (Pitfall 6) |
| T-07-04-04 | Denial of Service | cleanupTestData deleting rows in the wrong order and wedging every suite | mitigate | WhatsApp deletes inserted before consultation/pet/owner/clinic deletes inside the existing `$transaction`; a factories test asserts a full fixture graph cleans up with no FK error and that cleanup is idempotent |
| T-07-04-05 | Tampering | unimplemented behaviors reported as passing tests | mitigate | Scaffolds use `it.todo` (reported pending), never `it.skip` or an empty `it`; acceptance criteria greps for zero `it.skip` |
| T-07-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (07-RESEARCH § Package Legitimacy Audit). Any install would require slopcheck `-e npm` plus a blocking human checkpoint |
</threat_model>

<verification>
- `pnpm --filter @breeyo/api test -- --run src/lib/__tests__/ist-date.test.ts` passes
- `pnpm --filter @breeyo/api test -- --run tests/helpers/__tests__/factories.test.ts` passes
- `pnpm --filter @breeyo/api test -- --run tests/whatsapp` passes with todos pending
- `pnpm --filter @breeyo/api test -- --run tests/patient tests/queue tests/clinic src/modules/queue` passes (no regression)
- `pnpm --filter @breeyo/api exec tsc --noEmit` exits 0
</verification>

<success_criteria>
1. `apps/api/src/lib/ist-date.ts` exports getTodayIST/addDaysIST/IST_TIMEZONE with tests covering IST boundary cases
2. QueueRepository and midnight-archive delegate to the shared module; Phase 3 queue tests still pass
3. AuditEvent carries eight Phase 7 compliance events (Pitfall 6)
4. Root .env.example documents seven WHATSAPP_* vars, simulator-by-default, no secret values
5. Ten new factories create valid rows against the migrated schema
6. cleanupTestData removes all eight WhatsApp tables in FK-safe order and is idempotent
7. Twelve integration suite files exist with it.todo placeholders and WHA-0X/D-XX annotations
</success_criteria>

<output>
After completion, create `.planning/phases/07-whatsapp-communication/07-04-SUMMARY.md`
</output>
