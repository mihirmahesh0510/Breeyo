---
phase: 08-scheduling-calendar
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/20260813100000_add_scheduling_models/migration.sql
  - apps/api/tests/helpers/factories.ts
  - apps/api/tests/scheduling/schema-shape.test.ts
autonomous: true
requirements: [SCH-01, SCH-02]

must_haves:
  truths:
    - "Five scheduling tables exist: appointments, appointment_pets, vet_availability_templates, availability_overrides, blocked_periods"
    - "ServiceCatalog carries a durationMinutes column and Appointment snapshots the resolved duration at booking time so later catalog edits never resize past appointments"
    - "QueueEntry carries a non-null queuePriorityAt column and a nullable appointmentId, and QueueEntryStatus includes EXPECTED"
    - "Appointment carries three independent idempotency marker columns so each sweep pass is a no-op on re-run"
    - "The migration set alone reproduces schema.prisma from an empty database, verified by prisma migrate diff exiting 0"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Appointment, AppointmentPet, VetAvailabilityTemplate, AvailabilityOverride, BlockedPeriod models, AppointmentStatus and BlockedPeriodReason enums, ServiceCatalog.durationMinutes, QueueEntry.queuePriorityAt, QueueEntryStatus.EXPECTED"
      contains: "model Appointment"
    - path: "apps/api/prisma/migrations/20260813100000_add_scheduling_models/migration.sql"
      provides: "Scheduling DDL including the hand-written queue_priority_at backfill"
      contains: "queue_priority_at"
    - path: "apps/api/tests/scheduling/schema-shape.test.ts"
      provides: "Database-level assertions for the new columns, indexes, enum values and cross-tenant scoping"
      contains: "queue_priority_at"
    - path: "apps/api/tests/helpers/factories.ts"
      provides: "createTestAppointment, createTestVetAvailabilityTemplate, createTestAvailabilityOverride, createTestBlockedPeriod plus FK-safe cleanup"
      contains: "createTestAppointment"
  key_links:
    - from: "apps/api/prisma/schema.prisma Appointment"
      to: "apps/api/prisma/schema.prisma QueueEntry"
      via: "AppointmentPet.queueEntryId records which QueueEntry each pet on the appointment spawned (D-21)"
      pattern: "queueEntryId"
    - from: "apps/api/prisma/schema.prisma Appointment.durationMinutes"
      to: "apps/api/prisma/schema.prisma ServiceCatalog.durationMinutes"
      via: "duration is read from the catalog at booking time and snapshotted onto the appointment row"
      pattern: "durationMinutes"
---

<objective>
Create the Phase 8 persistence layer: three availability tables, the appointment and appointment-pet tables, the `ServiceCatalog.durationMinutes` column D-02 requires, the `QueueEntry.queuePriorityAt` column that delivers D-10's ordering with no new sort logic, the `EXPECTED` enum value, their migration, test factories and a database-shape test.

Purpose: every remaining Phase 8 plan reads or writes these tables. `08-PATTERNS.md` § Cross-Phase Reality Check records a verified gap — `ServiceCatalog` (`schema.prisma:537-557`) has no duration field at all, so D-02 is a schema migration, not a read-only dependency. `queuePriorityAt` is what makes D-10 a one-line `orderBy` change in plan 08-04 instead of a raw-SQL coalesce. The three nullable marker columns on `Appointment` are what make plan 08-09's 5-minute sweep idempotent under restart, duplicate scheduler firing or manual re-trigger.
Output: schema, migration, factories, cleanup order, and a database-shape test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduling-calendar/08-CONTEXT.md
@.planning/phases/08-scheduling-calendar/08-RESEARCH.md
@.planning/phases/08-scheduling-calendar/08-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the five scheduling models, three enums, and the ServiceCatalog / QueueEntry / QueueEntryStatus modifications to schema.prisma</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (all of it — in particular `QueueEntry` at lines 277-300 and `QueueEntryStatus` at 220-225, which are the exact convention template and the exact edit sites; `ServiceCatalog` at 537-557 which gains `durationMinutes`; `Clinic` at 37-64 and its back-relation block at line 55; `Pet` at 247-275 and its back-relation at 266; `PetOwner` at 227-244; the `User` model; `AuthAuditLog` at 163-177)
    - .planning/phases/08-scheduling-calendar/08-PATTERNS.md (§ `apps/api/prisma/schema.prisma (new models)` — the verbatim `QueueEntry` excerpt and the "conventions to carry over verbatim" list; § Cross-Phase Reality Check for the `ServiceCatalog` duration gap)
    - .planning/phases/08-scheduling-calendar/08-RESEARCH.md (§ Architecture Patterns Pattern 1 for the minutes-from-midnight availability model sketch; Pattern 2 for the three marker columns; Pattern 3 for `queuePriorityAt`)
    - .planning/phases/08-scheduling-calendar/08-CONTEXT.md (D-01, D-02, D-04, D-05, D-06, D-08, D-10, D-12, D-20, D-21, D-22)
    - packages/types/src/constants/scheduling.constants.ts (as written by plan 08-01 — enum member names here MUST match `AppointmentStatus`, `BlockedPeriodReason` and `AppointmentSource` exactly, and `DEFAULT_SERVICE_DURATION_MINUTES` must match the `durationMinutes` default chosen below)
  </read_first>
  <action>
Append a `// --- Phase 8: Scheduling and Calendar ---` banner section to `apps/api/prisma/schema.prisma`. Every new model copies the `QueueEntry` conventions verbatim: `id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid` (never `cuid()`), `@db.Uuid` on every FK column, `snake_case` `@map()` on every multi-word column, `@@map()` to a plural snake_case table, composite `@@index` entries leading with `clinicId`, and a back-relation entry added to `Clinic`.

**Enums** (place alongside the existing enums, where `QueueEntryStatus` lives):
- `enum AppointmentStatus { SCHEDULED CHECKED_IN COMPLETED CANCELLED NO_SHOW }` — D-20, with no `CONFIRMED` value.
- `enum AppointmentSource { STAFF WHATSAPP }` — D-12 distinguishes a staff booking from a formalized WhatsApp booking.
- `enum BlockedPeriodReason { LUNCH BREAK PERSONAL OFF_SITE MEETING OTHER }` — D-06.
- `enum QueueEntryStatus` — add `EXPECTED` as the first member (D-08).

**`VetAvailabilityTemplate`** to `vet_availability_templates` (D-01, D-04). Columns: `clinicId`; `vetId String @map("vet_id") @db.Uuid`; `weekday Int` (0=Sunday..6=Saturday, matching `WEEKDAY_LABELS` in `scheduling.constants.ts`); `isClosed Boolean @default(false) @map("is_closed")`; `openMinutes Int? @map("open_minutes")`; `closeMinutes Int? @map("close_minutes")`; `createdAt`; `updatedAt`. Relations to `Clinic` and to `User` as `vet` with an explicit relation name (e.g. `vet User @relation("VetAvailabilityTemplateVet", fields: [vetId], references: [id])`) because `User` ends up with several distinct relations to scheduling tables and Prisma requires disambiguation. `@@unique([clinicId, vetId, weekday])`, `@@map("vet_availability_templates")`.

**`AvailabilityOverride`** to `availability_overrides` (D-01 per-date overrides). Columns: `clinicId`; `vetId`; `date DateTime @db.Date`; `isClosed Boolean @default(true) @map("is_closed")` (most overrides are a day off); `openMinutes Int?`; `closeMinutes Int?`; `reason String?`; `createdAt`; `updatedAt`. `@@unique([clinicId, vetId, date])`, `@@map("availability_overrides")`. `@db.Date` rather than a full timestamp is deliberate: the unique constraint must be per calendar day so no timezone offset can produce two rows for the same IST date.

**`BlockedPeriod`** to `blocked_periods` (D-05, D-06). Columns: `clinicId`; `vetId`; `date DateTime @db.Date`; `startMinutes Int @map("start_minutes")`; `endMinutes Int @map("end_minutes")`; `reason BlockedPeriodReason`; `reasonText String? @map("reason_text")`; `createdById String @map("created_by_id") @db.Uuid`; `createdAt`. `@@index([clinicId, vetId, date])`, `@@map("blocked_periods")`.

**`Appointment`** to `appointments`. Columns:
- `clinicId`; `vetId` (relation `AppointmentVet` to `User`); `ownerId String @map("owner_id") @db.Uuid` (relation to `PetOwner` — D-21 scopes one appointment to one owner's pets); `serviceCatalogId String? @map("service_catalog_id") @db.Uuid` (nullable so a WhatsApp-originated booking with no service chosen can still exist).
- `status AppointmentStatus @default(SCHEDULED)`; `source AppointmentSource @default(STAFF)`.
- `scheduledFor DateTime @map("scheduled_for")`; `durationMinutes Int @map("duration_minutes")` — the D-02 snapshot. Add a schema comment above it: duration is resolved from `ServiceCatalog.durationMinutes` at booking time and frozen here, so editing a service's duration months later never retroactively resizes historical appointments (RESEARCH § Don't Hand-Roll, final row).
- `recurringSeriesId String? @map("recurring_series_id") @db.Uuid` and `recurrenceIndex Int? @map("recurrence_index")` — D-22. `recurringSeriesId` is a plain generated UUID shared by every row in one series, not an FK to a series table; RESEARCH § Don't Hand-Roll prefers a bounded loop generating N linked rows over an RRULE parser.
- `notes String?`; `createdById String @map("created_by_id") @db.Uuid` (relation `AppointmentCreatedBy` to `User`).
- `whatsappBookingRequestId String? @map("whatsapp_booking_request_id") @db.Uuid` — D-12's seam. Do NOT add a Prisma relation for this field. Phase 7's `WhatsAppBookingRequest` model may not exist in `schema.prisma` when this plan executes; declare it as a bare nullable UUID column with the comment `// D-12: Phase 7 WhatsAppBookingRequest.id; relation intentionally omitted so this model does not hard-depend on Phase 7 landing first`. Plan 08-10 adds the relation if and only if Phase 7 has landed.
- Idempotency markers (RESEARCH Pattern 2 — each sweep pass checks its own marker in the `WHERE` clause so a re-run is a natural no-op): `queueEntryCreatedAt DateTime? @map("queue_entry_created_at")`, `noShowFlippedAt DateTime? @map("no_show_flipped_at")`, `startingSoonNotifiedAt DateTime? @map("starting_soon_notified_at")`.
- Lifecycle timestamps: `checkedInAt DateTime? @map("checked_in_at")`; `cancelledAt DateTime? @map("cancelled_at")`; `cancelledById String? @map("cancelled_by_id") @db.Uuid`; `cancelReason String? @map("cancel_reason")`; `completedAt DateTime? @map("completed_at")`; `createdAt`; `updatedAt`.
- Relations: `pets AppointmentPet[]`, `queueEntries QueueEntry[]`.
- Indexes: `@@index([clinicId, scheduledFor])` (day/week range read, RESEARCH Pattern 4), `@@index([clinicId, vetId, scheduledFor])` (per-vet slot-conflict query), `@@index([clinicId, status, scheduledFor])` (the sweep's due-appointment scan), `@@index([clinicId, recurringSeriesId])`, `@@index([whatsappBookingRequestId])`. `@@map("appointments")`.

**`AppointmentPet`** to `appointment_pets` (D-21: one appointment covers multiple pets and at handoff spawns one `QueueEntry` per pet). Columns: `clinicId`; `appointmentId String @map("appointment_id") @db.Uuid`; `petId String @map("pet_id") @db.Uuid`; `queueEntryId String? @map("queue_entry_id") @db.Uuid` (set by plan 08-09's handoff pass; nullable until then); `createdAt`. Relations to `Clinic`, `Appointment` with `onDelete: Cascade`, `Pet`, and `QueueEntry?`. `@@unique([appointmentId, petId])`, `@@index([clinicId, petId])`, `@@index([queueEntryId])`, `@@map("appointment_pets")`.

**`ServiceCatalog` modification (D-02)** — add `durationMinutes Int @default(15) @map("duration_minutes")` with a comment naming D-02. The default must equal `DEFAULT_SERVICE_DURATION_MINUTES` in `packages/types/src/constants/scheduling.constants.ts`; if plan 08-01 chose a different value, match it and record the discrepancy in the summary. A default rather than a nullable column is deliberate: every preset service seeded in Phase 4 becomes bookable immediately instead of being silently unbookable.

**`QueueEntry` modifications** — add `queuePriorityAt DateTime @map("queue_priority_at")` (not nullable, not defaulted in the schema — the migration backfills it, see Task 2) and `appointmentId String? @map("appointment_id") @db.Uuid` plus `appointment Appointment? @relation(fields: [appointmentId], references: [id])`. Add `@@index([clinicId, status, queuePriorityAt])` — this is the index the reordered `orderBy` in plan 08-04 needs. Do NOT remove, rename or repurpose `checkedInAt`: it keeps meaning "when did this physically happen" and remains the basis of the midnight-archive date filter and the queue-card timestamp.

**Back-relations** — add `appointments Appointment[]`, `appointmentPets AppointmentPet[]`, `vetAvailabilityTemplates VetAvailabilityTemplate[]`, `availabilityOverrides AvailabilityOverride[]`, `blockedPeriods BlockedPeriod[]` to `Clinic`; `appointmentPets AppointmentPet[]` to `Pet`; `appointments Appointment[]` to `PetOwner`; `appointments Appointment[]` to `ServiceCatalog`; and the matching named back-relations on `User` for every `User` relation declared above.

Do not add RLS policies for these tables and do not add `FORCE ROW LEVEL SECURITY`. RESEARCH § Project Constraints records the verified dominant pattern: DB-level RLS exists on only three tables and every module injects `fastify.prisma` with explicit `clinicId` filters. Forcing RLS on these tables would break every scheduling read. Tenant isolation is enforced at the repository layer and asserted by tests — see this plan's threat model.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm exec prisma validate &amp;&amp; pnpm exec prisma format</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/api && pnpm exec prisma validate` exits 0.
    - `grep -Ec 'model Appointment\b|model AppointmentPet|model VetAvailabilityTemplate|model AvailabilityOverride|model BlockedPeriod' apps/api/prisma/schema.prisma` returns `5`.
    - `grep -Ec 'enum AppointmentStatus|enum AppointmentSource|enum BlockedPeriodReason' apps/api/prisma/schema.prisma` returns `3`.
    - `awk '/enum QueueEntryStatus/,/}/' apps/api/prisma/schema.prisma | grep -c EXPECTED` returns `1`.
    - `grep -c 'duration_minutes' apps/api/prisma/schema.prisma` returns `2` (one on `service_catalog`, one on `appointments`).
    - `awk '/model QueueEntry \{/,/^}/' apps/api/prisma/schema.prisma | grep -c 'queuePriorityAt DateTime @map'` returns `1`, and the same awk range piped to `grep -c 'queuePriorityAt DateTime?'` returns `0` (the column is not nullable).
    - `grep -Ec 'queue_entry_created_at|no_show_flipped_at|starting_soon_notified_at' apps/api/prisma/schema.prisma` returns `3`.
    - `awk '/Phase 8: Scheduling/,0' apps/api/prisma/schema.prisma | grep -c 'cuid()'` returns `0`.
    - `grep -c 'whatsappBookingRequestId' apps/api/prisma/schema.prisma` returns `1` and `grep -c 'WhatsAppBookingRequest ' apps/api/prisma/schema.prisma` returns `0` (no hard Phase 7 relation).
    - `grep -c 'checkedInAt' apps/api/prisma/schema.prisma` is greater than or equal to its pre-task value; record both numbers in the summary to prove `checkedInAt` was not removed.
  </acceptance_criteria>
  <done>`prisma validate` passes on a schema containing five scheduling tables, three new enums, the `EXPECTED` queue status, per-service durations snapshotted onto appointments, and the `queuePriorityAt` ordering column.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate the scheduling migration with a hand-written queue_priority_at backfill, apply it, regenerate the client, and prove migration/schema parity</name>
  <files>apps/api/prisma/migrations/20260813100000_add_scheduling_models/migration.sql</files>
  <read_first>
    - apps/api/prisma/schema.prisma (as written in Task 1)
    - apps/api/prisma/migrations/ (list the directory, then read the most recent existing `migration.sql` to match its DDL style and confirm this repo's timestamp-ordering convention)
    - apps/api/prisma/post-migrate.sql (read to confirm this plan needs no change there — the new tables are covered by the existing blanket `GRANT ... ON ALL TABLES IN SCHEMA public TO breeyo_app` and deliberately get no RLS policies)
    - .planning/phases/06-invoicing-payments/06-03-PLAN.md (Tasks 2 and 3 — the exact migrate-dev-create-only, hand-edit, apply, generate, parity-check convention this repo already uses; do NOT substitute a generic `prisma db push`)
    - CLAUDE.md (§ Database — the two DB roles and the RLS setup scripts)
  </read_first>
  <action>
This task is [BLOCKING] for the phase. Build and type checks pass without applying the migration because Prisma types come from `schema.prisma` and the generated client, not from the live database. Skipping this step produces a false-positive verification state for every downstream plan.

1. Generate: `cd apps/api && pnpm exec prisma migrate dev --name add_scheduling_models --create-only`. Rename the generated directory to `20260813100000_add_scheduling_models` if Prisma chose a different timestamp, so ordering is deterministic.

2. Handle the enum addition. Postgres cannot use a newly added enum value in the same transaction that adds it. Prisma emits `ALTER TYPE "QueueEntryStatus" ADD VALUE 'EXPECTED';`. Inspect the generated SQL and confirm no later statement in the same file references `'EXPECTED'` in a default, `CHECK` or index predicate. If one does, either move the `ALTER TYPE` to the very top of `migration.sql` followed by `COMMIT;` then `BEGIN;`, or place the `ALTER TYPE` alone in an earlier-timestamped migration directory. Record which approach was taken and why in the summary.

3. Hand-write the `queue_priority_at` backfill. Prisma will emit `ALTER TABLE "queue_entries" ADD COLUMN "queue_priority_at" TIMESTAMP(3) NOT NULL;`, which fails against any table containing rows. Replace that single statement with three, preceded by a SQL comment naming D-10 and RESEARCH Pattern 3:
   - `ALTER TABLE "queue_entries" ADD COLUMN "queue_priority_at" TIMESTAMP(3);`
   - `UPDATE "queue_entries" SET "queue_priority_at" = "checked_in_at" WHERE "queue_priority_at" IS NULL;` — for an organic walk-in, priority time and physical check-in time are the same value, so every pre-existing row backfills correctly and no historical ordering changes.
   - `ALTER TABLE "queue_entries" ALTER COLUMN "queue_priority_at" SET NOT NULL;`

4. Review the remaining generated SQL and confirm it creates all five tables, all three new enum types, every `@@index`/`@@unique` declared in Task 1, the `duration_minutes` column on both `service_catalog` and `appointments`, and the `appointment_id` column on `queue_entries`.

5. Apply and prove parity, from `apps/api`:
   - `pnpm exec prisma migrate reset --force --skip-seed`
   - `psql "$DATABASE_URL" -f prisma/post-migrate.sql`
   - `pnpm exec prisma migrate diff --from-schema-datasource "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --exit-code` must exit 0. A non-zero exit means the migration set alone does not reproduce `schema.prisma`; fix the migration rather than accepting drift, because CI enforces this parity.

6. `pnpm --filter @breeyo/api db:generate` so the Prisma client delegates and types are live for every later plan.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm exec prisma migrate reset --force --skip-seed &amp;&amp; psql "$DATABASE_URL" -f prisma/post-migrate.sql &amp;&amp; pnpm exec prisma migrate diff --from-schema-datasource "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --exit-code</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/api && pnpm exec prisma migrate diff --from-schema-datasource "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --exit-code` exits 0 after reset plus post-migrate.
    - `grep -c 'ALTER COLUMN "queue_priority_at" SET NOT NULL' apps/api/prisma/migrations/20260813100000_add_scheduling_models/migration.sql` returns `1`.
    - `grep -c 'SET "queue_priority_at" = "checked_in_at"' apps/api/prisma/migrations/20260813100000_add_scheduling_models/migration.sql` returns `1`.
    - `grep -c "ADD VALUE 'EXPECTED'" apps/api/prisma/migrations/20260813100000_add_scheduling_models/migration.sql` returns `1` (or the summary records the separate earlier migration directory that carries it).
    - `psql "$DATABASE_URL" -c "SELECT unnest(enum_range(NULL::\"QueueEntryStatus\"))"` output includes `EXPECTED`.
    - `psql "$DATABASE_URL" -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('appointments','appointment_pets','vet_availability_templates','availability_overrides','blocked_periods')"` returns `5`.
    - `psql "$DATABASE_URL" -c "SELECT is_nullable FROM information_schema.columns WHERE table_name='queue_entries' AND column_name='queue_priority_at'"` returns `NO`.
    - `cd apps/api && pnpm exec prisma generate` exits 0, and `node -e "const {PrismaClient}=require('@prisma/client');const p=new PrismaClient();for(const m of ['appointment','appointmentPet','vetAvailabilityTemplate','availabilityOverride','blockedPeriod'])if(!p[m]){console.error('MISSING delegate '+m);process.exit(1)}"` exits 0.
    - `grep -Eic 'row level security|create policy' apps/api/prisma/migrations/20260813100000_add_scheduling_models/migration.sql` returns `0` — no RLS was added to the scheduling tables.
  </acceptance_criteria>
  <done>An empty database built from the migration set alone reproduces `schema.prisma` exactly, `EXPECTED` exists in the Postgres enum, `queue_priority_at` is backfilled and NOT NULL, and the Prisma client exposes all five new delegates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add scheduling test factories with FK-safe cleanup and a database-shape test</name>
  <files>apps/api/tests/helpers/factories.ts, apps/api/tests/scheduling/schema-shape.test.ts</files>
  <read_first>
    - apps/api/tests/helpers/factories.ts (all of it — the `createTestX(requiredFks, overrides = {})` convention at lines 35-51, the `overrides.field || default` idiom, `randomUUID().slice(0, 6)` for uniqueness, and the hand-maintained reverse-FK-order `cleanupTestData` delete list at lines 103-137 with its comment documenting exactly this failure mode from Phases 3 and 4)
    - apps/api/tests/queue/queue-board.test.ts (lines 1-88 — the `buildTestApp`/`closeTestApp`, `setupTestContext`, and `app.inject` idiom; this repo uses `app.inject`, not supertest, for queue-adjacent tests)
    - apps/api/tests/helpers/app.ts and apps/api/vitest.config.ts (`fileParallelism: false`, 30s timeout, memoized single app instance)
    - apps/api/prisma/schema.prisma (as written in Task 1 — the exact field names the factories must supply)
    - .planning/phases/08-scheduling-calendar/08-PATTERNS.md (§ Tests — the factory style and the cleanup-order requirement that new tables be deleted before `queueEntry`, `pet` and `clinic`)
  </read_first>
  <behavior>
    - Test "scheduling tables exist and are clinic-scoped": for each of the five new tables, `information_schema.columns` contains a `clinic_id` column of type `uuid`.
    - Test "appointment duration is an integer snapshot": `information_schema.columns` reports `appointments.duration_minutes` as `integer` and `NOT NULL`, and `service_catalog.duration_minutes` as `integer` with a default.
    - Test "queue_priority_at is non-null": `information_schema.columns` reports `queue_entries.queue_priority_at` as `timestamp without time zone` (or the type Prisma emits for `DateTime`) with `is_nullable = 'NO'`.
    - Test "EXPECTED is a live enum value": creating a `QueueEntry` with `status: 'EXPECTED'` succeeds and reads back as `EXPECTED`.
    - Test "one appointment can carry multiple pets": `createTestAppointment` with two pets creates two `appointment_pets` rows, and inserting the same `(appointmentId, petId)` pair twice rejects with a unique violation (D-21).
    - Test "availability template is unique per vet-weekday": two templates for the same `(clinicId, vetId, weekday)` reject; the same weekday for a different vet succeeds (D-04).
    - Test "availability override is unique per vet-date": the same `(clinicId, vetId, date)` rejects twice (D-01).
    - Test "cross-tenant appointment read returns nothing": an appointment created for clinic B is absent from `prisma.appointment.findMany({ where: { clinicId: clinicA.id } })`. This is the repository-layer tenancy contract these tables rely on in place of DB-level RLS.
    - Test "cleanup leaves no scheduling rows": after `cleanupTestData()`, all five scheduling tables report zero rows.
  </behavior>
  <action>
Add these factories to `apps/api/tests/helpers/factories.ts`, matching the existing signature convention exactly (required FKs as positional parameters, everything else via a trailing `overrides = {}`):

- `createTestVetAvailabilityTemplate(clinicId, vetId, overrides = {})` — defaults `weekday: 1`, `isClosed: false`, `openMinutes: 540`, `closeMinutes: 1080` (9:00 to 18:00, matching the existing `getDefaultHours()` default in `apps/mobile/src/lib/wizard-utils.ts`).
- `createTestVetAvailabilityWeek(clinicId, vetId, overrides = {})` — convenience helper creating all seven rows, Sunday closed and Monday-Saturday open 540-1080, mirroring `getDefaultHours()`. Most availability tests need a whole week, not one day.
- `createTestAvailabilityOverride(clinicId, vetId, date, overrides = {})` — defaults `isClosed: true`, `openMinutes: null`, `closeMinutes: null`.
- `createTestBlockedPeriod(clinicId, vetId, date, createdById, overrides = {})` — defaults `startMinutes: 780`, `endMinutes: 840`, `reason: 'LUNCH'`, `reasonText: null`.
- `createTestAppointment(clinicId, vetId, ownerId, petIds, createdById, overrides = {})` — `petIds` is an array so D-21 multi-pet is the default-supported shape, not an afterthought. Defaults `status: 'SCHEDULED'`, `source: 'STAFF'`, `durationMinutes: 15`, `scheduledFor` at the next whole hour, `serviceCatalogId: null`, and a nested `pets: { create: petIds.map((petId) => ({ clinicId, petId })) }`. Return the appointment with `include: { pets: true }`.
- `createTestServiceForBooking(clinicId, overrides = {})` — a `ServiceCatalog` row with an explicit `durationMinutes` so D-02 duration-resolution tests have a fixture; defaults `name: 'Vaccination'`, `price: 50000`, `durationMinutes: 15`.

Extend `cleanupTestData()` by adding the scheduling deletions **before** the existing `queueEntry`, `pet`, `petOwner`, `serviceCatalog` and `clinic` deletions, in this order: `appointmentPet`, `appointment`, `blockedPeriod`, `availabilityOverride`, `vetAvailabilityTemplate`. The comment already in that function documents the exact reverse-FK-order failure mode from Phases 3 and 4 — do not repeat it.

Create `apps/api/tests/scheduling/schema-shape.test.ts` implementing all nine behaviors above. Use `prisma` from `factories.ts` for entity assertions and `$queryRaw` against `information_schema.columns` and `pg_enum`/`enum_range` for the column-type, nullability and enum assertions — those are database facts, and asserting them in TypeScript would prove nothing. Follow the `beforeAll(buildTestApp)` / `afterAll(closeTestApp)` / per-file `setupTestContext()` shape from `apps/api/tests/queue/queue-board.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @breeyo/api test -- --run tests/scheduling/schema-shape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @breeyo/api test -- --run tests/scheduling/schema-shape.test.ts` reports at least 9 passing tests.
    - `grep -Ec 'createTestAppointment|createTestVetAvailabilityTemplate|createTestVetAvailabilityWeek|createTestAvailabilityOverride|createTestBlockedPeriod|createTestServiceForBooking' apps/api/tests/helpers/factories.ts` returns at least `6`.
    - `awk '/export async function cleanupTestData/,/^}/' apps/api/tests/helpers/factories.ts | grep -n 'appointmentPet\|appointment\.\|blockedPeriod\|availabilityOverride\|vetAvailabilityTemplate\|queueEntry' ` shows all five scheduling deletions appearing on lines before the `queueEntry` deletion.
    - `pnpm --filter @breeyo/api test` passes in full — no existing Phase 3/4 suite regresses from the `queuePriorityAt` NOT NULL column (any existing `queueEntry.create` in a factory or test that does not supply `queuePriorityAt` will fail; fix those call sites by adding `queuePriorityAt` alongside `checkedInAt`, and list every file changed in the summary).
    - `pnpm --filter @breeyo/api exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Scheduling factories exist with multi-pet support by default, `cleanupTestData` deletes the new tables in FK-safe order, and nine database-level assertions confirm the columns, nullability, enum value, unique constraints and repository-layer tenant scoping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application query → scheduling tables | Appointment and availability rows for every clinic share one physical table with no DB-level RLS backstop |
| Phase 7 WhatsApp booking → `appointments.whatsapp_booking_request_id` | A cross-phase foreign identifier is persisted without a database-enforced relation |
| migration set → live database | Schema truth must be reproducible from migrations alone or CI parity fails silently in one direction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | cross-tenant read of `appointments`, `blocked_periods` or `vet_availability_templates` via a guessed UUID | mitigate | Every table carries a `clinic_id` column and every repository method in plans 08-05 and 08-07 takes `clinicId` as its explicit first parameter and filters on it (the verified dominant pattern in this codebase, RESEARCH § Project Constraints). Task 3 asserts a cross-tenant `appointment.findMany` returns nothing, and plan 08-11's integration tests assert 404 rather than 403 on a cross-tenant id so existence is not disclosed. DB-level RLS is deliberately NOT used here because `fastify.prisma` is unscoped and forcing RLS would break every read |
| T-08-08 | Tampering | a client-supplied `EXPECTED` status reaching a queue entry directly | mitigate | `EXPECTED` has no inbound transition in `QUEUE_TRANSITIONS` (plan 08-01), so plan 08-04's `isValidTransition` guard rejects every client-driven move into it; only the server-side sweep inserts `EXPECTED` rows |
| T-08-09 | Tampering | a duplicate sweep firing creating two `QueueEntry` rows for one appointment-pet | mitigate | `appointment_pets` has `@@unique([appointmentId, petId])` and carries `queue_entry_id`; combined with the `queue_entry_created_at` marker column checked in the sweep's `WHERE` clause, a re-run is a no-op (RESEARCH Pattern 2 belt-and-braces) |
| T-08-10 | Tampering | a later `ServiceCatalog` duration edit retroactively resizing historical appointments | mitigate | `appointments.duration_minutes` is a NOT NULL snapshot written at booking time, never read back through the catalog relation; Task 3 asserts the column is integer and non-null |
| T-08-11 | Repudiation | `queue_priority_at` mutated after creation, silently rewriting queue history | accept | Enforced by convention and by plan 08-04's tests (set once at creation, never touched on check-in) rather than by a database trigger; a trigger would be new machinery this codebase has no precedent for, and the audit trail extension in plan 08-05 records lifecycle changes |
| T-08-12 | Spoofing | a forged `whatsapp_booking_request_id` linking an appointment to another owner's WhatsApp thread | mitigate | The column is write-only from plan 08-10's server-side inbound bridge, which resolves the appointment through the thread's own owner linkage and never trusts a bare id from an inbound payload; no client endpoint accepts this field |
</threat_model>

<verification>
- `cd apps/api && pnpm exec prisma validate` exits 0.
- `cd apps/api && pnpm exec prisma migrate reset --force --skip-seed && psql "$DATABASE_URL" -f prisma/post-migrate.sql && pnpm exec prisma migrate diff --from-schema-datasource "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --exit-code` exits 0.
- `pnpm --filter @breeyo/api test` passes in full, including every pre-existing Phase 3/4 suite.
- `pnpm --filter @breeyo/api exec tsc --noEmit` exits 0.
</verification>

<success_criteria>
- Five scheduling tables and three enums exist in both `schema.prisma` and the live database.
- `EXPECTED` is a live `QueueEntryStatus` enum value in Postgres.
- `service_catalog.duration_minutes` and `appointments.duration_minutes` both exist, the latter non-null, delivering D-02's per-service slot length with a booking-time snapshot.
- `queue_entries.queue_priority_at` exists, is NOT NULL, is backfilled from `checked_in_at`, and is covered by a `(clinic_id, status, queue_priority_at)` index.
- `appointment_pets` supports D-21 multi-pet appointments and carries the `queue_entry_id` link the handoff pass writes.
- Migration/schema parity holds and the Prisma client exposes all five new delegates.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-calendar/08-03-SUMMARY.md` when done. Record: the final field list of each new model, the approach taken for the `ALTER TYPE ... ADD VALUE` transaction split, the `ServiceCatalog.durationMinutes` default chosen, every pre-existing test or factory call site that had to gain `queuePriorityAt`, and the exact migration directory name so later plans reference it correctly.
</output>
