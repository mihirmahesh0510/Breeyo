---
phase: 02-ui-ux-design-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/vitest.config.ts
  - packages/ui/tests/setup.ts
  - packages/ui/src/index.ts
  - packages/ui/src/theme/index.ts
  - packages/ui/src/theme/colors.ts
  - packages/ui/src/theme/typography.ts
  - packages/ui/src/theme/spacing.ts
  - packages/ui/src/theme/elevation.ts
  - packages/ui/src/theme/borderRadius.ts
  - packages/ui/src/theme/animation.ts
  - packages/ui/src/theme/theme.ts
  - packages/ui/src/theme/types.ts
  - packages/ui/src/theme/__tests__/tokens.test.ts
  - packages/ui/src/theme/__tests__/theme.test.ts
  - packages/ui/src/i18n/config.ts
  - packages/ui/src/i18n/locales/en/common.json
  - packages/ui/src/i18n/locales/hi/common.json
  - packages/ui/scripts/generate-css-tokens.ts
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "packages/ui installs and builds without errors"
    - "Design token file exports complete color palette with all M3 roles (primary, secondary, tertiary, error, surface, background)"
    - "Typography scale has exactly 7 levels (display, heading1, heading2, subheading, body, caption, overline)"
    - "Spacing scale has 10 values from 2px to 64px on 4/8px grid"
    - "Elevation system has 6 levels (0-5) matching Material Design 3"
    - "Border radius has 6 tokens (none, sm, md, lg, xl, full)"
    - "Theme extends MD3LightTheme and is consumable by PaperProvider"
    - "useAppTheme hook returns typed AppTheme with spacing, borderRadius, customTypography"
    - "Vitest runs and token tests pass"
    - "i18next initializes with English and Hindi locales"
    - "CSS token generator script outputs portal.css with custom properties"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "@breeyo/ui package with all design system dependencies"
      contains: "react-native-paper"
    - path: "packages/ui/src/theme/colors.ts"
      provides: "Complete color palette"
      contains: "#2E7D32"
    - path: "packages/ui/src/theme/typography.ts"
      provides: "7-level typography scale"
      contains: "heading1"
    - path: "packages/ui/src/theme/spacing.ts"
      provides: "10-value spacing scale"
      contains: "md"
    - path: "packages/ui/src/theme/theme.ts"
      provides: "Breeyo theme extending MD3LightTheme"
      contains: "breeyoTheme"
    - path: "packages/ui/src/theme/types.ts"
      provides: "useAppTheme hook"
      contains: "useAppTheme"
    - path: "packages/ui/vitest.config.ts"
      provides: "Test configuration for UI package"
      contains: "vitest"
  key_links:
    - from: "packages/ui/src/theme/theme.ts"
      to: "packages/ui/src/theme/colors.ts"
      via: "import colors"
      pattern: "import.*colors.*from.*colors"
    - from: "packages/ui/src/theme/types.ts"
      to: "packages/ui/src/theme/theme.ts"
      via: "import AppTheme type"
      pattern: "AppTheme"
---

<objective>
Scaffold the `packages/ui` monorepo package and establish the complete design token system with typed theme, test infrastructure, internationalization, and CSS token export for the owner portal.

Purpose: Every subsequent component and wireframe in this phase (and all future feature phases) depends on these tokens and the theme provider. This is the foundation layer.

Output: A working `@breeyo/ui` package with design tokens, Breeyo theme extending MD3LightTheme, Vitest test setup, i18n with English/Hindi, and a CSS token generator for the web portal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-ux-design-design-system/02-CONTEXT.md
@.planning/phases/02-ui-ux-design-design-system/02-RESEARCH.md
@.planning/phases/02-ui-ux-design-design-system/02-UI-SPEC.md

<interfaces>
<!-- Phase 1 establishes monorepo structure. This plan creates packages/ui within it. -->
<!-- Root package.json uses pnpm workspaces. turbo.json defines pipeline. -->
<!-- packages/types and packages/validators exist from Phase 1. -->
<!-- packages/config/tsconfig/react-native.json provides base TS config for RN packages. -->

From Phase 1 monorepo (expected structure):
- pnpm-workspace.yaml declares packages/* and apps/* workspaces
- turbo.json has build/test/lint pipelines
- packages/config/tsconfig/react-native.json provides base tsconfig for RN packages
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold packages/ui and create design token files</name>
  <files>
    packages/ui/package.json,
    packages/ui/tsconfig.json,
    packages/ui/vitest.config.ts,
    packages/ui/tests/setup.ts,
    packages/ui/src/index.ts,
    packages/ui/src/theme/index.ts,
    packages/ui/src/theme/colors.ts,
    packages/ui/src/theme/typography.ts,
    packages/ui/src/theme/spacing.ts,
    packages/ui/src/theme/elevation.ts,
    packages/ui/src/theme/borderRadius.ts,
    packages/ui/src/theme/animation.ts,
    packages/ui/src/theme/__tests__/tokens.test.ts
  </files>
  <read_first>
    packages/ui/package.json,
    pnpm-workspace.yaml,
    turbo.json,
    packages/config/tsconfig/react-native.json,
    .planning/phases/02-ui-ux-design-design-system/02-UI-SPEC.md,
    .planning/phases/02-ui-ux-design-design-system/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test: colors exports primary (#2E7D32), onPrimary (#FFFFFF), primaryContainer (#C8E6C9), onPrimaryContainer (#1B5E20), secondary (#5D4037), tertiary (#E65100), error (#BA1A1A), background (#FFFBF5), surface (#FFFBF5) and all other M3 color roles
    - Test: typography exports exactly 7 levels: display, heading1, heading2, subheading, body, caption, overline -- each with fontSize, lineHeight, fontWeight
    - Test: spacing exports exactly 10 tokens: xxs(2), xs(4), sm(8), md(16), lg(24), xl(32), 2xl(40), 3xl(48), 4xl(56), 5xl(64)
    - Test: elevation exports 6 levels: level0(0), level1(1), level2(3), level3(6), level4(8), level5(12)
    - Test: borderRadius exports 6 tokens: none(0), sm(4), md(8), lg(12), xl(16), full(28)
    - Test: animation exports durations (microFeedback:100, expandCollapse:200, screenTransition:250, toastEnter:150, toastExit:100, skeletonShimmer:1500, statusBadgeUpdate:300)
    - Test: all token objects are typed with "as const" (readonly)
  </behavior>
  <action>
    1. Create `packages/ui/package.json` with:
       - name: "@breeyo/ui"
       - version: "0.0.1"
       - main: "src/index.ts"
       - types: "src/index.ts"
       - dependencies: react-native-paper@^5.15.1, react-native-safe-area-context@^5.7.0, @gorhom/bottom-sheet@^5.2.9, react-native-skeleton-placeholder@^5.2.4, react-native-toast-message@^2.3.3, i18next@^26.0.5, react-i18next@^17.0.4
       - peerDependencies: react, react-native, react-native-reanimated@>=4.0.0, react-native-gesture-handler@>=2.31.0
       - devDependencies: vitest@latest, @testing-library/react-native@latest, typescript@^5.5.0

    2. Create `packages/ui/tsconfig.json` extending `../../packages/config/tsconfig/react-native.json` (if exists) or base tsconfig with:
       - strict: true, jsx: "react-jsx", moduleResolution: "bundler"
       - include: ["src/**/*", "tests/**/*"]

    3. Create `packages/ui/vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
         test: {
           globals: true,
           environment: 'node',
           setupFiles: ['./tests/setup.ts'],
           include: ['src/**/*.test.{ts,tsx}'],
         },
       });
       ```

    4. Create `packages/ui/tests/setup.ts` with mock for react-native (since Vitest runs in Node):
       ```typescript
       import { vi } from 'vitest';
       vi.mock('react-native', () => ({
         Platform: { OS: 'android', select: vi.fn((obj: any) => obj.android) },
         StyleSheet: { create: (styles: any) => styles },
         Dimensions: { get: () => ({ width: 375, height: 812, scale: 2, fontScale: 1 }) },
       }));
       ```

    5. Create `packages/ui/src/theme/colors.ts` with ALL colors from 02-UI-SPEC.md Color section:
       - Primary: primary=#2E7D32, onPrimary=#FFFFFF, primaryContainer=#C8E6C9, onPrimaryContainer=#1B5E20
       - Secondary: secondary=#5D4037, onSecondary=#FFFFFF, secondaryContainer=#D7CCC8, onSecondaryContainer=#3E2723
       - Tertiary: tertiary=#E65100, onTertiary=#FFFFFF, tertiaryContainer=#FFE0B2, onTertiaryContainer=#BF360C
       - Surface: background=#FFFBF5, onBackground=#1C1B1F, surface=#FFFBF5, onSurface=#1C1B1F, surfaceVariant=#F5F0EB, onSurfaceVariant=#49454F
       - Outline: outline=#79747E, outlineVariant=#CAC4D0
       - Disabled: surfaceDisabled="rgba(28, 27, 31, 0.12)", onSurfaceDisabled="rgba(28, 27, 31, 0.38)"
       - Semantic: error=#BA1A1A, onError=#FFFFFF, errorContainer=#FFDAD6, onErrorContainer=#410002, success=#2E7D32, warning=#E65100
       - Export as `export const colors = { ... } as const;`

    6. Create `packages/ui/src/theme/typography.ts` with 7 levels from 02-UI-SPEC.md Typography section:
       - display: fontSize:45, lineHeight:52, fontWeight:'400'
       - heading1: fontSize:32, lineHeight:40, fontWeight:'400'
       - heading2: fontSize:28, lineHeight:36, fontWeight:'400'
       - subheading: fontSize:16, lineHeight:24, fontWeight:'500', letterSpacing:0.15
       - body: fontSize:16, lineHeight:24, fontWeight:'400', letterSpacing:0.5
       - caption: fontSize:12, lineHeight:16, fontWeight:'400', letterSpacing:0.4
       - overline: fontSize:11, lineHeight:16, fontWeight:'500', letterSpacing:0.5
       - Export as `export const typography = { ... } as const;`

    7. Create `packages/ui/src/theme/spacing.ts` with 10 tokens from 02-UI-SPEC.md:
       xxs:2, xs:4, sm:8, md:16, lg:24, xl:32, '2xl':40, '3xl':48, '4xl':56, '5xl':64
       - Export as `export const spacing = { ... } as const;`

    8. Create `packages/ui/src/theme/elevation.ts` with 6 levels:
       level0:0, level1:1, level2:3, level3:6, level4:8, level5:12
       - Export as `export const elevation = { ... } as const;`

    9. Create `packages/ui/src/theme/borderRadius.ts` with 6 tokens:
       none:0, sm:4, md:8, lg:12, xl:16, full:28
       - Export as `export const borderRadius = { ... } as const;`

    10. Create `packages/ui/src/theme/animation.ts` with durations and easings from 02-UI-SPEC.md:
        ```typescript
        export const duration = {
          microFeedback: 100,
          expandCollapse: 200,
          screenTransition: 250,
          toastEnter: 150,
          toastExit: 100,
          skeletonShimmer: 1500,
          statusBadgeUpdate: 300,
        } as const;
        ```

    11. Create `packages/ui/src/theme/index.ts` barrel export re-exporting all token modules, theme, and types.

    12. Create `packages/ui/src/index.ts` barrel export re-exporting from theme/index.ts (will add components later).

    13. Write tests in `packages/ui/src/theme/__tests__/tokens.test.ts`:
        - Test each token module exports correct number of keys
        - Test specific values match UI-SPEC (e.g., colors.primary === '#2E7D32')
        - Test typography has all 7 levels each with fontSize, lineHeight, fontWeight
        - Test spacing has all 10 values from xxs(2) to 5xl(64)
        - Test elevation has 6 levels
        - Test borderRadius has 6 tokens
        - Test animation durations has 7 entries

    14. Run `pnpm install` from root to register new workspace package.
  </action>
  <verify>
    <automated>cd /Users/mihirmahesh/Documents/Projects/Breeyo && pnpm --filter @breeyo/ui test -- tokens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/package.json contains "name": "@breeyo/ui"
    - packages/ui/package.json contains "react-native-paper"
    - packages/ui/src/theme/colors.ts contains "#2E7D32" and "#FFFBF5" and "#E65100" and "#BA1A1A" and "#5D4037"
    - packages/ui/src/theme/typography.ts contains "display" and "heading1" and "heading2" and "subheading" and "body" and "caption" and "overline"
    - packages/ui/src/theme/spacing.ts contains "xxs" and "xs" and "sm" and "md" and "lg" and "xl" and "2xl" and "3xl" and "4xl" and "5xl"
    - packages/ui/src/theme/elevation.ts contains "level0" and "level1" and "level2" and "level3" and "level4" and "level5"
    - packages/ui/src/theme/borderRadius.ts contains "none" and "sm" and "md" and "lg" and "xl" and "full"
    - packages/ui/src/theme/animation.ts contains "microFeedback" and "skeletonShimmer" and "statusBadgeUpdate"
    - tokens.test.ts exits with code 0
  </acceptance_criteria>
  <done>All design token files exist with exact values from UI-SPEC, tokens.test.ts passes, packages/ui is a valid workspace package</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Breeyo theme extending MD3LightTheme with useAppTheme hook</name>
  <files>
    packages/ui/src/theme/theme.ts,
    packages/ui/src/theme/types.ts,
    packages/ui/src/theme/__tests__/theme.test.ts
  </files>
  <read_first>
    packages/ui/src/theme/colors.ts,
    packages/ui/src/theme/typography.ts,
    packages/ui/src/theme/spacing.ts,
    packages/ui/src/theme/elevation.ts,
    packages/ui/src/theme/borderRadius.ts,
    packages/ui/src/theme/animation.ts,
    packages/ui/src/theme/index.ts,
    .planning/phases/02-ui-ux-design-design-system/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test: breeyoTheme extends MD3LightTheme (has roundness, dark, fonts, colors from MD3)
    - Test: breeyoTheme.colors.primary === '#2E7D32' (overrides MD3 default purple)
    - Test: breeyoTheme.spacing exists and breeyoTheme.spacing.md === 16
    - Test: breeyoTheme.borderRadius exists and breeyoTheme.borderRadius.lg === 12
    - Test: breeyoTheme.customTypography exists with all 7 levels
    - Test: breeyoTheme.elevation.level3 === 6
    - Test: breeyoTheme.animation.duration.microFeedback === 100
    - Test: AppTheme type is exported
    - Test: useAppTheme is a function exported from types.ts
  </behavior>
  <action>
    1. Create `packages/ui/src/theme/theme.ts`:
       ```typescript
       import { MD3LightTheme } from 'react-native-paper';
       import { colors } from './colors';
       import { typography } from './typography';
       import { spacing } from './spacing';
       import { elevation } from './elevation';
       import { borderRadius } from './borderRadius';
       import { duration as animationDurations } from './animation';

       export const breeyoTheme = {
         ...MD3LightTheme,
         colors: {
           ...MD3LightTheme.colors,
           ...colors,
         },
         fonts: {
           ...MD3LightTheme.fonts,
           displayLarge: { ...MD3LightTheme.fonts.displayMedium, ...typography.display },
           headlineLarge: { ...MD3LightTheme.fonts.headlineLarge, ...typography.heading1 },
           headlineMedium: { ...MD3LightTheme.fonts.headlineMedium, ...typography.heading2 },
           titleMedium: { ...MD3LightTheme.fonts.titleMedium, ...typography.subheading },
           bodyLarge: { ...MD3LightTheme.fonts.bodyLarge, ...typography.body },
           bodySmall: { ...MD3LightTheme.fonts.bodySmall, ...typography.caption },
           labelSmall: { ...MD3LightTheme.fonts.labelSmall, ...typography.overline },
         },
         spacing,
         elevation,
         borderRadius,
         animation: { duration: animationDurations },
         customTypography: typography,
       } as const;

       export type AppTheme = typeof breeyoTheme;
       ```

    2. Create `packages/ui/src/theme/types.ts`:
       ```typescript
       import { useTheme } from 'react-native-paper';
       import type { AppTheme } from './theme';

       export const useAppTheme = () => useTheme<AppTheme>();
       export type { AppTheme };
       ```

    3. Update `packages/ui/src/theme/index.ts` to export breeyoTheme, useAppTheme, AppTheme, and all token modules.

    4. Update `packages/ui/src/index.ts` to re-export from theme.

    5. Write tests in `packages/ui/src/theme/__tests__/theme.test.ts`:
       - Import breeyoTheme, verify it spreads MD3LightTheme (has `dark: false`, `roundness`)
       - Verify colors override: breeyoTheme.colors.primary === '#2E7D32'
       - Verify spacing: breeyoTheme.spacing.md === 16
       - Verify borderRadius: breeyoTheme.borderRadius.lg === 12
       - Verify customTypography: all 7 levels present with correct fontSize values
       - Verify elevation: breeyoTheme.elevation.level3 === 6
       - Verify animation: breeyoTheme.animation.duration.microFeedback === 100
       - Verify AppTheme type is exportable (compile-time check via import)
       - Verify useAppTheme is a function

    6. Update tests/setup.ts to also mock react-native-paper's useTheme if not already mocked, returning breeyoTheme for test usage.
  </action>
  <verify>
    <automated>cd /Users/mihirmahesh/Documents/Projects/Breeyo && pnpm --filter @breeyo/ui test -- theme.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/theme/theme.ts contains "breeyoTheme" and "MD3LightTheme" and "AppTheme"
    - packages/ui/src/theme/types.ts contains "useAppTheme" and "useTheme"
    - packages/ui/src/theme/index.ts exports breeyoTheme, useAppTheme, AppTheme
    - packages/ui/src/index.ts re-exports from theme
    - theme.test.ts exits with code 0
  </acceptance_criteria>
  <done>Breeyo theme extends MD3LightTheme with all custom tokens, useAppTheme hook is typed, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Set up i18n with English/Hindi locales and CSS token generator for web portal</name>
  <files>
    packages/ui/src/i18n/config.ts,
    packages/ui/src/i18n/locales/en/common.json,
    packages/ui/src/i18n/locales/hi/common.json,
    packages/ui/scripts/generate-css-tokens.ts
  </files>
  <read_first>
    packages/ui/src/theme/colors.ts,
    packages/ui/src/theme/spacing.ts,
    packages/ui/src/theme/typography.ts,
    packages/ui/src/theme/borderRadius.ts,
    .planning/phases/02-ui-ux-design-design-system/02-UI-SPEC.md
  </read_first>
  <action>
    1. Create `packages/ui/src/i18n/config.ts` per RESEARCH Pattern i18n:
       ```typescript
       import i18n from 'i18next';
       import { initReactI18next } from 'react-i18next';
       import en_common from './locales/en/common.json';
       import hi_common from './locales/hi/common.json';

       const resources = {
         en: { common: en_common },
         hi: { common: hi_common },
       };

       i18n.use(initReactI18next).init({
         resources,
         lng: 'en',
         fallbackLng: 'en',
         defaultNS: 'common',
         interpolation: { escapeValue: false },
       });

       export default i18n;
       ```
       Note: expo-localization import is omitted at this stage since it's a runtime dependency available only in the mobile app. The mobile app's entry point will call `i18n.changeLanguage(Localization.getLocales()[0]?.languageCode ?? 'en')` at startup.

    2. Create `packages/ui/src/i18n/locales/en/common.json` with initial strings from UI-SPEC Copywriting Contract:
       ```json
       {
         "queue": {
           "fabLabel": "Check In",
           "emptyTitle": "No patients in queue yet",
           "emptyBody": "Tap Check In to add your first patient today.",
           "errorMessage": "Could not load queue. Pull down to try again.",
           "statusWaiting": "Waiting",
           "statusInConsult": "In Consult",
           "statusDone": "Done",
           "statusNoShow": "No Show",
           "checkInSuccess": "Patient checked in",
           "swipeDoneConfirm": "Mark as done?",
           "swipeNoShowConfirm": "Mark as no-show?"
         },
         "common": {
           "cancel": "Cancel",
           "save": "Save",
           "delete": "Delete",
           "retry": "Try again",
           "offline": "You are offline. Changes will sync when you reconnect."
         },
         "portal": {
           "verifyingLink": "Verifying your link...",
           "medicalHistory": "Medical History",
           "invoices": "Invoices",
           "payNow": "Pay Now",
           "paid": "Paid",
           "overdue": "Overdue",
           "processing": "Processing...",
           "paymentReceived": "Payment received. Thank you!",
           "paymentFailed": "Payment could not be completed. Please try again.",
           "expiredLinkTitle": "This link has expired",
           "expiredLinkBody": "Links are valid for 7 days. Request a new link below.",
           "requestNewLink": "Request New Link",
           "linkSentConfirmation": "A new link has been sent to your WhatsApp.",
           "invalidLink": "This link is not valid. Please contact your clinic.",
           "poweredBy": "Powered by Breeyo",
           "emrEmpty": "No medical records yet. Your vet will add records after your next visit."
         }
       }
       ```

    3. Create `packages/ui/src/i18n/locales/hi/common.json` with Hindi translations from UI-SPEC:
       ```json
       {
         "queue": {
           "fabLabel": "चेक इन",
           "emptyTitle": "कतार में अभी कोई मरीज नहीं है",
           "emptyBody": "अपने पहले मरीज को जोड़ने के लिए चेक इन पर टैप करें।",
           "errorMessage": "कतार लोड नहीं हो सकी। नीचे खींचकर फिर कोशिश करें।",
           "statusWaiting": "प्रतीक्षा में",
           "statusInConsult": "परामर्श में",
           "statusDone": "पूर्ण",
           "statusNoShow": "अनुपस्थित",
           "checkInSuccess": "मरीज चेक इन हो गया",
           "swipeDoneConfirm": "पूर्ण के रूप में चिह्नित करें?",
           "swipeNoShowConfirm": "अनुपस्थित के रूप में चिह्नित करें?"
         },
         "common": {
           "cancel": "रद्द करें",
           "save": "सहेजें",
           "delete": "हटाएं",
           "retry": "पुनः प्रयास करें",
           "offline": "आप ऑफ़लाइन हैं। कनेक्शन वापस आने पर बदलाव सिंक हो जाएंगे।"
         },
         "portal": {
           "verifyingLink": "आपका लिंक सत्यापित हो रहा है...",
           "medicalHistory": "चिकित्सा इतिहास",
           "invoices": "बिल",
           "payNow": "अभी भुगतान करें",
           "paid": "भुगतान हो गया",
           "overdue": "बकाया",
           "processing": "प्रोसेसिंग...",
           "paymentReceived": "भुगतान प्राप्त हुआ। धन्यवाद!",
           "paymentFailed": "भुगतान पूरा नहीं हो सका। कृपया पुनः प्रयास करें।",
           "expiredLinkTitle": "यह लिंक समाप्त हो गया है",
           "expiredLinkBody": "लिंक 7 दिनों तक मान्य हैं। नीचे नया लिंक अनुरोध करें।",
           "requestNewLink": "नया लिंक अनुरोध करें",
           "linkSentConfirmation": "आपके WhatsApp पर एक नया लिंक भेजा गया है।",
           "invalidLink": "यह लिंक मान्य नहीं है। कृपया अपने क्लिनिक से संपर्क करें।",
           "poweredBy": "Breeyo द्वारा संचालित",
           "emrEmpty": "अभी तक कोई चिकित्सा रिकॉर्ड नहीं है। आपका पशु चिकित्सक अगली विजिट के बाद रिकॉर्ड जोड़ेंगे।"
         }
       }
       ```

    4. Create `packages/ui/scripts/generate-css-tokens.ts` that reads TypeScript token source files and outputs `packages/ui/src/theme/portal.css` with CSS custom properties per UI-SPEC Token Export Format:
       ```typescript
       import { colors } from '../src/theme/colors';
       import { spacing } from '../src/theme/spacing';
       import { typography } from '../src/theme/typography';
       import { borderRadius } from '../src/theme/borderRadius';

       function toKebabCase(key: string): string {
         return key.replace(/([A-Z])/g, '-$1').toLowerCase()
                    .replace(/(\d+)xl/, '$1xl');
       }

       let css = ':root {\n';

       // Colors
       for (const [key, value] of Object.entries(colors)) {
         css += `  --color-${toKebabCase(key)}: ${value};\n`;
       }
       css += '\n';

       // Spacing
       for (const [key, value] of Object.entries(spacing)) {
         css += `  --spacing-${key}: ${value}px;\n`;
       }
       css += '\n';

       // Typography (portal subset: heading2, subheading, body, caption)
       css += `  --font-family: 'Inter', system-ui, sans-serif;\n`;
       for (const [key, value] of Object.entries(typography)) {
         css += `  --font-size-${toKebabCase(key)}: ${value.fontSize}px;\n`;
         css += `  --line-height-${toKebabCase(key)}: ${value.lineHeight}px;\n`;
       }
       css += '\n';

       // Border radius
       for (const [key, value] of Object.entries(borderRadius)) {
         css += `  --radius-${key}: ${value}px;\n`;
       }

       css += '}\n';

       // Write to stdout or file
       import { writeFileSync } from 'fs';
       import { resolve } from 'path';
       writeFileSync(
         resolve(__dirname, '../src/theme/portal.css'),
         css,
         'utf-8'
       );
       console.log('Generated portal.css');
       ```

       Note: Use tsx (TypeScript executor) to run this script. Add a `"generate:css-tokens"` script to package.json: `"tsx scripts/generate-css-tokens.ts"`.

    5. Add tsx as a devDependency in packages/ui/package.json: `"tsx": "^4.0.0"`.

    6. Add script to packages/ui/package.json:
       ```json
       "scripts": {
         "test": "vitest run",
         "generate:css-tokens": "tsx scripts/generate-css-tokens.ts"
       }
       ```

    7. Update `packages/ui/src/index.ts` to also export i18n config:
       `export { default as i18n } from './i18n/config';`

    8. Run `pnpm --filter @breeyo/ui generate:css-tokens` to verify CSS token generation works.

    9. Run full test suite: `pnpm --filter @breeyo/ui test` to verify all tests still pass.
  </action>
  <verify>
    <automated>cd /Users/mihirmahesh/Documents/Projects/Breeyo && pnpm --filter @breeyo/ui test && pnpm --filter @breeyo/ui generate:css-tokens</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/i18n/config.ts contains "i18next" and "initReactI18next" and "fallbackLng: 'en'"
    - packages/ui/src/i18n/locales/en/common.json contains "Check In" and "No patients in queue yet" and "Verifying your link..."
    - packages/ui/src/i18n/locales/hi/common.json contains "चेक इन" and "आपका लिंक सत्यापित हो रहा है..."
    - packages/ui/scripts/generate-css-tokens.ts contains "colors" and "spacing" and "typography" and ":root"
    - packages/ui/src/theme/portal.css exists after running generate:css-tokens
    - packages/ui/src/theme/portal.css contains "--color-primary: #2E7D32" and "--spacing-md: 16px" and "--font-family: 'Inter'"
    - pnpm --filter @breeyo/ui test exits with code 0
  </acceptance_criteria>
  <done>i18n configured with English and Hindi common strings, CSS token generator produces portal.css with all design tokens as CSS custom properties, all tests pass</done>
</task>

</tasks>

<verification>
1. Run `pnpm --filter @breeyo/ui test` -- all token and theme tests pass
2. Run `pnpm --filter @breeyo/ui generate:css-tokens` -- portal.css generated
3. Verify `packages/ui/src/theme/portal.css` contains `--color-primary: #2E7D32`
4. Verify `packages/ui/src/index.ts` exports breeyoTheme, useAppTheme, i18n
5. Verify `packages/ui/src/i18n/locales/en/common.json` has queue and portal namespace strings
</verification>

<success_criteria>
1. `@breeyo/ui` package exists in monorepo with all dependencies declared
2. Design tokens define: 23+ colors, 7 typography levels, 10 spacing values, 6 elevation levels, 6 border radii, 7 animation durations
3. breeyoTheme extends MD3LightTheme and overrides colors with Breeyo palette
4. useAppTheme hook is typed to return AppTheme with all custom extensions
5. Vitest configuration works and all tests (tokens + theme) pass
6. i18n initializes with English and Hindi locales containing queue and portal strings
7. CSS token generator outputs portal.css consumable by the owner portal (Phase 9)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-ux-design-design-system/02-01-SUMMARY.md`
</output>
